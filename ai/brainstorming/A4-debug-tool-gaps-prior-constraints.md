# A4 — Debug / Diagnose / Observability Tool Gaps (Prior Constraints)

- Agent: A4
- Mandate: Brainstorm debug/diagnose/observability tooling we should already have built under the OLD GitHub-Actions-only constraints (no direct kubectl/aws, only workflow_dispatch + log parsing) but never did — mined from retros and PR threads.
- Date: 2026-05-25
- Branch: claude/determined-pasteur-53fAN

| ID | Idea (one sentence) | Category | Justification | Applies to phase |
|---|---|---|---|---|
| A4-001 | A `read-cluster.yml` dispatch workflow with a `kind` + `name` + `namespace` input that runs `kubectl get -o yaml` and prints to log, giving the agent a 60-second cluster read without authoring a new workflow each time. | dispatch-workflow | Would have replaced the entire `phase-2-diagnose.yml` authoring cycle in PR #60 and the many ad-hoc dispatch PRs that followed; the prior session said "dispatch a workflow + wait 3 minutes + parse a 170 KB log just to read one MR's `.status.conditions`". | 1+ |
| A4-002 | A `kubectl-exec.yml` workflow that takes a free-text kubectl command, runs it read-only, and prints the result — gated by an allow-list of verbs (`get`/`describe`/`logs`/`events`/`top`). | dispatch-workflow | The prior sessions repeatedly hand-rolled one-off diagnose YAMLs because there was no generic read-bridge; would have eliminated PR #60 and the post-#65 workflow re-edits. | 1+ |
| A4-003 | An `aws-cli.yml` workflow that takes a service+command+args and dumps JSON output (read-only verbs only). | dispatch-workflow | Bug 5 (IRSA SA-hash mismatch, PR #66) needed `aws iam get-role` output that took multiple debug iterations because the agent had no AWS bridge. | 0+ |
| A4-004 | A `status-snapshot.yml` workflow that, on every dispatch, dumps a fixed bundle (nodes, pods -A, events -A, all Crossplane CRs, ArgoCD apps, helm releases, kyverno policyreports) to a single log section with stable headings. | status-snapshot | Would have given a 60-second "where is the cluster right now" view the prior sessions consistently lacked between PRs #41-#68. | 1+ |
| A4-005 | A `crossplane-trace.yml` workflow that takes a claim name + namespace and walks claim → XR → resourceRefs → MRs → atProvider, printing conditions at each layer. | dispatch-workflow | PR #66 + #67 + #68 cascading IRSA debug burned hours on what was "where in the chain is the break?"; one trace command would have collapsed it. | 2+ |
| A4-006 | An `irsa-check.yml` workflow that takes a role name, fetches trust policy, lists matching SAs in cluster, decodes their projected token sub claim, asserts match. | dispatch-workflow | Bug 5 was exactly "trust subject does not match SA name" and took multiple PRs of guessing; one dispatch would have surfaced the mismatch immediately. | 1+ |
| A4-007 | A `terraform-show-state.yml` workflow that dumps `terraform state list` + `state show <addr>` for a specified address. | dispatch-workflow | PR #67's `triggers_replace` miss ("Apply complete: 0 added" silent no-op) would have been one `state show` call away. | 0+ |
| A4-008 | A `terraform-plan-diff.yml` workflow that runs `terraform plan -out tfplan` then `show -json tfplan | jq` and prints the changed-resources summary up front. | dispatch-workflow | The "0 added" misread (run 26354235231 cited in handoff §"It applied successfully ≠") was specifically the agent failing to read a long plan output; up-front summary fixes that. | 0+ |
| A4-009 | A `workflow-log-fetch.sh` shell helper that pulls a given run's logs via `gh api`, strips ANSI/timestamps, and concatenates into a single grep-friendly file. | shell-helper | Every retro mentions parsing 60K-170K char workflow logs by hand; normalizing them once is cheap. | 0+ |
| A4-010 | A `runbook-irsa-debug.md` that lists the seven canonical IRSA failure signatures (trust mismatch, missing OIDC provider, audience wrong, missing namespace_service_accounts, SA annotation missing, pod missing token volume, ASM 403) with the workflow_dispatch command that proves each. | runbook | The PR #66 onion took multiple sessions because no canonical IRSA debug runbook existed. | 1+ |
| A4-011 | A `runbook-claim-not-ready.md` that gives the eight-step decision tree from claim → XR → composition function → MR → provider → IRSA → cloud. | runbook | Phase 2 burnt ~45 min per loop on "claim is Waiting, why" without a written tree. | 2+ |
| A4-012 | A `runbook-argocd-outofsync.md` enumerating the five known causes (Kyverno mutation/drift, CRD-not-yet-installed, sync-wave race, manifest hash mismatch, RBAC) with dispatch commands per. | runbook | Bug 3 (Kyverno drift on policy 09) recurred in three sessions because there was no enumerated playbook. | 1+ |
| A4-013 | A `runbook-apply-zero-resources.md` documenting the "Apply complete: 0 added" silent-no-op class with the `triggers_replace` hash-the-manifest pattern from PR #67. | runbook | Three independent failures in one session per handoff "It applied successfully ≠"; this is the highest-leverage runbook in the repo. | 0+ |
| A4-014 | A `dispatch-then-read-step.sh` helper that dispatches a workflow with a SHA, polls until done, then downloads ONLY the named step's log (not the wrapper conclusion). | shell-helper | Codifies the lesson from `verify-evidence-not-exit-codes` skill — eliminates the "conclusion: success while assertions failed" class. | 0+ |
| A4-015 | A `grep-workflow-log.sh <run-id> <pattern>` that fetches and greps in one shot. | shell-helper | Repeatedly invoked by hand across PRs #52-#68 with bespoke `gh api` invocations. | 0+ |
| A4-016 | A `phase-state-probe.yml` workflow that returns a JSON document mapping each phase 0-6 to `not-coded|code-only|applied|verified` by live-probing AWS/EKS/CRDs. | dispatch-workflow | Handoff §8.1 says treat the handoff phase states as "belief, not ground truth"; only a live probe is ground truth and there was none. | 0+ |
| A4-017 | A `dispatch-workflow-and-poll.py` that wraps `gh workflow run` + polling + log fetch + exit-code propagation into one call. | shell-helper | Every session reinvents this loop. | 0+ |
| A4-018 | A `kubectl-events-since.yml` workflow that dumps events newer than a `since` input, sorted by lastTimestamp. | dispatch-workflow | Crossplane sessions repeatedly missed events because `kubectl get events` is unordered by default and 60K-char dumps buried them. | 1+ |
| A4-019 | A `provider-health.yml` workflow that lists Crossplane Providers, ProviderRevisions, Functions, plus their Healthy/Installed conditions and image refs. | dispatch-workflow | Bug 5 had the wrong provider Deployment running; this would have shown it. | 2+ |
| A4-020 | A `helm-release-status.yml` workflow that prints `helm list -A` + `helm status` for every release + manifest diff against repo. | dispatch-workflow | The session said "helm_release can be 'created' while pods CrashLoop"; this surfaces both. | 1+ |
| A4-021 | A `pod-restart-watch.yml` workflow that lists pods with restartCount>0 + last termination reason + tail of previous-container logs. | dispatch-workflow | OOM/CrashLoop debugging was always ad-hoc; standardized format would have caught the ESO/Crossplane crash patterns. | 1+ |
| A4-022 | A `kyverno-explain-policy.yml` workflow that takes a policy name + a candidate resource yaml and reports which rules pass/fail and why. | dispatch-workflow | Bug 3 (ClusterPolicy 09 drift) was exactly Kyverno mutating fields; explain would have surfaced the diff. | 1+ |
| A4-023 | A `kyverno-violations-summary.yml` workflow that pulls all PolicyReports, groups by policy + resource kind, and prints counts. | dispatch-workflow | `kyverno-violations.sh` exists but only useful with live kubectl — workflow form makes it usable from the sandbox. | 1+ |
| A4-024 | A `route53-record-snapshot.yml` workflow that lists all records in the hosted zone with TTL + value + ExternalDNS owner-tag. | dispatch-workflow | ExternalDNS propagation races were the #2 ingress bug class; snapshot diff over time would catch drift. | 0+ |
| A4-025 | An `nlb-target-health.yml` workflow that walks Ingress → Service → NLB ARN → target group → health + SG rules. | dispatch-workflow | The seven-bug phase-1 bring-up (per `ai/TESTING-PLAN.md`) included NLB health misses; would have collapsed multi-hop debug. | 1+ |
| A4-026 | A `cognito-state.yml` workflow that dumps user pool + clients + groups + identity providers JSON. | dispatch-workflow | Phase 0 + phase 5 needed Cognito state visibility; nothing existed. | 0+ |
| A4-027 | An `acm-cert-status.yml` workflow that lists certs in the region with domain, status, in-use-by ARNs. | dispatch-workflow | ACM validation hangs are silent; status dump in 5s. | 0+ |
| A4-028 | A `vpc-snapshot.yml` workflow that dumps VPCs, subnets, route tables, NAT, IGW, EIPs against the sandbox quota lines. | dispatch-workflow | Cap-exceed = account kill; preflight that is currently absent. | 0+ |
| A4-029 | A `quota-preflight.yml` workflow that asserts EC2 count<8, EIP<5, NAT<2, VPC<1 BEFORE allowing a mutating dispatch. | dispatch-workflow | A4-028 dumps; this asserts and fails fast — kill-prevention. | 0+ |
| A4-030 | A `tf-state-show.yml` workflow that takes an address and dumps state attributes, with secret keys redacted. | dispatch-workflow | Pairs with A4-007; redaction is the bit that makes it shareable in PR comments. | 0+ |
| A4-031 | A `compare-plan-to-cluster.yml` workflow that diffs `terraform plan` JSON against the live K8s/AWS resource shape. | dispatch-workflow | The "applied successfully ≠ change reached cluster" class — only a real diff catches it. | 1+ |
| A4-032 | A `composition-render.yml` workflow that runs `crossplane render` against a claim + composition + function locally on the runner and prints the rendered manifests. | dispatch-workflow | Bug 4 (Composition string transform missing `type: Format`) would have shown up in a render diff before any cluster touched it. | 2+ |
| A4-033 | A `crd-installed.yml` workflow that asserts every expected CRD (from a manifest list) is present + Established and prints missing ones. | dispatch-workflow | The PR #52 bootstrap-order bug (PlatformSecret CRD not yet installed when policy 09 was applied) would have aborted apply early. | 2+ |
| A4-034 | A `xrd-condition-table.yml` workflow that lists every XRD with `[Established, Offered]` status side-by-side in a markdown table. | dispatch-workflow | Phase 2b chainsaw fights with non-deterministic condition order would have been caught upstream. | 2+ |
| A4-035 | An `eso-trace.yml` workflow that walks ExternalSecret → ClusterSecretStore → ASM ARN → IRSA role → fetched value redaction. | dispatch-workflow | ESO has six known failure modes; one trace per claim would replace 30-min loops. | 1+ |
| A4-036 | A `cluster-state-bundle.yml` workflow that tars cluster state + AWS describes + Terraform state into a single artifact uploaded to the run. | dispatch-workflow | "Send me everything" without re-dispatching N times; would have shortened every session's startup. | 1+ |
| A4-037 | A `diagnose-bundle-extractor.py` Python script that parses the bundle from A4-036 and prints a triage report (which conditions are False and why). | shell-helper | Lets a subagent answer "what's wrong" in one call instead of paging through artifacts. | 1+ |
| A4-038 | A `wait-for-condition.yml` workflow with `(kind, name, ns, condition, timeout)` inputs that runs `kubectl wait` and exits with the resource's full yaml on timeout. | wait-helper | Standardizes the most-copied loop in integration tests; the integration `wait_for` had the silent-PASS bug per PR #59. | 1+ |
| A4-039 | A `wait-for-argocd-app.yml` workflow that polls a named Application for `Synced/Healthy` with deadline and dumps the operation state on fail. | wait-helper | Sync-wave races (handoff "wait ≥3 min for ArgoCD") were the slowest part of every session. | 1+ |
| A4-040 | A `wait-for-crossplane-claim.yml` that waits for a claim Ready + dumps the trace from A4-005 on timeout. | wait-helper | Composes A4-005 + A4-038 into the most-used phase-2 verification. | 2+ |
| A4-041 | A `tail-pod-logs.yml` workflow with `(ns, selector, since, tail)` inputs that prints logs of matching pods, ordered by pod name. | dispatch-workflow | Every diagnose workflow inlined this; one workflow eliminates redundancy. | 1+ |
| A4-042 | A `crossplane-function-logs.yml` workflow that tails patch-and-transform function pod logs filtered to a specific XR name. | dispatch-workflow | Composition function input rejections (Bug 4) only surface in function logs; filtered access would have caught them earlier. | 2+ |
| A4-043 | A `previous-container-logs.yml` workflow that lists every pod with restartCount>0 and prints `--previous` logs for each. | dispatch-workflow | OOM/Crash post-mortem; container restart wipes current logs. | 1+ |
| A4-044 | A `compare-helm-render.yml` workflow that runs `helm template` against pinned chart+values and diffs against live cluster manifests. | dispatch-workflow | Helm `manage_aws_auth_configmap=true` removal-bug class (Terraform fix history); render-vs-live is the only ground truth. | 1+ |
| A4-045 | An `argocd-app-tree.yml` workflow that prints parent → child Application tree with sync/health colored. | dispatch-workflow | App-of-apps debugging in #43 had no tree view. | 1+ |
| A4-046 | An `argocd-app-sync-history.yml` workflow that prints last N sync operations per app with success/fail and error messages. | dispatch-workflow | Bug 3's self-heal drift would have shown a sync-loop pattern in the history. | 1+ |
| A4-047 | A `kubectl-diff.yml` workflow that runs `kubectl diff -f <path>` against the cluster and prints the diff. | dispatch-workflow | "Applied successfully ≠ change reached cluster" — diff is the cheapest definitive answer. | 1+ |
| A4-048 | An `ingress-reachability.yml` workflow that hits every Ingress host from inside the cluster + outside (curl from runner) and prints status codes. | dispatch-workflow | Phase 1 bring-up included multiple "ingress doesn't work" iterations. | 1+ |
| A4-049 | A `dns-resolves.yml` workflow that runs `dig` against the authoritative nameservers (not public resolvers) for every ExternalDNS-managed name. | dispatch-workflow | Handoff calls out "dig against public resolvers" as wrong — codify the right pattern. | 1+ |
| A4-050 | A `gather-bug-evidence.sh` shell helper that, given a PR number, fetches the failing run's relevant step logs, related kubectl dumps, and produces a single Markdown evidence pack. | shell-helper | The `subagent-log-extraction` skill exists post-#62 because manual extraction was wasting full sessions. | 0+ |
| A4-051 | A `workflow-yaml-lint.sh` pre-push helper that runs `python -c "yaml.safe_load(...)"` + actionlint on every changed workflow. | shell-helper | PR #65's heredoc-broke-YAML issue silently rejected the workflow until merged ("Workflow does not have 'workflow_dispatch' trigger"). | 0+ |
| A4-052 | An `argocd-app-revision-check.yml` workflow that confirms each Application's `targetRevision` matches `HEAD` of `main`. | dispatch-workflow | Detects stale-revision pin bugs across the GitOps boundary. | 1+ |
| A4-053 | A `crossplane-version-skew.yml` workflow that prints core + every provider + every function version side-by-side. | dispatch-workflow | The v2.0.1 vs v2.2 compat question (handoff item 8) needs a real version snapshot before bumping. | 2+ |
| A4-054 | A `tf-drift-detect.yml` workflow that runs `terraform plan -detailed-exitcode` and surfaces drift between state and reality. | dispatch-workflow | Out-of-band cluster changes (Kyverno admission mutation) cause drift; nothing detected it pre-PR #52. | 0+ |
| A4-055 | A `dispatch-summary-poster.py` Python script that, after any dispatch finishes, posts a concise step-by-step result table as a commit comment. | shell-helper | `post-comment.py` bug (PR #39) shows this is fragile; a hardened version with structural tests is the right form. | 0+ |
| A4-056 | A `chainsaw-rerun-last-fail.yml` workflow that re-runs only the last-failed chainsaw scenario with verbose dump pre-teardown. | dispatch-workflow | PR #56 added dump-on-fail but each scenario reruns the whole suite; targeted rerun would save 5+ min per loop. | 2+ |
| A4-057 | An `audit-policy-fire-log.yml` workflow that prints every Kyverno PolicyReport result newer than `since`, grouped by policy. | dispatch-workflow | Audit-mode policies are useless without surfaced violations; nothing surfaced them. | 1+ |
| A4-058 | A `sec-pod-list.yml` workflow that lists Secrets across all namespaces with size + age + owner (ExternalSecret-managed or not). | dispatch-workflow | ESO debug needs "is the Secret actually in the namespace yet" — repeatedly hand-rolled. | 1+ |
| A4-059 | A `decision-tree-runner.sh` helper that walks an enumerated runbook (A4-010/011/012/013) and dispatches the right diagnose workflow per branch. | shell-helper | The runbooks alone don't reduce loops — automating "if X then dispatch Y" closes the loop. | 1+ |
| A4-060 | A `last-N-failed-runs.yml` summary workflow that lists the last 20 failed `terraform-test.yml` runs with first-failed-step + first error line. | dispatch-workflow | Pattern detection across runs is currently impossible without manual `gh run list` looping. | 0+ |
| A4-061 | A `which-secret-is-stale.yml` workflow that compares every ExternalSecret's last-sync time to its source ASM secret's last-modified time and prints stragglers. | dispatch-workflow | Rotation bugs are silent until consumers crash. | 1+ |
| A4-062 | A `terraform-data-trigger-audit.sh` lint that scans every `terraform_data` block and ensures its `triggers_replace` includes a `sha256` of every manifest body the local-exec applies. | shell-helper | PR #67's whole-bug root cause; encode as a lint instead of a one-off fix. | 0+ |
| A4-063 | A `provider-runtime-config-binding.yml` workflow that asserts each Provider Deployment's `serviceAccountName` matches its `DeploymentRuntimeConfig` spec. | dispatch-workflow | PR #68 root cause — Crossplane doesn't roll the Deployment on RuntimeConfig changes. | 2+ |
| A4-064 | A `serviceaccount-irsa-annotation-audit.yml` workflow that lists every SA with `eks.amazonaws.com/role-arn` annotation + asserts the role exists + trust matches. | dispatch-workflow | IRSA misannotation is silent until a pod tries AssumeRole. | 1+ |
| A4-065 | An `apply-and-verify-evidence.yml` workflow that wraps `terraform-test apply-and-verify` and POSTS the evidence-quote lines (per `verify-evidence-not-exit-codes`) directly into the workflow summary. | dispatch-workflow | Codifies the rule into the tool — agents can't forget to read the evidence if the dispatcher surfaces it. | 1+ |
| A4-066 | A `time-budget-tracker.sh` that records dispatch start, completion, and "wall clock burned" per phase, accumulated to a CSV. | shell-helper | Time-boxed sandbox; nobody knows how much budget remains. | 0+ |
| A4-067 | A `dispatch-history.md` doc auto-updated by a workflow listing every dispatch with phase/action/run-id/conclusion/duration. | dashboard | Session handoff needs this; currently reconstructed by hand from `gh run list`. | 0+ |
| A4-068 | A `compare-irsa-trust-to-tf.yml` workflow that pulls every IRSA role's trust JSON and diffs against `terraform/management/irsa.tf`'s declared subjects. | dispatch-workflow | Bug 5 (PR #66 SA-hash mismatch) and any future trust-drift caught in one dispatch. | 1+ |
| A4-069 | A `crossplane-mr-not-found.yml` workflow that, given an XR, lists `spec.resourceRefs` and asserts each MR exists; if not, prints composition reconciler logs filtered by XR name. | dispatch-workflow | The "XR has resourceRefs but MRs are NotFound" class (handoff Step 7) is unique and needs its own probe. | 2+ |
| A4-070 | A `gh-pr-checks-evidence.sh` helper that, for a PR, lists every check + its conclusion + a one-line excerpt of the failing step. | shell-helper | "PR is red — what failed?" answered in 2s instead of clicking through Actions UI. | 0+ |
| A4-071 | A `cluster-bootstrap-order-trace.yml` workflow that walks `terraform_data.argocd_bootstrap` → ArgoCD root app → child apps → sync-wave order, printing the order resources actually landed in. | dispatch-workflow | PR #52 bug class (CRD-installed-after-policy) needs ordering visibility. | 1+ |
| A4-072 | A `route53-zone-drift.yml` workflow that diffs the live hosted zone records against ExternalDNS's expected set computed from Ingress resources. | dispatch-workflow | DNS drift is invisible without diff. | 1+ |
| A4-073 | A `chainsaw-condition-coverage.sh` lint that asserts every chainsaw scenario uses `kubectl wait --for=condition=` rather than positional `assert:` blocks on `.status.conditions[]`. | shell-helper | Codifies the non-deterministic-order lesson (PR #53) as static check. | 2+ |
| A4-074 | A `pre-mutating-dispatch.sh` wrapper that requires a recent `phase-2-diagnose` or `status-snapshot` run before allowing apply-and-verify. | shell-helper | `diagnose-before-mutate` skill from #62 — enforce, don't suggest. | 1+ |
| A4-075 | A `terraform-apply-line-summary.py` script that parses the apply log and prints the resource-counts line + per-resource "Creation complete" lines in a 5-row table. | shell-helper | The "Apply complete: 0 added" misread (run 26354235231) was specifically log-parsing failure; structured view fixes it. | 0+ |
| A4-076 | A `silent-pass-detector.sh` lint that scans every `tests/integration/*.sh` for `set -uo pipefail` without `-e`, plus assignment to bash readonly builtins. | shell-helper | PR #59 added two of these; the bundle should be in one lint that names the bug class. | 0+ |
| A4-077 | A `helm-values-effective-diff.yml` workflow that prints the merged Helm values for each release vs the values committed under `platform-services/`. | dispatch-workflow | Helm override drift was an unverified suspicion repeatedly raised in retros. | 1+ |
| A4-078 | An `eks-control-plane-log-tail.yml` workflow that tails the EKS control plane CloudWatch log group for the last N minutes. | dispatch-workflow | Even without enabling all log types, the default subset would have answered IRSA + RBAC questions; nothing tailed it. | 1+ |
| A4-079 | A `cloudtrail-recent-failures.yml` workflow that prints `LookupEvents` for failed API calls in the last hour, grouped by API + error code. | dispatch-workflow | Permission errors (AssumeRoleWithWebIdentity 403, ASM denied) are post-hoc only visible here. | 1+ |
| A4-080 | An `iam-policy-simulator.yml` workflow that takes a role ARN + action + resource and prints `simulate-principal-policy` results. | dispatch-workflow | "Why does this AssumeRole fail" answered before pod even runs. | 1+ |
| A4-081 | A `bug-class-registry-check.sh` lint that asserts every row in `ai/TESTING-PLAN.md`'s bug-class registry has a corresponding `tests/unit/test_<class>.sh`. | shell-helper | The registry exists but enforcement that defending tests follow doesn't. | 0+ |
| A4-082 | A `dispatch-recipe-cookbook.md` documenting the exact `gh workflow run` + `gh run watch` + `gh run view --log` commands for every workflow in the repo, with copy-paste-ready inputs. | runbook | Every session relearns these from scratch. | 0+ |
| A4-083 | An `mr-status-table.yml` workflow that lists every Crossplane managed resource with kind, name, Ready, Synced, atProvider.arn, age. | dispatch-workflow | Per-MR `kubectl get` was burned hundreds of times; one wide-table view would replace the pattern. | 2+ |
| A4-084 | A `dispatch-fast-fail.yml` mode for terraform-test that runs only init+validate+plan (no apply) and finishes in <2 min for sanity checks. | dispatch-workflow | apply-and-verify is 15 min; many checks needed only plan-time evidence. | 0+ |
| A4-085 | A `sync-on-demand.yml` workflow that hits ArgoCD `/api/v1/applications/<name>/sync` to force a refresh without waiting the 3-min poll. | dispatch-workflow | The 3-min ArgoCD refresh was the dominant phase-2 loop cost. | 1+ |
| A4-086 | A `cluster-secret-store-test.yml` workflow that runs ESO's `kubectl create secret --dry-run` flow against the CSS to prove fetch works. | dispatch-workflow | ESO failures usually masked as "Secret not found"; this proves vs disproves. | 1+ |
| A4-087 | A `pod-projected-token-decode.yml` workflow that exec's into a target pod, reads `/var/run/secrets/eks.amazonaws.com/serviceaccount/token`, and prints decoded claims. | dispatch-workflow | The other half of IRSA debug; A4-006 reads server side, this reads pod side. | 1+ |
| A4-088 | A `crossplane-events-by-xr.yml` workflow that lists events with `involvedObject.name == <xr>`. | dispatch-workflow | Composition events scrolled off in the noise; XR-scoped view is the answer. | 2+ |
| A4-089 | A `composition-input-validator.yml` workflow that runs the function's input schema against a candidate composition body, offline on the runner. | dispatch-workflow | Bug 4 (string transform `type` missing) failed validation; offline check catches it pre-apply. | 2+ |
| A4-090 | A `repo-grep-bug-history.sh` helper that, given an error string, greps retrospectives + handoff + closed-PR comments for prior occurrences. | shell-helper | Bug 3 recurred across sessions because nobody searched "have I seen this before". | 0+ |
| A4-091 | A `dispatch-watch-tee.sh` wrapper that streams a workflow's live logs to stdout AND a local file, so the agent can grep while the run is still in progress. | shell-helper | Currently the agent waits for completion to read logs; tee+grep cuts loop time. | 0+ |
| A4-092 | A `phase-bring-up-evidence-template.md` checklist of every line the agent should quote-back when reporting "phase N applied" (per `verify-evidence-not-exit-codes`). | runbook | Codifies the rule; future sessions cannot forget to quote evidence. | 0+ |
| A4-093 | A `dispatch-on-merge.yml` workflow that auto-dispatches the heavy verify when a PR labeled `verify-on-merge` lands on main, with the SHA pre-pinned. | dispatch-workflow | Verify-then-PR pattern requires the agent remember; auto-dispatch closes the loop. | 0+ |
| A4-094 | A `policy-mutate-detector.yml` workflow that diffs every Kyverno-managed ClusterPolicy against its repo source, listing default-filled fields. | dispatch-workflow | Bug 3 was Kyverno default-filling fields; one detector eliminates the bug class. | 1+ |
| A4-095 | A `gh-comment-extractor.sh` helper that pulls all review comments + thread replies from a PR into a single Markdown file for context loading. | shell-helper | Manual reconstruction of PR threads was a per-session tax. | 0+ |
| A4-096 | A `subagent-prompt-template.md` for diagnose-log extraction with: file path, schema, three specific questions, 1500-word cap, verbatim-quote directive. | runbook | Codifies the working pattern from PR #60 diagnose subagent (per `subagent-log-extraction` skill). | 0+ |
| A4-097 | A `dispatch-with-input-table.sh` helper that pretty-prints workflow inputs before dispatch so a typo in `phase=mananagement` is caught pre-run. | shell-helper | One typo costs a full 15-min wait. | 0+ |
| A4-098 | A `cluster-tear-down-preview.yml` workflow that lists every resource that WOULD be deleted by `tear down phase X` per AGENTS.md §5.1 — dry-run-only. | dispatch-workflow | Teardown invariants are scary; a preview is safety net. | 2+ |
| A4-099 | An `orphan-aws-resource-scan.yml` workflow that lists AWS resources tagged `managed-by=k8-platform` not referenced by any TF state or Crossplane XR. | dispatch-workflow | Orphan resources count against sandbox cap = account-kill risk. | 0+ |
| A4-100 | A `golden-snapshot-diff.yml` workflow that captures and saves a known-good cluster-state snapshot per phase, and diffs current state against it on demand. | dispatch-workflow | "Worked yesterday, broken today" answered in one dispatch. | 1+ |

## Cross-review additions

Additive, collaborative extensions from every other agent and the primary orchestrator. No criticism — only amplifications, related ideas, pairings, and meta-observations.

### from A1

| ID | Idea | Category | Justification | Applies to phase |
|---|---|---|---|---|
| A1→A4-001 | Pair A4-001/002/041 (read/exec/tail workflows) into a single `kubectl-bridge.yml` that takes a verb + structured args and returns JSON output for machine consumption. | dispatch-workflow-extension | One canonical bridge is easier to allow-list and to teach skills to use. | 1+ |
| A1→A4-002 | Extend A4-004 (status snapshot) to upload its bundle to S3 with a timestamped key so historical "what did the cluster look like at 14:32" queries become trivial. | status-snapshot-extension | Time-machine debugging is impossible without retained snapshots. | 1+ |
| A1→A4-003 | Pair A4-005 (crossplane-trace) with an emitted Mermaid sequence diagram in the workflow summary so the chain is visual, not textual. | dispatch-workflow-extension | Mermaid in GH UI makes 8-hop chains comprehensible at a glance. | 2+ |
| A1→A4-004 | Add a `correlation-id-injector.sh` that injects a UUID into every dispatch as an env var, then propagates it via labels/annotations into k8s objects + log lines for cross-system trace. | shell-helper | Distributed-trace correlation across terraform/k8s/AWS is the single biggest debug-time multiplier. | 0+ |
| A1→A4-005 | Pair A4-016 (phase-state-probe) with a saved baseline JSON committed to repo so the probe's diff highlights *what changed*, not just current state. | dispatch-workflow-extension | Current-state alone is useless without expected-state. | 0+ |
| A1→A4-006 | Extend A4-036 (cluster-state-bundle) by including a `prom-snapshot.tar.gz` (Prometheus TSDB block of last 1h) so post-mortems include time-series, not just point-in-time. | dispatch-workflow-extension | Most bugs are best understood as a time-series, not a snapshot. | 1+ |
| A1→A4-007 | Pair A4-035 (eso-trace) with a `kubectl debug` ephemeral container that mounts the same SA token and runs `aws secretsmanager get-secret-value` from inside the pod — proves the path end-to-end. | dispatch-workflow-extension | The only test of the *actual* network path the ESO controller takes. | 1+ |
| A1→A4-008 | Add a `flamegraph-of-reconcile.yml` that captures controller pprof for 30s and renders a flame graph for slow reconciles. | dispatch-workflow | Composition-function slowness has no other observability path. | 2+ |
| A1→A4-009 | Pair A4-040 (wait-for-claim with trace on timeout) with auto-opening a draft GitHub issue containing the trace + correlation-id when timeout fires. | wait-helper-extension | Closes the loop: timeout → triage ticket with all evidence pre-attached. | 2+ |
| A1→A4-010 | Extend A4-067 (dispatch-history dashboard) with cost attribution: dollars per phase per session, plus AWS API call counts. | dashboard-extension | Sandbox quota visibility + cost-per-iteration drives optimization. | 0+ |
| A1→A4-011 | Add an `slo-budget-tracker.yml` that records phase-N-bring-up duration and alerts when 7-day p95 exceeds threshold — turns perf creep visible. | dispatch-workflow | Bring-up perf degrades silently across many small changes. | 1+ |
| A1→A4-012 | Pair A4-068 (irsa-trust-vs-tf diff) with an automatic GitHub Issue creation if drift > 0, with the diff inline — passive observability. | dispatch-workflow-extension | Drift detected but unfiled is drift unfixed. | 1+ |
| A1→A4-013 | Extend A4-094 (policy-mutate-detector) by storing the diff in a CloudWatch metric so drift over time is graphable. | dispatch-workflow-extension | One-shot detection is good; trend detection is better. | 1+ |
| A1→A4-014 | Add a `dump-everything-on-pager.sh` that, when a CloudWatch alarm fires, auto-dispatches A4-004 + A4-036 + A4-079 and posts the bundle to a Slack/issue thread. | shell-helper | Pager-driven evidence capture eliminates "I missed the moment of failure". | 1+ |
| A1→A4-015 | Pair A4-079 (cloudtrail-recent-failures) with a saved Athena query library against the CloudTrail S3 bucket for deeper retrospectives beyond the 90-day LookupEvents window. | dispatch-workflow-extension | Long-tail debugging needs Athena, not LookupEvents. | 1+ |
| A1→A4-016 | Extend A4-100 (golden-snapshot-diff) with an automatic per-merge snapshot capture, so every commit on main has a known-good baseline. | dispatch-workflow-extension | "What changed between v3.2.4 and v3.2.5" answerable instantly. | 1+ |

### from A2

| ID | Idea | Category | Justification | Applies to phase |
|---|---|---|---|---|
| A2→A4-001 | Re-implement A4-001 (`read-cluster.yml`) as a local-shell `read-cluster` script that A2 e2e tests use as the canonical "read live resource" primitive. | test-oracle-promotion | One read path, used both interactively and in tests. | 1+ |
| A2→A4-002 | Promote A4-005 (`crossplane-trace`) to a chainsaw `error:` hook so every failed scenario emits the full claim→XR→MR trace. | test-oracle-promotion | Failure post-mortem is structured. | 2+ |
| A2→A4-003 | Adopt A4-006 (`irsa-check`) as the precondition for every A2 IRSA test (A2-003..A2-006) — refuse to run if check fails. | test-oracle-promotion | Tests fail loudly at preflight instead of producing confusing errors mid-run. | 1+ |
| A2→A4-004 | Use A4-019 (provider-health) as the gating oracle for A2-022 chaos test recovery assertions. | test-oracle-promotion | Reuses one source of truth for "Crossplane is healthy". | 2+ |
| A2→A4-005 | Add a smoke test on top of A4-022 (kyverno-explain) that feeds a known-violating manifest through and asserts the explainer reports the expected failing rule. | smoke-test | Validates the explainer itself. | 1+ |
| A2→A4-006 | Pair A4-024 (route53 snapshot) with A2-020 (ExternalDNS round-trip) to assert the new record appears in the next snapshot diff. | extension | Snapshot tool gains a regression harness. | 1+ |
| A2→A4-007 | Promote A4-025 (NLB target health walker) to the oracle for A2-024 ingress chaos test. | test-oracle-promotion | One walker, two consumers. | 1+ |
| A2→A4-008 | Add an e2e test that A4-032 (composition-render) and the live Composition apply produce byte-identical rendered MRs. | new-scenario | Catches render-vs-apply skew (a known v2 footgun). | 2+ |
| A2→A4-009 | Use A4-035 (eso-trace) as the failure-mode oracle for A2-053 (ASM rotation propagation). | test-oracle-promotion | Six-failure-mode walker collapses into a one-call assertion. | 2+ |
| A2→A4-010 | Pair A4-038 (wait-for-condition) with A1-021 helper to standardize the wait primitive across all A2 soak tests. | extension | Eliminates per-test wait drift. | 1+ |
| A2→A4-011 | Build a regression suite on A4-053 (crossplane-version-skew) that runs nightly and pages on any unexpected drift between core/providers/functions. | smoke-test | Version-skew bugs are silent until they bite; nightly catches them early. | 2+ |
| A2→A4-012 | Extend A4-061 (which-secret-is-stale) into an SLO alarm: any ExternalSecret > 2x refreshInterval since last sync fires a synthetic CW metric. | extension | Stale secrets become observable. | 1+ |
| A2→A4-013 | Use A4-064 (SA IRSA annotation audit) as the live-cluster half of A2-005/A2-006 invariants. | test-oracle-promotion | Bidirectional invariant gets one tool. | 1+ |
| A2→A4-014 | Pair A4-068 (IRSA trust vs TF diff) with A2-003 simulate-principal-policy to catch both subject and permission drift in one run. | extension | Trust + permission coverage in one gate. | 1+ |
| A2→A4-015 | Promote A4-080 (IAM policy simulator) to a contract test that runs for every IRSA role on every PR. | test-oracle-promotion | Pre-flight semantic check instead of mid-deploy AccessDenied. | 1+ |
| A2→A4-016 | Add a chainsaw scenario built on A4-087 (pod projected-token decode) to assert every platform SA's projected token carries the expected `aud` claim. | new-scenario | Tokens silently misconfigured at the pod side is invisible without this. | 1+ |
| A2→A4-017 | Use A4-100 (golden-snapshot diff) as the e2e regression oracle after every A2 chaos test — assert post-recovery state matches the golden. | test-oracle-promotion | Recovery is defined as "return to golden", not just "Ready=True". | 1+ |

### from A3

| ID | Idea | Category | Justification | Applies to phase |
|---|---|---|---|---|
| A3→A4-001 | Pair A4-006 (`irsa-check.yml`) with a permanent unit test that asserts every IRSA role in `terraform/management/irsa.tf` is exercised by at least one workflow input fixture — would have made Bug 5 a build-time failure. | regression-extension | Locks the bug class into the lint surface, not only the diagnose surface. | 1+ |
| A3→A4-002 | Extend A4-008 (`terraform-plan-diff.yml`) to emit a structured "expected-zero-resources" flag the agent can set; if non-zero, the run must explicitly acknowledge. | regression-extension | Prevents the run-26354235231 misread class from recurring. | 0+ |
| A3→A4-003 | A4-013 runbook for "Apply complete: 0 added" could be paired with `silent-pass-detector.sh` (A4-076) so the runbook entry is enforced not just documented. | regression-extension | Doc-only lessons rot; lint-enforced lessons don't. | 0+ |
| A3→A4-004 | A4-022 Kyverno explain workflow should ship with a corpus of known-mutating policies (incl. policy 09) and assert mutation behavior is unchanged across PRs. | regression-extension | Bug 3 specifically; corpus locks behavior. | 1+ |
| A3→A4-005 | Pair A4-033 (CRD-installed assertion) with a phase-2 bootstrap-order regression test that intentionally races CRD install vs policy apply to prove ordering. | regression-extension | PR #52 root cause as positive test. | 2+ |
| A3→A4-006 | A4-038 `wait-for-condition.yml` should refuse to claim success if zero condition rows ever appeared (counts the silent-PASS PR #59 bug class explicitly). | regression-extension | Direct mitigation of the wait_for silent-PASS bug. | 1+ |
| A3→A4-007 | Extend A4-053 (Crossplane version skew) to record image digests + a baseline file in repo so any silent upgrade is caught as a digest diff. | regression-extension | The v2.0.1 vs v2.2 question becomes a tracked, diffable fact. | 2+ |
| A3→A4-008 | A4-062 (terraform_data triggers_replace audit) deserves a CI lint that also fires on `null_resource` blocks — historical equivalent footgun. | regression-extension | Generalizes PR #67's bug class. | 0+ |
| A3→A4-009 | A4-063 (provider runtime config binding assert) could be a chainsaw step that intentionally mutates the RuntimeConfig and asserts the binding detector fires. | regression-extension | Locks PR #68 as a permanent guard. | 2+ |
| A3→A4-010 | Pair A4-073 (chainsaw condition coverage lint) with a once-per-PR run that diffs the lint's findings against an allowlist file so new violations require explicit ack. | regression-extension | Codifies PR #53 lesson with an evolving allowlist. | 2+ |
| A3→A4-011 | A4-076 silent-pass detector could grow a sub-rule for "test asserts only on substring of kubectl output without `--no-headers`" — another known false-PASS shape. | regression-extension | Expands the detector to the next-most-common silent-pass shape. | 0+ |
| A3→A4-012 | Extend A4-094 (policy mutate detector) to also diff CRD `default:` blocks — defaults are another silent-fill source distinct from Kyverno. | regression-extension | Generalizes Bug 3 to non-Kyverno defaulting sources. | 1+ |
| A3→A4-013 | A4-100 golden snapshot diff workflow should encode the snapshot per region (us-east-1 vs us-west-2) so cross-region drift is detected separately. | regression-extension | Sandbox supports both regions; cross-region silent drift is currently invisible. | 1+ |
| A3→A4-014 | Add a runbook entry "ESO ASM 403 vs ESO secret-not-found" to the A4-010 IRSA debug runbook — these two error strings are confusable and historically misdiagnosed. | regression-extension | Disambiguates a recurring false-trail. | 1+ |

### from A5

| ID | Idea | Category | Justification | Applies to phase |
|---|---|---|---|---|
| A5→A4-001 | A4-001 `read-cluster.yml` becomes a single sandbox alias `kc-read`; under A5 orchestration the dispatch wrapper is moot, but the JSON output schema is still valuable for subagent consumption. | refactor | Preserves the contract, drops the substrate. | 1+ |
| A5→A4-002 | A4-004 `status-snapshot.yml` should run as a background subagent at session start (A5-007 pattern), populating `/tmp/snapshot/` so any later question reads from a hot cache. | prewarm+cache | Snapshot is ready before asked. | 1+ |
| A5→A4-003 | A4-005 crossplane-trace can stream to A5-027 event bus per layer-traversal, enabling reactive subagents downstream. | pipeline | Trace becomes a stream not a dump. | 2+ |
| A5→A4-004 | A4-016 phase-state-probe is the same probe as A5→A1-002 — run it as one parallel `make -j7` job. | dedupe | One probe, two callers (A1 + A4). | 0+ |
| A5→A4-005 | A4-021 pod-restart-watch should be a long-lived watcher subagent subscribed to the cluster-events stream, not a one-shot. | pipeline | Push not poll. | 1+ |
| A5→A4-006 | A4-036 cluster-state-bundle should run in parallel with apply so the bundle is up-to-date on apply completion. | speculative-probe | Bundle ready when needed. | 1+ |
| A5→A4-007 | A4-038/A4-039/A4-040 wait-for-* helpers are all instances of A5-043 (`await condition:... && cmd`) — implement once, reuse. | dedupe | Unified DSL. | 1+ |
| A5→A4-008 | A4-058 secret-staleness scan runs in parallel across all namespaces (one subprocess per ns) instead of one sweep. | parallel-probe | NS-count parallelism free. | 1+ |
| A5→A4-009 | A4-068 IRSA-trust diff is independent per role — fan out with `xargs -P` over `aws iam list-roles` output. | parallel-probe | Cuts N-role diff to max-role. | 1+ |
| A5→A4-010 | A4-082 dispatch cookbook should also document the parallel-fan-out idiom for every read workflow. | runbook | Embeds A5 patterns in muscle memory. | 0+ |
| A5→A4-011 | A4-100 golden-snapshot-diff plus A5-040 kind rehearsal cluster: keep one golden per cluster type, diff in parallel. | parallel-test | Multi-cluster regression in one round. | 1+ |
| A5→A4-012 | A4-099 orphan-resource scan can run continuously in background as a quota guardian (composes with A1-037 alarm). | pipeline | Continuous quota safety. | 0+ |
| A5→A4-013 | A4-060 last-N-failed-runs becomes "tail the event bus for failure events" once A5-027 is in place — zero gh-api round trips. | pipeline | Event bus subsumes the workflow. | 0+ |

### from A6

| ID | Idea | Category | Justification | Applies to phase |
|---|---|---|---|---|
| A6→A4-001 | A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. | simplification | One generic bridge subsumes six specific ones. | 1+ |
| A6→A4-002 | A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. | simplification | One generic AWS bridge replaces seven specific dumps. | 0+ |
| A6→A4-003 | Now that the sandbox runs kubectl/aws directly, *every* `*.yml` dispatch workflow in A4 (~70 items) becomes a shell script — retire the dispatch-workflow pattern entirely once the new sandbox is the norm. | refactor | Dispatch workflows existed only because of the prior constraint. | 0+ |
| A6→A4-004 | A4-014 (dispatch-then-read-step) + A4-017 (dispatch-and-poll) + A4-091 (watch-tee) collapse into one canonical `gh-run.sh` helper. | refactor | Three helpers, one job. | 0+ |
| A6→A4-005 | A4-009 (workflow-log-fetch) + A4-015 (grep-workflow-log) + A4-050 (gather-bug-evidence) + A4-070 (gh-pr-checks-evidence) form one "GH log toolkit" — ship as one script with subcommands. | refactor | Unified surface. | 0+ |
| A6→A4-006 | A4-038 (wait-for-condition) + A4-039 (wait-for-argocd) + A4-040 (wait-for-claim) collapse into one parameterized waiter mirroring A1-021. | simplification | Same loop, three callers. | 1+ |
| A6→A4-007 | A4-010..A4-013 (four runbooks) merge into one `runbooks/` directory with a shared decision-tree format; A4-059 (decision-tree-runner) then drives all of them. | refactor | Consistent format, single runner. | 0+ |
| A6→A4-008 | A4-005's crossplane-trace, once it exists as a script (per A1-019), retires the dispatch-workflow form of the same idea. | simplification | One implementation. | 2+ |
| A6→A4-009 | A4-067 (auto-updated dispatch-history.md) lets us delete the manual "what did I run this session?" reconstruction in handoff. | simplification | Auto > manual. | 0+ |
| A6→A4-010 | A4-029's quota-preflight + A1-035's local quota-check are duplicates — keep one, drop the other. | refactor | One preflight. | 0+ |
| A6→A4-011 | A4-075 (apply-line-summary) + A4-008 (plan-diff summary) form one "tf-output-summarizer" — collapse. | refactor | Same parser, two outputs. | 0+ |
| A6→A4-012 | A4-051 (workflow-yaml-lint) + A3-014 (workflow YAML safe_load lint) are the same lint — keep one. | simplification | One yaml validator. | 0+ |
| A6→A4-013 | A4-068's IRSA trust-vs-tf diff replaces the entire post-#66 "manually check the role" runbook step. | simplification | Diff is the runbook. | 1+ |
| A6→A4-014 | A4-036 + A4-037 (bundle producer + extractor) lets us delete every ad-hoc "describe everything" workflow scattered across the .github/workflows/ tree. | refactor | One bundle format. | 1+ |
| A6→A4-015 | A4-076 (silent-pass-detector) and A3-004 + A3-005 are the same lint family — merge into one `lint_shell_strictness.sh`. | refactor | Cross-file dedupe. | 0+ |

### from PRIMARY

| ID | Idea | Category | Justification | Applies to phase |
|---|---|---|---|---|
| P→A4-001 | A `scripts/diagnose/snapshot.sh` that emits a tarball capturing every relevant cluster + AWS read-only state at a moment in time, named by SHA — agent can paste a download link or untar locally. | diagnose-script | Replaces 6 different one-off diagnose workflows with one durable artifact. | 1+ |
| P→A4-002 | A retro template field "what command did we wish existed?" — every retro author lists 1-3 commands; once a quarter we promote the top votes into `scripts/`. | runbook | Turns retro signal into actual tooling without requiring a separate brainstorming session. | 0+ |
| P→A4-003 | A `scripts/diagnose/why-not-ready.sh <resource>` that walks status conditions of the resource and every dependency until it finds the leaf cause, prints a one-paragraph English summary. | diagnose-script | Collapses 30-minute "trace through resourceRefs" loops into one command. | 1+ |
| P→A4-004 | Pre-canned CloudWatch Logs Insights queries committed under `scripts/insights/` (one .json per common question — "IRSA AssumeRole failures last hour", "Crossplane reconcile errors for resource X"). | logs-insights-query | Removes the "what was that query syntax again" tax every debug loop. | 1+ |

