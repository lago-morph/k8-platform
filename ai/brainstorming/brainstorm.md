# Brainstorm corpus — rendered view

_Generated from `ai/brainstorming/brainstorm.json` by `ai/brainstorming/tools/render_markdown.py`. This file is a derived view — every value here is also in the source markdown (A1-A6 + cross-review-from-\*) and the JSON. Do not edit by hand; rebuild via `python3 ai/brainstorming/tools/render_markdown.py`._

| Field | Value |
|---|---|
| Session | 2026-05-25 six-agent brainstorm fanout (A1-A6 + cross-review + primary pass) |
| Date | 2026-05-25 |
| Branch | `claude/determined-pasteur-53fAN` |
| PR | #73 |
| Agents | 6 |
| Ideas | 400 |
| Comments (distinct rows) | 466 |
| Comment ↔ idea associations | 430 |
| General (unanchored) comments | 122 |

## Table of contents

- [A1 — Max-capability debug tools](#a1--max-capability-debug-tools)
- [A2 — Max-capability integration tests](#a2--max-capability-integration-tests)
- [A3 — Test gap mining](#a3--test-gap-mining)
- [A4 — Debug gap mining](#a4--debug-gap-mining)
- [A5 — Orchestration parallelism opportunities](#a5--orchestration-parallelism-opportunities)
- [A6 — Cruft removal targets](#a6--cruft-removal-targets)

## A1 — Max-capability debug tools

> **Mandate.** Brainstorm debug, observability, and CloudWatch tooling that exploits full AWS admin + direct kubectl + ArgoCD/Keycloak API + unlimited egress to answer "what is happening with X" in seconds across phases 0–6.

_Source file: [`ai/brainstorming/A1-debug-tools-max-capability.md`](../../ai/brainstorming/A1-debug-tools-max-capability.md)_

_Idea count: 70_

| ID | Phase | Idea (with category and justification) | Comments (bulleted by commenter) |
|---|---|---|---|
| `A1-001` | 0+ | `scripts/whereami.sh` one-shot that prints account ID, region, EKS name, zone, kubectl ctx, ArgoCD URL, Crossplane version.<br><br><sub>_debug-assist_ · First thing every session needs; replaces five separate `aws`/`kubectl` invocations._</sub> | • **A2** (A2→A1-001 · _test-oracle-promotion_ · phase 0+): Turn `scripts/whereami.sh` (A1-001) into a JSON-emitting mode so e2e tests can assert account/region/cluster fixtures match `terraform.tfvars` before running. — _Reusing a debug script as a precondition gate prevents tests from running against the wrong sandbox._<br>• **A5** (A5→A1-001 · _cache+prewarm_ · phase 0+): Run A1-001 `whereami.sh` once at session start AND export its outputs to `/tmp/session.env` so every subagent sources it instead of re-probing. — _Avoids N subagents hammering STS/EKS describe at session start._<br>• **A6** (A6→A1-001 · _refactor_ · phase 0+): Once `scripts/whereami.sh` (A1-001) lands, retire the per-script preambles that re-echo account/region/cluster in every diag tool. — _Single source; preamble noise vanishes._ |
| `A1-002` | 0+ | `scripts/phase-status.sh` that walks phases 0–6 and prints state (`not-coded` / `code-only` / `applied` / `verified`) by probing live resources, not the handoff.<br><br><sub>_debug-assist_ · Handoff state drifts; live probe is ground truth (per AGENTS.md §8.1)._</sub> | • **A2** (A2→A1-002 · _test-oracle-promotion_ · phase 1+): Promote `phase-status.sh` (A1-002) to a chainsaw `assert:` oracle that verifies each phase's `verified` state inside a smoke test. — _Live-probe truth becomes an automated regression guard, not just a human aid._<br>• **A3** (A3→A1-001 · _regression-extension_ · phase 0+): Extend A1-002 `phase-status.sh` to also emit a JSON manifest a regression test can diff against the previous run, turning live probes into a drift oracle. — _Doubles the debug tool as a recurring test artifact; would have surfaced Bug 3 (Kyverno drift) on cadence._<br>• **A5** (A5→A1-002 · _parallel-probe_ · phase 0+): Phase-status probe (A1-002) can fan out per-phase with `make -j7 probe-phase-{0..6}`; each probe is independent. — _Critical path becomes max-probe-time not sum._<br>• **A6** (A6→A1-002 · _simplification_ · phase 0+): When `scripts/phase-status.sh` (A1-002) is the oracle, delete the Environment State table in `ai/handoff.md` in favor of a generated include. — _Removes the drift class A1-002 was built to eliminate._ |
| `A1-003` | 1+ | Enable EKS control-plane logging (api, audit, authenticator, controllerManager, scheduler) on cluster creation in `terraform/management/eks.tf`.<br><br><sub>_cloudwatch-setup_ · Authenticator/audit logs are the only way to debug IRSA & RBAC denials post-hoc._</sub> | • **A2** (A2→A1-003 · _smoke-test_ · phase 1+): Add an integration test that, after enabling EKS control-plane logging (A1-003), writes a synthetic `system:anonymous` request and asserts the audit log group receives the line within 60s. — _Proves the logging pipeline actually emits, not just that the cluster claims it's enabled._<br>• **A4** (A4→A1-003 · _runbook_ · phase 1+): Have EKS control-plane logging include a `scripts/eks-logs-tail.sh` thin wrapper that pre-selects the audit log group + last 5 min — surfaces what A1-003 enables. — _Enabling logs without a paired reader leaves them undiscovered._<br>• **A5** (A5→A1-003 · _pipeline_ · phase 1+): Stream EKS control-plane logs (A1-003) live into the conductor's event bus (A5-027) so audit denials trigger downstream subagents instantly. — _Turns CloudWatch logs into push not pull._<br>• **A6** (A6→A1-003 · _grep RBAC" workaround in `tests/integration/`._ · phase 1+): EKS control-plane logging (A1-003) lets us delete every workflow-side "kubectl get events — _refactor &#124; Audit log is durable; pod-side scraping was a hack._ |
| `A1-004` | 0+ | Enable VPC flow logs to CloudWatch in `terraform/base/vpc.tf` with a 7-day retention.<br><br><sub>_cloudwatch-setup_ · NAT/SG/route bugs are invisible without flow logs; cheap insurance._</sub> | _(no comments)_ |
| `A1-005` | 0+ | CloudTrail trail to a dedicated CloudWatch log group with 7-day retention.<br><br><sub>_cloudwatch-setup_ · Post-mortem any IAM/API failure with a single Logs Insights query._</sub> | _(no comments)_ |
| `A1-006` | 1+ | Logs Insights saved query: "AssumeRoleWithWebIdentity failures last 1h, grouped by role and SA subject".<br><br><sub>_logs-insights-query_ · This is the canonical IRSA-misconfig signature; recurring root cause._</sub> | • **A2** (A2→A1-010 · _smoke-test_ · phase 1+): Add a contract test that every saved Logs Insights query (A1-006..A1-009, A1-061..A1-062) parses and returns ≤1 row when run against a known-good fixture stream. — _Saved queries rot silently when log shapes change; a regression test pins them._<br>• **A4** (A4→A1-006 · _shell-helper_ · phase 1+): Add an `irsa-failure-quick-quote.sh` that runs the A1-006 Logs Insights query and emits a single verbatim quote line per failure — feeds the verify-evidence-not-exit-codes skill. — _Evidence-quoting cannot be skipped if the tool emits the quote ready-to-paste._<br>• **A6** (A6→A1-004 · _simplification_ · phase 1+): Logs Insights saved queries (A1-006..A1-009) supersede the bash-grep helpers in `scripts/diag-component.sh`; retire those subcommands. — _One query language, not two._ |
| `A1-007` | 1+ | Logs Insights saved query: "EKS audit log denies grouped by user/group/verb last 1h".<br><br><sub>_logs-insights-query_ · RBAC misconfig diagnosis in 5 seconds._</sub> | _(no comments)_ |
| `A1-008` | 2+ | Logs Insights saved query: "Crossplane core reconcile errors filtered by XR name".<br><br><sub>_logs-insights-query_ · Replaces `kubectl logs ... &#124; grep`; survives pod restarts._</sub> | _(no comments)_ |
| `A1-009` | 1+ | Logs Insights saved query: "ESO sync failures grouped by ExternalSecret name".<br><br><sub>_logs-insights-query_ · ESO failures are silent until consumer pod crashes._</sub> | • **A2** (A2→A1-010 · _smoke-test_ · phase 1+): Add a contract test that every saved Logs Insights query (A1-006..A1-009, A1-061..A1-062) parses and returns ≤1 row when run against a known-good fixture stream. — _Saved queries rot silently when log shapes change; a regression test pins them._<br>• **A6** (A6→A1-004 · _simplification_ · phase 1+): Logs Insights saved queries (A1-006..A1-009) supersede the bash-grep helpers in `scripts/diag-component.sh`; retire those subcommands. — _One query language, not two._ |
| `A1-010` | 1+ | CloudWatch metric filter on EKS audit log for `system:anonymous` requests → alarm.<br><br><sub>_metric-filter_ · Detects misconfigured webhooks/ingress that bypass auth._</sub> | _(no comments)_ |
| `A1-011` | 2+ | CloudWatch metric filter on Crossplane core logs for `reconcile error` → counter metric per provider.<br><br><sub>_metric-filter_ · Quantifies churn; flips an alarm on sustained failure._</sub> | _(no comments)_ |
| `A1-012` | 1+ | CloudWatch alarm: NLB `UnHealthyHostCount > 0` for 2 min.<br><br><sub>_alarm_ · Ingress death is otherwise invisible until users complain._</sub> | _(no comments)_ |
| `A1-013` | 2+ | CloudWatch alarm: ASM `SecretRotationFailed` on any k8-platform/* secret.<br><br><sub>_alarm_ · Phase 2 PlatformSecret rotation should be observable._</sub> | _(no comments)_ |
| `A1-014` | 1+ | CloudWatch dashboard "k8-platform overview" with EKS node CPU/mem, NLB request rate, ArgoCD sync status (custom metric), Crossplane reconcile error rate.<br><br><sub>_dashboard_ · Single-pane glance; nobody opens 8 tabs in a hurry._</sub> | • **A5** (A5→A1-004 · _parallel-apply_ · phase 1+): Run A1-014 dashboard creation in parallel with A1-015 + A1-016; all three are independent CloudWatch puts. — _Cuts dashboard-bootstrap from 3x serial to 1x._ |
| `A1-015` | 1+ | CloudWatch dashboard "IRSA debug" with per-role AssumeRole success/failure counts.<br><br><sub>_dashboard_ · IRSA is the #1 bug class; dashboard surfaces it instantly._</sub> | • **A5** (A5→A1-004 · _parallel-apply_ · phase 1+): Run A1-014 dashboard creation in parallel with A1-015 + A1-016; all three are independent CloudWatch puts. — _Cuts dashboard-bootstrap from 3x serial to 1x._ |
| `A1-016` | 2+ | CloudWatch dashboard "Crossplane provisioning" with MR creation rate, XR Ready=True ratio, Composition function errors.<br><br><sub>_dashboard_ · Diagnose phase 2/3 silent-failure modes (XR with zero conditions)._</sub> | • **A5** (A5→A1-004 · _parallel-apply_ · phase 1+): Run A1-014 dashboard creation in parallel with A1-015 + A1-016; all three are independent CloudWatch puts. — _Cuts dashboard-bootstrap from 3x serial to 1x._ |
| `A1-017` | 1+ | CloudWatch Container Insights enabled via CloudWatch agent DaemonSet IRSA role in `terraform/management/`.<br><br><sub>_cloudwatch-setup_ · Pod-level CPU/mem/disk/network for free._</sub> | _(no comments)_ |
| `A1-018` | 1+ | Python helper `scripts/irsa_trust_validator.py <role>` that fetches role trust, decodes OIDC provider, prints expected `sub` claim, lists matching SAs in cluster.<br><br><sub>_python-helper_ · Caught bug 5 (PR #66 SA-hash mismatch) in 10 seconds instead of 2 hours._</sub> | • **A2** (A2→A1-004 · _test-oracle-promotion_ · phase 1+): E2E pairing for A1-018 (`irsa_trust_validator.py`): run it across every IRSA role in CI and fail the build on any mismatch — making the debug tool the oracle for A2-005/A2-006. — _One script, two jobs: human triage and CI invariant._<br>• **A3** (A3→A1-002 · _regression-extension_ · phase 1+): Pair A1-018 IRSA trust validator with a CI mode that exits non-zero so it can run as a pre-merge gate, not only an interactive helper. — _Bug 5 (PR #66) recurred because no gate enforced the trust=SA invariant._<br>• **A5** (A5→A1-005 · _parallel-probe_ · phase 1+): A1-018 `irsa_trust_validator.py` fans out trivially: run it for every role concurrently (one subprocess per role). — _Eliminates the per-role serial loop suggested by today's bash patterns._ |
| `A1-019` | 2+ | `scripts/crossplane-trace.sh <claim>` that walks claim → XR → resourceRefs → MRs → underlying AWS object, printing `.status.conditions` at each layer.<br><br><sub>_runbook_ · One command answers "where is the chain broken"._</sub> | • **A2** (A2→A1-005 · _test-oracle-promotion_ · phase 2+): Wrap `crossplane-trace.sh` (A1-019) output as a chainsaw `error:` block — when an assert fails, the trace dumps automatically. — _Failure evidence is structured at no extra cost._<br>• **A3** (A3→A1-003 · _regression-extension_ · phase 2+): Add a "snapshot mode" to A1-019 `crossplane-trace.sh` that writes condition snapshots to disk so two consecutive runs can diff XR/MR state over time. — _Catches "XR went silent" between reconciles; would have flagged the v2.0.1 zero-conditions bug._<br>• **A5** (A5→A1-006 · _pipeline_ · phase 2+): A1-019 `crossplane-trace.sh` should write to the event bus on each layer-flip so a watcher subagent can react to "MR appeared" without polling. — _Composes with A5-027/A5-030._<br>• **A6** (A6→A1-006 · _simplification_ · phase 2+): `scripts/crossplane-trace.sh` (A1-019) lets us delete `phase-2-diagnose.yml` entirely — the dispatch workflow exists only because no local trace did. — _Removes a 250-line workflow yaml._ |
| `A1-020` | 1+ | `scripts/argocd-app-poll.sh <app>` that hits ArgoCD API and waits until `Synced/Healthy` with a deadline.<br><br><sub>_wait-loop-helper_ · Replaces hand-rolled retry loops in integration scripts._</sub> | _(no comments)_ |
| `A1-021` | 2+ | `scripts/wait-for-claim.sh <kind> <name> [ns] [timeout]` that watches conditions and dumps composition events on timeout.<br><br><sub>_wait-loop-helper_ · Standardizes the most-repeated pattern in tests/integration._</sub> | • **A2** (A2→A1-006 · _test-oracle-promotion_ · phase 2+): Use `wait-for-claim.sh` (A1-021) as the canonical wait primitive in every A2 soak/concurrency test (A2-016, A2-017, A2-050). — _One bug surface for wait semantics across the test suite._<br>• **A6** (A6→A1-007 · _refactor_ · phase 2+): `scripts/wait-for-claim.sh` (A1-021) replaces every bespoke `until kubectl get ...; do sleep` loop in tests/integration/*.sh. — _Codifies one helper; deletes ~10 copy-pastes._ |
| `A1-022` | 1+ | curl recipe: ArgoCD app sync via REST API with bearer token (one-liner).<br><br><sub>_curl-recipe_ · Faster than `argocd app sync` (no install); useful in workflows._</sub> | _(no comments)_ |
| `A1-023` | 5+ | curl recipe: Keycloak admin login → realm export → diff against repo source-of-truth.<br><br><sub>_curl-recipe_ · Detects manual drift in Keycloak realm config._</sub> | _(no comments)_ |
| `A1-024` | 0+ | curl recipe: Cognito hosted-UI smoke test (start auth flow, follow redirect, confirm 200).<br><br><sub>_curl-recipe_ · Auth-flow break is otherwise discovered by user._</sub> | _(no comments)_ |
| `A1-025` | 1+ | Terraform post-apply verifier that re-queries every IRSA role's trust and asserts the SA subject matches `irsa.tf`'s declared `namespace_service_accounts`.<br><br><sub>_terraform-postcheck_ · Bug 5 would have failed apply, not silently created broken roles._</sub> | • **A2** (A2→A1-014 · _smoke-test_ · phase 1+): Add a regression test that the terraform post-apply verifiers (A1-025/026/027) actually fail when fed a broken fixture — meta-test the verifier. — _Verifier-without-tests is just faith._<br>• **A3** (A3→A1-004 · _regression-extension_ · phase 1+): Extend A1-025 (TF post-apply IRSA verifier) to also verify the SA actually exists in cluster, not just that subjects parse — catches the SA-rename half of Bug 5. — _Trust JSON can be syntactically valid while pointing at a deleted SA._<br>• **A5** (A5→A1-007 · _parallel-probe_ · phase 1+): The terraform-postcheck verifiers (A1-025/026/027) can all run as a single `make -j` target after apply — independent IAM/Helm/Provider probes. — _Cuts post-apply verify from serial to parallel._<br>• **A6** (A6→A1-005 · _refactor_ · phase 1+): Terraform post-apply verifiers (A1-025..A1-027) make the standalone `tests/integration/05_irsa_*.sh` redundant once apply itself fails. — _Shift-left; one gate not two._ |
| `A1-026` | 1+ | Terraform post-apply verifier that confirms every `helm_release` reports `STATUS: deployed` via the K8s API.<br><br><sub>_terraform-postcheck_ · `helm_release` resource can be "created" while pods CrashLoop._</sub> | _(no comments)_ |
| `A1-027` | 2+ | Terraform post-apply verifier that asserts every Crossplane Provider's `Healthy=True` and `Installed=True`.<br><br><sub>_terraform-postcheck_ · Caught the post-#68 hash-SA bug in the apply, not a subsequent claim._</sub> | • **A3** (A3→A1-010 · _regression-extension_ · phase 2+): Pair A1-027 (Crossplane Provider healthy verifier) with a record of the provider image digest so silent image-pin rollbacks are caught. — _Provider re-tag silently breaks Compositions; digest pin is the canonical guard._<br>• **A6** (A6→A1-005 · _refactor_ · phase 1+): Terraform post-apply verifiers (A1-025..A1-027) make the standalone `tests/integration/05_irsa_*.sh` redundant once apply itself fails. — _Shift-left; one gate not two._ |
| `A1-028` | 1+ | `scripts/eso-trace.sh <externalsecret> [ns]` that prints the chain: ExternalSecret → ClusterSecretStore → AWS ASM Secret → IRSA role → fetched value redaction.<br><br><sub>_runbook_ · ESO has 6 ways to fail; this collapses them._</sub> | • **A2** (A2→A1-011 · _test-oracle-promotion_ · phase 1+): Promote `eso-trace.sh` (A1-028) and `eso-cluster-secret-store-validate.sh` (A1-052) into the A2-023 ESO chaos test as the in-band oracle. — _Same debugging logic surfaces failures both interactively and in CI._ |
| `A1-029` | 1+ | `scripts/kyverno-decision-explain.sh <resource>` that simulates Kyverno admission on a yaml and prints which policies pass/fail and why.<br><br><sub>_debug-assist_ · Phase 2 had ClusterPolicy 09 drift; this would have caught it._</sub> | • **A3** (A3→A1-005 · _regression-extension_ · phase 1+): A1-029 Kyverno explain helper could keep a golden corpus of "expected-pass" and "expected-fail" YAMLs so it doubles as a regression suite for policy refactors. — _Policy 09 drift recurred; corpus-based regression locks behavior._ |
| `A1-030` | 1+ | Auto-create a CloudWatch log group per ArgoCD app sync round via a controller, retention 7d.<br><br><sub>_cloudwatch-setup_ · Long-lived audit trail of GitOps actions; correlate with cluster events._</sub> | _(no comments)_ |
| `A1-031` | 1+ | `scripts/r53-watch.sh <name>` that polls Route53 record changes until propagation confirmed via authoritative dig.<br><br><sub>_wait-loop-helper_ · ExternalDNS races are the #2 ingress bug._</sub> | _(no comments)_ |
| `A1-032` | 1+ | `scripts/nlb-trace.sh` that walks Ingress → Service → NLB ARN → target group health → SG rules → reach test from a debug pod.<br><br><sub>_runbook_ · Six-hop debug collapsed into one command._</sub> | _(no comments)_ |
| `A1-033` | 1+ | A "krew-style" wrapper `kubectl k8p <subcmd>` (status, claim, secret, irsa, syncwave) that dispatches into our scripts.<br><br><sub>_debug-assist_ · Discoverable surface; lowers cognitive cost._</sub> | • **A5** (A5→A1-008 · _pipeline_ · phase 1+): A1-033 `kubectl k8p` dispatcher can launch its subcommands in background by default (returning a handle), enabling pipelined diag. — _Subcommands become composable Unix-style._<br>• **A6** (A6→A1-011 · _simplification_ · phase 1+): The `kubectl k8p` krew-style wrapper (A1-033) supersedes the README "common commands" section — make the wrapper self-documenting. — _One source of truth for the surface._ |
| `A1-034` | 0+ | `scripts/handoff-update.sh` that re-runs all live probes and rewrites the Environment State block in `ai/handoff.md`.<br><br><sub>_python-helper_ · AGENTS.md invariant #2 (always update handoff) automated._</sub> | • **A6** (A6→A1-012 · _simplification_ · phase 0+): The handoff-update script (A1-034) lets us retire the manual "Environment State" maintenance ritual described in AGENTS.md invariant #2. — _The rule survives; the manual labor doesn't._ |
| `A1-035` | 0+ | Pre-flight `scripts/quota-check.sh` that confirms current EC2 count + EIP + NAT + VPC against the sandbox limits before apply.<br><br><sub>_python-helper_ · Sandbox kill = account loss; this is cheap insurance._</sub> | • **A5** (A5→A1-011 · _orchestration_ · phase 0+): A1-035 quota-check.sh is the perfect input to A5-042 back-pressure signal: emit a metric instead of a boolean. — _Lets parallel subagents self-throttle._ |
| `A1-036` | 0+ | CloudWatch metric filter on CloudTrail for `RunInstances` events that would exceed sandbox 9-instance cap.<br><br><sub>_metric-filter_ · Alarm fires before account terminates._</sub> | _(no comments)_ |
| `A1-037` | 0+ | CloudWatch alarm on aggregate EC2 instance count > 7.<br><br><sub>_alarm_ · Two-instance margin against the 9-cap._</sub> | _(no comments)_ |
| `A1-038` | 1+ | `scripts/diff-state.sh phase=N` that compares `terraform plan` output to live cluster state and highlights drift.<br><br><sub>_terraform-postcheck_ · "Apply complete: 0 added" silent-no-op bug class (handoff §"It applied successfully ≠")._</sub> | • **A3** (A3→A1-006 · _regression-extension_ · phase 1+): Have A1-038 `diff-state.sh` write its drift report to a CloudWatch log group so any drift over time is auditable across sessions. — _Cross-session drift is currently invisible between handoffs._<br>• **A5** (A5→A1-013 · _pipeline_ · phase 1+): A1-038 diff-state.sh can run continuously in the background as a watcher and notify on drift, rather than on-demand. — _Drift becomes push, not pull._ |
| `A1-039` | 2+ | `scripts/crossplane-version-report.sh` that prints Crossplane core, every Provider, every Function, every DeploymentRuntimeConfig binding.<br><br><sub>_debug-assist_ · Version-skew bugs (v2.0.1 vs v2.2) are silent until they bite._</sub> | _(no comments)_ |
| `A1-040` | 2+ | Composition render dry-run helper that runs `crossplane render` locally against a claim + composition + function.<br><br><sub>_debug-assist_ · Catches composition function input rejections before cluster sees them._</sub> | • **A3** (A3→A1-007 · _regression-extension_ · phase 2+): A1-040 composition render dry-run helper should support a fixture directory so every Composition has an expected rendered output checked into git. — _Locks in Bug 4 (string transform `type: Format`) as a permanent guard._ |
| `A1-041` | 5+ | `scripts/keycloak-realm-diff.sh` that exports live realm and diffs against `clusters/.../keycloak/realm.json`.<br><br><sub>_runbook_ · Detects manual drift; AuthN bugs are the worst class to hunt._</sub> | • **A2** (A2→A1-007 · _smoke-test_ · phase 5+): Add an e2e test that runs `keycloak-realm-diff.sh` (A1-041) nightly and posts diff to a slack channel — drift becomes a tracked event. — _Operationalizes drift detection._ |
| `A1-042` | 0+ | `scripts/cognito-user-create.sh` and `scripts/cognito-token-fetch.sh` helpers that exercise the user-pool with no AWS console clicks.<br><br><sub>_runbook_ · E2E auth tests need real tokens; this scripts them._</sub> | • **A2** (A2→A1-009 · _test-oracle-promotion_ · phase 0+): Use `cognito-token-fetch.sh` (A1-042) as the credential source for A2-009/A2-010 OIDC e2e tests. — _Removes hand-rolled OIDC dance from each test._ |
| `A1-043` | 2+ | `scripts/argocd-syncwave-view.sh <app>` that prints resources in sync-wave order with status.<br><br><sub>_debug-assist_ · Sync-wave bugs in phase 2a were painful; this surfaces order._</sub> | _(no comments)_ |
| `A1-044` | 1+ | CloudWatch Logs subscription filter that mirrors Kyverno PolicyReport failures to a dedicated log group.<br><br><sub>_cloudwatch-setup_ · Persistent record of policy violations; survives pod churn._</sub> | _(no comments)_ |
| `A1-045` | 1+ | `scripts/ebs-volume-list.sh` that lists volumes with size, attached node, age, against the 100 GB cap.<br><br><sub>_runbook_ · Prevents the silent cap-exceeded sandbox kill._</sub> | _(no comments)_ |
| `A1-046` | 0+ | `scripts/cert-watcher.sh` that polls ACM cert ARNs and asserts ISSUED + matches NLB listener.<br><br><sub>_wait-loop-helper_ · Cert mismatch breaks everything downstream; cheap to detect._</sub> | _(no comments)_ |
| `A1-047` | 1+ | `scripts/oidc-introspect.sh <jwt>` that decodes a pod-projected token's sub/aud/iss claims for IRSA debug.<br><br><sub>_debug-assist_ · Pairs with A1-018; both halves of IRSA mismatch._</sub> | • **A2** (A2→A1-015 · _test-oracle-promotion_ · phase 1+): Pair `oidc-introspect.sh` (A1-047) with A2-004 to compare projected-token claims to IAM role expectations inside the assertion. — _Closes the loop on both halves of IRSA mismatch into a single oracle._<br>• **A4** (A4→A1-028 · _runbook_ · phase 1+): Add `eso-trace.sh --decode-token` to also dump the projected SA token claims of the ESO pod — folds A1-047 in for one-call ESO debug. — _ESO + IRSA are the same bug 80% of the time; one call instead of two._ |
| `A1-048` | 1+ | Run-bundle uploader `scripts/diag-bundle.sh` that tars cluster state (`kubectl get all -A`, events, CRDs, Crossplane providers, ArgoCD apps) into a timestamped tgz.<br><br><sub>_debug-assist_ · One-shot "send me everything"; replaces ad-hoc Slack triage._</sub> | • **A2** (A2→A1-013 · _test-oracle-promotion_ · phase 1+): Use `diag-bundle.sh` (A1-048) as the on-failure artifact for every A2 chaos test — uploaded automatically to the run. — _One bundle format means one post-mortem workflow._<br>• **A3** (A3→A1-012 · _regression-extension_ · phase 1+): Extend A1-048 diag bundle to include `terraform state pull` snapshot — completes the "send me everything" picture and supports apply-vs-state diff offline. — _The "Apply complete: 0 added" silent-no-op class is invisible without state._<br>• **A5** (A5→A1-009 · _speculative-probe_ · phase 1+): A1-048 diag-bundle could run while phase apply is still in flight — the bundle script doesn't mutate, so it's safe to overlap. — _Pre-positions evidence so failures are diagnosable instantly._ |
| `A1-049` | 2+ | Per-XRD Chainsaw scenario that asserts `.status.conditions[*].type` contains all expected condition types within timeout.<br><br><sub>_debug-assist_ · Catches "XR has zero conditions" bug class explicitly._</sub> | • **A3** (A3→A1-013 · _regression-extension_ · phase 2+): A1-049 per-XRD chainsaw scenario should be templated from XRD source so adding a new XRD auto-generates the condition-coverage test. — _Prevents future XRDs from shipping without condition assertions._ |
| `A1-050` | 1+ | CloudWatch composite alarm: any of {NLB unhealthy, EKS node NotReady, IRSA failures>10/min, Crossplane reconcile errors>5/min}.<br><br><sub>_alarm_ · Single page-able signal for "platform is sick"._</sub> | • **A2** (A2→A1-008 · _smoke-test_ · phase 1+): Add an integration test that fires the CloudWatch composite alarm (A1-050) via synthetic metric injection to prove the page-able path works. — _Untested alarms are decoration; this proves the wiring._ |
| `A1-051` | 1+ | `scripts/argocd-app-tree.sh <app>` rendering the parent → child Application tree with sync/health colored.<br><br><sub>_debug-assist_ · App-of-apps debugging needs a tree view._</sub> | _(no comments)_ |
| `A1-052` | 1+ | `scripts/eso-cluster-secret-store-validate.sh` that confirms CSS auth IRSA works by trying `aws secretsmanager list-secrets` from inside the ESO pod.<br><br><sub>_runbook_ · ESO failures are usually IRSA, but masked as "Secret not found"._</sub> | • **A2** (A2→A1-011 · _test-oracle-promotion_ · phase 1+): Promote `eso-trace.sh` (A1-028) and `eso-cluster-secret-store-validate.sh` (A1-052) into the A2-023 ESO chaos test as the in-band oracle. — _Same debugging logic surfaces failures both interactively and in CI._ |
| `A1-053` | 1+ | CloudWatch alarm on `aws_eks_cluster` API server 5xx rate > 1%.<br><br><sub>_alarm_ · API server health is otherwise invisible._</sub> | _(no comments)_ |
| `A1-054` | 0+ | Auto-tagging policy that adds `phase=N` and `managed-by=k8-platform` to every AWS resource for easy CloudTrail/billing filters.<br><br><sub>_cloudwatch-setup_ · Per-phase cost and audit attribution._</sub> | • **A6** (A6→A1-008 · _simplification_ · phase 0+): Auto-tagging policy (A1-054) lets us retire the per-resource "Name = k8-platform-<x>" naming hacks used as a poor-man's tag. — _Tags are queryable; name-prefix is not._<br>• **A6** (A6→A1-010 · _simplification_ · phase 0+): `scripts/cleanup-orphans.sh` (A1-070) plus auto-tagging (A1-054) lets us drop the manual "remember to delete X" notes scattered across retros. — _Tool replaces tribal knowledge._ |
| `A1-055` | 0+ | `scripts/cost-report.sh` that uses Cost Explorer API to print this-month spend bucketed by phase tag.<br><br><sub>_python-helper_ · Sandbox has hidden cost caps too; visibility is cheap._</sub> | _(no comments)_ |
| `A1-056` | 2+ | `scripts/crossplane-reset-provider.sh <provider>` that deletes the Deployment + waits for Crossplane to re-render.<br><br><sub>_runbook_ · Codifies the PR #68 hack; one command instead of remembering steps._</sub> | _(no comments)_ |
| `A1-057` | 2+ | `kubectl events --watch` wrapper that filters to a single XR/claim and color-codes by type.<br><br><sub>_debug-assist_ · Tail-style debug; nicer than `kubectl get events`._</sub> | _(no comments)_ |
| `A1-058` | 1+ | Pre-commit hook that validates every YAML in `crossplane/`, `argocd/`, `clusters/`, `policies/` with `kubeconform` against the registered CRDs.<br><br><sub>_debug-assist_ · Catches Bug 4 (Composition string transform missing type) at author time._</sub> | • **A3** (A3→A1-008 · _regression-extension_ · phase 2+): Extend A1-058 pre-commit kubeconform hook to also verify Compositions against the function input schema, not only against the XRD. — _Function-input-schema mismatches are a distinct silent-failure class._ |
| `A1-059` | 1+ | `scripts/jwks-check.sh <oidc-issuer>` that fetches issuer's JWKS and confirms it matches the IAM OIDC provider's thumbprint.<br><br><sub>_python-helper_ · OIDC issuer rotation is rare but silent-catastrophic._</sub> | _(no comments)_ |
| `A1-060` | 1+ | Synthetic canary: a Lambda or GH Action that hits ArgoCD `/healthz`, NLB `/`, Cognito `/.well-known/openid-configuration` every 5 min and posts to a CloudWatch metric.<br><br><sub>_cloudwatch-setup_ · First-derivative health signal independent of pods/nodes._</sub> | • **A2** (A2→A1-012 · _smoke-test_ · phase 1+): Add an e2e canary harness on top of A1-060 that runs A2-049 (Cognito hosted-UI Selenium) every 15 min and feeds a synthetic-metric CloudWatch alarm. — _Browser-level auth flow becomes an SLO._<br>• **A3** (A3→A1-009 · _regression-extension_ · phase 1+): Add a "self-test" mode to A1-060 synthetic canary that intentionally injects a known-bad URL and asserts the metric goes red within deadline. — _Validates the alarming pipeline itself (alarm-on-alarm-system)._<br>• **A5** (A5→A1-010 · _prewarm_ · phase 1+): A1-060 synthetic canary can be a long-lived background sandbox process triggered at session start rather than a Lambda — same signal, zero AWS spend. — _Composes with A5-007 watcher pattern._ |
| `A1-061` | 1+ | Logs Insights saved query: "ExternalDNS Route53 API call errors last 1h grouped by record".<br><br><sub>_logs-insights-query_ · DNS bugs are slow; this is the fastest signal._</sub> | • **A2** (A2→A1-010 · _smoke-test_ · phase 1+): Add a contract test that every saved Logs Insights query (A1-006..A1-009, A1-061..A1-062) parses and returns ≤1 row when run against a known-good fixture stream. — _Saved queries rot silently when log shapes change; a regression test pins them._ |
| `A1-062` | 2+ | Logs Insights saved query: "Crossplane composition function panics or timeouts".<br><br><sub>_logs-insights-query_ · Function pod crashes silently strand XRs._</sub> | • **A2** (A2→A1-010 · _smoke-test_ · phase 1+): Add a contract test that every saved Logs Insights query (A1-006..A1-009, A1-061..A1-062) parses and returns ≤1 row when run against a known-good fixture stream. — _Saved queries rot silently when log shapes change; a regression test pins them._ |
| `A1-063` | 5+ | `scripts/keycloak-token-roundtrip.sh` that performs full OIDC dance against Keycloak and asserts ID-token group claim equals expected.<br><br><sub>_runbook_ · EKS-via-Keycloak (REQ-AUTH-07..10) needs this as health check._</sub> | _(no comments)_ |
| `A1-064` | 5+ | `scripts/probe-workload-cluster.sh <cluster>` that walks ClusterRoleBinding → kc:group → Cognito group membership end-to-end.<br><br><sub>_runbook_ · Phase 5/6 access debugging._</sub> | _(no comments)_ |
| `A1-065` | 1+ | `scripts/oom-report.sh` that lists pods with restart count > 0 + reason from container status.<br><br><sub>_debug-assist_ · OOMs masquerade as flakes; this surfaces the pattern._</sub> | _(no comments)_ |
| `A1-066` | 1+ | CloudWatch alarm on EBS volume `BurstBalance < 20%` (only relevant on gp2).<br><br><sub>_alarm_ · Disk throttling explains slow apiserver behavior._</sub> | _(no comments)_ |
| `A1-067` | 0+ | `scripts/sg-explain.sh <sg>` that prints inbound/outbound rules grouped, with referenced SG names resolved.<br><br><sub>_debug-assist_ · SG debug is the worst kind without this._</sub> | _(no comments)_ |
| `A1-068` | 0+ | `scripts/sts-decode.sh` that prints the current caller's effective permissions via IAM simulator API.<br><br><sub>_python-helper_ · "Why does this fail?" answered without console clicks._</sub> | _(no comments)_ |
| `A1-069` | 1+ | A unified `scripts/diag.sh` dispatcher: `diag.sh irsa<br><br><sub>_eso_ · argocd&#124;crossplane&#124;nlb&#124;cert&#124;cognito&#124;kyverno&#124;all` invoking the right specialist. &#124; debug-assist &#124; Memorize one command, not nine._</sub> | • **A5** (A5→A1-012 · _parallel-probe_ · phase 1+): A1-069 `diag.sh` dispatcher should accept a `--parallel` flag that runs every specialist concurrently and merges output. — _"All-in" diag in one round trip._<br>• **A6** (A6→A1-009 · _refactor_ · phase 1+): `scripts/diag.sh` dispatcher (A1-069) lets us delete the now-redundant top-level wrappers `kyverno-violations.sh`, `route53-records.sh`, `diag-component.sh`. — _One entrypoint replaces nine._ |
| `A1-070` | 0+ | `scripts/cleanup-orphans.sh` that lists AWS resources tagged `managed-by=k8-platform` but not referenced by terraform state or any Crossplane XR.<br><br><sub>_runbook_ · Sandbox kill class: drifted resources past the cap._</sub> | • **A3** (A3→A1-011 · _regression-extension_ · phase 0+): A1-070 orphan-cleanup script could emit a baseline expected-resource manifest per phase; deviations become a test failure not just a "list". — _Codifies sandbox-cap invariant against silent leaks._<br>• **A6** (A6→A1-010 · _simplification_ · phase 0+): `scripts/cleanup-orphans.sh` (A1-070) plus auto-tagging (A1-054) lets us drop the manual "remember to delete X" notes scattered across retros. — _Tool replaces tribal knowledge._ |

### A1 — General comments (no source-ID anchor)

Cross-review rows targeted at this agent but without an explicit `A{N}-NNN` reference in the comment text; preserved verbatim.

| Comment ID | From | Phase | Comment | Category & justification |
|---|---|---|---|---|
| `A4→A1-001` | A4 | 0+ | Add a `scripts/whereami.sh --json` mode so a subagent can `jq` the output instead of re-parsing prose. | _debug-assist_ — The retro pain was always "parse my own helper's output"; emitting JSON closes that loop. |
| `A4→A1-002` | A4 | 0+ | Pair `phase-status.sh` with `phase-status.md` runbook that names which probe distinguishes `code-only` from `applied` for each phase. | _runbook_ — Live probes are only useful if the operator knows what failing-probe means; runbook collapses interpretation. |
| `A4→A1-004` | A4 | 0+ | Add a `scripts/vpc-flow-grep.sh <src> <dst>` helper that compiles the most common SG-deny query into one command. | _runbook_ — Flow logs alone burned hours in prior sessions because nobody remembered the schema. |
| `A4→A1-005` | A4 | 0+ | Pair CloudTrail enablement with `runbook-cloudtrail-iam-debug.md` listing the five canonical Insights queries (AssumeRole denies, GetSecretValue denies, eks:* failures, sts:* failures, kms:* failures). | _runbook_ — The data is only useful if the query is memorized. |
| `A4→A1-007` | A4 | 1+ | Add a `tee` mode to the audit-deny query that mirrors live results to `/tmp/last-audit-denies.json` for cross-session memory. | _shell-helper_ — "Was this happening yesterday?" answerable across sessions. |
| `A4→A1-014` | A4 | 1+ | Pair the dashboard with `runbook-dashboard-read.md` that names which panel rules out which bug class — turns glance into decision. | _runbook_ — Dashboards without a decision tree are just wallpaper. |
| `A4→A1-018` | A4 | 1+ | Add an `irsa_trust_validator.py --all` mode that iterates every role and prints a single table — turn one-shot validator into a fleet sweep. | _python-helper_ — Bug 5 manifested across multiple roles; per-role invocation misses the pattern. |
| `A4→A1-019` | A4 | 2+ | Have `crossplane-trace.sh` accept `--watch` to re-print the chain every 10 s — close the "is it making progress" loop. | _runbook_ — Sessions burned re-running the trace manually every minute. |
| `A4→A1-032` | A4 | 1+ | Add `nlb-trace.sh --record` to capture a baseline trace artifact per phase so future runs can diff. | _runbook_ — "Worked yesterday" answerable in one diff. |
| `A4→A1-048` | A4 | 1+ | Pair `diag-bundle.sh` with `diag-bundle-explain.py` that reads a bundle and prints a triage paragraph (Conditions=False rows + first-failing event). | _shell-helper_ — Bundles are useless if the operator has to page through them. |
| `A4→A1-069` | A4 | 1+ | Make `diag.sh` accept `diag.sh suggest` mode that prints the most likely specialist to invoke based on a one-line symptom. | _debug-assist_ — Operator doesn't always know which subcommand to pick — guided entry shortens loops. |
| `A4→A1-070` | A4 | 0+ | Add `cleanup-orphans.sh --dry-run --json` plus a CI check that asserts orphan-count==0 so leakage gets caught in PRs, not at session start. | _runbook_ — Orphan detection is most useful as a guardrail, not a manual sweep. |
| `P→A1-001` | PRIMARY | 1+ | A single `inspect <resource>` shell wrapper that auto-dispatches (claim → XR → MR → IRSA → cloud) based on `kind` so the agent types one verb regardless of what's broken. | _shell-helper_ — Eliminates the "which command do I run for this kind?" cognitive tax that recurs every debug loop. |
| `P→A1-002` | PRIMARY | 0+ | A `make doctor` target that runs every cheap read-only health probe (caller-id, kubeconfig age, ArgoCD reachable, CRDs present, provider Healthy, IRSA SA-name pinned) and emits a one-screen status board. | _runbook_ — Cheap pre-flight that catches "stale kubeconfig" / "rotated account" before they cost a debug loop. |
| `P→A1-003` | PRIMARY | 0+ | Auto-create a CloudWatch Logs log group `/k8-platform/agent-session/$DATE` and tee every `aws`/`kubectl` invocation into it for postmortem replay. | _cloudwatch-setup_ — Makes "what did the agent actually do?" answerable after the sandbox is torn down. |
| `P→A1-004` | PRIMARY | 1+ | A `dashboards/` folder of one JSON per dashboard (EKS, Crossplane, ESO, ArgoCD, ExternalDNS, Kyverno, Cognito) that Terraform applies idempotently on every phase-1 apply. | _dashboard_ — Re-bootstrappable across account rotations; zero clicks to get to "what's going on right now". |
| `P→A1-005` | PRIMARY | 1+ | A "diagnose digest" Python script that pulls the last hour from all relevant log groups + last 50 events from all crossplane-system pods + all MR statuses into a single ~5 KB digest the agent can paste back into context. | _python-helper_ — Shrinks 170 KB diagnose logs into agent-readable evidence; closes the "context overflow" failure mode from the old diagnose workflows. |
| `P→A1-006` | PRIMARY | 0+ | Annotate every Terraform-managed resource with `tags = { k8platform-phase = "0" | _"1"_ — "2", k8platform-component = "..." }` so CloudWatch / cost / drift queries can filter by phase. &#124; terraform-postcheck &#124; Lets dashboards/alarms scope to the phase being worked on without recomputing arn lists. |
| `P→A1-007` | PRIMARY | 1+ | A `wait-for` polyglot CLI (`wait-for claim/X ready`, `wait-for mr/Y synced`, `wait-for argo-app/Z healthy`, `wait-for irsa/<role> assumable`) — one tool, many backends. | _wait-loop-helper_ — Replaces ad-hoc `until` loops scattered across runbooks. |

## A2 — Max-capability integration tests

> **Mandate.** Brainstorm integration / E2E / chaos / soak / negative / concurrency / contract tests that become trivial now that the agent has full AWS admin, direct kubectl, ArgoCD/Keycloak admin APIs, unlimited egress, and CloudWatch SDK access.

_Source file: [`ai/brainstorming/A2-integration-e2e-tests-max-capability.md`](../../ai/brainstorming/A2-integration-e2e-tests-max-capability.md)_

_Idea count: 65_

| ID | Phase | Idea (with category and justification) | Comments (bulleted by commenter) |
|---|---|---|---|
| `A2-001` | 2+ | End-to-end PlatformSecret claim round-trip that creates the claim, waits Ready, then uses the AWS Secrets Manager SDK to read the resulting ASM secret and assert tags/KMS/replication match the Composition.<br><br><sub>_claim-roundtrip-test_ · The composition can render a "Ready" MR while the actual ASM Secret has wrong tags/region/KMS — only an SDK readback proves the cloud truth._</sub> | • **A1** (A1→A2-001 · _observability-pair_ · phase 2+): Pair every claim-roundtrip (A2-001/044) with an automatic CloudTrail cross-check that asserts the exact AWS principal + IRSA role chain that performed the write, captured by event-name. — _Proves *who* did the write, not just that the write happened — catches privilege drift._<br>• **A5** (A5→A2-003 · _pipeline_ · phase 2+): A2-018 soak test can run in background while A2-001 round-trip and A2-007 drift tests run in foreground — three independent tests, one wall-clock. — _Soak is wall-clock-bound, not CPU; overlap freely._<br>• **A5** (A5→A2-008 · _pipeline_ · phase 2+): A2-053 ASM rotation test can be triggered by event bus the instant A2-001 round-trip subagent reports Ready, no sleep. — _Removes explicit wait._<br>• **A5** (A5→A2-013 · _subagent-pool_ · phase 2+): A2-001/002/044/045 claim-roundtrip tests share scaffolding — extract a "claim-rt-harness" subagent that all four call, instead of duplicating setup. — _DRY at the orchestration layer._<br>• **A6** (A6→A2-001 · _refactor_ · phase 2+): Once A2-001's SDK readback works, retire any chainsaw `assert` block that only checks `.status.conditions[*].Ready` on the XR — the SDK is a stronger oracle. — _Stronger gate replaces weaker one._<br>• **A6** (A6→A2-006 · _refactor_ · phase 2+): Full E2E claim->ASM byte-compare (A2-034) makes the layered tests A2-001 + A2-012 + A2-053 partially redundant — once the byte-compare passes, those become smoke-only. — _Single end-to-end oracle subsumes mid-chain probes._ |
| `A2-002` | 2+ | Same PlatformSecret round-trip but probe the cloud truth from a DIFFERENT region's STS+SecretsManager client to catch global vs regional endpoint bugs.<br><br><sub>_claim-roundtrip-test_ · Region-specific Composition bugs (wrong provider region override) silently pass single-region tests._</sub> | _(no comments)_ |
| `A2-003` | 1+ | IRSA correctness test that, for every IRSA role in `terraform/management/irsa.tf`, calls `aws iam simulate-principal-policy` with the exact actions the SA's pods need and asserts allowed.<br><br><sub>_irsa-test_ · Permissions drift is invisible until a claim fails in production — simulate-principal catches it pre-flight._</sub> | • **A1** (A1→A2-002 · _observability-extension_ · phase 1+): Extend A2-003 (simulate-principal) to also dump the resolved effective policy JSON per role into an artifact so future debugging has a baseline diff. — _Snapshot becomes a regression oracle for IAM policy drift._<br>• **A5** (A5→A2-002 · _parallel-test_ · phase 1+): A2-003 IRSA simulate-principal-policy fans out: one async call per role, gather with asyncio. — _Cuts N-role check from sum to max._<br>• **A6** (A6→A2-002 · _simplification_ · phase 1+): IRSA simulate-principal-policy test (A2-003) supersedes the inline `aws iam get-role-policy` greps in `test_irsa_policy_completeness.sh`. — _API-level oracle replaces text-grep heuristics._ |
| `A2-004` | 1+ | IRSA live-token test that runs `aws sts assume-role-with-web-identity` using a freshly projected SA token from each platform SA, confirming trust+permissions at the token level.<br><br><sub>_irsa-test_ · The cluster<->IAM federation can break in five places; only a live AssumeRole proves the chain works._</sub> | • **A6** (A6→A2-003 · _simplification_ · phase 1+): A2-004 (live AssumeRoleWithWebIdentity) makes every "does this IRSA work" smoke test pod (deployed just to call STS) deletable. — _Test runner does it directly; no test workload needed._ |
| `A2-005` | 1+ | Cross-resource invariant test: every IRSA role's trust `sub` must match a SA actually present in the live cluster (catches stale role drift).<br><br><sub>_irsa-test_ · The PR #66 SA-hash bug went undetected because no test compared trust-subject to live SA list._</sub> | • **A3** (A3→A2-001 · _regression-extension_ · phase 1+): Add a Bug-5 regression scenario to A2-005 (trust=SA invariant): rename a platform SA in a fixture and assert the invariant test fails loudly. — _Locks PR #66 as a permanent guard rather than an emergent property._<br>• **A6** (A6→A2-004 · _refactor_ · phase 1+): A2-005 + A2-006 bidirectional IRSA invariants collapse `test_irsa_trust_subject.sh` and `test_irsa_sa_annotation.sh` into one parameterized test. — _One invariant, two directions._ |
| `A2-006` | 1+ | Inverse invariant test: every platform SA with `eks.amazonaws.com/role-arn` annotation must point at an IRSA role whose trust list contains its (namespace, name).<br><br><sub>_irsa-test_ · Catches the opposite drift — SA annotation rewritten to a deleted role._</sub> | • **A6** (A6→A2-004 · _refactor_ · phase 1+): A2-005 + A2-006 bidirectional IRSA invariants collapse `test_irsa_trust_subject.sh` and `test_irsa_sa_annotation.sh` into one parameterized test. — _One invariant, two directions._ |
| `A2-007` | 1+ | ArgoCD admin-API drift test that diffs every Application's live manifest hash against git HEAD and fails on any unexpected drift.<br><br><sub>_e2e-test_ · ArgoCD self-heal can silently flap; an out-of-band diff is the only honest oracle._</sub> | • **A1** (A1→A2-003 · _classification-extension_ · phase 1+): Pair A2-007 (ArgoCD drift diff) with a Kyverno PolicyReport snapshot so drift attributable to admission mutation is auto-classified vs human edits. — _Bug 3 root-cause classification becomes one-shot._<br>• **A3** (A3→A2-004 · _regression-extension_ · phase 1+): Add a Bug-3 regression to A2-007 ArgoCD drift test: pre-seed a Kyverno mutation that flips a field, assert the diff oracle flags it within one cycle. — _Codifies the Kyverno-drift recurrence as a test, not a debug expedition._<br>• **A5** (A5→A2-003 · _pipeline_ · phase 2+): A2-018 soak test can run in background while A2-001 round-trip and A2-007 drift tests run in foreground — three independent tests, one wall-clock. — _Soak is wall-clock-bound, not CPU; overlap freely._<br>• **A6** (A6→A2-005 · _simplification_ · phase 1+): ArgoCD admin-API drift test (A2-007) retires manual "argocd app diff" runbooks in `ai/runbooks/` (or wherever the historical equivalents live). — _Automated drift > documented drift hunt._ |
| `A2-008` | 1+ | ArgoCD admin-API forced-sync test that triggers `POST /api/v1/applications/<name>/sync` for every app and asserts `Synced/Healthy` within deadline.<br><br><sub>_e2e-test_ · Tests that ArgoCD can recover from arbitrary out-of-sync state, not just steady state._</sub> | • **A5** (A5→A2-010 · _parallel-test_ · phase 1+): A2-008 forced-sync test fans out trivially across all apps with `argocd app sync --async` (A5-034). — _Sum-of-N becomes max-of-N._ |
| `A2-009` | 5+ | Keycloak admin-API realm-lifecycle test that creates a temp realm, adds a client, adds a user, performs an OIDC password flow, then deletes the realm.<br><br><sub>_oidc-flow-test_ · Real Keycloak realm CRUD is impossible from GH Actions but trivial now; covers REQ-AUTH-07..10._</sub> | • **A6** (A6→A2-014 · _simplification_ · phase 5+): A2-009's realm-lifecycle test removes the need for the manually-maintained "Keycloak smoke test steps" doc. — _Code replaces prose._ |
| `A2-010` | 5+ | Real OIDC authorization-code flow against Keycloak using `requests` + a headless redirect handler, asserting an ID token with `groups` claim.<br><br><sub>_oidc-flow-test_ · The only test that proves the federation chain Cognito->Keycloak->client works end-to-end._</sub> | _(no comments)_ |
| `A2-011` | 5+ | kubectl-as-Keycloak-user test that exchanges a Keycloak token via `oidc-login` and verifies the EKS API server accepts it with the expected `kc:` group prefix.<br><br><sub>_oidc-flow-test_ · This is the entire point of phase 5; nothing else proves the EKS OIDC client config works._</sub> | _(no comments)_ |
| `A2-012` | 2+ | CloudWatch Logs Insights query as oracle: after applying a claim, query Crossplane logs for the XR name and assert zero `reconcile error` rows.<br><br><sub>_integration-test_ · Cluster-side `kubectl logs` is racy with pod restarts; CloudWatch is durable._</sub> | • **A1** (A1→A2-004 · _observability-extension_ · phase 2+): Extend A2-012 / A2-013 (CloudWatch oracles) with a saved Logs Insights query library committed to the repo (`observability/queries/*.yml`) callable by test-name. — _Codifies oracle queries so tests share, version, and review them._<br>• **A5** (A5→A2-015 · _cache_ · phase 2+): A2-012/013 CloudWatch oracle queries can be batched into a single Logs Insights multi-query call instead of two round trips. — _Halves CloudWatch API cost._<br>• **A6** (A6→A2-006 · _refactor_ · phase 2+): Full E2E claim->ASM byte-compare (A2-034) makes the layered tests A2-001 + A2-012 + A2-053 partially redundant — once the byte-compare passes, those become smoke-only. — _Single end-to-end oracle subsumes mid-chain probes._ |
| `A2-013` | 5+ | CloudWatch Logs Insights query as oracle: after an OIDC flow, query EKS audit log for the user's first `get pods` and assert `system:authenticated`.<br><br><sub>_integration-test_ · Verifies the cluster *accepted* the OIDC user, not just that Keycloak issued the token._</sub> | • **A1** (A1→A2-004 · _observability-extension_ · phase 2+): Extend A2-012 / A2-013 (CloudWatch oracles) with a saved Logs Insights query library committed to the repo (`observability/queries/*.yml`) callable by test-name. — _Codifies oracle queries so tests share, version, and review them._ |
| `A2-014` | 2+ | Negative test: delete an IRSA role mid-claim-provisioning and assert Crossplane MR enters a clean failed state with a useful error condition.<br><br><sub>_negative-test_ · Real recovery semantics matter; silent reconciliation loops are worse than failures._</sub> | • **A6** (A6→A2-015 · _refactor_ · phase 2+): Combine A2-014 + A2-015 + A2-022 + A2-023 into a single chaos-suite runner; each test currently bootstraps its own kill-and-watch scaffolding. — _Shared scaffolding cuts code by ~60%._ |
| `A2-015` | 2+ | Recovery test: re-create the IRSA role from A2-014 and assert the claim returns to Ready=True without manual intervention.<br><br><sub>_negative-test_ · Self-healing is a documented contract; only a chaos+recovery test proves it._</sub> | • **A6** (A6→A2-015 · _refactor_ · phase 2+): Combine A2-014 + A2-015 + A2-022 + A2-023 into a single chaos-suite runner; each test currently bootstraps its own kill-and-watch scaffolding. — _Shared scaffolding cuts code by ~60%._ |
| `A2-016` | 2+ | Concurrency test: kubectl-apply 20 PlatformSecret claims in parallel across 5 namespaces and assert all reach Ready within deadline with no UID collisions on ASM names.<br><br><sub>_concurrency-test_ · The Composition uses `<XR-uid>` to avoid collisions — only fan-out testing exercises the collision path._</sub> | • **A1** (A1→A2-006 · _tracing-extension_ · phase 2+): Extend A2-016 (concurrent claims) with an X-Ray-style trace ID injected per claim via XR annotation, then assert end-to-end propagation through CloudWatch logs. — _Distributed-tracing of XR pipelines turns concurrency debugging from log-grep to span-view._<br>• **A3** (A3→A2-002 · _regression-extension_ · phase 2+): Pair A2-016 (20 parallel PlatformSecret claims) with a CloudWatch oracle that asserts no Crossplane `reconcile error` spike during the burst. — _Catches throttling-induced silent retries that don't surface in claim status._<br>• **A5** (A5→A2-001 · _cache_ · phase 2+): A2-016 (20 parallel claims) can share a single kubeconfig via A5-032 to avoid 20 STS calls. — _Avoids STS rate-limit fan-out._<br>• **A6** (A6→A2-007 · _simplification_ · phase 2+): A2-016 (parallel claim fan-out) lets us delete the now-pointless "test_uid_uniqueness.sh" lint — runtime exercises it for real. — _Runtime test trumps static heuristic._ |
| `A2-017` | 3+ | Concurrency test: 10 simultaneous PlatformCluster claims to confirm EKS module idempotency and IRSA-provider rate-limit handling.<br><br><sub>_concurrency-test_ · Phase 3 EKS-via-XRD will hit AWS API throttling under fan-out; better to catch in test._</sub> | _(no comments)_ |
| `A2-018` | 2+ | Soak test: a single PlatformSecret left running for 60 minutes with `refreshInterval: 30s` while a sidecar verifies every refresh against ASM SDK.<br><br><sub>_soak-test_ · Catches token-expiry, ESO re-auth, and IRSA-token-renewal bugs that only surface after the 1h SA token TTL._</sub> | • **A1** (A1→A2-011 · _observability-extension_ · phase 2+): Extend A2-018/050 (soak tests) with continuous Prometheus scraping of controller workqueue depth + reconcile latency p99, asserting no monotonic drift. — _Memory leaks and queue creep show in workqueue metrics hours before pod OOMs._<br>• **A3** (A3→A2-007 · _regression-extension_ · phase 2+): Add a soak-test variant of A2-018 that runs across an SA token's 1h TTL boundary specifically (timed for T+59m), asserting refresh path works. — _The 1h TTL is the documented IRSA failure-time landmark._<br>• **A5** (A5→A2-003 · _pipeline_ · phase 2+): A2-018 soak test can run in background while A2-001 round-trip and A2-007 drift tests run in foreground — three independent tests, one wall-clock. — _Soak is wall-clock-bound, not CPU; overlap freely._ |
| `A2-019` | 1+ | Soak test: long-running ArgoCD Application sync stability over 90 minutes asserting zero spurious OutOfSync transitions.<br><br><sub>_soak-test_ · Phase-2 had perennial OutOfSync from Kyverno drift (Bug 3); a soak catches the next instance instantly._</sub> | • **A3** (A3→A2-013 · _regression-extension_ · phase 1+): Pair A2-019 ArgoCD soak with a Logs Insights oracle that asserts zero `OutOfSync` log lines for any app over the 90-min window. — _Belt-and-suspenders for the documented Bug-3 recurrence._<br>• **A5** (A5→A2-004 · _speculative-test_ · phase 1+): A2-019 90-min ArgoCD soak should be a session-start subagent so its result is ready when needed, not blocked on. — _Hides 90 min behind everything else._<br>• **A6** (A6→A2-013 · _simplification_ · phase 1+): A2-019's soak test obviates ad-hoc "is ArgoCD still OK?" status polling in long sessions. — _Soak test result is the answer._ |
| `A2-020` | 1+ | ExternalDNS round-trip test that creates an Ingress, polls `dig @ns-<...>.awsdns-<...>.com` until propagation, then HTTPS-GETs the URL.<br><br><sub>_e2e-test_ · Public-recursor DNS lies for minutes; authoritative dig is the only honest oracle (per `scripts/route53-records.sh` lesson)._</sub> | • **A1** (A1→A2-007 · _observability-pair_ · phase 1+): Pair A2-020 (ExternalDNS round-trip) with a Route53 query-log capture (enable on the hosted zone) and assert the test's resolver IP appears, proving the propagation path. — _Distinguishes "DNS not propagated" from "resolver cached NXDOMAIN"._<br>• **A6** (A6→A2-009 · _refactor_ · phase 1+): A2-020 (authoritative `dig`) lets us delete every public-resolver `dig @8.8.8.8` invocation lingering in scripts/runbooks. — _One correct DNS oracle._ |
| `A2-021` | 1+ | ExternalDNS deletion-cleanup test that deletes the Ingress and asserts the Route53 record disappears within the propagation window.<br><br><sub>_e2e-test_ · Stale DNS records are the #2 cleanup leak; only an SDK probe catches them._</sub> | _(no comments)_ |
| `A2-022` | 2+ | Chaos test: kill the Crossplane controller pod mid-reconcile and assert no managed resource is left in `<no-status>` purgatory.<br><br><sub>_chaos-test_ · The v2.0.1 composite-reconciler bug observed in handoff Step 7 looks exactly like a kill-mid-reconcile failure mode._</sub> | • **A1** (A1→A2-005 · _chaos-extension_ · phase 2+): Pair chaos kills (A2-022/023) with an EBPF/`crictl` capture of the dying pod's last 30s of syscalls via a sidecar DaemonSet for richer post-mortem. — _Goes beyond logs to syscall-level evidence when controllers die mid-reconcile._<br>• **A3** (A3→A2-003 · _regression-extension_ · phase 2+): Extend A2-022 chaos test to also kill the composition function pod, not just the core controller — separate failure surface. — _Bug 4-class composition-function input rejections look like controller bugs but aren't._<br>• **A5** (A5→A2-006 · _parallel-test_ · phase 1+): A2-022/A2-023/A2-024 chaos tests are independent failure injections — fan them out across one cluster simultaneously and assert each surfaces its own alarm. — _Tests cross-blast-radius too._<br>• **A6** (A6→A2-008 · _simplification_ · phase 2+): A2-022 chaos test on Crossplane pod kill obviates the manual `crossplane-reset-provider.sh` runbook step — automated test proves recovery works. — _Test = living documentation._<br>• **A6** (A6→A2-015 · _refactor_ · phase 2+): Combine A2-014 + A2-015 + A2-022 + A2-023 into a single chaos-suite runner; each test currently bootstraps its own kill-and-watch scaffolding. — _Shared scaffolding cuts code by ~60%._ |
| `A2-023` | 1+ | Chaos test: kill the ESO controller pod during a secret rotation and assert no Secret is left with stale value.<br><br><sub>_chaos-test_ · ESO failure modes are silent until consumers read the wrong value._</sub> | • **A5** (A5→A2-006 · _parallel-test_ · phase 1+): A2-022/A2-023/A2-024 chaos tests are independent failure injections — fan them out across one cluster simultaneously and assert each surfaces its own alarm. — _Tests cross-blast-radius too._<br>• **A6** (A6→A2-015 · _refactor_ · phase 2+): Combine A2-014 + A2-015 + A2-022 + A2-023 into a single chaos-suite runner; each test currently bootstraps its own kill-and-watch scaffolding. — _Shared scaffolding cuts code by ~60%._ |
| `A2-024` | 1+ | Chaos test: scale ingress-nginx to zero, assert NLB UnHealthyHostCount alarms, scale back, assert recovery.<br><br><sub>_chaos-test_ · Verifies the alarming path A1-012 actually fires._</sub> | • **A5** (A5→A2-006 · _parallel-test_ · phase 1+): A2-022/A2-023/A2-024 chaos tests are independent failure injections — fan them out across one cluster simultaneously and assert each surfaces its own alarm. — _Tests cross-blast-radius too._ |
| `A2-025` | 1+ | Chaos test: revoke the management cluster's OIDC provider in IAM and assert every IRSA-dependent controller surfaces an actionable error within 5 minutes.<br><br><sub>_chaos-test_ · OIDC provider misconfig is catastrophic and silent; chaos test proves observability fires._</sub> | • **A1** (A1→A2-008 · _observability-extension_ · phase 1+): Extend A2-025 (OIDC provider revoke chaos) with a baseline CloudWatch alarm assertion that the platform's `IRSAFailureCount` metric crosses threshold within SLO. — _Validates not just the failure mode but the alerting SLO._ |
| `A2-026` | 2+ | Negative test: apply a PlatformSecret claim with a schema-violating extra field and assert kubectl-apply fails with a clear strict-schema error.<br><br><sub>_negative-test_ · XRD strict-schema gate (followup #6) needs an actual rejection oracle._</sub> | • **A3** (A3→A2-005 · _regression-extension_ · phase 2+): A2-026 (extra-field rejection) should also cover a Composition with a missing `type: Format` on a string transform — the literal PR #61 bug. — _Pairs the schema-violation negative with the historical concrete bug._ |
| `A2-027` | 2+ | Negative test: apply a PlatformSecret claim in a namespace not on the Kyverno allowlist and assert the audit policy emits a violation report.<br><br><sub>_negative-test_ · Kyverno audit-mode failures are visible only by query; needs an explicit test._</sub> | _(no comments)_ |
| `A2-028` | 2+ | Contract test: every Composition's `resourceRefs` schema must match the live MR types installed by the provider-family-aws CRDs.<br><br><sub>_contract-test_ · Composition drift vs CRD versions silently breaks XRs (looks like the v2.0 zero-conditions bug)._</sub> | • **A5** (A5→A2-014 · _parallel-test_ · phase 1+): A2-028/029/030/031/032/033 contract tests are pure-static and can run in `make -j` at session start with zero AWS calls. — _Free wall-clock win._ |
| `A2-029` | 2+ | Contract test: every XRD `claimNames` listed in `crossplane/xrds/` must have a matching Kyverno policy in `policies/audit/` if cross-namespace concerns apply.<br><br><sub>_contract-test_ · Pairs XRDs with their guardrails so a new XRD without policy is caught at PR time._</sub> | _(no comments)_ |
| `A2-030` | 1+ | Contract test: every ArgoCD Application's `repoURL` must point at the same monorepo, not a forked branch.<br><br><sub>_contract-test_ · Bootstrap-from-fork is a class of incident the agent has hit before._</sub> | _(no comments)_ |
| `A2-031` | 1+ | Contract test: every `helm_release` chart version in `terraform/management/variables.tf` must match the version pinned in `tests/chainsaw/run.sh`.<br><br><sub>_contract-test_ · The chainsaw harness silently drifted from prod versions in past sessions; lint freezes them in lockstep._</sub> | • **A3** (A3→A2-008 · _regression-extension_ · phase 1+): Pair A2-031 (helm chart version lock) with a contract test that the chart digests in terraform match those resolved at `helm pull` time. — _Version pin + digest pin = real reproducibility._<br>• **A6** (A6→A2-012 · _simplification_ · phase 1+): A2-031 contract test (chart-version mirror) lets us delete the manual chart-version-bump checklist in PR templates. — _Lint catches drift; checklist becomes redundant._ |
| `A2-032` | 1+ | Cross-resource invariant: every ClusterSecretStore must reference an IRSA SA whose role grants `secretsmanager:GetSecretValue` on the queried ASM path.<br><br><sub>_contract-test_ · ESO config error surfaces only when a consumer reads — invariant catches it pre-flight._</sub> | _(no comments)_ |
| `A2-033` | 2+ | Cross-resource invariant: every Crossplane Composition writing to a region must match the provider's configured region or use explicit region override.<br><br><sub>_contract-test_ · Cross-region Composition bugs are notoriously silent (followup #4)._</sub> | _(no comments)_ |
| `A2-034` | 2+ | E2E test: full claim->XR->MR->ASM Secret->ExternalSecret->K8s Secret round-trip including reading the K8s Secret payload and comparing to the SDK-read ASM value.<br><br><sub>_e2e-test_ · Six-stage chain has many possible silent failures; only end-to-end byte-compare proves correctness._</sub> | • **A6** (A6→A2-006 · _refactor_ · phase 2+): Full E2E claim->ASM byte-compare (A2-034) makes the layered tests A2-001 + A2-012 + A2-053 partially redundant — once the byte-compare passes, those become smoke-only. — _Single end-to-end oracle subsumes mid-chain probes._ |
| `A2-035` | 2+ | Concurrency+chaos hybrid: 10 claims in parallel while bouncing the Crossplane controller every 30s; all must reach Ready.<br><br><sub>_chaos-test_ · Stresses leader-election and work-queue ordering under realistic conditions._</sub> | • **A3** (A3→A2-014 · _regression-extension_ · phase 2+): Extend A2-035 (concurrency + chaos) with a CloudWatch metric oracle for AWS API throttling — assert no `ThrottlingException` surge during the burst. — _Sandbox-cap-style invariant on API quota, not just instance count._<br>• **A5** (A5→A2-007 · _orchestration_ · phase 2+): A2-035 concurrency+chaos hybrid is the perfect input to A5-042 back-pressure subagent — feed throttle signals during the run. — _Surfaces realistic throttle behavior._ |
| `A2-036` | 1+ | Negative test: create an Ingress with a TLS host outside the wildcard ACM cert SAN and assert ALB attachment fails with diagnosable error.<br><br><sub>_negative-test_ · Hostname/cert mismatch is a recurring confusing failure; explicit test makes the error class loud._</sub> | _(no comments)_ |
| `A2-037` | 5+ | OIDC flow test: revoke a Keycloak user's group membership and assert the next EKS API call fails with `forbidden` (proves group-claim freshness, not just token validity).<br><br><sub>_oidc-flow-test_ · Token caching masks revocations; needs a test of the no-cache path._</sub> | _(no comments)_ |
| `A2-038` | 5+ | OIDC flow test: rotate the Keycloak realm signing key and assert in-flight kubectl sessions either succeed via JWKS refresh or fail cleanly.<br><br><sub>_oidc-flow-test_ · Key rotation procedures are theoretical until tested._</sub> | _(no comments)_ |
| `A2-039` | 5+ | Concurrency test: 50 OIDC token requests against Keycloak in parallel asserting all succeed within deadline (proves Keycloak HA / DB connection pool).<br><br><sub>_concurrency-test_ · Keycloak is a single-pod SPOF until proven otherwise._</sub> | • **A5** (A5→A2-005 · _subagent-pool_ · phase 5+): A2-039 (50 OIDC requests) and A2-049 Cognito browser-flow can share the same subagent pool launched once. — _Reuses auth-flow harness._ |
| `A2-040` | 5+ | Soak test: keep a kubectl `watch pods` open for 60 minutes via the Keycloak-federated OIDC user, asserting the token refresh path works.<br><br><sub>_soak-test_ · Token expiry mid-watch is a recurring kubectl surprise._</sub> | _(no comments)_ |
| `A2-041` | 3+ | Integration test: dispatch an ArgoCD ApplicationSet, wait for fan-out, then delete and assert no orphan Applications.<br><br><sub>_integration-test_ · ApplicationSet cleanup bugs are subtle._</sub> | _(no comments)_ |
| `A2-042` | 0+ | CloudWatch oracle test: assert that within 5 min of any `terraform apply` the CloudTrail log contains an `eks:UpdateClusterConfig` (or equivalent) that ties the apply to a specific principal.<br><br><sub>_integration-test_ · Provides an auditable chain of custody for every apply._</sub> | • **A3** (A3→A2-009 · _regression-extension_ · phase 0+): Extend A2-042 (CloudTrail apply audit oracle) to also assert no out-of-band `RunInstances` events occurred during the apply window — sandbox-cap guard. — _Cap exceedance has been the most catastrophic class historically._ |
| `A2-043` | 1+ | Negative test: attempt to deploy a Pod with `hostNetwork: true` and assert Kyverno enforce-mode (when enabled) rejects it.<br><br><sub>_negative-test_ · Validates the upgrade path from audit-mode to enforce-mode is real._</sub> | _(no comments)_ |
| `A2-044` | 3+ | E2E test: provision a PlatformCluster claim, then run `aws eks describe-cluster` and assert version, addons, IRSA OIDC provider, and node group match the Composition.<br><br><sub>_claim-roundtrip-test_ · EKS-via-XRD is phase 3's biggest contract; SDK readback is the only oracle._</sub> | • **A5** (A5→A2-009 · _speculative-test_ · phase 3+): A2-044 EKS-via-XRD validation should run against the kind rehearsal cluster (A5-040) first, in parallel with the real apply. — _Catches composition bugs in seconds._ |
| `A2-045` | 3+ | E2E test: from the management cluster, kubectl against the spoke EKS provisioned by A2-044 using the bootstrap admin to confirm reachability.<br><br><sub>_e2e-test_ · Cluster Ready != cluster reachable; explicit kubectl probe needed._</sub> | _(no comments)_ |
| `A2-046` | 3+ | Negative test: tear down a spoke PlatformCluster claim and assert all spoke-side AWS resources (NodeGroup, OIDC provider, ENIs, security groups, EBS volumes) are gone via SDK enumeration.<br><br><sub>_negative-test_ · Crossplane delete-cascade has known orphan classes; enumeration is the only oracle._</sub> | • **A1** (A1→A2-012 · _observability-pair_ · phase 3+): Pair A2-046 (PlatformCluster teardown enumeration) with AWS Config history queries so the SDK enumeration sees *deleted* resources, not just missing ones (Config retains tombstones). — _Distinguishes "deleted cleanly" from "never created" — critical for cleanup tests._<br>• **A3** (A3→A2-006 · _regression-extension_ · phase 3+): Extend A2-046 (spoke teardown) to enumerate by tag `managed-by=k8-platform` against the sandbox account, catching orphans across regions us-east-1 + us-west-2. — _Sandbox-kill class; cross-region orphans are uniquely silent._<br>• **A5** (A5→A2-011 · _pipeline_ · phase 3+): A2-046 spoke teardown enumeration can run in parallel with the spoke's own provisioning of the NEXT test claim. — _Cleanup tax disappears._<br>• **A6** (A6→A2-010 · _simplification_ · phase 3+): A2-046 (post-teardown SDK enumeration) makes manual "did I clean up?" handoff checklist items obsolete. — _Test enforces invariant._ |
| `A2-047` | 1+ | Concurrency test: two ArgoCD sync operations against the same App from two clients to confirm idempotent serialization.<br><br><sub>_concurrency-test_ · Race-condition syncs can leave the App in an oscillating state._</sub> | _(no comments)_ |
| `A2-048` | 1+ | Chaos test: cordon both nodes simultaneously, assert PDBs prevent total outage, uncordon, assert auto-recovery.<br><br><sub>_chaos-test_ · The 2-node cap (sandbox limit) makes PDB testing nontrivial but important._</sub> | _(no comments)_ |
| `A2-049` | 0+ | E2E test: trigger Cognito hosted-UI login programmatically (Selenium or requests-with-cookie-jar), complete the flow, assert a session JWT.<br><br><sub>_e2e-test_ · Cognito UI breaks silently when SPA config drifts; only browser-flow tests catch it._</sub> | • **A1** (A1→A2-014 · _observability-pair_ · phase 0+): Pair A2-049 (Cognito hosted-UI flow) with a CloudTrail `Authentication` event query asserting the same flow appears server-side. — _Catches client-side mocks pretending to be a real flow._<br>• **A5** (A5→A2-005 · _subagent-pool_ · phase 5+): A2-039 (50 OIDC requests) and A2-049 Cognito browser-flow can share the same subagent pool launched once. — _Reuses auth-flow harness._ |
| `A2-050` | 2+ | Soak test: every 5 min for 2 hours, dispatch a probe PlatformSecret claim, measure time-to-Ready, fail if any iteration exceeds 90s baseline by 3x.<br><br><sub>_soak-test_ · Latency creep is invisible until it becomes outage; baseline + threshold catches it._</sub> | • **A5** (A5→A2-012 · _orchestration_ · phase 2+): A2-050 (5-min cadence latency probe) should subscribe to the event bus and back off when A5-042 reports throttling. — _Cooperative load._ |
| `A2-051` | 1+ | Negative test: revoke the ArgoCD admin password and assert subsequent CLI sync fails with auth error rather than silent no-op.<br><br><sub>_negative-test_ · Authentication-failure-as-success is a known anti-pattern._</sub> | _(no comments)_ |
| `A2-052` | 1+ | Contract test: every ExternalSecret must reference a ClusterSecretStore that actually exists in the cluster.<br><br><sub>_contract-test_ · Dangling refs render silently in `kubectl get` but never sync._</sub> | _(no comments)_ |
| `A2-053` | 2+ | Integration test: rotate an ASM secret value via SDK, wait `refreshInterval`+10s, read the downstream K8s Secret and assert the new value propagated.<br><br><sub>_integration-test_ · Phase 2's whole value proposition is rotation; needs an end-to-end oracle._</sub> | • **A1** (A1→A2-010 · _observability-pair_ · phase 2+): Pair A2-053 (rotation propagation) with an ASM rotation-Lambda invocation count metric to detect double-rotation races. — _Rotation race conditions are silent until two consumers disagree._<br>• **A5** (A5→A2-008 · _pipeline_ · phase 2+): A2-053 ASM rotation test can be triggered by event bus the instant A2-001 round-trip subagent reports Ready, no sleep. — _Removes explicit wait._<br>• **A6** (A6→A2-006 · _refactor_ · phase 2+): Full E2E claim->ASM byte-compare (A2-034) makes the layered tests A2-001 + A2-012 + A2-053 partially redundant — once the byte-compare passes, those become smoke-only. — _Single end-to-end oracle subsumes mid-chain probes._ |
| `A2-054` | 2+ | Concurrency test: create a PlatformSecret claim while simultaneously deleting it (within 1s), assert no orphan ASM Secret remains.<br><br><sub>_concurrency-test_ · Create-delete race exposes finalizer ordering bugs._</sub> | _(no comments)_ |
| `A2-055` | 5+ | OIDC flow test: assume a Keycloak break-glass admin path via realm-export-import to verify disaster recovery procedure works.<br><br><sub>_oidc-flow-test_ · DR procedures rot in docs until tested._</sub> | _(no comments)_ |
| `A2-056` | 4+ | CloudWatch metric-filter regression test: emit a known synthetic log line and assert the metric increments within 60s.<br><br><sub>_integration-test_ · Validates the alerting pipeline itself, not just that alarms are configured._</sub> | _(no comments)_ |
| `A2-057` | 1+ | Chaos test: induce ECR pull throttling (rapid-pull loop) and assert pod restart back-off survives without crashing the cluster.<br><br><sub>_chaos-test_ · Real-world failure mode that's hard to reproduce except by deliberate induction._</sub> | _(no comments)_ |
| `A2-058` | 2+ | Negative test: apply a Composition with a `type: Format` missing on a string transform (the PR #61 bug) and assert chainsaw fails fast with a meaningful error.<br><br><sub>_negative-test_ · Codifies bug 4 as a permanent guard._</sub> | • **A3** (A3→A2-010 · _regression-extension_ · phase 2+): A2-058 (PR #61 `type: Format` regression) could be promoted to a fixture-driven negative suite where every past Composition bug gets its own fixture file. — _Bug-class registry as living test corpus, not just doc._ |
| `A2-059` | 1+ | Soak test: 4-hour ArgoCD self-heal loop with a deliberately mutated resource every 10 min, asserting reversion every cycle without leak.<br><br><sub>_soak-test_ · Self-heal feedback loops can degrade slowly; soak surfaces it._</sub> | _(no comments)_ |
| `A2-060` | 6+ | E2E test: from a workload cluster (phase 6), call the management cluster's ArgoCD admin API to register itself and assert appearance in `argocd cluster list`.<br><br><sub>_e2e-test_ · Multi-cluster registration is phase 6's central contract._</sub> | • **A1** (A1→A2-015 · _observability-extension_ · phase 6+): Extend A2-060 (multi-cluster registration) with a Route53 health-check probe from each cluster to the mgmt ArgoCD endpoint, asserting bidirectional reachability. — _Registration appearing != reachable; reachability is the actual contract._ |
| `A2-061` | 1+ | Concurrency test: scale a Deployment from 1 to 50 replicas and back to 1, assert ExternalDNS doesn't thrash records.<br><br><sub>_concurrency-test_ · Rapid endpoint churn historically broke ExternalDNS._</sub> | _(no comments)_ |
| `A2-062` | 1+ | Chaos test: simulate AWS regional API failure by deny-listing the SecretsManager endpoint via NetworkPolicy and assert ESO surfaces error within 2 min.<br><br><sub>_chaos-test_ · Network-level failure injection is impossible without cluster-admin._</sub> | _(no comments)_ |
| `A2-063` | 1+ | Negative test: deploy two Ingresses with identical host, assert only one wins and the other reports a clear conflict status.<br><br><sub>_negative-test_ · Silent overwrite is a known ingress-nginx footgun._</sub> | _(no comments)_ |
| `A2-064` | 2+ | E2E test: dispatch `terraform destroy` for phase 2 only and assert phase 1 helm releases remain Healthy (AGENTS.md §5 invariant 1).<br><br><sub>_e2e-test_ · Invariant 1 is load-bearing and currently unenforced by any test._</sub> | • **A3** (A3→A2-011 · _regression-extension_ · phase 2+): Extend A2-064 (destroy phase 2 only) to verify phase-1 IRSA roles still match their phase-1 SAs after the destroy, not just helm health. — _Cross-phase blast-radius checks; AGENTS.md §5 invariant 1 has IRSA implications._<br>• **A6** (A6→A2-011 · _simplification_ · phase 2+): A2-064 (phase-2-destroy invariant) lets us delete the verbal AGENTS.md §5 invariant 1 reminder — test enforces it. — _Executable invariants > prose invariants._ |
| `A2-065` | 1+ | Contract test: every CloudWatch log group referenced in saved Logs Insights queries (A1-006..A1-009) actually exists post-apply.<br><br><sub>_contract-test_ · Dangling query targets render silently empty results._</sub> | _(no comments)_ |

### A2 — General comments (no source-ID anchor)

Cross-review rows targeted at this agent but without an explicit `A{N}-NNN` reference in the comment text; preserved verbatim.

| Comment ID | From | Phase | Comment | Category & justification |
|---|---|---|---|---|
| `A1→A2-009` | A1 | 0+ | Add a contract test that every test in this file produces a structured JSON result artifact in a shared schema, enabling cross-run trend analytics. | _meta-test_ — Lets you ask "which test got 30% slower this month?" — impossible without structured artifacts. |
| `A1→A2-013` | A1 | 1+ | Add a per-test "evidence pack" assertion: every failing test must auto-attach (cluster events ±5 min, controller logs, CloudTrail ±5 min, Prom snapshot) before exiting non-zero. | _meta-test_ — Eliminates the "rerun to gather evidence" loop. |
| `A3→A2-012` | A3 | 0+ | Add a regression for the "Apply complete: 0 added silent no-op": run a known-mutating change with `triggers_replace` hash deliberately missing one manifest, assert the apply-and-verify oracle flags it. | _regression-extension_ — Codifies PR #67 root cause as a test. |
| `A3→A2-015` | A3 | 1+ | Add a contract test that every ClusterPolicy in `policies/audit/` has a corresponding fixture both for pass and fail to prove enforce-mode upgrade is safe. | _regression-extension_ — Audit-to-enforce upgrade has historically been the highest-risk transition. |
| `A4→A2-001` | A4 | 2+ | Pair the round-trip test with a `runbook-claim-roundtrip-debug.md` covering the four likely-failure points (Composition render, MR provisioning, AWS API, tag/KMS drift). | _runbook_ — Test failures need a debug map ready, not authored mid-incident. |
| `A4→A2-003` | A4 | 1+ | Capture the `simulate-principal-policy` output as a per-PR artifact so diffs across PRs surface drift trends. | _shell-helper_ — Per-run only catches absolutes; diff-over-time catches creep. |
| `A4→A2-005` | A4 | 1+ | Add a companion live-probe workflow `compare-irsa-trust-to-cluster.yml` that runs the invariant on demand from the sandbox between PRs. | _dispatch-workflow_ — The test catches at PR time; the workflow catches drift between PRs. |
| `A4→A2-009` | A4 | 5+ | Save the temp-realm test's auth-flow HAR/curl trace as an artifact for replay — debug speed-up for AuthN regressions. | _shell-helper_ — OIDC failures are easier to replay than recreate. |
| `A4→A2-012` | A4 | 2+ | Have the CloudWatch oracle output the exact Insights query URL so an operator can deep-link from a test failure. | _shell-helper_ — Test failures should hand you the next click. |
| `A4→A2-016` | A4 | 2+ | Pair the concurrency test with `runbook-claim-fanout-debug.md` listing the three known throttling signatures (ASM rate limit, IAM Create rate, EKS API 429). | _runbook_ — When 20-claim fan-out goes red, knowing the throttle source saves hours. |
| `A4→A2-018` | A4 | 2+ | Add a `soak-summary.py` post-processor that prints first-failure timestamp + suspected cause class — turns 60-min trace into one paragraph. | _shell-helper_ — Soak data is voluminous; the summary is the actionable part. |
| `A4→A2-022` | A4 | 2+ | After the chaos test, auto-emit a CloudWatch annotation marking the kill window so future Insights queries can correlate. | _shell-helper_ — Correlation between chaos and observed behavior is currently manual. |
| `A4→A2-025` | A4 | 1+ | Pair the OIDC-revoke chaos test with `runbook-oidc-revoke-recovery.md` documenting the exact `aws iam create-open-id-connect-provider` re-bind steps. | _runbook_ — Chaos is only safe with a documented recovery path. |
| `A4→A2-034` | A4 | 2+ | Have the full chain test emit a `chain-trace.json` artifact (timestamps per hop) so latency creep per hop becomes graphable. | _shell-helper_ — Six-stage chain hides which hop is slowing. |
| `A4→A2-046` | A4 | 3+ | Pair the teardown invariant test with `runbook-orphan-resource-sweep.md` listing the 7 known orphan classes (ENI, EBS, SG, OIDC provider, NodeGroup remnants, EKS log group, IAM role). | _runbook_ — Enumeration is the only way to be sure. |
| `A4→A2-053` | A4 | 2+ | Add a `rotation-latency-histogram.py` post-processor — track p50/p95 rotation time over sessions. | _shell-helper_ — Rotation correctness is binary; latency creep is the silent killer. |
| `A4→A2-058` | A4 | 2+ | Pair Bug-4 negative test with a `pre-commit-composition-render.sh` that runs `crossplane render` on every changed Composition before commit — same defense, shorter loop. | _shell-helper_ — Catching at commit time is faster than catching in chainsaw. |
| `A4→A2-064` | A4 | 2+ | Emit the per-phase teardown evidence as a verifiable quote bundle (per verify-evidence-not-exit-codes) so the PR body links to literal proofs. | _shell-helper_ — Invariant-1 is load-bearing; evidence must be auditable. |
| `P→A2-001` | PRIMARY | 2+ | A "test pyramid health" report that emits coverage-by-phase × layer (unit/kyverno/integration/chainsaw/e2e) and flags layers that haven't been touched in 30 days. | _meta-test_ — Catches "we stopped writing chainsaw tests halfway through phase 2" before it becomes a habit. |
| `P→A2-002` | PRIMARY | 2+ | A property-based test harness that generates random valid PlatformSecret claim specs and asserts the round-trip invariant (claim accepted ⇒ ASM secret exists with matching tags). | _property-test_ — Surfaces edge cases (long names, unicode descriptions, weird refresh intervals) that example-based tests miss. |
| `P→A2-003` | PRIMARY | 0+ | A "bug regression corpus" directory: every retro'd bug gets a one-file reproducer kept forever; CI runs the whole corpus on every PR. | _regression-corpus_ — Turns institutional memory of past bugs into permanent guardrails. |
| `P→A2-004` | PRIMARY | 2+ | A cross-phase soak test that holds phase-2 claims live for 24 hours and asserts no MR flapping, no IRSA token expiry, no ArgoCD drift events. | _soak-test_ — Catches slow-burn issues (token-lifetime mismatches, refresh-interval bugs) invisible to short tests. |
| `P→A2-005` | PRIMARY | 2+ | A "tear-down completeness" test that deletes a phase-2 claim and asserts every cloud resource it created is actually gone (ASM secret, IAM bindings, tags). | _e2e-test_ — Defends against "ghost resources" that quietly accumulate cost. |
| `P→A2-006` | PRIMARY | 1+ | A negative-IRSA test that intentionally swaps the SA name on a deployment and asserts the provider goes Unhealthy within N seconds (regression for PR #66/#68). | _irsa-test_ — Locks the SA-pinning fix permanently. |
| `P→A2-007` | PRIMARY | 1+ | A "manifest hash drift" test that mutates the body of a manifest controlled by `terraform_data.triggers_replace` without bumping the trigger and asserts the diff is detected (regression for PR #67). | _unit-test_ — Prevents reintroduction of "edited the manifest but apply was a no-op". |

## A3 — Test gap mining

> **Mandate.** Brainstorm unit / Kyverno / integration / chainsaw / e2e tests that we should already have written under the OLD constraints (GH Actions only, no admin AWS, no direct kubectl) but never did — mined from retros + PR history — plus refactor/simplify ideas for tests we have.

_Source file: [`ai/brainstorming/A3-test-gaps-prior-constraints.md`](../../ai/brainstorming/A3-test-gaps-prior-constraints.md)_

_Idea count: 65_

| ID | Phase | Idea (with category and justification) | Comments (bulleted by commenter) |
|---|---|---|---|
| `A3-001` | 1+ | Static lint that every `helm_release` in `terraform/management/helm.tf` has a corresponding `irsa.tf` module AND vice versa (bidirectional).<br><br><sub>_unit-test_ · Would have caught bug 4 (ExternalDNS helm_release entirely missing) from PR #34 — IRSA module existed but no chart consumed it._</sub> | • **A5** (A5→A3-001 · _parallel-test_ · phase 0+): All A3 unit/static lints (A3-001..A3-021, A3-039..A3-058) are pure-CPU and parallelize trivially under `make -j$(nproc)`. — _Unit-test wall-clock drops from N×100ms to ~100ms._ |
| `A3-002` | 1+ | Helm-render assertion that for any chart with per-component ServiceAccounts (argo-cd, crossplane), the IRSA annotation lives at `<component>.serviceAccount.annotations`, not the top-level.<br><br><sub>_unit-test_ · Bug 3 from PR #34 (argocd IRSA on wrong SA) — silent until e2e verify._</sub> | • **A2** (A2→A3-001 · _extension_ · phase 1+): Promote the helm-render IRSA-annotation lint (A3-002) into a live e2e probe that, post-apply, kubectl-reads the SA and asserts the annotation matches. — _Static + runtime double-cover catches drift between helm values and rendered cluster state._ |
| `A3-003` | 1+ | Static lint that every Kyverno ClusterPolicy JMESPath literal between backticks parses as valid JSON.<br><br><sub>_unit-test_ · Already exists (`test_kyverno_policy_lint.sh`) — but extend to assert non-empty AND valid type per Kyverno schema for `count(@)`, `length(@)` etc._</sub> | _(no comments)_ |
| `A3-004` | 0+ | Static lint forbidding `set -uo pipefail` without `-e` in every `tests/**/*.sh` and `scripts/**/*.sh`.<br><br><sub>_unit-test_ · Silent-PASS bug from run 26347839740 (PR #59); the existing `test_integration_scripts_strict_mode.sh` only covers `tests/integration/`._</sub> | • **A6** (A6→A3-003 · _refactor_ · phase 0+): A3-005 (forbid assignments to bash readonly built-ins) plus A3-004 (strict-mode lint) belong in one `lint_shell_strictness.sh` — collapse them. — _One lint, two rules._ |
| `A3-005` | 0+ | Static lint forbidding assignment to bash readonly built-ins (`UID`, `EUID`, `BASHPID`, `RANDOM`, `SECONDS`, `LINENO`) across all `*.sh` (not just tests).<br><br><sub>_unit-test_ · Bug PR #59 in 11_platform_secret_e2e.sh; lint exists but should scan scripts/ and .github/scripts/._</sub> | • **A6** (A6→A3-003 · _refactor_ · phase 0+): A3-005 (forbid assignments to bash readonly built-ins) plus A3-004 (strict-mode lint) belong in one `lint_shell_strictness.sh` — collapse them. — _One lint, two rules._ |
| `A3-006` | 1+ | Unit test asserting every `argocd/apps/*.yaml` Application has either `syncPolicy.automated` OR a doc-comment opt-out header.<br><br><sub>_unit-test_ · Would have caught PR #55 manual-sync regression._</sub> | _(no comments)_ |
| `A3-007` | 2+ | Unit test asserting every XRD with `served: true` also has `referenceable: true` on the storage version.<br><br><sub>_unit-test_ · Bug class registered in TESTING-PLAN.md — partial coverage in `test_platform_secret_xrd.sh`, generalize to all XRDs._</sub> | • **A2** (A2→A3-002 · _extension_ · phase 2+): Extend A3-007 (XRD `served`/`referenceable`) with an e2e test that creates a claim against every XRD and asserts the API server actually returns the storage version. — _Marker-bit lint plus live verification._ |
| `A3-008` | 1+ | Helm-render assertion that the rendered argocd-server Ingress has `metadata.name` ending in `-server` (regardless of release name) — selector by label `app.kubernetes.io/component=server`.<br><br><sub>_refactor-test_ · Pending follow-up #2 in handoff: existing `test_helm_render.sh` checks broken with chart 6.7.3 release-name change; tolerated via `continue-on-error`._</sub> | • **A5** (A5→A3-009 · _cache_ · phase 1+): A3-008/A3-044 helm-render brittleness can be cached: render once at session start into `/tmp/helm-rendered/`, lints read from there. — _Avoids N redundant `helm template` calls._ |
| `A3-009` | 2+ | Unit test asserting every Composition's `function-patch-and-transform` `type: string` transform has `.string.type: Format` (or other valid subtype).<br><br><sub>_unit-test_ · Already exists (`test_composition_string_transform_type.sh`); extend to validate the format string references resolvable patches._</sub> | _(no comments)_ |
| `A3-010` | 2+ | Unit test asserting every Composition `patch` with `fromFieldPath: metadata.namespace` on a cluster-scoped XR is replaced with `spec.claimRef.namespace`.<br><br><sub>_unit-test_ · Bug-class row in TESTING-PLAN.md ("ExternalSecret silently lands in crossplane-system"); partial coverage in `test_platform_secret_composition.sh`, generalize._</sub> | _(no comments)_ |
| `A3-011` | 1+ | Static lint that every `terraform_data.<x>.triggers_replace` lifecycle hash includes `sha256()` of any inline manifest the resource provisions.<br><br><sub>_unit-test_ · Bug PR #67 (`triggers_replace` miss, manifest edit was a no-op)._</sub> | • **A6** (A6→A3-006 · _simplification_ · phase 1+): A3-011 + A3-060 are the same lint (manifest sha256 in triggers_replace); merge them. — _Two rows, one tool._ |
| `A3-012` | 2+ | Unit test asserting `tests/chainsaw/versions.env` mirrors every Crossplane-related pin in `terraform/management/variables.tf`.<br><br><sub>_unit-test_ · Already exists for chart version (`test_chainsaw_kind_config.sh`); extend to provider-family-aws, function-patch-and-transform, kind version._</sub> | • **A5** (A5→A3-010 · _pipeline_ · phase 1+): A3-012/A3-048 chart-version drift checks can run as a pre-commit hook in addition to test-time, catching it 100x earlier. — _Shift-left._ |
| `A3-013` | 0+ | Unit test asserting any GitHub Actions workflow that runs `curl ... -o /usr/local/bin/X` is preceded by `sudo` (or uses `$HOME/.local/bin`).<br><br><sub>_unit-test_ · Bug PR #51 (chainsaw install missing sudo on ubuntu-24.04)._</sub> | _(no comments)_ |
| `A3-014` | 0+ | YAML-validity lint over every `.github/workflows/*.yml` that parses via `python -c yaml.safe_load` and rejects unindented heredocs inside `run:<br><br><sub>_` blocks._ · unit-test &#124; Bug PR #65 (workflow_dispatch silently absent on malformed YAML)._</sub> | • **A6** (A6→A3-009 · _refactor_ · phase 0+): A3-014 + A3-015 + A3-049 are all "workflow YAML correctness" linters; ship them as one actionlint config plus one custom rule pack. — _One toolchain not three._ |
| `A3-015` | 0+ | Unit test asserting every `.github/workflows/*.yml` job with `workflow_dispatch` declares all `inputs` used in the body via `${{ inputs.X }}`.<br><br><sub>_unit-test_ · Multiple workflow_dispatch issues across PRs #57, #58, #60._</sub> | • **A6** (A6→A3-009 · _refactor_ · phase 0+): A3-014 + A3-015 + A3-049 are all "workflow YAML correctness" linters; ship them as one actionlint config plus one custom rule pack. — _One toolchain not three._ |
| `A3-016` | 1+ | Helm-render lint asserting `server.ingress.hostname` is set when ingress is enabled (singular key for chart 6.7.x).<br><br><sub>_unit-test_ · Bug 7 PR #34 (`hosts[0]` vs `hostname`)._</sub> | _(no comments)_ |
| `A3-017` | 1+ | IRSA-policy completeness test extension: assert ExternalDNS policy includes `route53:ListHostedZones` AND `ListHostedZonesByName` AND `ChangeResourceRecordSets`.<br><br><sub>_iam-policy-completeness-test_ · Bug 6 PR #34 — runtime AccessDenied loop._</sub> | • **A1** (A1→A3-003 · _iam-policy-completeness-extension_ · phase 1+): Pair A3-017 (ExternalDNS IAM completeness) with a lint that every IRSA inline policy's actions are also enumerated in a matching `iam-actions-required.yaml` doc — drift-proof permission spec. — _Source-of-truth split prevents silent policy widening._<br>• **A2** (A2→A3-003 · _extension_ · phase 1+): Couple A3-017 (ExternalDNS IAM policy completeness) with a live test that performs each listed Route53 API call from inside an ExternalDNS pod via `kubectl exec`. — _Static policy presence + real API call = real coverage._ |
| `A3-018` | 1+ | IRSA trust-policy completeness test: assert every IRSA role's trust `Condition.StringEquals` subject EXACTLY matches the SA name in the consuming helm_release values.<br><br><sub>_irsa-trust-test_ · Bug 5 PR #66 (hash-suffixed SA name not pinned)._</sub> | • **A6** (A6→A3-008 · _refactor_ · phase 1+): A3-018 (IRSA trust subject pinned to SA name) and A3-019 (DeploymentRuntimeConfig SA pinned) collapse into one "IRSA binding integrity" suite covering the whole PR #66/#68 bug chain. — _One test asserts the chain end-to-end._ |
| `A3-019` | 2+ | Unit test asserting `DeploymentRuntimeConfig.spec.serviceAccountTemplate.metadata.name` is pinned (non-empty) for every Crossplane provider that uses IRSA.<br><br><sub>_unit-test_ · Bug 5 PR #66 — direct defending lint._</sub> | • **A2** (A2→A3-012 · _extension_ · phase 2+): Add a chainsaw companion to A3-019 (DeploymentRuntimeConfig pinned name) that asserts the live Crossplane Deployment uses the pinned SA after a forced rollout. — _Runtime witness to the static lint._<br>• **A6** (A6→A3-008 · _refactor_ · phase 1+): A3-018 (IRSA trust subject pinned to SA name) and A3-019 (DeploymentRuntimeConfig SA pinned) collapse into one "IRSA binding integrity" suite covering the whole PR #66/#68 bug chain. — _One test asserts the chain end-to-end._ |
| `A3-020` | 1+ | Unit test scanning Kyverno ClusterPolicies for missing `spec.admission`, `spec.background`, or `pod-policies.kyverno.io/autogen-controllers` annotation (the drift triad).<br><br><sub>_unit-test_ · Already partially in `test_kyverno_policy_lint.sh`; extend to every audit policy. Bug 3 from PR #64._</sub> | • **A6** (A6→A3-004 · _simplification_ · phase 1+): A3-022 (Kyverno policy enforcing A3-020) makes A3-020 the authoritative source; the static lint can then be retired. — _Runtime gate is stronger than static._<br>• **A6** (A6→A3-013 · _simplification_ · phase 1+): Once A3-033 (Kyverno drift detection runtime test) is live, the related static lint A3-020 can be downgraded to a fast smoke check or removed. — _Runtime > static for drift._ |
| `A3-021` | 1+ | Static lint asserting every ArgoCD Application uses a pinned `targetRevision` (commit SHA or tag, not `HEAD` or branch).<br><br><sub>_unit-test_ · Already exists (`test_argocd_app_revision_pinned.sh`); extend to forbid `main` as a value._</sub> | • **A5** (A5→A3-001 · _parallel-test_ · phase 0+): All A3 unit/static lints (A3-001..A3-021, A3-039..A3-058) are pure-CPU and parallelize trivially under `make -j$(nproc)`. — _Unit-test wall-clock drops from N×100ms to ~100ms._<br>• **A6** (A6→A3-012 · _refactor_ · phase 1+): A3-021 + A3-052 + A3-064 are "every X must point at known Y in allowlist Z" lints — generalize as a single declarative ref-integrity tool driven by a YAML config. — _One tool replaces N hardcoded lints._ |
| `A3-022` | 1+ | Kyverno audit policy: deny ClusterPolicy without explicit `spec.admission` field.<br><br><sub>_kyverno-policy_ · Runtime equivalent of A3-020 — catches drift introduced by hand._</sub> | • **A5** (A5→A3-011 · _parallel-test_ · phase 1+): A3-022..A3-028 Kyverno audit policies can be `kyverno test`'d in parallel against fixtures in `make -j`. — _Policy lint becomes O(1) wall-clock._<br>• **A6** (A6→A3-004 · _simplification_ · phase 1+): A3-022 (Kyverno policy enforcing A3-020) makes A3-020 the authoritative source; the static lint can then be retired. — _Runtime gate is stronger than static._ |
| `A3-023` | 2+ | Kyverno audit policy: warn on any Deployment in `crossplane-system` whose `spec.template.spec.serviceAccountName` does NOT match a pinned `upbound-provider-*` allowlist.<br><br><sub>_kyverno-policy_ · Bug 5 PR #66 visible at runtime._</sub> | _(no comments)_ |
| `A3-024` | 2+ | Kyverno audit policy: deny ExternalSecret whose `spec.secretStoreRef.name` is not in the known ClusterSecretStore allowlist.<br><br><sub>_kyverno-policy_ · Catches typos that silently fail to fetch secrets._</sub> | _(no comments)_ |
| `A3-025` | 2+ | Kyverno audit policy: deny Composition with `mode: Resources` (legacy v1 mode) — require Pipeline.<br><br><sub>_kyverno-policy_ · PR #51 migrated to Pipeline; prevents regression._</sub> | _(no comments)_ |
| `A3-026` | 2+ | Kyverno audit policy: deny Crossplane Provider without a referenced DeploymentRuntimeConfig.<br><br><sub>_kyverno-policy_ · Defends the IRSA-binding contract at runtime._</sub> | _(no comments)_ |
| `A3-027` | 1+ | Kyverno audit policy: warn on ArgoCD Application with `syncPolicy.automated.selfHeal: false`.<br><br><sub>_kyverno-policy_ · Drift surface; PR #54/#55 introduced manual-sync apps that need explicit opt-out documentation._</sub> | • **A2** (A2→A3-014 · _new-scenario_ · phase 1+): Add a soak test on top of A3-027 (selfHeal: false warn) that intentionally mutates the resource over 1h to confirm drift remains visible. — _Sustained drift exercises alarm fatigue and dashboard surfacing._ |
| `A3-028` | 1+ | Kyverno audit policy: deny any Secret in any namespace whose `data` size > 4KB without `eso.io/managed: true` annotation.<br><br><sub>_kyverno-policy_ · Detects hand-applied secrets that bypass ESO._</sub> | • **A5** (A5→A3-011 · _parallel-test_ · phase 1+): A3-022..A3-028 Kyverno audit policies can be `kyverno test`'d in parallel against fixtures in `make -j`. — _Policy lint becomes O(1) wall-clock._ |
| `A3-029` | 2+ | Integration test that dispatches `phase-2-diagnose.yml` and asserts every expected dump section is present (XR yaml, MR yamls, events, controller logs).<br><br><sub>_integration-test_ · PR #60 has no regression test — could silently lose a section._</sub> | • **A1** (A1→A3-004 · _observability-test_ · phase 2+): Extend A3-029 (diagnose dump regression test) by asserting each dump section's byte size is within ±50% of a recorded baseline — empty dumps become loud. — _Silent regressions where a section becomes empty are common._<br>• **A5** (A5→A3-002 · _speculative-test_ · phase 2+): A3-029 diagnose-completeness integration test can run on the kind rehearsal cluster (A5-040) instead of real EKS. — _Zero AWS dependency._<br>• **A6** (A6→A3-007 · _simplification_ · phase 2+): A3-029's diagnose-section-presence test, once it lands, lets us retire the manual review step "did the diagnose dump include X?" in PR templates. — _Test catches it; humans don't have to._ |
| `A3-030` | 2+ | Integration test that applies a PlatformSecret claim with `spec.foo: bar` (unknown field) and asserts the apply is REJECTED by strict-schema.<br><br><sub>_integration-test_ · Pending follow-up #6 in handoff (strict-schema rejection scenario)._</sub> | _(no comments)_ |
| `A3-031` | 2+ | Integration test that creates two PlatformSecret claims in different namespaces with the SAME name and asserts no ASM key collision.<br><br><sub>_integration-test_ · UID-shadowing bug PR #59 root cause (ASM key was `k8-platform/1001` for every claim); the runtime defense._</sub> | • **A1** (A1→A3-012 · _unit-test_ · phase 2+): Pair A3-031 (UID-shadowing integration test) with a lint that every ASM-writing Composition's external-name template includes `${xr.metadata.uid}` (or equivalent) — bug 5 at authoring time. — _Defends UID-collision at PR time._<br>• **A2** (A2→A3-004 · _new-scenario_ · phase 2+): Add a soak-companion to A3-031 (UID-shadow collision) that runs the cross-namespace create loop for 30 min to catch slow-collision modes. — _Sustained pressure surfaces collision bugs the single-shot misses._<br>• **A5** (A5→A3-007 · _parallel-test_ · phase 2+): A3-031 cross-namespace UID-collision test fans out by launching N claims in parallel with one subagent each. — _Real concurrency, not sequential pseudo-concurrency._ |
| `A3-032` | 2+ | Integration test that performs a Composition edit via git → ArgoCD sync → assert the XR sees the new Composition revision within 3 min.<br><br><sub>_integration-test_ · PR #67/#68 cluster-state-divergence bug class ("apply succeeded ≠ change reached cluster")._</sub> | • **A2** (A2→A3-005 · _extension_ · phase 2+): Extend A3-032 (Composition edit → ArgoCD sync) with an SDK readback that asserts the new field actually changed the underlying ASM tag/value. — _"Synced" is necessary not sufficient; SDK truth closes the gap._<br>• **A5** (A5→A3-003 · _pipeline_ · phase 2+): A3-032 commit→ArgoCD-sync latency test should subscribe to the A5-027 event bus rather than poll for 3 min. — _Sub-second oracle._ |
| `A3-033` | 1+ | Integration test that mutates a Kyverno ClusterPolicy field on the cluster directly and asserts ArgoCD reports OutOfSync within 3 min.<br><br><sub>_integration-test_ · Bug 3 PR #64 — Kyverno drift detection requires this test._</sub> | • **A1** (A1→A3-006 · _unit-test_ · phase 1+): Pair A3-033 (Kyverno drift detection integration test) with a unit-level lint that every Kyverno mutate-rule has `mutateExistingOnPolicyUpdate: false` unless explicitly opted in. — _Defends Bug 3 at authoring time, not just runtime._<br>• **A2** (A2→A3-006 · _extension_ · phase 1+): Pair A3-033 (Kyverno drift detection) with an automated revert step and assert ArgoCD self-heal restores within deadline. — _One test exercises both detection and recovery._<br>• **A6** (A6→A3-013 · _simplification_ · phase 1+): Once A3-033 (Kyverno drift detection runtime test) is live, the related static lint A3-020 can be downgraded to a fast smoke check or removed. — _Runtime > static for drift._ |
| `A3-034` | 2+ | Chainsaw scenario: apply PlatformSecret claim, edit `spec.refreshInterval` via patch, assert new value propagates to ExternalSecret.<br><br><sub>_chainsaw_ · Composition patch-back not covered by existing 00/01/02 scenarios._</sub> | • **A5** (A5→A3-004 · _parallel-test_ · phase 2+): A3-034..A3-038 chainsaw scenarios run today as one big harness — split into one-scenario-per-process and run with `xargs -P`. — _N-scenario time becomes max-scenario time._ |
| `A3-035` | 2+ | Chainsaw scenario: delete the Composition while a claim exists, assert XR transitions to `Synced=False` with informative message.<br><br><sub>_chainsaw_ · Failure-mode coverage; today undefined._</sub> | _(no comments)_ |
| `A3-036` | 2+ | Chainsaw scenario: apply a malformed claim (missing required field) and assert the API server rejects with the OpenAPI message, not a generic 500.<br><br><sub>_chainsaw_ · XRD schema strictness defense._</sub> | _(no comments)_ |
| `A3-037` | 2+ | Chainsaw scenario: simulate provider package install failure (404 image) and assert the Composition reports `pkg.crossplane.io/healthy=False` within 60s.<br><br><sub>_chainsaw_ · Bug class: silent provider non-install in PR #51._</sub> | • **A2** (A2→A3-007 · _new-scenario_ · phase 2+): Add an e2e companion to A3-037 (provider package install failure) that confirms a subsequent push of a fixed image recovers within 5 min. — _Failure detection + recovery in one scenario._ |
| `A3-038` | 2+ | Chainsaw scenario for PlatformCluster: apply claim, assert XR's `resourceRefs` lists all 8 expected MRs (2 IAM roles, 4 attachments, EKS, NodeGroup).<br><br><sub>_chainsaw_ · XRD/Composition shape regression defense for phase 2b._</sub> | • **A2** (A2→A3-008 · _extension_ · phase 2+): Extend A3-038 (PlatformCluster resourceRefs shape) with `aws eks describe-cluster` + `aws iam get-role` assertions per MR. — _Cloud-truth oracle for the composite._<br>• **A5** (A5→A3-004 · _parallel-test_ · phase 2+): A3-034..A3-038 chainsaw scenarios run today as one big harness — split into one-scenario-per-process and run with `xargs -P`. — _N-scenario time becomes max-scenario time._ |
| `A3-039` | 3+ | Unit test asserting every `clusters/<cluster>/*.yaml` ArgoCD app references an existing `crossplane/claims/` file.<br><br><sub>_unit-test_ · Phase-3 scaffolding (PR #55) introduced this pattern with no lint._</sub> | • **A5** (A5→A3-001 · _parallel-test_ · phase 0+): All A3 unit/static lints (A3-001..A3-021, A3-039..A3-058) are pure-CPU and parallelize trivially under `make -j$(nproc)`. — _Unit-test wall-clock drops from N×100ms to ~100ms._<br>• **A6** (A6→A3-005 · _refactor_ · phase 2+): A3-039 (cluster-app references claim file) combined with A3-052 (providerConfigRef exists) becomes a single "reference-integrity" linter across all YAML. — _Pattern is "ref → must exist"; one tool._ |
| `A3-040` | 0+ | Unit test asserting `.github/scripts/post-comment.py` `OUTCOMES` and `STEP_LABELS` keys are equal sets.<br><br><sub>_unit-test_ · Already exists (`test_post_comment.sh`); add scenarios for every `phase` value._</sub> | _(no comments)_ |
| `A3-041` | 0+ | Unit test asserting every shell script under `scripts/` and `tests/` runs `shellcheck` clean (or has documented SC#### exclusions).<br><br><sub>_unit-test_ · Latent `$SELECTOR` typo PR #46 — shellcheck would catch it._</sub> | • **A1** (A1→A3-007 · _refactor-test_ · phase 0+): Extend A3-041 (shellcheck across scripts) to also run `shfmt -d` so formatting drift is a separate, fast signal from semantic issues. — _Makes shellcheck output less noisy._ |
| `A3-042` | 1+ | Refactor: replace per-test `wait_for` bash loops in `tests/integration/*.sh` with a single library helper that exits non-zero AND prints last-seen state.<br><br><sub>_refactor-test_ · Silent-PASS bug PR #59 — strict-mode + canonical helper would remove the bug class._</sub> | • **A2** (A2→A3-009 · _extension_ · phase 1+): Promote A3-042 (canonical `wait_for` library) and reuse it from every A2 concurrency/soak test (A2-016/017/018/050). — _One wait primitive across unit, integration, and e2e layers._<br>• **A5** (A5→A3-005 · _dedupe_ · phase 1+): A3-042 (canonical wait_for library) should be the same helper as A5-043 (`await k8s/condition:...`); one DSL, two callers. — _Single source of truth across tests and orchestration._<br>• **A6** (A6→A3-002 · _simplification_ · phase 1+): A3-042's library helper for `wait_for` lets us delete every per-test bash retry loop AND simplify A1-021's wait-for-claim — both can call the same lib. — _One wait lib, used by helpers + tests._ |
| `A3-043` | 2+ | Simplify: collapse `tests/unit/test_platform_secret_xrd.sh` and `test_platform_cluster_xrd.sh` into a single parameterized XRD lint runner.<br><br><sub>_simplify-test_ · Duplication grows linearly per XRD; one lint per invariant scales._</sub> | • **A6** (A6→A3-001 · _refactor_ · phase 2+): A3-043 (XRD lint consolidation) is the right model — extend it: every per-claim-kind test in `tests/unit/test_platform_*.sh` should become a single fixture-driven runner. — _Eliminates linear-growth duplication._ |
| `A3-044` | 1+ | Simplify: replace bespoke yq selectors in `test_helm_render.sh` with a fixture-driven loop reading expected resource shapes from `tests/unit/fixtures/helm/*.yaml`.<br><br><sub>_refactor-test_ · Pending follow-up #2 — chart-version selector brittleness._</sub> | • **A5** (A5→A3-009 · _cache_ · phase 1+): A3-008/A3-044 helm-render brittleness can be cached: render once at session start into `/tmp/helm-rendered/`, lints read from there. — _Avoids N redundant `helm template` calls._ |
| `A3-045` | 1+ | Helm-render test asserting the ingress class on every chart-rendered Ingress matches `nginx` (not `alb`, not empty).<br><br><sub>_helm-render-test_ · Pre-existing `02-ingress-must-have-class.yaml` is runtime-only; authoring-time would be faster._</sub> | _(no comments)_ |
| `A3-046` | 1+ | Unit test asserting every `policies/audit/*.yaml` policy has `validationFailureAction: Audit` (never Enforce by accident).<br><br><sub>_unit-test_ · Drift-prevention — Enforce on a buggy policy bricks apply._</sub> | • **A1** (A1→A3-009 · _unit-test_ · phase 1+): Pair A3-046 (audit-only Kyverno lint) with a unit test that every `policies/enforce/` policy has a corresponding audit-mode predecessor in git history (proven via shadow run). — _Codifies the safe enforce-mode promotion path._ |
| `A3-047` | 2+ | Unit test asserting every Crossplane Composition `function` reference uses a pinned version (no `latest`, no missing tag).<br><br><sub>_unit-test_ · Composition v2 pipeline migration PR #51 — function pinning matters for behavior reproducibility._</sub> | _(no comments)_ |
| `A3-048` | 1+ | Unit test asserting `terraform/management/variables.tf` chart-version defaults match versions referenced in `argocd/apps/*.yaml` Helm-chart Applications.<br><br><sub>_unit-test_ · Cross-source-of-truth drift — caught zero times today._</sub> | • **A5** (A5→A3-010 · _pipeline_ · phase 1+): A3-012/A3-048 chart-version drift checks can run as a pre-commit hook in addition to test-time, catching it 100x earlier. — _Shift-left._ |
| `A3-049` | 0+ | Unit test asserting every `.github/workflows/*.yml` that does an AWS API call has `permissions: id-token: write` (OIDC) or documents long-lived creds.<br><br><sub>_unit-test_ · Slightly out-of-period for current creds model but defends future migration._</sub> | • **A6** (A6→A3-009 · _refactor_ · phase 0+): A3-014 + A3-015 + A3-049 are all "workflow YAML correctness" linters; ship them as one actionlint config plus one custom rule pack. — _One toolchain not three._ |
| `A3-050` | 1+ | Unit test asserting `terraform/management/eks.tf` enables all 5 control-plane log types (api, audit, authenticator, controllerManager, scheduler).<br><br><sub>_unit-test_ · Operational visibility — IRSA/RBAC bugs cannot be retro-debugged without audit/authenticator logs._</sub> | • **A1** (A1→A3-001 · _observability-pair_ · phase 1+): Extend A3-050 (control-plane log types) with a unit test that every enabled log type has a matching CloudWatch Logs Insights saved-query in the repo. — _Enabled logs nobody queries are just storage cost._ |
| `A3-051` | 1+ | Unit test asserting every IRSA role has a non-empty `inline_policy` or `managed_policy_arns` (catches half-finished modules).<br><br><sub>_iam-policy-completeness-test_ · Bug class: a role exists but has no permissions — produces silent AccessDenied._</sub> | • **A2** (A2→A3-013 · _extension_ · phase 1+): Promote A3-051 (IRSA role policy non-empty) into an `aws iam simulate-principal-policy` smoke test (per A2-003) that runs in CI. — _"Non-empty" is necessary but not sufficient; simulator gives semantic correctness._ |
| `A3-052` | 2+ | Unit test asserting every Crossplane ProviderConfig referenced by a Composition resource exists in `crossplane/providers/`.<br><br><sub>_unit-test_ · Composition with `providerConfigRef: nope` is a silent runtime failure._</sub> | • **A6** (A6→A3-005 · _refactor_ · phase 2+): A3-039 (cluster-app references claim file) combined with A3-052 (providerConfigRef exists) becomes a single "reference-integrity" linter across all YAML. — _Pattern is "ref → must exist"; one tool._<br>• **A6** (A6→A3-012 · _refactor_ · phase 1+): A3-021 + A3-052 + A3-064 are "every X must point at known Y in allowlist Z" lints — generalize as a single declarative ref-integrity tool driven by a YAML config. — _One tool replaces N hardcoded lints._ |
| `A3-053` | 1+ | Helm-render assertion that the rendered Crossplane chart includes a DeploymentRuntimeConfig matching the IRSA SA name expected by `irsa.tf`.<br><br><sub>_helm-render-test_ · Authoring-time defense for the bug 5 / PR #66 chain._</sub> | _(no comments)_ |
| `A3-054` | 2+ | Unit test asserting every Composition `patch` with `type: ToCompositeFieldPath` has a matching `FromCompositeFieldPath` in the inverse direction (catches stale patches).<br><br><sub>_unit-test_ · Composition rot — patches that only flow one way silently drop status._</sub> | _(no comments)_ |
| `A3-055` | 2+ | Integration test that applies a Composition revision bump and asserts the existing XR reconciles the change within 3 min (vs being orphaned at old revision).<br><br><sub>_integration-test_ · Composition versioning semantics — currently undefined behavior._</sub> | • **A2** (A2→A3-010 · _new-scenario_ · phase 2+): Add a metamorphic test variant of A3-055 (Composition revision bump) that fires the bump under concurrent claim creation to exercise the race. — _Concurrency lens on the revision-handoff bug class._<br>• **A5** (A5→A3-006 · _pipeline_ · phase 2+): A3-055 Composition revision-bump test can be triggered by event bus on every `git push` automatically. — _Continuous regression coverage._ |
| `A3-056` | 2+ | Chainsaw scenario asserting that a deleted XRD properly cleans up all composed MRs (no orphans).<br><br><sub>_chainsaw_ · Lifecycle invariant for phase-2 teardown — PR #58._</sub> | • **A2** (A2→A3-011 · _extension_ · phase 2+): Pair A3-056 (XRD teardown orphan cleanup) with an SDK enumeration step asserting no orphan AWS objects remain (per A2-046). — _Composes A3 + A2 into a definitive teardown oracle._ |
| `A3-057` | 0+ | Unit test asserting `scripts/diag-component.sh` handles every component name listed in its own usage string (parity check).<br><br><sub>_unit-test_ · Latent `$SELECTOR` typo PR #46 — parity test catches per-component breakage._</sub> | _(no comments)_ |
| `A3-058` | 1+ | Unit test asserting every Kyverno ClusterPolicy uses `apiVersion: kyverno.io/v1` consistently (no v1beta1 drift).<br><br><sub>_unit-test_ · API drift detection; cheap._</sub> | • **A1** (A1→A3-010 · _unit-test_ · phase 1+): Extend A3-058 (Kyverno apiVersion lint) to also assert `failurePolicy: Fail` is intentional and reviewed (annotation required), defending against accidental fail-open. — _Fail-open Kyverno bricks half a policy with no signal._<br>• **A5** (A5→A3-001 · _parallel-test_ · phase 0+): All A3 unit/static lints (A3-001..A3-021, A3-039..A3-058) are pure-CPU and parallelize trivially under `make -j$(nproc)`. — _Unit-test wall-clock drops from N×100ms to ~100ms._<br>• **A6** (A6→A3-010 · _refactor_ · phase 1+): A3-058 (apiVersion drift) is one instance of a wider class — generalize to "every YAML manifest in repo uses the apiVersion currently served by the registered CRD". — _One generalized lint kills a family of drift bugs._ |
| `A3-059` | 1+ | Refactor: replace `tests/integration/run.sh`'s sequential test runner with `set -e` orchestration that aborts on first failure, then dumps state.<br><br><sub>_refactor-test_ · Silent-PASS PR #59 — runner-level defense._</sub> | • **A5** (A5→A3-008 · _orchestration_ · phase 1+): A3-059 (run.sh `set -e` + dump) should call A5-037 phase status board on each failure so the dump lands in a known location. — _Discoverable evidence._<br>• **A6** (A6→A3-011 · _simplification_ · phase 1+): A3-059's `set -e` orchestration in run.sh lets us delete the per-test "did the previous test pass?" guard code. — _Runner enforces; tests stay focused._ |
| `A3-060` | 1+ | Unit test asserting every `terraform_data` resource with `provisioner local-exec` includes `command` body content sha256 in `triggers_replace`.<br><br><sub>_unit-test_ · Direct generalization of bug PR #67._</sub> | • **A6** (A6→A3-006 · _simplification_ · phase 1+): A3-011 + A3-060 are the same lint (manifest sha256 in triggers_replace); merge them. — _Two rows, one tool._ |
| `A3-061` | 2+ | Chainsaw scenario asserting Composition pipeline step ordering produces deterministic XR resourceRefs (no flaky test).<br><br><sub>_chainsaw_ · Bug PR #53 (non-deterministic XRD condition order)._</sub> | _(no comments)_ |
| `A3-062` | 2+ | Unit test asserting every chainsaw `script:` block uses POSIX-only constructs (no `[[`, no arrays, no `set -o pipefail`).<br><br><sub>_unit-test_ · Bug PR #53 (`pipefail` on dash)._</sub> | _(no comments)_ |
| `A3-063` | 1+ | Helm-render test that argocd-server Service is of type ClusterIP (NLB termination is at ingress only).<br><br><sub>_helm-render-test_ · Sneaky regression class — chart upgrade could flip default._</sub> | _(no comments)_ |
| `A3-064` | 2+ | Unit test asserting every Composition that references AWS resources sets `spec.providerConfigRef.name` to a value in the known allowlist (`default`, `irsa-default`).<br><br><sub>_unit-test_ · Catches typos pre-apply._</sub> | • **A6** (A6→A3-012 · _refactor_ · phase 1+): A3-021 + A3-052 + A3-064 are "every X must point at known Y in allowlist Z" lints — generalize as a single declarative ref-integrity tool driven by a YAML config. — _One tool replaces N hardcoded lints._ |
| `A3-065` | 2+ | Integration test that probes ArgoCD `application.argoproj.io/bootstrap` is `Synced/Healthy` AND its self-exclusion ignoreDifferences is preserved.<br><br><sub>_integration-test_ · App-of-apps PR #43 has no live-cluster test._</sub> | • **A1** (A1→A3-014 · _integration-test-extension_ · phase 2+): Extend A3-065 (bootstrap App integration test) with a CloudTrail event assertion that ArgoCD's IRSA role's last AssumeRoleWithWebIdentity is < 10 min old. — _Detects bootstrap-up-but-token-stale._ |

### A3 — General comments (no source-ID anchor)

Cross-review rows targeted at this agent but without an explicit `A{N}-NNN` reference in the comment text; preserved verbatim.

| Comment ID | From | Phase | Comment | Category & justification |
|---|---|---|---|---|
| `A1→A3-002` | A1 | 1+ | Add a lint that every Kyverno policy in `policies/audit/` has a corresponding CloudWatch metric filter on PolicyReport events so violations are graphable. | _observability-test_ — Audit-mode invisible without surfaced metrics. |
| `A1→A3-005` | A1 | 1+ | Add a unit test that every CloudWatch alarm in terraform has an `alarm_actions` (SNS topic) and at least one subscriber — defends against silent alarms. | _observability-test_ — Alarms without actions are a known anti-pattern. |
| `A1→A3-008` | A1 | 0+ | Add a unit test asserting every Terraform `aws_cloudwatch_log_group` has `retention_in_days` set explicitly (no infinite retention by accident — sandbox quota matters). | _unit-test_ — Storage creep can hit sandbox cost limits. |
| `A1→A3-011` | A1 | 2+ | Add a static lint that every Crossplane Composition references at least one observability annotation (`platform.io/owner`, `platform.io/runbook-url`) so XR debugging has a starting point. | _unit-test_ — Orphan XRs with no owner take forever to triage. |
| `A1→A3-013` | A1 | 1+ | Add a contract test that every IRSA role has an `aws_iam_role.tags.purpose` tag that matches the SA name — observability tag invariant. | _iam-policy-completeness-test_ — Untaggged roles are unsearchable in CloudTrail. |
| `A1→A3-015` | A1 | 1+ | Add a unit test asserting every `tests/integration/*.sh` includes a `trap '<state-dump-fn>' EXIT` so failures auto-attach evidence (codifies Silent-PASS PR #59 lesson positively). | _unit-test_ — Universal evidence-on-exit is the cheapest reliability win. |
| `A4→A3-001` | A4 | 1+ | Pair the bidirectional helm↔irsa lint with a `runbook-orphan-irsa-debug.md` for when the lint fires — names the three fixes (delete role, add helm, mark intentional). | _runbook_ — Lint without a fix-tree confuses operators. |
| `A4→A3-004` | A4 | 0+ | Add a `pre-commit-hook` form of the strict-mode lint that prints the exact diff to apply — fixes get faster. | _unit-test_ — Same lint, shorter loop. |
| `A4→A3-009` | A4 | 2+ | Pair the `type: Format` lint with a quickfix mode that suggests the correct subtype based on adjacent patches. | _unit-test_ — Lint + suggestion shortens the iteration over guessing. |
| `A4→A3-011` | A4 | 1+ | Couple the `triggers_replace` SHA lint with `runbook-tf-data-zero-added.md` documenting how to verify the lint did its job after the next apply. | _runbook_ — The bug is specifically a silent no-op; runbook is what confirms it's no longer silent. |
| `A4→A3-018` | A4 | 1+ | Pair the IRSA trust-subject lint with `compare-irsa-trust-to-tf.yml` dispatch workflow (A4-068) — same invariant, live form. | _dispatch-workflow_ — Author-time + runtime form catches drift from both directions. |
| `A4→A3-019` | A4 | 2+ | Add a `runbook-deployment-runtime-config.md` that lists every step required to safely change a Provider's SA name (delete deploy, wait for re-render). | _runbook_ — PR #68 hack was the proof that operators don't know this. |
| `A4→A3-029` | A4 | 2+ | Pair the phase-2-diagnose dump-presence test with `runbook-diagnose-output-schema.md` describing every section's expected shape — failures point at the missing section. | _runbook_ — Test catches missing sections; runbook explains what each should contain. |
| `A4→A3-031` | A4 | 2+ | Pair the same-name-collision integration test with `runbook-asm-uid-naming.md` describing the rule (UID, not name). | _runbook_ — Bug PR #59 root cause was operator-side; runbook codifies the rule. |
| `A4→A3-033` | A4 | 1+ | Add a live `kyverno-drift-watch.sh` that polls every 5 min and posts a one-line summary to the dispatch-history doc — earliest possible warning. | _shell-helper_ — Tests catch on next CI; live watch catches in 5 min. |
| `A4→A3-037` | A4 | 2+ | After the provider-404 chainsaw scenario, also dump the registry lookup chain (resolved image, last-pulled digest) for triage. | _chainsaw_ — The 404 may have multiple causes; dump narrows quickly. |
| `A4→A3-038` | A4 | 2+ | Pair the 8-MR assertion with a `mr-status-table.yml` workflow (A4-083) form so the same shape is queryable on demand. | _chainsaw_ — Test = author-time; workflow = runtime. |
| `A4→A3-042` | A4 | 1+ | Have the canonical `wait_for` helper auto-quote its last-seen state on timeout in markdown form ready to paste into a PR comment. | _refactor-test_ — Evidence-ready failure output saves a step. |
| `A4→A3-049` | A4 | 0+ | Add a `runbook-aws-auth-debug.md` covering the OIDC vs static-creds bug surface — paired with the lint. | _runbook_ — Lint catches misconfig; runbook explains the fix. |
| `A4→A3-050` | A4 | 1+ | Pair the EKS log-types lint with `runbook-eks-logs-where.md` naming the CloudWatch log group per type. | _runbook_ — Knowing the lint passed is not enough; operators need to find the data. |
| `A4→A3-061` | A4 | 2+ | Add a `composition-pipeline-trace.sh` that prints step-by-step `crossplane render` output between pipeline steps — catches non-determinism at author time. | _chainsaw_ — Same defense, shorter loop. |
| `P→A3-001` | PRIMARY | 0+ | A unit test that lints every shell script in the repo for `set -euo pipefail` + no readonly built-in assignment (`UID=`, `PWD=`, etc.) — the silent-PASS class. | _unit-test_ — Catches the bug class behind PRs #46, #59 once and forever. |
| `P→A3-002` | PRIMARY | 0+ | A test that greps every workflow YAML for `continue-on-error: true` and requires a justification comment on the same line, or fails. | _unit-test_ — Prevents "passed because the step was allowed to fail" — a recurring evidence-trust bug. |
| `P→A3-003` | PRIMARY | 0+ | A `tests/unit/test_no_account_id_hardcoded.sh` that fails on any 12-digit AWS account ID in `ai/`, `terraform/`, `crossplane/`, `argocd/`, `scripts/` (per AGENTS.md §8.1). | _unit-test_ — Makes the §8.1 rule enforceable rather than aspirational. |
| `P→A3-004` | PRIMARY | 1+ | A Kyverno audit policy asserting every ServiceAccount referenced by an IRSA role's trust policy actually exists in the cluster (cross-resource consistency). | _kyverno-policy_ — Catches the SA-name-drift class at runtime, not just apply-time. |
| `P→A3-005` | PRIMARY | 2+ | A chainsaw scenario per XRD that boots a kind cluster with crossplane + a mocked AWS provider and exercises the composition; runs in <2 min on every PR. | _chainsaw_ — Provides "did the composition actually render?" coverage without touching real AWS. |

## A4 — Debug gap mining

> **Mandate.** Brainstorm debug/diagnose/observability tooling we should already have built under the OLD GitHub-Actions-only constraints (no direct kubectl/aws, only workflow_dispatch + log parsing) but never did — mined from retros and PR threads.

_Source file: [`ai/brainstorming/A4-debug-tool-gaps-prior-constraints.md`](../../ai/brainstorming/A4-debug-tool-gaps-prior-constraints.md)_

_Idea count: 100_

| ID | Phase | Idea (with category and justification) | Comments (bulleted by commenter) |
|---|---|---|---|
| `A4-001` | 1+ | A `read-cluster.yml` dispatch workflow with a `kind` + `name` + `namespace` input that runs `kubectl get -o yaml` and prints to log, giving the agent a 60-second cluster read without authoring a new workflow each time.<br><br><sub>_dispatch-workflow_ · Would have replaced the entire `phase-2-diagnose.yml` authoring cycle in PR #60 and the many ad-hoc dispatch PRs that followed; the prior session said "dispatch a workflow + wait 3 minutes + parse a 170 KB log just to read one MR's `.status.conditions`"._</sub> | • **A1** (A1→A4-001 · _dispatch-workflow-extension_ · phase 1+): Pair A4-001/002/041 (read/exec/tail workflows) into a single `kubectl-bridge.yml` that takes a verb + structured args and returns JSON output for machine consumption. — _One canonical bridge is easier to allow-list and to teach skills to use._<br>• **A2** (A2→A4-001 · _test-oracle-promotion_ · phase 1+): Re-implement A4-001 (`read-cluster.yml`) as a local-shell `read-cluster` script that A2 e2e tests use as the canonical "read live resource" primitive. — _One read path, used both interactively and in tests._<br>• **A5** (A5→A4-001 · _refactor_ · phase 1+): A4-001 `read-cluster.yml` becomes a single sandbox alias `kc-read`; under A5 orchestration the dispatch wrapper is moot, but the JSON output schema is still valuable for subagent consumption. — _Preserves the contract, drops the substrate._<br>• **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-002` | 1+ | A `kubectl-exec.yml` workflow that takes a free-text kubectl command, runs it read-only, and prints the result — gated by an allow-list of verbs (`get`/`describe`/`logs`/`events`/`top`).<br><br><sub>_dispatch-workflow_ · The prior sessions repeatedly hand-rolled one-off diagnose YAMLs because there was no generic read-bridge; would have eliminated PR #60 and the post-#65 workflow re-edits._</sub> | • **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-003` | 0+ | An `aws-cli.yml` workflow that takes a service+command+args and dumps JSON output (read-only verbs only).<br><br><sub>_dispatch-workflow_ · Bug 5 (IRSA SA-hash mismatch, PR #66) needed `aws iam get-role` output that took multiple debug iterations because the agent had no AWS bridge._</sub> | • **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-004` | 1+ | A `status-snapshot.yml` workflow that, on every dispatch, dumps a fixed bundle (nodes, pods -A, events -A, all Crossplane CRs, ArgoCD apps, helm releases, kyverno policyreports) to a single log section with stable headings.<br><br><sub>_status-snapshot_ · Would have given a 60-second "where is the cluster right now" view the prior sessions consistently lacked between PRs #41-#68._</sub> | • **A1** (A1→A4-002 · _status-snapshot-extension_ · phase 1+): Extend A4-004 (status snapshot) to upload its bundle to S3 with a timestamped key so historical "what did the cluster look like at 14:32" queries become trivial. — _Time-machine debugging is impossible without retained snapshots._<br>• **A1** (A1→A4-014 · _shell-helper_ · phase 1+): Add a `dump-everything-on-pager.sh` that, when a CloudWatch alarm fires, auto-dispatches A4-004 + A4-036 + A4-079 and posts the bundle to a Slack/issue thread. — _Pager-driven evidence capture eliminates "I missed the moment of failure"._<br>• **A5** (A5→A4-002 · _prewarm+cache_ · phase 1+): A4-004 `status-snapshot.yml` should run as a background subagent at session start (A5-007 pattern), populating `/tmp/snapshot/` so any later question reads from a hot cache. — _Snapshot is ready before asked._ |
| `A4-005` | 2+ | A `crossplane-trace.yml` workflow that takes a claim name + namespace and walks claim → XR → resourceRefs → MRs → atProvider, printing conditions at each layer.<br><br><sub>_dispatch-workflow_ · PR #66 + #67 + #68 cascading IRSA debug burned hours on what was "where in the chain is the break?"; one trace command would have collapsed it._</sub> | • **A1** (A1→A4-003 · _dispatch-workflow-extension_ · phase 2+): Pair A4-005 (crossplane-trace) with an emitted Mermaid sequence diagram in the workflow summary so the chain is visual, not textual. — _Mermaid in GH UI makes 8-hop chains comprehensible at a glance._<br>• **A2** (A2→A4-002 · _test-oracle-promotion_ · phase 2+): Promote A4-005 (`crossplane-trace`) to a chainsaw `error:` hook so every failed scenario emits the full claim→XR→MR trace. — _Failure post-mortem is structured._<br>• **A5** (A5→A4-003 · _pipeline_ · phase 2+): A4-005 crossplane-trace can stream to A5-027 event bus per layer-traversal, enabling reactive subagents downstream. — _Trace becomes a stream not a dump._<br>• **A6** (A6→A4-008 · _simplification_ · phase 2+): A4-005's crossplane-trace, once it exists as a script (per A1-019), retires the dispatch-workflow form of the same idea. — _One implementation._ |
| `A4-006` | 1+ | An `irsa-check.yml` workflow that takes a role name, fetches trust policy, lists matching SAs in cluster, decodes their projected token sub claim, asserts match.<br><br><sub>_dispatch-workflow_ · Bug 5 was exactly "trust subject does not match SA name" and took multiple PRs of guessing; one dispatch would have surfaced the mismatch immediately._</sub> | • **A2** (A2→A4-003 · _test-oracle-promotion_ · phase 1+): Adopt A4-006 (`irsa-check`) as the precondition for every A2 IRSA test (A2-003..A2-006) — refuse to run if check fails. — _Tests fail loudly at preflight instead of producing confusing errors mid-run._<br>• **A3** (A3→A4-001 · _regression-extension_ · phase 1+): Pair A4-006 (`irsa-check.yml`) with a permanent unit test that asserts every IRSA role in `terraform/management/irsa.tf` is exercised by at least one workflow input fixture — would have made Bug 5 a build-time failure. — _Locks the bug class into the lint surface, not only the diagnose surface._ |
| `A4-007` | 0+ | A `terraform-show-state.yml` workflow that dumps `terraform state list` + `state show <addr>` for a specified address.<br><br><sub>_dispatch-workflow_ · PR #67's `triggers_replace` miss ("Apply complete: 0 added" silent no-op) would have been one `state show` call away._</sub> | _(no comments)_ |
| `A4-008` | 0+ | A `terraform-plan-diff.yml` workflow that runs `terraform plan -out tfplan` then `show -json tfplan<br><br><sub>_jq` and prints the changed-resources summary up front._ · dispatch-workflow &#124; The "0 added" misread (run 26354235231 cited in handoff §"It applied successfully ≠") was specifically the agent failing to read a long plan output; up-front summary fixes that._</sub> | • **A3** (A3→A4-002 · _regression-extension_ · phase 0+): Extend A4-008 (`terraform-plan-diff.yml`) to emit a structured "expected-zero-resources" flag the agent can set; if non-zero, the run must explicitly acknowledge. — _Prevents the run-26354235231 misread class from recurring._<br>• **A6** (A6→A4-011 · _refactor_ · phase 0+): A4-075 (apply-line-summary) + A4-008 (plan-diff summary) form one "tf-output-summarizer" — collapse. — _Same parser, two outputs._ |
| `A4-009` | 0+ | A `workflow-log-fetch.sh` shell helper that pulls a given run's logs via `gh api`, strips ANSI/timestamps, and concatenates into a single grep-friendly file.<br><br><sub>_shell-helper_ · Every retro mentions parsing 60K-170K char workflow logs by hand; normalizing them once is cheap._</sub> | • **A6** (A6→A4-005 · _refactor_ · phase 0+): A4-009 (workflow-log-fetch) + A4-015 (grep-workflow-log) + A4-050 (gather-bug-evidence) + A4-070 (gh-pr-checks-evidence) form one "GH log toolkit" — ship as one script with subcommands. — _Unified surface._ |
| `A4-010` | 1+ | A `runbook-irsa-debug.md` that lists the seven canonical IRSA failure signatures (trust mismatch, missing OIDC provider, audience wrong, missing namespace_service_accounts, SA annotation missing, pod missing token volume, ASM 403) with the workflow_dispatch command that proves each.<br><br><sub>_runbook_ · The PR #66 onion took multiple sessions because no canonical IRSA debug runbook existed._</sub> | • **A3** (A3→A4-014 · _regression-extension_ · phase 1+): Add a runbook entry "ESO ASM 403 vs ESO secret-not-found" to the A4-010 IRSA debug runbook — these two error strings are confusable and historically misdiagnosed. — _Disambiguates a recurring false-trail._<br>• **A6** (A6→A4-007 · _refactor_ · phase 0+): A4-010..A4-013 (four runbooks) merge into one `runbooks/` directory with a shared decision-tree format; A4-059 (decision-tree-runner) then drives all of them. — _Consistent format, single runner._ |
| `A4-011` | 2+ | A `runbook-claim-not-ready.md` that gives the eight-step decision tree from claim → XR → composition function → MR → provider → IRSA → cloud.<br><br><sub>_runbook_ · Phase 2 burnt ~45 min per loop on "claim is Waiting, why" without a written tree._</sub> | _(no comments)_ |
| `A4-012` | 1+ | A `runbook-argocd-outofsync.md` enumerating the five known causes (Kyverno mutation/drift, CRD-not-yet-installed, sync-wave race, manifest hash mismatch, RBAC) with dispatch commands per.<br><br><sub>_runbook_ · Bug 3 (Kyverno drift on policy 09) recurred in three sessions because there was no enumerated playbook._</sub> | _(no comments)_ |
| `A4-013` | 0+ | A `runbook-apply-zero-resources.md` documenting the "Apply complete: 0 added" silent-no-op class with the `triggers_replace` hash-the-manifest pattern from PR #67.<br><br><sub>_runbook_ · Three independent failures in one session per handoff "It applied successfully ≠"; this is the highest-leverage runbook in the repo._</sub> | • **A3** (A3→A4-003 · _regression-extension_ · phase 0+): A4-013 runbook for "Apply complete: 0 added" could be paired with `silent-pass-detector.sh` (A4-076) so the runbook entry is enforced not just documented. — _Doc-only lessons rot; lint-enforced lessons don't._<br>• **A6** (A6→A4-007 · _refactor_ · phase 0+): A4-010..A4-013 (four runbooks) merge into one `runbooks/` directory with a shared decision-tree format; A4-059 (decision-tree-runner) then drives all of them. — _Consistent format, single runner._ |
| `A4-014` | 0+ | A `dispatch-then-read-step.sh` helper that dispatches a workflow with a SHA, polls until done, then downloads ONLY the named step's log (not the wrapper conclusion).<br><br><sub>_shell-helper_ · Codifies the lesson from `verify-evidence-not-exit-codes` skill — eliminates the "conclusion: success while assertions failed" class._</sub> | • **A6** (A6→A4-004 · _refactor_ · phase 0+): A4-014 (dispatch-then-read-step) + A4-017 (dispatch-and-poll) + A4-091 (watch-tee) collapse into one canonical `gh-run.sh` helper. — _Three helpers, one job._ |
| `A4-015` | 0+ | A `grep-workflow-log.sh <run-id> <pattern>` that fetches and greps in one shot.<br><br><sub>_shell-helper_ · Repeatedly invoked by hand across PRs #52-#68 with bespoke `gh api` invocations._</sub> | • **A6** (A6→A4-005 · _refactor_ · phase 0+): A4-009 (workflow-log-fetch) + A4-015 (grep-workflow-log) + A4-050 (gather-bug-evidence) + A4-070 (gh-pr-checks-evidence) form one "GH log toolkit" — ship as one script with subcommands. — _Unified surface._ |
| `A4-016` | 0+ | A `phase-state-probe.yml` workflow that returns a JSON document mapping each phase 0-6 to `not-coded<br><br><sub>_code-only_ · applied&#124;verified` by live-probing AWS/EKS/CRDs. &#124; dispatch-workflow &#124; Handoff §8.1 says treat the handoff phase states as "belief, not ground truth"; only a live probe is ground truth and there was none._</sub> | • **A1** (A1→A4-005 · _dispatch-workflow-extension_ · phase 0+): Pair A4-016 (phase-state-probe) with a saved baseline JSON committed to repo so the probe's diff highlights *what changed*, not just current state. — _Current-state alone is useless without expected-state._<br>• **A5** (A5→A4-004 · _dedupe_ · phase 0+): A4-016 phase-state-probe is the same probe as A5→A1-002 — run it as one parallel `make -j7` job. — _One probe, two callers (A1 + A4)._ |
| `A4-017` | 0+ | A `dispatch-workflow-and-poll.py` that wraps `gh workflow run` + polling + log fetch + exit-code propagation into one call.<br><br><sub>_shell-helper_ · Every session reinvents this loop._</sub> | • **A6** (A6→A4-004 · _refactor_ · phase 0+): A4-014 (dispatch-then-read-step) + A4-017 (dispatch-and-poll) + A4-091 (watch-tee) collapse into one canonical `gh-run.sh` helper. — _Three helpers, one job._ |
| `A4-018` | 1+ | A `kubectl-events-since.yml` workflow that dumps events newer than a `since` input, sorted by lastTimestamp.<br><br><sub>_dispatch-workflow_ · Crossplane sessions repeatedly missed events because `kubectl get events` is unordered by default and 60K-char dumps buried them._</sub> | • **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-019` | 2+ | A `provider-health.yml` workflow that lists Crossplane Providers, ProviderRevisions, Functions, plus their Healthy/Installed conditions and image refs.<br><br><sub>_dispatch-workflow_ · Bug 5 had the wrong provider Deployment running; this would have shown it._</sub> | • **A2** (A2→A4-004 · _test-oracle-promotion_ · phase 2+): Use A4-019 (provider-health) as the gating oracle for A2-022 chaos test recovery assertions. — _Reuses one source of truth for "Crossplane is healthy"._ |
| `A4-020` | 1+ | A `helm-release-status.yml` workflow that prints `helm list -A` + `helm status` for every release + manifest diff against repo.<br><br><sub>_dispatch-workflow_ · The session said "helm_release can be 'created' while pods CrashLoop"; this surfaces both._</sub> | _(no comments)_ |
| `A4-021` | 1+ | A `pod-restart-watch.yml` workflow that lists pods with restartCount>0 + last termination reason + tail of previous-container logs.<br><br><sub>_dispatch-workflow_ · OOM/CrashLoop debugging was always ad-hoc; standardized format would have caught the ESO/Crossplane crash patterns._</sub> | • **A5** (A5→A4-005 · _pipeline_ · phase 1+): A4-021 pod-restart-watch should be a long-lived watcher subagent subscribed to the cluster-events stream, not a one-shot. — _Push not poll._<br>• **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-022` | 1+ | A `kyverno-explain-policy.yml` workflow that takes a policy name + a candidate resource yaml and reports which rules pass/fail and why.<br><br><sub>_dispatch-workflow_ · Bug 3 (ClusterPolicy 09 drift) was exactly Kyverno mutating fields; explain would have surfaced the diff._</sub> | • **A2** (A2→A4-005 · _smoke-test_ · phase 1+): Add a smoke test on top of A4-022 (kyverno-explain) that feeds a known-violating manifest through and asserts the explainer reports the expected failing rule. — _Validates the explainer itself._<br>• **A3** (A3→A4-004 · _regression-extension_ · phase 1+): A4-022 Kyverno explain workflow should ship with a corpus of known-mutating policies (incl. policy 09) and assert mutation behavior is unchanged across PRs. — _Bug 3 specifically; corpus locks behavior._ |
| `A4-023` | 1+ | A `kyverno-violations-summary.yml` workflow that pulls all PolicyReports, groups by policy + resource kind, and prints counts.<br><br><sub>_dispatch-workflow_ · `kyverno-violations.sh` exists but only useful with live kubectl — workflow form makes it usable from the sandbox._</sub> | _(no comments)_ |
| `A4-024` | 0+ | A `route53-record-snapshot.yml` workflow that lists all records in the hosted zone with TTL + value + ExternalDNS owner-tag.<br><br><sub>_dispatch-workflow_ · ExternalDNS propagation races were the #2 ingress bug class; snapshot diff over time would catch drift._</sub> | • **A2** (A2→A4-006 · _extension_ · phase 1+): Pair A4-024 (route53 snapshot) with A2-020 (ExternalDNS round-trip) to assert the new record appears in the next snapshot diff. — _Snapshot tool gains a regression harness._<br>• **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-025` | 1+ | An `nlb-target-health.yml` workflow that walks Ingress → Service → NLB ARN → target group → health + SG rules.<br><br><sub>_dispatch-workflow_ · The seven-bug phase-1 bring-up (per `ai/TESTING-PLAN.md`) included NLB health misses; would have collapsed multi-hop debug._</sub> | • **A2** (A2→A4-007 · _test-oracle-promotion_ · phase 1+): Promote A4-025 (NLB target health walker) to the oracle for A2-024 ingress chaos test. — _One walker, two consumers._ |
| `A4-026` | 0+ | A `cognito-state.yml` workflow that dumps user pool + clients + groups + identity providers JSON.<br><br><sub>_dispatch-workflow_ · Phase 0 + phase 5 needed Cognito state visibility; nothing existed._</sub> | • **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-027` | 0+ | An `acm-cert-status.yml` workflow that lists certs in the region with domain, status, in-use-by ARNs.<br><br><sub>_dispatch-workflow_ · ACM validation hangs are silent; status dump in 5s._</sub> | • **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-028` | 0+ | A `vpc-snapshot.yml` workflow that dumps VPCs, subnets, route tables, NAT, IGW, EIPs against the sandbox quota lines.<br><br><sub>_dispatch-workflow_ · Cap-exceed = account kill; preflight that is currently absent._</sub> | • **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-029` | 0+ | A `quota-preflight.yml` workflow that asserts EC2 count<8, EIP<5, NAT<2, VPC<1 BEFORE allowing a mutating dispatch.<br><br><sub>_dispatch-workflow_ · A4-028 dumps; this asserts and fails fast — kill-prevention._</sub> | • **A6** (A6→A4-010 · _refactor_ · phase 0+): A4-029's quota-preflight + A1-035's local quota-check are duplicates — keep one, drop the other. — _One preflight._ |
| `A4-030` | 0+ | A `tf-state-show.yml` workflow that takes an address and dumps state attributes, with secret keys redacted.<br><br><sub>_dispatch-workflow_ · Pairs with A4-007; redaction is the bit that makes it shareable in PR comments._</sub> | _(no comments)_ |
| `A4-031` | 1+ | A `compare-plan-to-cluster.yml` workflow that diffs `terraform plan` JSON against the live K8s/AWS resource shape.<br><br><sub>_dispatch-workflow_ · The "applied successfully ≠ change reached cluster" class — only a real diff catches it._</sub> | _(no comments)_ |
| `A4-032` | 2+ | A `composition-render.yml` workflow that runs `crossplane render` against a claim + composition + function locally on the runner and prints the rendered manifests.<br><br><sub>_dispatch-workflow_ · Bug 4 (Composition string transform missing `type: Format`) would have shown up in a render diff before any cluster touched it._</sub> | • **A2** (A2→A4-008 · _new-scenario_ · phase 2+): Add an e2e test that A4-032 (composition-render) and the live Composition apply produce byte-identical rendered MRs. — _Catches render-vs-apply skew (a known v2 footgun)._ |
| `A4-033` | 2+ | A `crd-installed.yml` workflow that asserts every expected CRD (from a manifest list) is present + Established and prints missing ones.<br><br><sub>_dispatch-workflow_ · The PR #52 bootstrap-order bug (PlatformSecret CRD not yet installed when policy 09 was applied) would have aborted apply early._</sub> | • **A3** (A3→A4-005 · _regression-extension_ · phase 2+): Pair A4-033 (CRD-installed assertion) with a phase-2 bootstrap-order regression test that intentionally races CRD install vs policy apply to prove ordering. — _PR #52 root cause as positive test._ |
| `A4-034` | 2+ | A `xrd-condition-table.yml` workflow that lists every XRD with `[Established, Offered]` status side-by-side in a markdown table.<br><br><sub>_dispatch-workflow_ · Phase 2b chainsaw fights with non-deterministic condition order would have been caught upstream._</sub> | _(no comments)_ |
| `A4-035` | 1+ | An `eso-trace.yml` workflow that walks ExternalSecret → ClusterSecretStore → ASM ARN → IRSA role → fetched value redaction.<br><br><sub>_dispatch-workflow_ · ESO has six known failure modes; one trace per claim would replace 30-min loops._</sub> | • **A1** (A1→A4-007 · _dispatch-workflow-extension_ · phase 1+): Pair A4-035 (eso-trace) with a `kubectl debug` ephemeral container that mounts the same SA token and runs `aws secretsmanager get-secret-value` from inside the pod — proves the path end-to-end. — _The only test of the *actual* network path the ESO controller takes._<br>• **A2** (A2→A4-009 · _test-oracle-promotion_ · phase 2+): Use A4-035 (eso-trace) as the failure-mode oracle for A2-053 (ASM rotation propagation). — _Six-failure-mode walker collapses into a one-call assertion._ |
| `A4-036` | 1+ | A `cluster-state-bundle.yml` workflow that tars cluster state + AWS describes + Terraform state into a single artifact uploaded to the run.<br><br><sub>_dispatch-workflow_ · "Send me everything" without re-dispatching N times; would have shortened every session's startup._</sub> | • **A1** (A1→A4-006 · _dispatch-workflow-extension_ · phase 1+): Extend A4-036 (cluster-state-bundle) by including a `prom-snapshot.tar.gz` (Prometheus TSDB block of last 1h) so post-mortems include time-series, not just point-in-time. — _Most bugs are best understood as a time-series, not a snapshot._<br>• **A1** (A1→A4-014 · _shell-helper_ · phase 1+): Add a `dump-everything-on-pager.sh` that, when a CloudWatch alarm fires, auto-dispatches A4-004 + A4-036 + A4-079 and posts the bundle to a Slack/issue thread. — _Pager-driven evidence capture eliminates "I missed the moment of failure"._<br>• **A5** (A5→A4-006 · _speculative-probe_ · phase 1+): A4-036 cluster-state-bundle should run in parallel with apply so the bundle is up-to-date on apply completion. — _Bundle ready when needed._<br>• **A6** (A6→A4-014 · _refactor_ · phase 1+): A4-036 + A4-037 (bundle producer + extractor) lets us delete every ad-hoc "describe everything" workflow scattered across the .github/workflows/ tree. — _One bundle format._ |
| `A4-037` | 1+ | A `diagnose-bundle-extractor.py` Python script that parses the bundle from A4-036 and prints a triage report (which conditions are False and why).<br><br><sub>_shell-helper_ · Lets a subagent answer "what's wrong" in one call instead of paging through artifacts._</sub> | • **A6** (A6→A4-014 · _refactor_ · phase 1+): A4-036 + A4-037 (bundle producer + extractor) lets us delete every ad-hoc "describe everything" workflow scattered across the .github/workflows/ tree. — _One bundle format._ |
| `A4-038` | 1+ | A `wait-for-condition.yml` workflow with `(kind, name, ns, condition, timeout)` inputs that runs `kubectl wait` and exits with the resource's full yaml on timeout.<br><br><sub>_wait-helper_ · Standardizes the most-copied loop in integration tests; the integration `wait_for` had the silent-PASS bug per PR #59._</sub> | • **A2** (A2→A4-010 · _extension_ · phase 1+): Pair A4-038 (wait-for-condition) with A1-021 helper to standardize the wait primitive across all A2 soak tests. — _Eliminates per-test wait drift._<br>• **A3** (A3→A4-006 · _regression-extension_ · phase 1+): A4-038 `wait-for-condition.yml` should refuse to claim success if zero condition rows ever appeared (counts the silent-PASS PR #59 bug class explicitly). — _Direct mitigation of the wait_for silent-PASS bug._<br>• **A5** (A5→A4-007 · _dedupe_ · phase 1+): A4-038/A4-039/A4-040 wait-for-* helpers are all instances of A5-043 (`await condition:... && cmd`) — implement once, reuse. — _Unified DSL._<br>• **A6** (A6→A4-006 · _simplification_ · phase 1+): A4-038 (wait-for-condition) + A4-039 (wait-for-argocd) + A4-040 (wait-for-claim) collapse into one parameterized waiter mirroring A1-021. — _Same loop, three callers._ |
| `A4-039` | 1+ | A `wait-for-argocd-app.yml` workflow that polls a named Application for `Synced/Healthy` with deadline and dumps the operation state on fail.<br><br><sub>_wait-helper_ · Sync-wave races (handoff "wait ≥3 min for ArgoCD") were the slowest part of every session._</sub> | • **A5** (A5→A4-007 · _dedupe_ · phase 1+): A4-038/A4-039/A4-040 wait-for-* helpers are all instances of A5-043 (`await condition:... && cmd`) — implement once, reuse. — _Unified DSL._<br>• **A6** (A6→A4-006 · _simplification_ · phase 1+): A4-038 (wait-for-condition) + A4-039 (wait-for-argocd) + A4-040 (wait-for-claim) collapse into one parameterized waiter mirroring A1-021. — _Same loop, three callers._ |
| `A4-040` | 2+ | A `wait-for-crossplane-claim.yml` that waits for a claim Ready + dumps the trace from A4-005 on timeout.<br><br><sub>_wait-helper_ · Composes A4-005 + A4-038 into the most-used phase-2 verification._</sub> | • **A1** (A1→A4-009 · _wait-helper-extension_ · phase 2+): Pair A4-040 (wait-for-claim with trace on timeout) with auto-opening a draft GitHub issue containing the trace + correlation-id when timeout fires. — _Closes the loop: timeout → triage ticket with all evidence pre-attached._<br>• **A5** (A5→A4-007 · _dedupe_ · phase 1+): A4-038/A4-039/A4-040 wait-for-* helpers are all instances of A5-043 (`await condition:... && cmd`) — implement once, reuse. — _Unified DSL._<br>• **A6** (A6→A4-006 · _simplification_ · phase 1+): A4-038 (wait-for-condition) + A4-039 (wait-for-argocd) + A4-040 (wait-for-claim) collapse into one parameterized waiter mirroring A1-021. — _Same loop, three callers._ |
| `A4-041` | 1+ | A `tail-pod-logs.yml` workflow with `(ns, selector, since, tail)` inputs that prints logs of matching pods, ordered by pod name.<br><br><sub>_dispatch-workflow_ · Every diagnose workflow inlined this; one workflow eliminates redundancy._</sub> | • **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-042` | 2+ | A `crossplane-function-logs.yml` workflow that tails patch-and-transform function pod logs filtered to a specific XR name.<br><br><sub>_dispatch-workflow_ · Composition function input rejections (Bug 4) only surface in function logs; filtered access would have caught them earlier._</sub> | _(no comments)_ |
| `A4-043` | 1+ | A `previous-container-logs.yml` workflow that lists every pod with restartCount>0 and prints `--previous` logs for each.<br><br><sub>_dispatch-workflow_ · OOM/Crash post-mortem; container restart wipes current logs._</sub> | • **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-044` | 1+ | A `compare-helm-render.yml` workflow that runs `helm template` against pinned chart+values and diffs against live cluster manifests.<br><br><sub>_dispatch-workflow_ · Helm `manage_aws_auth_configmap=true` removal-bug class (Terraform fix history); render-vs-live is the only ground truth._</sub> | _(no comments)_ |
| `A4-045` | 1+ | An `argocd-app-tree.yml` workflow that prints parent → child Application tree with sync/health colored.<br><br><sub>_dispatch-workflow_ · App-of-apps debugging in #43 had no tree view._</sub> | _(no comments)_ |
| `A4-046` | 1+ | An `argocd-app-sync-history.yml` workflow that prints last N sync operations per app with success/fail and error messages.<br><br><sub>_dispatch-workflow_ · Bug 3's self-heal drift would have shown a sync-loop pattern in the history._</sub> | _(no comments)_ |
| `A4-047` | 1+ | A `kubectl-diff.yml` workflow that runs `kubectl diff -f <path>` against the cluster and prints the diff.<br><br><sub>_dispatch-workflow_ · "Applied successfully ≠ change reached cluster" — diff is the cheapest definitive answer._</sub> | • **A6** (A6→A4-001 · _simplification_ · phase 1+): A4-002's generic `kubectl-exec.yml` lets us delete A4-001 (read-cluster), A4-018 (events-since), A4-021 (pod-restart-watch), A4-041 (tail-pod-logs), A4-043 (previous-container-logs), and A4-047 (kubectl-diff) — all are kubectl-verb specializations. — _One generic bridge subsumes six specific ones._ |
| `A4-048` | 1+ | An `ingress-reachability.yml` workflow that hits every Ingress host from inside the cluster + outside (curl from runner) and prints status codes.<br><br><sub>_dispatch-workflow_ · Phase 1 bring-up included multiple "ingress doesn't work" iterations._</sub> | _(no comments)_ |
| `A4-049` | 1+ | A `dns-resolves.yml` workflow that runs `dig` against the authoritative nameservers (not public resolvers) for every ExternalDNS-managed name.<br><br><sub>_dispatch-workflow_ · Handoff calls out "dig against public resolvers" as wrong — codify the right pattern._</sub> | _(no comments)_ |
| `A4-050` | 0+ | A `gather-bug-evidence.sh` shell helper that, given a PR number, fetches the failing run's relevant step logs, related kubectl dumps, and produces a single Markdown evidence pack.<br><br><sub>_shell-helper_ · The `subagent-log-extraction` skill exists post-#62 because manual extraction was wasting full sessions._</sub> | • **A6** (A6→A4-005 · _refactor_ · phase 0+): A4-009 (workflow-log-fetch) + A4-015 (grep-workflow-log) + A4-050 (gather-bug-evidence) + A4-070 (gh-pr-checks-evidence) form one "GH log toolkit" — ship as one script with subcommands. — _Unified surface._ |
| `A4-051` | 0+ | A `workflow-yaml-lint.sh` pre-push helper that runs `python -c "yaml.safe_load(...)"` + actionlint on every changed workflow.<br><br><sub>_shell-helper_ · PR #65's heredoc-broke-YAML issue silently rejected the workflow until merged ("Workflow does not have 'workflow_dispatch' trigger")._</sub> | • **A6** (A6→A4-012 · _simplification_ · phase 0+): A4-051 (workflow-yaml-lint) + A3-014 (workflow YAML safe_load lint) are the same lint — keep one. — _One yaml validator._ |
| `A4-052` | 1+ | An `argocd-app-revision-check.yml` workflow that confirms each Application's `targetRevision` matches `HEAD` of `main`.<br><br><sub>_dispatch-workflow_ · Detects stale-revision pin bugs across the GitOps boundary._</sub> | _(no comments)_ |
| `A4-053` | 2+ | A `crossplane-version-skew.yml` workflow that prints core + every provider + every function version side-by-side.<br><br><sub>_dispatch-workflow_ · The v2.0.1 vs v2.2 compat question (handoff item 8) needs a real version snapshot before bumping._</sub> | • **A2** (A2→A4-011 · _smoke-test_ · phase 2+): Build a regression suite on A4-053 (crossplane-version-skew) that runs nightly and pages on any unexpected drift between core/providers/functions. — _Version-skew bugs are silent until they bite; nightly catches them early._<br>• **A3** (A3→A4-007 · _regression-extension_ · phase 2+): Extend A4-053 (Crossplane version skew) to record image digests + a baseline file in repo so any silent upgrade is caught as a digest diff. — _The v2.0.1 vs v2.2 question becomes a tracked, diffable fact._ |
| `A4-054` | 0+ | A `tf-drift-detect.yml` workflow that runs `terraform plan -detailed-exitcode` and surfaces drift between state and reality.<br><br><sub>_dispatch-workflow_ · Out-of-band cluster changes (Kyverno admission mutation) cause drift; nothing detected it pre-PR #52._</sub> | _(no comments)_ |
| `A4-055` | 0+ | A `dispatch-summary-poster.py` Python script that, after any dispatch finishes, posts a concise step-by-step result table as a commit comment.<br><br><sub>_shell-helper_ · `post-comment.py` bug (PR #39) shows this is fragile; a hardened version with structural tests is the right form._</sub> | _(no comments)_ |
| `A4-056` | 2+ | A `chainsaw-rerun-last-fail.yml` workflow that re-runs only the last-failed chainsaw scenario with verbose dump pre-teardown.<br><br><sub>_dispatch-workflow_ · PR #56 added dump-on-fail but each scenario reruns the whole suite; targeted rerun would save 5+ min per loop._</sub> | _(no comments)_ |
| `A4-057` | 1+ | An `audit-policy-fire-log.yml` workflow that prints every Kyverno PolicyReport result newer than `since`, grouped by policy.<br><br><sub>_dispatch-workflow_ · Audit-mode policies are useless without surfaced violations; nothing surfaced them._</sub> | _(no comments)_ |
| `A4-058` | 1+ | A `sec-pod-list.yml` workflow that lists Secrets across all namespaces with size + age + owner (ExternalSecret-managed or not).<br><br><sub>_dispatch-workflow_ · ESO debug needs "is the Secret actually in the namespace yet" — repeatedly hand-rolled._</sub> | • **A5** (A5→A4-008 · _parallel-probe_ · phase 1+): A4-058 secret-staleness scan runs in parallel across all namespaces (one subprocess per ns) instead of one sweep. — _NS-count parallelism free._ |
| `A4-059` | 1+ | A `decision-tree-runner.sh` helper that walks an enumerated runbook (A4-010/011/012/013) and dispatches the right diagnose workflow per branch.<br><br><sub>_shell-helper_ · The runbooks alone don't reduce loops — automating "if X then dispatch Y" closes the loop._</sub> | • **A6** (A6→A4-007 · _refactor_ · phase 0+): A4-010..A4-013 (four runbooks) merge into one `runbooks/` directory with a shared decision-tree format; A4-059 (decision-tree-runner) then drives all of them. — _Consistent format, single runner._ |
| `A4-060` | 0+ | A `last-N-failed-runs.yml` summary workflow that lists the last 20 failed `terraform-test.yml` runs with first-failed-step + first error line.<br><br><sub>_dispatch-workflow_ · Pattern detection across runs is currently impossible without manual `gh run list` looping._</sub> | • **A5** (A5→A4-013 · _pipeline_ · phase 0+): A4-060 last-N-failed-runs becomes "tail the event bus for failure events" once A5-027 is in place — zero gh-api round trips. — _Event bus subsumes the workflow._ |
| `A4-061` | 1+ | A `which-secret-is-stale.yml` workflow that compares every ExternalSecret's last-sync time to its source ASM secret's last-modified time and prints stragglers.<br><br><sub>_dispatch-workflow_ · Rotation bugs are silent until consumers crash._</sub> | • **A2** (A2→A4-012 · _extension_ · phase 1+): Extend A4-061 (which-secret-is-stale) into an SLO alarm: any ExternalSecret > 2x refreshInterval since last sync fires a synthetic CW metric. — _Stale secrets become observable._ |
| `A4-062` | 0+ | A `terraform-data-trigger-audit.sh` lint that scans every `terraform_data` block and ensures its `triggers_replace` includes a `sha256` of every manifest body the local-exec applies.<br><br><sub>_shell-helper_ · PR #67's whole-bug root cause; encode as a lint instead of a one-off fix._</sub> | • **A3** (A3→A4-008 · _regression-extension_ · phase 0+): A4-062 (terraform_data triggers_replace audit) deserves a CI lint that also fires on `null_resource` blocks — historical equivalent footgun. — _Generalizes PR #67's bug class._ |
| `A4-063` | 2+ | A `provider-runtime-config-binding.yml` workflow that asserts each Provider Deployment's `serviceAccountName` matches its `DeploymentRuntimeConfig` spec.<br><br><sub>_dispatch-workflow_ · PR #68 root cause — Crossplane doesn't roll the Deployment on RuntimeConfig changes._</sub> | • **A3** (A3→A4-009 · _regression-extension_ · phase 2+): A4-063 (provider runtime config binding assert) could be a chainsaw step that intentionally mutates the RuntimeConfig and asserts the binding detector fires. — _Locks PR #68 as a permanent guard._ |
| `A4-064` | 1+ | A `serviceaccount-irsa-annotation-audit.yml` workflow that lists every SA with `eks.amazonaws.com/role-arn` annotation + asserts the role exists + trust matches.<br><br><sub>_dispatch-workflow_ · IRSA misannotation is silent until a pod tries AssumeRole._</sub> | • **A2** (A2→A4-013 · _test-oracle-promotion_ · phase 1+): Use A4-064 (SA IRSA annotation audit) as the live-cluster half of A2-005/A2-006 invariants. — _Bidirectional invariant gets one tool._ |
| `A4-065` | 1+ | An `apply-and-verify-evidence.yml` workflow that wraps `terraform-test apply-and-verify` and POSTS the evidence-quote lines (per `verify-evidence-not-exit-codes`) directly into the workflow summary.<br><br><sub>_dispatch-workflow_ · Codifies the rule into the tool — agents can't forget to read the evidence if the dispatcher surfaces it._</sub> | _(no comments)_ |
| `A4-066` | 0+ | A `time-budget-tracker.sh` that records dispatch start, completion, and "wall clock burned" per phase, accumulated to a CSV.<br><br><sub>_shell-helper_ · Time-boxed sandbox; nobody knows how much budget remains._</sub> | _(no comments)_ |
| `A4-067` | 0+ | A `dispatch-history.md` doc auto-updated by a workflow listing every dispatch with phase/action/run-id/conclusion/duration.<br><br><sub>_dashboard_ · Session handoff needs this; currently reconstructed by hand from `gh run list`._</sub> | • **A1** (A1→A4-010 · _dashboard-extension_ · phase 0+): Extend A4-067 (dispatch-history dashboard) with cost attribution: dollars per phase per session, plus AWS API call counts. — _Sandbox quota visibility + cost-per-iteration drives optimization._<br>• **A6** (A6→A4-009 · _simplification_ · phase 0+): A4-067 (auto-updated dispatch-history.md) lets us delete the manual "what did I run this session?" reconstruction in handoff. — _Auto > manual._ |
| `A4-068` | 1+ | A `compare-irsa-trust-to-tf.yml` workflow that pulls every IRSA role's trust JSON and diffs against `terraform/management/irsa.tf`'s declared subjects.<br><br><sub>_dispatch-workflow_ · Bug 5 (PR #66 SA-hash mismatch) and any future trust-drift caught in one dispatch._</sub> | • **A1** (A1→A4-012 · _dispatch-workflow-extension_ · phase 1+): Pair A4-068 (irsa-trust-vs-tf diff) with an automatic GitHub Issue creation if drift > 0, with the diff inline — passive observability. — _Drift detected but unfiled is drift unfixed._<br>• **A2** (A2→A4-014 · _extension_ · phase 1+): Pair A4-068 (IRSA trust vs TF diff) with A2-003 simulate-principal-policy to catch both subject and permission drift in one run. — _Trust + permission coverage in one gate._<br>• **A5** (A5→A4-009 · _parallel-probe_ · phase 1+): A4-068 IRSA-trust diff is independent per role — fan out with `xargs -P` over `aws iam list-roles` output. — _Cuts N-role diff to max-role._<br>• **A6** (A6→A4-013 · _simplification_ · phase 1+): A4-068's IRSA trust-vs-tf diff replaces the entire post-#66 "manually check the role" runbook step. — _Diff is the runbook._ |
| `A4-069` | 2+ | A `crossplane-mr-not-found.yml` workflow that, given an XR, lists `spec.resourceRefs` and asserts each MR exists; if not, prints composition reconciler logs filtered by XR name.<br><br><sub>_dispatch-workflow_ · The "XR has resourceRefs but MRs are NotFound" class (handoff Step 7) is unique and needs its own probe._</sub> | _(no comments)_ |
| `A4-070` | 0+ | A `gh-pr-checks-evidence.sh` helper that, for a PR, lists every check + its conclusion + a one-line excerpt of the failing step.<br><br><sub>_shell-helper_ · "PR is red — what failed?" answered in 2s instead of clicking through Actions UI._</sub> | • **A6** (A6→A4-005 · _refactor_ · phase 0+): A4-009 (workflow-log-fetch) + A4-015 (grep-workflow-log) + A4-050 (gather-bug-evidence) + A4-070 (gh-pr-checks-evidence) form one "GH log toolkit" — ship as one script with subcommands. — _Unified surface._ |
| `A4-071` | 1+ | A `cluster-bootstrap-order-trace.yml` workflow that walks `terraform_data.argocd_bootstrap` → ArgoCD root app → child apps → sync-wave order, printing the order resources actually landed in.<br><br><sub>_dispatch-workflow_ · PR #52 bug class (CRD-installed-after-policy) needs ordering visibility._</sub> | _(no comments)_ |
| `A4-072` | 1+ | A `route53-zone-drift.yml` workflow that diffs the live hosted zone records against ExternalDNS's expected set computed from Ingress resources.<br><br><sub>_dispatch-workflow_ · DNS drift is invisible without diff._</sub> | _(no comments)_ |
| `A4-073` | 2+ | A `chainsaw-condition-coverage.sh` lint that asserts every chainsaw scenario uses `kubectl wait --for=condition=` rather than positional `assert:` blocks on `.status.conditions[]`.<br><br><sub>_shell-helper_ · Codifies the non-deterministic-order lesson (PR #53) as static check._</sub> | • **A3** (A3→A4-010 · _regression-extension_ · phase 2+): Pair A4-073 (chainsaw condition coverage lint) with a once-per-PR run that diffs the lint's findings against an allowlist file so new violations require explicit ack. — _Codifies PR #53 lesson with an evolving allowlist._ |
| `A4-074` | 1+ | A `pre-mutating-dispatch.sh` wrapper that requires a recent `phase-2-diagnose` or `status-snapshot` run before allowing apply-and-verify.<br><br><sub>_shell-helper_ · `diagnose-before-mutate` skill from #62 — enforce, don't suggest._</sub> | _(no comments)_ |
| `A4-075` | 0+ | A `terraform-apply-line-summary.py` script that parses the apply log and prints the resource-counts line + per-resource "Creation complete" lines in a 5-row table.<br><br><sub>_shell-helper_ · The "Apply complete: 0 added" misread (run 26354235231) was specifically log-parsing failure; structured view fixes it._</sub> | • **A6** (A6→A4-011 · _refactor_ · phase 0+): A4-075 (apply-line-summary) + A4-008 (plan-diff summary) form one "tf-output-summarizer" — collapse. — _Same parser, two outputs._ |
| `A4-076` | 0+ | A `silent-pass-detector.sh` lint that scans every `tests/integration/*.sh` for `set -uo pipefail` without `-e`, plus assignment to bash readonly builtins.<br><br><sub>_shell-helper_ · PR #59 added two of these; the bundle should be in one lint that names the bug class._</sub> | • **A3** (A3→A4-003 · _regression-extension_ · phase 0+): A4-013 runbook for "Apply complete: 0 added" could be paired with `silent-pass-detector.sh` (A4-076) so the runbook entry is enforced not just documented. — _Doc-only lessons rot; lint-enforced lessons don't._<br>• **A3** (A3→A4-011 · _regression-extension_ · phase 0+): A4-076 silent-pass detector could grow a sub-rule for "test asserts only on substring of kubectl output without `--no-headers`" — another known false-PASS shape. — _Expands the detector to the next-most-common silent-pass shape._<br>• **A6** (A6→A4-015 · _refactor_ · phase 0+): A4-076 (silent-pass-detector) and A3-004 + A3-005 are the same lint family — merge into one `lint_shell_strictness.sh`. — _Cross-file dedupe._ |
| `A4-077` | 1+ | A `helm-values-effective-diff.yml` workflow that prints the merged Helm values for each release vs the values committed under `platform-services/`.<br><br><sub>_dispatch-workflow_ · Helm override drift was an unverified suspicion repeatedly raised in retros._</sub> | _(no comments)_ |
| `A4-078` | 1+ | An `eks-control-plane-log-tail.yml` workflow that tails the EKS control plane CloudWatch log group for the last N minutes.<br><br><sub>_dispatch-workflow_ · Even without enabling all log types, the default subset would have answered IRSA + RBAC questions; nothing tailed it._</sub> | _(no comments)_ |
| `A4-079` | 1+ | A `cloudtrail-recent-failures.yml` workflow that prints `LookupEvents` for failed API calls in the last hour, grouped by API + error code.<br><br><sub>_dispatch-workflow_ · Permission errors (AssumeRoleWithWebIdentity 403, ASM denied) are post-hoc only visible here._</sub> | • **A1** (A1→A4-014 · _shell-helper_ · phase 1+): Add a `dump-everything-on-pager.sh` that, when a CloudWatch alarm fires, auto-dispatches A4-004 + A4-036 + A4-079 and posts the bundle to a Slack/issue thread. — _Pager-driven evidence capture eliminates "I missed the moment of failure"._<br>• **A1** (A1→A4-015 · _dispatch-workflow-extension_ · phase 1+): Pair A4-079 (cloudtrail-recent-failures) with a saved Athena query library against the CloudTrail S3 bucket for deeper retrospectives beyond the 90-day LookupEvents window. — _Long-tail debugging needs Athena, not LookupEvents._<br>• **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-080` | 1+ | An `iam-policy-simulator.yml` workflow that takes a role ARN + action + resource and prints `simulate-principal-policy` results.<br><br><sub>_dispatch-workflow_ · "Why does this AssumeRole fail" answered before pod even runs._</sub> | • **A2** (A2→A4-015 · _test-oracle-promotion_ · phase 1+): Promote A4-080 (IAM policy simulator) to a contract test that runs for every IRSA role on every PR. — _Pre-flight semantic check instead of mid-deploy AccessDenied._<br>• **A6** (A6→A4-002 · _simplification_ · phase 0+): A4-003's generic `aws-cli.yml` similarly subsumes A4-024 (route53), A4-026 (cognito), A4-027 (ACM), A4-028 (VPC), A4-079 (CloudTrail) and A4-080 (IAM simulator) once read-only allowlist is in place. — _One generic AWS bridge replaces seven specific dumps._ |
| `A4-081` | 0+ | A `bug-class-registry-check.sh` lint that asserts every row in `ai/TESTING-PLAN.md`'s bug-class registry has a corresponding `tests/unit/test_<class>.sh`.<br><br><sub>_shell-helper_ · The registry exists but enforcement that defending tests follow doesn't._</sub> | _(no comments)_ |
| `A4-082` | 0+ | A `dispatch-recipe-cookbook.md` documenting the exact `gh workflow run` + `gh run watch` + `gh run view --log` commands for every workflow in the repo, with copy-paste-ready inputs.<br><br><sub>_runbook_ · Every session relearns these from scratch._</sub> | • **A5** (A5→A4-010 · _runbook_ · phase 0+): A4-082 dispatch cookbook should also document the parallel-fan-out idiom for every read workflow. — _Embeds A5 patterns in muscle memory._ |
| `A4-083` | 2+ | An `mr-status-table.yml` workflow that lists every Crossplane managed resource with kind, name, Ready, Synced, atProvider.arn, age.<br><br><sub>_dispatch-workflow_ · Per-MR `kubectl get` was burned hundreds of times; one wide-table view would replace the pattern._</sub> | _(no comments)_ |
| `A4-084` | 0+ | A `dispatch-fast-fail.yml` mode for terraform-test that runs only init+validate+plan (no apply) and finishes in <2 min for sanity checks.<br><br><sub>_dispatch-workflow_ · apply-and-verify is 15 min; many checks needed only plan-time evidence._</sub> | _(no comments)_ |
| `A4-085` | 1+ | A `sync-on-demand.yml` workflow that hits ArgoCD `/api/v1/applications/<name>/sync` to force a refresh without waiting the 3-min poll.<br><br><sub>_dispatch-workflow_ · The 3-min ArgoCD refresh was the dominant phase-2 loop cost._</sub> | _(no comments)_ |
| `A4-086` | 1+ | A `cluster-secret-store-test.yml` workflow that runs ESO's `kubectl create secret --dry-run` flow against the CSS to prove fetch works.<br><br><sub>_dispatch-workflow_ · ESO failures usually masked as "Secret not found"; this proves vs disproves._</sub> | _(no comments)_ |
| `A4-087` | 1+ | A `pod-projected-token-decode.yml` workflow that exec's into a target pod, reads `/var/run/secrets/eks.amazonaws.com/serviceaccount/token`, and prints decoded claims.<br><br><sub>_dispatch-workflow_ · The other half of IRSA debug; A4-006 reads server side, this reads pod side._</sub> | • **A2** (A2→A4-016 · _new-scenario_ · phase 1+): Add a chainsaw scenario built on A4-087 (pod projected-token decode) to assert every platform SA's projected token carries the expected `aud` claim. — _Tokens silently misconfigured at the pod side is invisible without this._ |
| `A4-088` | 2+ | A `crossplane-events-by-xr.yml` workflow that lists events with `involvedObject.name == <xr>`.<br><br><sub>_dispatch-workflow_ · Composition events scrolled off in the noise; XR-scoped view is the answer._</sub> | _(no comments)_ |
| `A4-089` | 2+ | A `composition-input-validator.yml` workflow that runs the function's input schema against a candidate composition body, offline on the runner.<br><br><sub>_dispatch-workflow_ · Bug 4 (string transform `type` missing) failed validation; offline check catches it pre-apply._</sub> | _(no comments)_ |
| `A4-090` | 0+ | A `repo-grep-bug-history.sh` helper that, given an error string, greps retrospectives + handoff + closed-PR comments for prior occurrences.<br><br><sub>_shell-helper_ · Bug 3 recurred across sessions because nobody searched "have I seen this before"._</sub> | _(no comments)_ |
| `A4-091` | 0+ | A `dispatch-watch-tee.sh` wrapper that streams a workflow's live logs to stdout AND a local file, so the agent can grep while the run is still in progress.<br><br><sub>_shell-helper_ · Currently the agent waits for completion to read logs; tee+grep cuts loop time._</sub> | • **A6** (A6→A4-004 · _refactor_ · phase 0+): A4-014 (dispatch-then-read-step) + A4-017 (dispatch-and-poll) + A4-091 (watch-tee) collapse into one canonical `gh-run.sh` helper. — _Three helpers, one job._ |
| `A4-092` | 0+ | A `phase-bring-up-evidence-template.md` checklist of every line the agent should quote-back when reporting "phase N applied" (per `verify-evidence-not-exit-codes`).<br><br><sub>_runbook_ · Codifies the rule; future sessions cannot forget to quote evidence._</sub> | _(no comments)_ |
| `A4-093` | 0+ | A `dispatch-on-merge.yml` workflow that auto-dispatches the heavy verify when a PR labeled `verify-on-merge` lands on main, with the SHA pre-pinned.<br><br><sub>_dispatch-workflow_ · Verify-then-PR pattern requires the agent remember; auto-dispatch closes the loop._</sub> | _(no comments)_ |
| `A4-094` | 1+ | A `policy-mutate-detector.yml` workflow that diffs every Kyverno-managed ClusterPolicy against its repo source, listing default-filled fields.<br><br><sub>_dispatch-workflow_ · Bug 3 was Kyverno default-filling fields; one detector eliminates the bug class._</sub> | • **A1** (A1→A4-013 · _dispatch-workflow-extension_ · phase 1+): Extend A4-094 (policy-mutate-detector) by storing the diff in a CloudWatch metric so drift over time is graphable. — _One-shot detection is good; trend detection is better._<br>• **A3** (A3→A4-012 · _regression-extension_ · phase 1+): Extend A4-094 (policy mutate detector) to also diff CRD `default:` blocks — defaults are another silent-fill source distinct from Kyverno. — _Generalizes Bug 3 to non-Kyverno defaulting sources._ |
| `A4-095` | 0+ | A `gh-comment-extractor.sh` helper that pulls all review comments + thread replies from a PR into a single Markdown file for context loading.<br><br><sub>_shell-helper_ · Manual reconstruction of PR threads was a per-session tax._</sub> | _(no comments)_ |
| `A4-096` | 0+ | A `subagent-prompt-template.md` for diagnose-log extraction with: file path, schema, three specific questions, 1500-word cap, verbatim-quote directive.<br><br><sub>_runbook_ · Codifies the working pattern from PR #60 diagnose subagent (per `subagent-log-extraction` skill)._</sub> | _(no comments)_ |
| `A4-097` | 0+ | A `dispatch-with-input-table.sh` helper that pretty-prints workflow inputs before dispatch so a typo in `phase=mananagement` is caught pre-run.<br><br><sub>_shell-helper_ · One typo costs a full 15-min wait._</sub> | _(no comments)_ |
| `A4-098` | 2+ | A `cluster-tear-down-preview.yml` workflow that lists every resource that WOULD be deleted by `tear down phase X` per AGENTS.md §5.1 — dry-run-only.<br><br><sub>_dispatch-workflow_ · Teardown invariants are scary; a preview is safety net._</sub> | _(no comments)_ |
| `A4-099` | 0+ | An `orphan-aws-resource-scan.yml` workflow that lists AWS resources tagged `managed-by=k8-platform` not referenced by any TF state or Crossplane XR.<br><br><sub>_dispatch-workflow_ · Orphan resources count against sandbox cap = account-kill risk._</sub> | • **A5** (A5→A4-012 · _pipeline_ · phase 0+): A4-099 orphan-resource scan can run continuously in background as a quota guardian (composes with A1-037 alarm). — _Continuous quota safety._ |
| `A4-100` | 1+ | A `golden-snapshot-diff.yml` workflow that captures and saves a known-good cluster-state snapshot per phase, and diffs current state against it on demand.<br><br><sub>_dispatch-workflow_ · "Worked yesterday, broken today" answered in one dispatch._</sub> | • **A1** (A1→A4-016 · _dispatch-workflow-extension_ · phase 1+): Extend A4-100 (golden-snapshot-diff) with an automatic per-merge snapshot capture, so every commit on main has a known-good baseline. — _"What changed between v3.2.4 and v3.2.5" answerable instantly._<br>• **A2** (A2→A4-017 · _test-oracle-promotion_ · phase 1+): Use A4-100 (golden-snapshot diff) as the e2e regression oracle after every A2 chaos test — assert post-recovery state matches the golden. — _Recovery is defined as "return to golden", not just "Ready=True"._<br>• **A3** (A3→A4-013 · _regression-extension_ · phase 1+): A4-100 golden snapshot diff workflow should encode the snapshot per region (us-east-1 vs us-west-2) so cross-region drift is detected separately. — _Sandbox supports both regions; cross-region silent drift is currently invisible._<br>• **A5** (A5→A4-011 · _parallel-test_ · phase 1+): A4-100 golden-snapshot-diff plus A5-040 kind rehearsal cluster: keep one golden per cluster type, diff in parallel. — _Multi-cluster regression in one round._ |

### A4 — General comments (no source-ID anchor)

Cross-review rows targeted at this agent but without an explicit `A{N}-NNN` reference in the comment text; preserved verbatim.

| Comment ID | From | Phase | Comment | Category & justification |
|---|---|---|---|---|
| `A1→A4-004` | A1 | 0+ | Add a `correlation-id-injector.sh` that injects a UUID into every dispatch as an env var, then propagates it via labels/annotations into k8s objects + log lines for cross-system trace. | _shell-helper_ — Distributed-trace correlation across terraform/k8s/AWS is the single biggest debug-time multiplier. |
| `A1→A4-008` | A1 | 2+ | Add a `flamegraph-of-reconcile.yml` that captures controller pprof for 30s and renders a flame graph for slow reconciles. | _dispatch-workflow_ — Composition-function slowness has no other observability path. |
| `A1→A4-011` | A1 | 1+ | Add an `slo-budget-tracker.yml` that records phase-N-bring-up duration and alerts when 7-day p95 exceeds threshold — turns perf creep visible. | _dispatch-workflow_ — Bring-up perf degrades silently across many small changes. |
| `A6→A4-003` | A6 | 0+ | Now that the sandbox runs kubectl/aws directly, *every* `*.yml` dispatch workflow in A4 (~70 items) becomes a shell script — retire the dispatch-workflow pattern entirely once the new sandbox is the norm. | _refactor_ — Dispatch workflows existed only because of the prior constraint. |
| `P→A4-001` | PRIMARY | 1+ | A `scripts/diagnose/snapshot.sh` that emits a tarball capturing every relevant cluster + AWS read-only state at a moment in time, named by SHA — agent can paste a download link or untar locally. | _diagnose-script_ — Replaces 6 different one-off diagnose workflows with one durable artifact. |
| `P→A4-002` | PRIMARY | 0+ | A retro template field "what command did we wish existed?" — every retro author lists 1-3 commands; once a quarter we promote the top votes into `scripts/`. | _runbook_ — Turns retro signal into actual tooling without requiring a separate brainstorming session. |
| `P→A4-003` | PRIMARY | 1+ | A `scripts/diagnose/why-not-ready.sh <resource>` that walks status conditions of the resource and every dependency until it finds the leaf cause, prints a one-paragraph English summary. | _diagnose-script_ — Collapses 30-minute "trace through resourceRefs" loops into one command. |
| `P→A4-004` | PRIMARY | 1+ | Pre-canned CloudWatch Logs Insights queries committed under `scripts/insights/` (one .json per common question — "IRSA AssumeRole failures last hour", "Crossplane reconcile errors for resource X"). | _logs-insights-query_ — Removes the "what was that query syntax again" tax every debug loop. |

## A5 — Orchestration parallelism opportunities

> **Mandate.** Brainstorm orchestration/parallelization wins enabled by running terraform/kubectl/aws directly from the sandbox instead of via GitHub Actions, covering phases 0 → 6.

_Source file: [`ai/brainstorming/A5-orchestration-post-actions.md`](../../ai/brainstorming/A5-orchestration-post-actions.md)_

_Idea count: 50_

| ID | Phase | Idea (with category and justification) | Comments (bulleted by commenter) |
|---|---|---|---|
| `A5-001` | 0+ | Split `terraform/base` into independent `vpc/`, `route53/`, `acm/`, `cognito/` root modules and `terraform apply` them in parallel from the sandbox.<br><br><sub>_parallel-apply_ · Today phase-0 is one serial graph (~5 min); VPC and Cognito share nothing, so concurrent applies cut the critical path to whichever is slowest._</sub> | • **A2** (A2→A5-001 · _smoke-test_ · phase 0+): Add a smoke test that runs A5-001 (split base parallel applies) and asserts total wall-clock < 60% of the serial baseline. — _Locks in the orchestration win as a measurable contract._<br>• **A3** (A3→A5-001 · _regression-extension_ · phase 0+): Pair A5-001 (parallel base applies) with a post-apply contract test that VPC + Cognito outputs are written atomically — race-condition consumers fail otherwise. — _Parallel applies introduce a new "outputs not all written" bug class worth testing for._<br>• **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-002` | 0–6 | Launch a subagent pool of N workers, each owning one terraform root, coordinated by a "conductor" agent that holds the dependency DAG.<br><br><sub>_subagent-pool_ · Removes the single-agent serialization bottleneck inherent in the current "one bash at a time" loop._</sub> | • **A1** (A1→A5-001 · _observability-pair_ · phase 0–6): Pair A5-002 (subagent pool) with a per-subagent structured-log stream tagged with subagent-id + correlation-id, aggregated to a single live tail. — _Parallel agents become illegible without per-agent stream tagging._<br>• **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-003` | 0–6 | Generate a Makefile-style `targets` file from a YAML phase manifest so `make -j8 phase-0` runs every leaf in parallel automatically.<br><br><sub>_dependency-graph_ · Pushes parallel scheduling down to a battle-tested tool instead of bespoke orchestration._</sub> | • **A2** (A2→A5-002 · _new-scenario_ · phase 0+): Add an e2e idempotency test for A5-003 (Makefile -j parallel): run `make -j8 phase-0` twice and assert second run is a no-op. — _Parallel orchestration must remain idempotent._<br>• **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-004` | 1+ | Pre-cache Helm chart tarballs (`helm pull` for argo-cd, crossplane, kyverno, ingress-nginx, cert-manager) into `/tmp/charts` in parallel while EKS is still being created.<br><br><sub>_prewarm_ · Saves the ~30–60 s of serial chart downloads at the start of phase-1 helm_release steps._</sub> | _(no comments)_ |
| `A5-005` | 1+ | Pre-pull provider Docker images (`crossplane/provider-aws-secretsmanager`, `function-patch-and-transform`) onto the node group via a DaemonSet manifest applied the instant EKS reports Ready.<br><br><sub>_prewarm_ · Provider install today blocks on first-pull from upbound; warming the layer cache shaves a minute off composite-Ready._</sub> | _(no comments)_ |
| `A5-006` | 1+ | Run `aws eks update-kubeconfig` inside the terraform apply via a `local-exec` so kubectl is hot the second the cluster API is reachable, no manual step.<br><br><sub>_local-tf-driver_ · Eliminates the human "wait then run kubeconfig" gap and lets phase-2 verification start immediately._</sub> | • **A6** (A6→A5-001 · _simplification_ · phase 1+): Once A5-006 (kubeconfig via local-exec) lands, retire the manual "run aws eks update-kubeconfig" step from every phase runbook. — _One less hand-off step._ |
| `A5-007` | 1+ | Dispatch a "watcher" subagent that polls `aws eks describe-cluster` and emits a notification the millisecond status flips to ACTIVE so the next phase can fire.<br><br><sub>_pipeline_ · Removes fixed-duration `sleep 600` style waits in favor of event-driven hand-off._</sub> | • **A1** (A1→A5-002 · _observability-extension_ · phase 1+): Extend A5-007 (EKS-Ready watcher) to also assert audit-log delivery is flowing within 60s of Ready — proves observability is up, not just API. — _A cluster that's Ready but not logging is half-broken._<br>• **A2** (A2→A5-003 · _new-scenario_ · phase 1+): Build a chaos variant of A5-007 (event-driven watcher) that injects a transient cluster failure and asserts the watcher correctly retries rather than firing prematurely. — _Event-driven hand-offs must tolerate flakes._<br>• **A3** (A3→A5-002 · _regression-extension_ · phase 1+): Extend A5-007 (event-driven cluster-Ready watcher) to also emit a `cluster-API-reachable` event (separate from ACTIVE) — ACTIVE doesn't mean kubectl works. — _Historical bug class: ACTIVE then 5xx for a minute._<br>• **A6** (A6→A5-013 · _refactor_ · phase 1+): A5-007 (event-driven cluster-ready watcher) plus A5-027 (event bus) lets us delete every `sleep 600` style fixed wait remaining in phase scripts. — _All waits become event subscriptions._ |
| `A5-008` | 0–6 | Use multiple terraform workspaces against the same backend so two sandboxes (or two tabs) can hold different cluster bring-ups concurrently without state collision.<br><br><sub>_orchestration_ · Lets one engineer A/B two compositions in parallel sandboxes instead of tearing down between runs._</sub> | _(no comments)_ |
| `A5-009` | 0–6 | Compile a "phase plan" graph from `depends_on`/remote_state references and emit a topologically-sorted DOT file plus a parallel execution schedule.<br><br><sub>_dependency-graph_ · Surfaces hidden serial chokepoints that today are invisible inside one terraform graph._</sub> | • **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-010` | 0–6 | Run `terraform plan -refresh=false` speculatively in background while waiting on apply so the next phase's plan is ready instantly.<br><br><sub>_speculative-apply_ · Hides plan latency (~20 s) behind apply latency that's already happening._</sub> | _(no comments)_ |
| `A5-011` | 0–6 | Spin up a localstack-or-moto twin and run a dry-rehearsal apply against it in parallel with the real AWS apply, surfacing config errors 30 s in instead of 5 min in.<br><br><sub>_speculative-apply_ · Catches typos and bad refs before they burn real wall-clock on real AWS rate-limits._</sub> | • **A1** (A1→A5-003 · _speculative-apply-extension_ · phase 0–6): Pair A5-011 (localstack rehearsal) with diff'ing localstack's CloudTrail-equivalent against real CloudTrail post-apply to catch API behaviors localstack doesn't model. — _Surfaces localstack-vs-AWS divergence as a known set._<br>• **A2** (A2→A5-005 · _smoke-test_ · phase 0+): Pair A5-011 (localstack rehearsal) with an A2 contract test that asserts the rehearsal apply graph matches the real apply graph node-for-node. — _Localstack rehearsal is only valuable if shape-faithful._ |
| `A5-012` | 4+ | Pre-warm a Keycloak realm-config JSON in parallel with the ingress provisioning so the moment ingress is up, the realm import takes ~2 s.<br><br><sub>_prewarm_ · Today realm config is authored only after ingress; doing it concurrently removes a serial leg._</sub> | _(no comments)_ |
| `A5-013` | 1+ | Kick `argocd app sync --prune` for `bootstrap` immediately when the helm_release lands, instead of waiting for the 3-min refresh window.<br><br><sub>_pipeline_ · Cuts the well-documented 3-minute ArgoCD sync wait described in handoff Step 4._</sub> | • **A2** (A2→A5-004 · _smoke-test_ · phase 1+): Add an A2-style soak around A5-013 (immediate ArgoCD sync) that fires it 100 times in a row and asserts no sync queue starvation. — _Fast-path sync mustn't break under volume._<br>• **A3** (A3→A5-003 · _regression-extension_ · phase 1+): A5-013 (`argocd app sync` on helm landing) deserves a chaos test that runs sync before the helm release finishes settling — proves sync is idempotent or fails loudly. — _Catches sync-too-early race that today is hidden by the 3-min wait._<br>• **A6** (A6→A5-002 · _simplification_ · phase 1+): A5-013 + A5-048 (kick ArgoCD sync immediately / hard-refresh) lets us delete every `sleep 180` waiting for ArgoCD's 3-min refresh window in scripts and tests. — _Event-driven replaces fixed sleeps._ |
| `A5-014` | 1+ | Have one subagent install Crossplane providers while a sibling subagent pre-creates the IRSA roles those providers need, joining at the SA-annotation step.<br><br><sub>_parallel-apply_ · Today both happen in a single helm.tf serial chain; splitting cuts ~90 s._</sub> | _(no comments)_ |
| `A5-015` | 0–6 | Use `terraform apply -target=...` per leaf resource across N parallel terraform invocations sharing a single state via `-lock-timeout`.<br><br><sub>_parallel-apply_ · Lets one root module exploit intra-module parallelism beyond terraform's default `-parallelism=10`._</sub> | • **A2** (A2→A5-006 · _smoke-test_ · phase 0+): Add a concurrency test on top of A5-015 (per-leaf -target parallel apply) that confirms no state-lock collision under 8 parallel invocations. — _The shared-state pattern is fragile; needs explicit coverage._<br>• **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-016` | 0–6 | Adopt Terragrunt's `run-all apply` with explicit `dependencies` blocks to get DAG-aware parallel applies across roots for free.<br><br><sub>_dependency-graph_ · Replaces hand-rolled phase scripts with a tool whose entire reason for existing is this problem._</sub> | • **A1** (A1→A5-004 · _observability-extension_ · phase 0–6): Extend A5-016 (Terragrunt run-all) with a Gantt chart artifact per session showing actual parallelism achieved vs theoretical maximum. — _Visualizes critical path so it can be optimized._<br>• **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-017` | 0–6 | Maintain a long-lived "infrastructure shell" subagent whose only job is `terraform apply` calls, freeing the main agent to read logs concurrently.<br><br><sub>_subagent-pool_ · Today the main loop blocks on apply for 5–15 min; offloading reclaims that wall-clock._</sub> | • **A3** (A3→A5-013 · _regression-extension_ · phase 0+): Extend A5-017 (long-lived TF shell subagent) with a heartbeat assertion: if no apply within N min, exit — prevents zombie subagents from holding state locks. — _Hung subagents holding DynamoDB locks are a known sandbox stall pattern._ |
| `A5-018` | 1–6 | Fire `kubectl wait --for=condition=...` in a background process the second a manifest is applied, so verification overlaps with the next apply.<br><br><sub>_pipeline_ · Removes the "apply → wait-for-ready → next apply" anti-pattern._</sub> | • **A1** (A1→A5-014 · _pipeline-extension_ · phase 1–6): Extend A5-018 (background kubectl-wait) with structured exit-status persistence to `/tmp/wait-results.json` so the conductor can join on multiple waits. — _Conductor-pattern needs a join primitive._<br>• **A6** (A6→A5-004 · _simplification_ · phase 1+): A5-018 (background `kubectl wait`) lets us remove the explicit "wait for X" steps from sequential apply scripts; verification overlaps for free. — _Removes serial-wait pattern._ |
| `A5-019` | 0+ | Pre-create the ACM cert (long DNS-validation tail) at session start so it's ISSUED by the time anything actually needs it.<br><br><sub>_prewarm_ · ACM validation is the longest single wait in phase-0; starting it first overlaps with everything._</sub> | • **A3** (A3→A5-004 · _regression-extension_ · phase 0+): Pair A5-019 (pre-create ACM cert) with a verification that the cert's DNS validation record is in Route53 BEFORE waiting on ISSUED — speeds up failure detection. — _Validation hang is silent; pre-checking the CNAME is the fast oracle._ |
| `A5-020` | 1+ | Stand up Crossplane providers via `kubectl apply` directly (not helm) while helm is still installing argo-cd, since they share no controllers.<br><br><sub>_parallel-apply_ · Breaks the current helm-then-helm-then-crossplane chain in `terraform/management/helm.tf`._</sub> | _(no comments)_ |
| `A5-021` | 0–6 | Use a "chained wait-then-trigger" CLI (e.g. `until kubectl get ... ; do sleep 2; done && next-cmd`) launched in background per phase boundary.<br><br><sub>_pipeline_ · Replaces manual checking with event hooks the agent can subscribe to via Monitor._</sub> | _(no comments)_ |
| `A5-022` | 1+ | Treat `terraform/base` outputs as a static manifest, regenerate once, and serve to phase 1 via a local file instead of `terraform_remote_state` reading from S3 every plan.<br><br><sub>_cache_ · Removes the S3 GET round-trip on every phase-1 plan/apply._</sub> | • **A6** (A6→A5-007 · _refactor_ · phase 1+): A5-022 (cache base outputs locally) lets us remove the `terraform_remote_state` data source from phase-1 modules, simplifying graph + reducing S3 chatter. — _Local file > remote_state for static outputs._ |
| `A5-023` | 0–6 | Build a "phase compiler" that takes a YAML high-level plan ("bring up mgmt with secrets+keycloak") and emits parallel terraform + kubectl + argocd commands.<br><br><sub>_orchestration_ · Codifies the bring-up sequence so any agent (or human) can replay it deterministically._</sub> | • **A6** (A6→A5-003 · _refactor_ · phase 0+): A5-016 (Terragrunt run-all) supersedes A5-001, A5-002, A5-003, A5-009, A5-015, and A5-023 — adopting it lets us delete all the bespoke orchestration scaffolding. — _One tool replaces six homegrown ideas._ |
| `A5-024` | 0–6 | Cache `terraform init`'s `.terraform/providers/` in a shared volume across roots so each root doesn't re-download hashicorp/aws 600 MB.<br><br><sub>_cache_ · First `init` per root costs ~30 s today; shared cache makes subsequent inits ~2 s._</sub> | • **A3** (A3→A5-005 · _regression-extension_ · phase 0+): A5-024 shared provider cache should have a contract test that the cache directory's provider versions match what each root's `versions.tf` declares — silent-mismatch guard. — _Shared caches introduce cross-root version-coupling bugs._<br>• **A6** (A6→A5-014 · _simplification_ · phase 0+): A5-024 (shared `.terraform/providers/` cache) lets us remove per-root `terraform init` from the documented bring-up steps — first session sets up, subsequents inherit. — _Cache hides the step._ |
| `A5-025` | 0–6 | Run `terraform validate && tflint` in parallel across all roots at session start as a pre-flight before any apply.<br><br><sub>_speculative-apply_ · Catches syntactic errors in zero AWS wall-clock instead of mid-apply._</sub> | _(no comments)_ |
| `A5-026` | 1+ | Pre-render every Helm chart with `helm template` against `--api-versions` and run `kyverno test` locally in parallel with the real cluster apply.<br><br><sub>_speculative-apply_ · Catches policy violations before they cause Sync failure on the real cluster._</sub> | _(no comments)_ |
| `A5-027` | 0–6 | Maintain a Redis/SQLite "event bus" in `/tmp` that subagents post phase-completion events to, so listeners trigger downstream work without polling.<br><br><sub>_pipeline_ · Eliminates polling overhead and lets dozens of downstream tasks subscribe to one event._</sub> | • **A1** (A1→A5-005 · _pipeline-extension_ · phase 0–6): Pair A5-027 (event bus) with an OpenTelemetry collector so each event is a span, queryable in Jaeger/Tempo. — _Free distributed tracing of the orchestration graph itself._<br>• **A2** (A2→A5-007 · _test-oracle-promotion_ · phase 0+): Use A5-027 (Redis/SQLite event bus) as the substrate for the A2 e2e suite's test-orchestrator coordinating chaos+claim+verification subagents. — _Same event bus drives ops and tests._<br>• **A3** (A3→A5-006 · _regression-extension_ · phase 0+): Extend A5-027 (event bus) with a "deadletter" log: events with no subscriber within deadline trigger an alarm — invisible orchestration bugs become visible. — _Missed-event class is the hardest event-driven bug to debug._<br>• **A6** (A6→A5-005 · _refactor_ · phase 0+): A5-027's event bus retires every polling loop in scripts/ — they become subscribers. — _Push > poll._<br>• **A6** (A6→A5-013 · _refactor_ · phase 1+): A5-007 (event-driven cluster-ready watcher) plus A5-027 (event bus) lets us delete every `sleep 600` style fixed wait remaining in phase scripts. — _All waits become event subscriptions._ |
| `A5-028` | 0–6 | Use `tmux` panes (or a TUI) so the human can watch base-apply, mgmt-apply, helm-install, and argo-sync logs side by side instead of reading one giant log.<br><br><sub>_orchestration_ · Improves human verification latency from "scroll a 170 KB log" to "glance at pane 3"._</sub> | _(no comments)_ |
| `A5-029` | 2–6 | Push ArgoCD `Application` manifests into the cluster *before* the workloads exist, so they go OutOfSync→Synced automatically the moment CRDs land.<br><br><sub>_speculative-apply_ · Removes the explicit "now apply this Application" hand-off step from each phase._</sub> | • **A2** (A2→A5-008 · _smoke-test_ · phase 2+): Add a smoke test that A5-029 (pre-staged ArgoCD apps) transitions OutOfSync→Synced within deadline once CRDs land — assert via ArgoCD admin API. — _Speculative-apply pattern only works if the eventual sync actually happens._<br>• **A3** (A3→A5-007 · _regression-extension_ · phase 2+): A5-029 (pre-applied Applications) needs a regression test that an Application sitting in OutOfSync for >N minutes (because CRD never landed) becomes a hard failure, not silent waiting. — _Pre-applied Apps can sit forever; need a deadline._<br>• **A6** (A6→A5-008 · _simplification_ · phase 2+): A5-029 (apply Applications pre-CRD) lets us delete the explicit sync-wave annotations on a chunk of `argocd/apps/*.yaml` — natural ordering handles it. — _Eventual-consistency obviates wave hints._ |
| `A5-030` | 2+ | Use `crossplane beta trace` or a custom controller-runtime watcher to stream XR/MR condition changes live to the agent's stdout.<br><br><sub>_pipeline_ · Replaces `phase-2-diagnose.yml`'s batch snapshot with continuous evidence._</sub> | • **A1** (A1→A5-006 · _pipeline-extension_ · phase 2+): Extend A5-030 (live XR/MR condition stream) with a TUI dashboard (e.g. `k9s` plugin or bespoke) that highlights condition flips with timestamps. — _Human pattern-recognition kicks in when state changes are visualized._<br>• **A2** (A2→A5-009 · _smoke-test_ · phase 2+): Build a chaos companion to A5-030 (live XR/MR streamer) that kills the streamer mid-stream and asserts the agent's downstream consumer reconnects cleanly. — _Long-lived streams break; reconnect must be tested._ |
| `A5-031` | 2–6 | Have one subagent run end-to-end teardown of a previous test namespace while another brings up the next test claim — no serial cleanup tax.<br><br><sub>_parallel-apply_ · Cleanup currently blocks the next iteration for ~30 s per claim._</sub> | _(no comments)_ |
| `A5-032` | 1–6 | Cache `aws-iam-authenticator` / `aws eks get-token` tokens for kubectl across subagents via `KUBECONFIG=/tmp/shared-kubeconfig`.<br><br><sub>_cache_ · Avoids each agent re-hitting STS and burning the 6/s rate limit._</sub> | _(no comments)_ |
| `A5-033` | 3+ | Drive a "speculative phase-3" apply against a scratch namespace while phase-2 is still settling, then promote if upstream succeeds.<br><br><sub>_speculative-apply_ · Hides phase-3 wall-clock under phase-2's tail._</sub> | _(no comments)_ |
| `A5-034` | 2–6 | Use `argocd app sync --async` for every Application in one shot, then `argocd app wait` in parallel, instead of one-at-a-time sync.<br><br><sub>_parallel-apply_ · Trivially cuts N-app reconcile time from sum-of-N to max-of-N._</sub> | • **A2** (A2→A5-010 · _smoke-test_ · phase 2+): Pair A5-034 (`argocd app sync --async` fan-out) with a concurrency test that asserts no app is stuck in `Progressing` past deadline. — _Fan-out can starve individual apps; test catches it._<br>• **A3** (A3→A5-008 · _regression-extension_ · phase 2+): Pair A5-034 (parallel argocd sync --async) with a per-app timeout map so one slow app doesn't mask successes — catches the "1/15 stuck" pattern. — _Aggregate "max" hides single-app stuck cases._<br>• **A6** (A6→A5-009 · _refactor_ · phase 2+): A5-034 (`argocd app sync --async` fan-out) replaces the per-app serial sync script in any current bring-up automation. — _Trivially parallel._ |
| `A5-035` | 1+ | Pre-build a self-signed CA + Cognito client config in the sandbox and inject as a TF variable so phase-1 doesn't have to mint these mid-apply.<br><br><sub>_prewarm_ · Removes a serial AWS-API leg from inside the management apply._</sub> | _(no comments)_ |
| `A5-036` | 0–6 | Add a `terraform apply -auto-approve -compact-warnings -parallelism=30` wrapper script that's the canonical entrypoint, bumping the default 10.<br><br><sub>_local-tf-driver_ · Cheapest single throughput win; AWS API rate limits, not terraform's default, are the real ceiling._</sub> | • **A6** (A6→A5-015 · _refactor_ · phase 0+): A5-036 (canonical apply wrapper with -parallelism=30) lets us delete every per-script terraform-flags string and consolidate flag policy in one place. — _One wrapper, one policy._ |
| `A5-037` | 0–6 | Maintain a "phase status board" markdown file that subagents append to as they finish, giving a real-time gantt of the bring-up.<br><br><sub>_orchestration_ · Gives both human and the conductor agent a single pane for "where are we"._</sub> | • **A1** (A1→A5-007 · _orchestration-extension_ · phase 0–6): Pair A5-037 (status board markdown) with an auto-generated svg gantt embedded so PR reviewers see parallelism, not just text. — _Visual evidence of orchestration wins makes them defensible._<br>• **A6** (A6→A5-012 · _simplification_ · phase 0+): A5-037 (live status board) makes the manual handoff "what's the current state" prose section much shorter — board is the source of truth. — _Generated > written._ |
| `A5-038` | 0–6 | Use AWS SDK pagination + `aws cloudformation` events-style polling (or `aws ec2 wait`) directly instead of terraform's internal polling.<br><br><sub>_local-tf-driver_ · Sometimes terraform polls every 30 s; direct waiters poll every 5 s._</sub> | _(no comments)_ |
| `A5-039` | 0+ | Pre-create the S3 backend bucket + DynamoDB lock table once at account-rotation time and bake their names into the sandbox env, eliminating bootstrap.<br><br><sub>_prewarm_ · Removes the awkward "chicken-and-egg" backend bootstrap from every fresh account._</sub> | • **A6** (A6→A5-011 · _simplification_ · phase 0+): A5-039 (pre-baked S3/Dynamo backend) retires the chicken-and-egg `bootstrap-backend.sh` and its README section. — _One-time setup, never reinvoked._ |
| `A5-040` | 2+ | Run a "rehearsal cluster" on kind/k3d locally in parallel with the real EKS bring-up, so Crossplane composition logic is verified without AWS latency.<br><br><sub>_speculative-apply_ · Decouples composition-function iteration from EKS-creation wall-clock._</sub> | • **A1** (A1→A5-013 · _speculative-apply-extension_ · phase 2+): Pair A5-040 (kind rehearsal cluster) with a webhook that auto-promotes a successful kind run to a real-EKS dispatch — closes the loop. — _Two-step "rehearse then promote" eliminates the manual gate._<br>• **A2** (A2→A5-011 · _new-scenario_ · phase 2+): Add an e2e harness on top of A5-040 (kind rehearsal cluster) that runs the entire A2 chaos suite locally before the real-EKS run. — _Two-cluster discipline halves real-EKS feedback loop._<br>• **A3** (A3→A5-009 · _regression-extension_ · phase 2+): A5-040 (rehearsal kind cluster) should snapshot composition function inputs from the rehearsal and diff them against the real EKS run — proves equivalence. — _Kind rehearsal is only valuable if proven equivalent to EKS._ |
| `A5-041` | 2–6 | Use `kubectl apply --server-side --force-conflicts` in a fan-out across all CRDs at once instead of letting ArgoCD reconcile them serially.<br><br><sub>_parallel-apply_ · ArgoCD applies in waves; direct fan-out collapses waves into one round trip._</sub> | _(no comments)_ |
| `A5-042` | 0–6 | Have one subagent watch CloudWatch metrics live (AWS API throttling, EKS API latency) and feed back-pressure signals to other subagents.<br><br><sub>_orchestration_ · Prevents N parallel agents from collectively tripping AWS rate limits._</sub> | • **A1** (A1→A5-009 · _observability-pair_ · phase 0–6): Pair A5-042 (rate-limit back-pressure) with auto-emitting CloudWatch custom metrics `OrchestratorBackpressureEvents` so throttling is graphable. — _Proves the back-pressure actually fires when AWS APIs get hot._<br>• **A2** (A2→A5-012 · _smoke-test_ · phase 0+): Add a soak test around A5-042 (CloudWatch back-pressure feedback) that runs 20 subagents in parallel for 30 min asserting zero AWS throttle errors. — _Back-pressure controller must work under sustained load._<br>• **A3** (A3→A5-010 · _regression-extension_ · phase 0+): Extend A5-042 (backpressure watcher) with a regression test that throttling above threshold actually causes subagents to slow down — the watcher must demonstrably feed back. — _Backpressure systems are notorious for silently not back-pressuring._ |
| `A5-043` | 1–6 | Build a "trigger-on-condition" wrapper: `await k8s/condition:Ready/<resource> && cmd` so chained pipelines read like a Makefile.<br><br><sub>_pipeline_ · DSL clarity makes complex bring-up sequences reviewable._</sub> | • **A2** (A2→A5-013 · _test-oracle-promotion_ · phase 1+): Use A5-043 (await-condition DSL) as the canonical wait primitive in every A2 e2e scenario. — _Same DSL for ops and tests._ |
| `A5-044` | 1+ | Cache `helm dependency build` output across sessions in the repo (`charts/.cache/`) so chart deps don't re-resolve every fresh sandbox.<br><br><sub>_cache_ · Saves ~10 s per chart, x5 charts = 50 s per fresh sandbox._</sub> | _(no comments)_ |
| `A5-045` | 0–6 | Use `terraform state pull` once, then operate on a local copy in parallel speculative plans, and only `state push` when confirmed.<br><br><sub>_speculative-apply_ · Lets multiple "what-if" plans run without contending on the real state lock._</sub> | _(no comments)_ |
| `A5-046` | 1–2 | Fire phase-2 ArgoCD bootstrap manifest as a `kubernetes_manifest` *inside* the management terraform so it's atomic with the helm release.<br><br><sub>_orchestration_ · Removes a manual hand-off; bring-up becomes one `terraform apply`._</sub> | • **A2** (A2→A5-014 · _smoke-test_ · phase 1+): Add a contract test that A5-046 (atomic helm+bootstrap apply) leaves the cluster in the same final state as the two-step path. — _Consolidation must not regress final state._<br>• **A3** (A3→A5-011 · _regression-extension_ · phase 1-2): Pair A5-046 (atomic Application via kubernetes_manifest) with a tear-down test asserting `terraform destroy` cleanly removes the Application without leaving cluster-side orphan. — _kubernetes_manifest has historical destroy-time edge cases._<br>• **A6** (A6→A5-006 · _simplification_ · phase 1+): A5-046 (atomic ArgoCD bootstrap inside terraform) lets us delete the separate `bootstrap-argocd.sh` script and its accompanying handoff step. — _One apply, one outcome._ |
| `A5-047` | 1–6 | Run `chainsaw` smoke tests against a kind cluster in parallel with the real apply so policy regressions surface in seconds, not minutes.<br><br><sub>_speculative-apply_ · Today chainsaw runs only via CI workflow; local-parallel collapses the feedback loop._</sub> | _(no comments)_ |
| `A5-048` | 2+ | Convert phase 2's "wait 3 min for ArgoCD refresh" into a `argocd app sync --hard-refresh` triggered the instant a manifest commit lands.<br><br><sub>_pipeline_ · The single biggest documented wait (handoff Step 4) becomes a sub-second push._</sub> | • **A1** (A1→A5-011 · _pipeline-extension_ · phase 2+): Pair A5-048 (hard-refresh on commit) with an emitted "sync latency" metric per Application so the speedup is provable and regression-able. — _Without metrics, the win evaporates over time._<br>• **A6** (A6→A5-002 · _simplification_ · phase 1+): A5-013 + A5-048 (kick ArgoCD sync immediately / hard-refresh) lets us delete every `sleep 180` waiting for ArgoCD's 3-min refresh window in scripts and tests. — _Event-driven replaces fixed sleeps._ |
| `A5-049` | 0–1 | Introduce a "warm pool" of pre-applied phase-0 VPCs left running between sessions (within sandbox limits) so phase-1 can start without phase-0 wait.<br><br><sub>_prewarm_ · Saves the entire 5-min phase-0 critical path when iterating on phase-1/2 fixes._</sub> | • **A1** (A1→A5-010 · _prewarm-extension_ · phase 0–1): Extend A5-049/050 (warm pool VPCs) with a daily tag-audit job asserting all warm pool resources still carry `lifecycle=warm` — defends against accidental promotion to ephemeral. — _Warm pool resources getting GC'd by cleanup scripts is the obvious risk._<br>• **A2** (A2→A5-015 · _smoke-test_ · phase 0+): Add a teardown e2e that confirms A5-049/A5-050 (warm VPC pool) can be safely imported then destroyed without orphans. — _Warm-pool patterns introduce orphan risk; explicit teardown test._<br>• **A3** (A3→A5-012 · _regression-extension_ · phase 0-1): A5-049/A5-050 (warm pool / import existing VPC) need a session-start invariant test that the warm pool VPC matches the expected CIDR and subnet count — drift after import is silent. — _Imported state can mismatch source-of-truth; explicit assert is cheap._<br>• **A6** (A6→A5-010 · _simplification_ · phase 0+): A5-049 + A5-050 (warm pool / import) lets us delete the phase-0 step from session bring-up checklists entirely when iterating downstream. — _Skip-phase-0 becomes the default fast path._ |
| `A5-050` | 0–1 | Use `terraform import` + `terraform refresh` against a pre-existing warm VPC instead of recreating it, so phase-1 attaches to an extant network.<br><br><sub>_speculative-apply_ · Even bigger win than A5-049: zero phase-0 wall-clock at all._</sub> | • **A2** (A2→A5-015 · _smoke-test_ · phase 0+): Add a teardown e2e that confirms A5-049/A5-050 (warm VPC pool) can be safely imported then destroyed without orphans. — _Warm-pool patterns introduce orphan risk; explicit teardown test._<br>• **A3** (A3→A5-012 · _regression-extension_ · phase 0-1): A5-049/A5-050 (warm pool / import existing VPC) need a session-start invariant test that the warm pool VPC matches the expected CIDR and subnet count — drift after import is silent. — _Imported state can mismatch source-of-truth; explicit assert is cheap._<br>• **A6** (A6→A5-010 · _simplification_ · phase 0+): A5-049 + A5-050 (warm pool / import) lets us delete the phase-0 step from session bring-up checklists entirely when iterating downstream. — _Skip-phase-0 becomes the default fast path._ |

### A5 — General comments (no source-ID anchor)

Cross-review rows targeted at this agent but without an explicit `A{N}-NNN` reference in the comment text; preserved verbatim.

| Comment ID | From | Phase | Comment | Category & justification |
|---|---|---|---|---|
| `A1→A5-008` | A1 | 0–6 | Add an "orchestration-as-code" lint asserting every parallel-eligible terraform root declares `# parallel-safe-with: [...]` so future refactors can re-derive the DAG. | _orchestration-extension_ — Implicit parallelism becomes self-documenting. |
| `A1→A5-012` | A1 | 0–6 | Add a `phase-bring-up-replay.sh` that takes a session ID and re-runs the exact dispatch sequence from the orchestrator log — turns sessions into reproducible tapes. | _orchestration_ — Reproducibility is the second-order win of recording orchestration. |
| `A1→A5-015` | A1 | 0–6 | Add a sandbox-wide AWS API rate dashboard (CloudWatch metric on `ThrottlingException` counts per service) so the rate-limit ceiling is visible, not just guessed. | _observability_ — Knowing the actual ceiling enables max-parallelism tuning. |
| `A4→A5-001` | A4 | 0+ | Pair parallel root applies with a `runbook-parallel-apply-debug.md` documenting how to attribute a failure to the right root when N applies are in flight. | _runbook_ — Parallel without traceability is parallel chaos. |
| `A4→A5-002` | A4 | 0+ | Add a `subagent-pool-status.md` auto-updated dashboard so the conductor's view is also the operator's view. | _dashboard_ — Avoids the "what is the conductor thinking" debug class. |
| `A4→A5-007` | A4 | 1+ | Pair the EKS watcher with a `notify-when-condition.sh` generic helper used everywhere — one tool, many uses. | _shell-helper_ — Generic primitives outlive bespoke watchers. |
| `A4→A5-010` | A4 | 0+ | Cache the speculative-plan JSON to a known path so the next agent can read it without re-running. | _cache_ — Shaves another 20s every time. |
| `A4→A5-013` | A4 | 1+ | Pair `argocd app sync` with `runbook-argocd-sync-failure.md` enumerating the five blockers (RBAC, sync-wave, CRD missing, hook fail, prune-conflict). | _runbook_ — Sync errors are often opaque; runbook decodes them. |
| `A4→A5-018` | A4 | 1+ | Have the background `kubectl wait` emit a `/tmp/wait-events.log` line per resource transition so a debugger can replay. | _pipeline_ — Wait-in-background loses observability without a log. |
| `A4→A5-021` | A4 | 0+ | Add a `wait-then-trigger.sh --on-fail` mode that runs a diagnose helper instead of just exiting — auto-debug on first failure. | _pipeline_ — "Wait then trigger" is good; "wait then debug if fail" is better. |
| `A4→A5-027` | A4 | 0+ | Make the event bus topology + topic list a one-page `event-bus-topics.md` runbook so subagents know what to subscribe to. | _runbook_ — Buses without topic registries become noise. |
| `A4→A5-028` | A4 | 0+ | Pair tmux panes with a `tmux-layout-phase-N.sh` script per phase that auto-arranges the panes for that phase's likely debug session. | _orchestration_ — One command vs five tmux invocations. |
| `A4→A5-030` | A4 | 2+ | Pipe `crossplane beta trace` stream output into the dispatch-history doc, so the trace becomes part of the session record. | _pipeline_ — Ephemeral traces are forgotten; persistent ones become evidence. |
| `A4→A5-034` | A4 | 2+ | Add a `argocd-app-sync-all-and-quote.sh` wrapper that emits a markdown-ready table of post-sync conditions, satisfying verify-evidence requirement. | _parallel-apply_ — Sync without evidence-quote is the silent-pass class. |
| `A4→A5-037` | A4 | 0+ | Have the status board auto-generate a final `session-gantt.svg` at session end for the handoff — visualizes wall-clock waste. | _orchestration_ — Visual artifact drives next-session optimization. |
| `A4→A5-042` | A4 | 0+ | Pair the back-pressure subagent with a `runbook-aws-throttle.md` listing the per-service throttle codes and recommended backoff multipliers. | _runbook_ — Throttle detection without remediation is just complaining. |
| `A4→A5-047` | A4 | 1+ | After local-kind chainsaw, auto-diff results vs the last real-cluster run — surfaces simulation drift early. | _speculative-apply_ — Local-rehearsal is only useful if its accuracy is tracked. |
| `A4→A5-049` | A4 | 0+ | Pair warm-pool VPCs with `warm-pool-status.sh` that asserts the warm VPC still matches sandbox quota and isn't itself the cap-killer. | _prewarm_ — Warm pool can become orphan-leak; needs its own guardrail. |
| `P→A5-001` | PRIMARY | 0+ | A `scripts/orchestrate/phase-graph.json` that declares phase × component DAG; a `phase-up.py` reads it and runs everything that can run in parallel at each frontier, with a Gantt-style progress dashboard. | _dependency-graph_ — Codifies parallelism so future agents don't have to rediscover it. |
| `P→A5-002` | PRIMARY | 1+ | A "warm pool" subagent that, while phase-1 EKS is creating (15 min), pre-pulls every Helm chart, pre-renders every manifest, pre-validates every IAM policy doc, and pre-warms ECR images — net zero wall-clock cost. | _parallel-apply_ — Captures the explicit "parallel unrelated work" insight from the user's prompt. |
| `P→A5-003` | PRIMARY | 1+ | Use the existing two-AZ constraint to deliberately structure modules so AZ-A and AZ-B resources can be applied as two parallel `terraform apply -target=` invocations. | _parallel-apply_ — Same wall-clock budget gets twice the work done; safe within sandbox caps. |
| `P→A5-004` | PRIMARY | 1+ | A "bring-up race report" that records, per phase, which step was on the critical path; PR description auto-includes it so the next session knows where to attack next. | _orchestration_ — Continuous improvement loop on cold-start time. |
| `P→A5-005` | PRIMARY | 0+ | Run the brainstorming-style fan-out pattern itself (this very session) as a reusable harness: `scripts/orchestrate/fanout.py <brief> <N>` spawns N subagents on a shared workspace. | _subagent-pool_ — Generalizes what worked here so future brainstorms / reviews / audits are one command. |

## A6 — Cruft removal targets

> **Mandate.** Brainstorm things to remove, refactor, or simplify now that the sandbox has direct AWS + kubectl + egress and the old GitHub-Actions-only execution substrate is no longer the only path.

_Source file: [`ai/brainstorming/A6-removal-refactor.md`](../../ai/brainstorming/A6-removal-refactor.md)_

_Idea count: 50_

| ID | Phase | Idea (with category and justification) | Comments (bulleted by commenter) |
|---|---|---|---|
| `A6-001` | 0+ | Delete `.claude/skills/ext-github/` entirely.<br><br><sub>_deprecate-skill_ · jentic-bridged Actions API access only existed because direct egress to api.github.com was blocked; sandbox now has `gh` and unrestricted egress._</sub> | • **A1** (A1→A6-003 · _archive-before-delete_ · phase 0+): Before deleting any debug-bridge skill (A6-001/002), archive its `ext-*` schema descriptors into `ai/archive/` so future similar bridges can be re-derived if egress ever closes again. — _Cheap insurance against environmental regression._<br>• **A2** (A2→A6-015 · _preserve-via-test_ · phase 0+): Before A6-001/A6-002/A6-003 (delete ext-github + bridge + spec), add an A2 smoke that direct `gh` CLI calls succeed from the sandbox (proves the bridge is truly obsolete). — _One-time test confirms the migration premise._ |
| `A6-002` | 0+ | Delete `.claude/skills/external-api-bridge/` meta-skill and its `reference/procedure.md`.<br><br><sub>_deprecate-skill_ · The meta-skill exists only to author `ext-*` jentic bridges, which are obsolete once egress is open._</sub> | • **A2** (A2→A6-015 · _preserve-via-test_ · phase 0+): Before A6-001/A6-002/A6-003 (delete ext-github + bridge + spec), add an A2 smoke that direct `gh` CLI calls succeed from the sandbox (proves the bridge is truly obsolete). — _One-time test confirms the migration premise._ |
| `A6-003` | 0+ | Delete `ai/specs/ext-github-design.md` (and the `ai/specs/` dir if it ends up empty).<br><br><sub>_removal_ · Design spec for the now-obsolete jentic bridge — single-purpose, no longer drives any live code._</sub> | • **A2** (A2→A6-015 · _preserve-via-test_ · phase 0+): Before A6-001/A6-002/A6-003 (delete ext-github + bridge + spec), add an A2 smoke that direct `gh` CLI calls succeed from the sandbox (proves the bridge is truly obsolete). — _One-time test confirms the migration premise._ |
| `A6-004` | 0+ | Remove the "Capability profile" section from `.claude/skills/terraform-ci-watch/SKILL.md` and collapse to a single `gh`-only path.<br><br><sub>_simplify_ · Three-profile (`gh` / `github-mcp` / `ext-github`) branching existed only to handle sandboxes without `gh`; current sandbox always has it._</sub> | _(no comments)_ |
| `A6-005` | 0+ | Delete `.claude/skills/terraform-ci-watch/reference/capabilities.md`.<br><br><sub>_removal_ · Per-profile dispatch table is dead with single-profile collapse._</sub> | _(no comments)_ |
| `A6-006` | 0+ | Simplify `.claude/skills/terraform-ci-watch/reference/log-fetching.md` to assume `gh run view --log`.<br><br><sub>_simplify_ · Most of the file describes jentic single-run-GET catalog gaps and partial-MCP fallback — gone with the bridge._</sub> | _(no comments)_ |
| `A6-007` | 2+ | Delete `.github/workflows/phase-2-diagnose.yml`.<br><br><sub>_deprecate-workflow_ · Whole workflow was a dispatch-only state dumper because the agent could not reach `kubectl`; agent now runs the same kubectl commands directly in 2 seconds instead of dispatching+waiting 3 minutes._</sub> | • **A1** (A1→A6-001 · _replace-not-remove_ · phase 2+): Pair A6-007 (delete phase-2-diagnose.yml) with promoting its kubectl bundle into a `scripts/diag-bundle.sh` that also captures CloudTrail + Prom snapshot — net upgrade not just deletion. — _Keeps the institutional knowledge encoded in the workflow's bundle list._<br>• **A2** (A2→A6-001 · _preserve-via-test_ · phase 2+): Before removing `phase-2-diagnose.yml` (A6-007), port its known-good dump shape into an A2 chainsaw `error:` block so failure post-mortems retain the same level of detail. — _Don't lose the artifact, just relocate it._<br>• **A3** (A3→A6-001 · _regression-extension_ · phase 2+): Before deleting `phase-2-diagnose.yml` (A6-007), extract its bug-3/bug-4 named query bodies into a `tests/regression/` corpus so the historical evidence survives the deletion. — _Lessons embedded in the workflow shouldn't vanish with it._ |
| `A6-008` | 2+ | Delete `.github/workflows/chainsaw-verify.yml`.<br><br><sub>_deprecate-workflow_ · Existed only as the "look-aside SHA verifier" half of a dispatch-then-prove-it-ran-green dance the agent had to do because it couldn't actually run chainsaw locally; with direct sandbox compute the agent can run the harness itself._</sub> | • **A1** (A1→A6-002 · _replace-not-remove_ · phase 2+): Pair A6-008 (delete chainsaw-verify.yml) with adding a local `chainsaw-evidence-bundler.sh` that produces the same evidence pack the workflow used to emit — preserves the "evidence quote" pattern. — _Evidence-quoting was the *value*, not the dispatch indirection._<br>• **A2** (A2→A6-002 · _preserve-via-test_ · phase 2+): Before deleting `chainsaw-verify.yml` (A6-008), add an e2e smoke test that asserts every push triggers chainsaw exactly once. — _Replace SHA-handshake guard with positive coverage._<br>• **A3** (A3→A6-002 · _regression-extension_ · phase 2+): A6-008 (delete chainsaw-verify) — pair removal with a new lint that fails any PR introducing a dispatch-then-verify handshake pattern in a workflow. — _Prevents the pattern from re-emerging accidentally._ |
| `A6-009` | 2+ | Collapse the chainsaw `commit_sha` input + verifier handshake in `.github/workflows/chainsaw.yml` to a plain `push`-triggered run.<br><br><sub>_simplify_ · The "agent dispatches against a SHA, then a separate workflow looks it up" pattern is GH-Actions-only indirection; with sandbox compute it's just `bash tests/chainsaw/run.sh`._</sub> | • **A5** (A5→A6-010 · _dedupe_ · phase 2+): A6-044 (`make chainsaw`) is the same Makefile as A5→A6-009 and A5-003 — one Makefile, three callers. — _Single Makefile is the orchestration plane._ |
| `A6-010` | 2+ | Inline `.github/workflows/integration-tests.yml` into a `scripts/run-integration.sh` wrapper.<br><br><sub>_inline_ · The workflow is essentially `aws eks update-kubeconfig && tests/integration/run.sh`; agent can run that directly with current sandbox capabilities._</sub> | • **A2** (A2→A6-003 · _preserve-via-test_ · phase 2+): When inlining `integration-tests.yml` (A6-010) into `scripts/run-integration.sh`, add a unit test that the script exits non-zero on the silent-PASS class (replicates PR #59 fixture). — _Don't lose the silent-PASS defense._<br>• **A5** (A5→A6-003 · _parallel-test_ · phase 2+): A6-010 inlining integration-tests.yml into `scripts/run-integration.sh` enables A5→A3-004 parallel scenario fan-out (workflow steps can't fan out, scripts can). — _Removing the workflow unlocks parallelism._ |
| `A6-011` | 2+ | Remove the `test_filter` workflow input plumbing on `integration-tests.yml`.<br><br><sub>_simplify_ · Input existed because the only way to scope was via dispatch; locally `tests/integration/run.sh 11_*` does the same thing._</sub> | _(no comments)_ |
| `A6-012` | 2+ | Delete `ai/PHASE-2-LIFECYCLE-PLAN.md` after folding its bash blocks into a small `scripts/phase-2-lifecycle.sh`.<br><br><sub>_consolidate-script_ · Doc explicitly says it carries the verbatim contents of removed workflow `mode=` steps as a runbook — better as a script the agent invokes._</sub> | • **A5** (A5→A6-014 · _orchestration_ · phase 2+): A6-012 (PHASE-2-LIFECYCLE-PLAN → script) is the natural input to A5-023 phase compiler — feed the script's steps as a YAML manifest. — _Lifecycle becomes the declarative input._ |
| `A6-013` | 0+ | Drop `RUNNER_TEMP`, `GITHUB_REPOSITORY`, `GITHUB_SHA`, `GITHUB_REF_NAME` plumbing from `.github/scripts/post-comment.py` once it no longer needs to run inside Actions.<br><br><sub>_refactor_ · The PR-summary comment is best produced locally via `gh pr comment` now that the agent runs terraform itself._</sub> | • **A1** (A1→A6-004 · _refactor-extension_ · phase 0+): Pair A6-013 (drop GitHub env plumbing from post-comment.py) with extracting any structured-summary logic into a reusable `scripts/render-evidence-md.py` so the formatter survives. — _The Markdown rendering was non-trivial; preserve, don't lose._ |
| `A6-014` | 0+ | Delete `.github/scripts/post-comment.py` outright.<br><br><sub>_removal_ · Once `terraform apply` runs in-sandbox, the agent reports plan/apply diffs in the PR body directly instead of via auto-comment._</sub> | • **A1** (A1→A6-013 · _archive-before-delete_ · phase 0+): Before A6-014 (delete post-comment.py), capture its rendered output for the last 30 days as fixtures so the replacement (agent-written summary) can be regression-tested. — _Lossless migration requires before/after fixtures._<br>• **A2** (A2→A6-004 · _preserve-via-test_ · phase 0+): Add an A2 e2e regression after removing `post-comment.py` (A6-014) that the in-PR-body apply summary actually appears on every plan/apply. — _New reporting path needs its own test._<br>• **A3** (A3→A6-003 · _regression-extension_ · phase 0+): Before A6-014 (delete post-comment.py) lands, port its truncation+section-heading logic into a `scripts/pr-body-format.sh` so PR summaries keep their shape. — _Output-format regression is the silent kind._ |
| `A6-015` | 0+ | Delete `tests/unit/test_post_comment.sh`.<br><br><sub>_removal_ · Unit test only exists to defend the auto-comment script that goes away._</sub> | _(no comments)_ |
| `A6-016` | 0+ | Inline `.github/scripts/compute-gates.sh` into a tiny terraform `Makefile` or shell helper.<br><br><sub>_inline_ · Gate booleans (`base_init`, `base_apply`, ...) are entirely a workflow-step-mux artifact; in-sandbox you just call `terraform init/plan/apply` directly._</sub> | • **A2** (A2→A6-008 · _preserve-via-test_ · phase 0+): When inlining `compute-gates.sh` (A6-016), add a unit test that the new entry script implements the same gate-skip semantics for already-applied phases. — _Behavior parity is the contract._<br>• **A5** (A5→A6-009 · _consolidate-script_ · phase 0+): A6-016 (compute-gates → Makefile) is the natural home for A5-003 `make -j8 phase-N` parallel targets. — _Makefile already wanted; A5 adds parallel targets._ |
| `A6-017` | 0+ | Delete `tests/unit/test_compute_gates.sh`.<br><br><sub>_removal_ · Defends `compute-gates.sh`, which goes away with the workflow gate fan-out._</sub> | _(no comments)_ |
| `A6-018` | 0+ | Collapse `terraform-test.yml`'s 11-step `phase`×`action` matrix into a thin "run `scripts/terraform.sh <phase> <action>`" workflow.<br><br><sub>_refactor_ · The huge step-conditional matrix exists to mux gates inside Actions; with direct exec, all that lives in one bash function._</sub> | • **A1** (A1→A6-005 · _refactor-extension_ · phase 0+): Extend A6-018 (collapse terraform-test.yml matrix) by emitting an OpenTelemetry span per `<phase, action>` so post-collapse, observability of phase timing is preserved. — _Matrix UI exposed timings; replacement should not lose that view._<br>• **A2** (A2→A6-012 · _preserve-via-test_ · phase 0+): Pair A6-018 (collapse terraform-test matrix) with an A2 e2e that asserts the new entry script supports each phase×action the old matrix did. — _Behavior parity test for the migration._<br>• **A3** (A3→A6-004 · _regression-extension_ · phase 0+): A6-018 (collapse terraform-test matrix) deserves a regression test that the new `scripts/terraform.sh` accepts every (phase, action) tuple the matrix supported — easy to drop a combination. — _Matrix-to-script migrations historically lose edge cases._<br>• **A5** (A5→A6-001 · _refactor_ · phase 0+): After A6-018 collapses the terraform-test matrix, the resulting `scripts/terraform.sh` is the natural place to add A5-036's `-parallelism=30` default. — _One canonical entrypoint, parallel by default._ |
| `A6-019` | 0+ | Remove `terraform-test.yml`'s Bootstrap state-backend step.<br><br><sub>_refactor_ · Bucket+DynamoDB create can be a one-shot `scripts/bootstrap-tf-state.sh` the agent runs once per fresh account, not a per-run idempotency dance._</sub> | • **A5** (A5→A6-002 · _prewarm_ · phase 0+): A6-019 (one-shot bootstrap script) pairs with A5-039 (pre-create backend at account-rotation): same operation, prewarm form. — _Bootstrap moves out of every run._ |
| `A6-020` | 0+ | Remove `terraform-test.yml`'s "Generate Cognito test credentials" step.<br><br><sub>_refactor_ · A per-CI-run secret generation block exists because secrets couldn't be set by the agent; sandbox can write `terraform.tfvars` directly with a one-time generated value._</sub> | _(no comments)_ |
| `A6-021` | 0+ | Drop the Route53 zone auto-discovery step from `terraform-test.yml`.<br><br><sub>_simplify_ · The zone is in `aws route53 list-hosted-zones`; agent reads it once per session and exports `TF_VAR_*` — no need for the workflow to redo it on every dispatch._</sub> | • **A2** (A2→A6-010 · _preserve-via-test_ · phase 0+): Add an A2 contract test that, after A6-021 (drop zone auto-discovery), confirms `TF_VAR_*` populated from session env matches `aws route53 list-hosted-zones`. — _Session-env path needs its own consistency check._<br>• **A3** (A3→A6-013 · _regression-extension_ · phase 0+): Before A6-021 removes the Route53 zone auto-discovery step, add a session-start `scripts/route53-zone-lock.sh` invariant check that the zone hasn't changed between sessions. — _"AWS account rotated; no Route53 zone" was a documented surprise class._ |
| `A6-022` | 0+ | Remove the `terraform-validate.yml` workflow and run `terraform fmt -check && terraform validate` as a pre-commit hook.<br><br><sub>_deprecate-workflow_ · The push-only validate workflow exists because the agent had no local terraform; sandbox has it._</sub> | • **A1** (A1→A6-006 · _replace-not-remove_ · phase 0+): Pair A6-022 (delete validate workflow + pre-commit hook) with a Git-server-side hook (or `pre-push`) that emits a structured `validate-result.json` artifact — pre-commit alone misses CI evidence trail. — _Local-only validation has no auditable trail._<br>• **A3** (A3→A6-005 · _regression-extension_ · phase 0+): When A6-022 removes terraform-validate.yml, pair the pre-commit hook with a CI-side check that the hook actually ran — pre-commit can be bypassed. — _Pre-commit hooks ship with bypass flags; redundancy is the test._ |
| `A6-023` | 0+ | Remove the `unit-tests.yml` workflow once the agent runs `tests/unit/run.sh` locally before push.<br><br><sub>_deprecate-workflow_ · Per-push CI for unit tests was a guardrail for an agent that couldn't run bash unit tests locally — no longer true._</sub> | • **A2** (A2→A6-013 · _preserve-via-test_ · phase 0+): After A6-023 (delete unit-tests.yml), add a pre-push git hook covered by a smoke test that confirms hook actually blocks a known-failing fixture. — _New guardrail surface needs proof._<br>• **A3** (A3→A6-006 · _regression-extension_ · phase 0+): A6-023 (remove unit-tests.yml) should keep one minimum CI run that asserts `tests/unit/run.sh` exits zero on main — local-only runs don't catch other contributors' breaks. — _Local-runs-only opens a "works on my machine" regression class._<br>• **A5** (A5→A6-004 · _parallel-test_ · phase 0+): A6-023 removal of unit-tests.yml lets unit tests run under `make -j$(nproc)` per A5→A3-001 — workflow steps were serial. — _Removing the workflow unlocks CPU parallelism._ |
| `A6-024` | 0+ | Collapse `tests/unit/run.sh` into a single `bats`-style runner instead of the per-test fan-out the unit-tests workflow currently mirrors.<br><br><sub>_simplify_ · Per-test workflow steps existed to surface failures in the GH UI; locally one runner with grep'able output is fine._</sub> | • **A5** (A5→A6-015 · _parallel-test_ · phase 0+): A6-024 (single bats-style unit runner) gains free parallelism from bats's built-in `-j` flag, no extra work to align with A5→A3-001. — _Tool already supports the model._ |
| `A6-025` | 0+ | Remove the `continue-on-error: true` toleration on `test_helm_render.sh` in `unit-tests.yml` and just fix or delete the test.<br><br><sub>_refactor_ · The toleration only exists because no one could iterate without dispatching CI — sandbox now has `helm`._</sub> | • **A2** (A2→A6-005 · _preserve-via-test_ · phase 0+): Before deleting `test_helm_render.sh` toleration (A6-025), promote its assertions into the A2 helm-render contract suite (paired with A3-008/A3-053). — _Don't lose the lint, fix it._ |
| `A6-026` | 2+ | Remove the `concurrency:` block from `chainsaw.yml`.<br><br><sub>_simplify_ · Concurrency-protect-the-shared-account collision only matters when many CI dispatches race; one-agent-in-one-sandbox just serializes._</sub> | • **A1** (A1→A6-007 · _safety-net-extension_ · phase 2+): Before applying A6-026/027 (remove chainsaw concurrency + RUN_ID), add a CloudWatch alarm on duplicate-AWS-resource-name errors so the safety net is retained at a different layer. — _Concurrency safety was real; replace with detection, not nothing._<br>• **A2** (A2→A6-006 · _meta-test_ · phase 2+): When collapsing chainsaw concurrency (A6-026/A6-027), add an A2 smoke test that two simultaneous local chainsaw runs DO collide (proves we know what we gave up). — _Document the assumption with a test._<br>• **A5** (A5→A6-006 · _simplify_ · phase 2+): A6-026/A6-027 concurrency-block removal aligns with A5-002/A5-014: one-agent-per-resource is the new model, multi-runner protection is moot. — _Concurrency model shifts substrate._ |
| `A6-027` | 2+ | Remove the `CHAINSAW_RUN_ID` per-run isolation prefix machinery in `chainsaw.yml`.<br><br><sub>_simplify_ · Same justification as above — multi-runner race protection is overkill for a single agent._</sub> | • **A2** (A2→A6-006 · _meta-test_ · phase 2+): When collapsing chainsaw concurrency (A6-026/A6-027), add an A2 smoke test that two simultaneous local chainsaw runs DO collide (proves we know what we gave up). — _Document the assumption with a test._<br>• **A5** (A5→A6-006 · _simplify_ · phase 2+): A6-026/A6-027 concurrency-block removal aligns with A5-002/A5-014: one-agent-per-resource is the new model, multi-runner protection is moot. — _Concurrency model shifts substrate._ |
| `A6-028` | 0+ | Delete or fold `.claude/skills/ext-github/resources/*.json` jentic recordings.<br><br><sub>_removal_ · The recordings are the persisted form of jentic catalog ops — useless without jentic._</sub> | _(no comments)_ |
| `A6-029` | 0+ | Collapse `scripts/aws-creds-check.sh` into a 3-line `aws sts get-caller-identity` snippet in `AGENTS.md` quickstart.<br><br><sub>_inline_ · The script wraps a single STS call + region echo — overkill for an interactive sandbox._</sub> | • **A2** (A2→A6-014 · _preserve-via-test_ · phase 0+): Pair A6-029 (inline aws-creds-check) with an A2 precondition assert at the top of every e2e suite that STS identity matches expected account. — _Move check from script to test framework._<br>• **A3** (A3→A6-014 · _regression-extension_ · phase 0+): A6-029 (inline aws-creds-check.sh) — keep a one-line `scripts/preflight.sh` that wraps STS + sandbox region whitelist (us-east-1, us-west-2) + EC2 instance-type whitelist check. — _Sandbox-kill prevention is too cheap to inline away entirely._ |
| `A6-030` | 1+ | Delete `scripts/k8s-logs.sh`.<br><br><sub>_removal_ · Wraps `kubectl logs -l ... --tail=N` in 4 lines; sandbox agent can just type the kubectl command._</sub> | • **A1** (A1→A6-008 · _replace-not-remove_ · phase 1+): Pair A6-030 (delete k8s-logs.sh) with adding a `tail-with-correlation-id.sh` that runs the same kubectl logs but injects + filters by correlation id — net capability up. — _Take the deletion as an opportunity to upgrade._ |
| `A6-031` | 0+ | Delete `scripts/route53-records.sh`.<br><br><sub>_removal_ · Two-line wrapper around `aws route53 list-resource-record-sets` — no value once the agent runs `aws` directly._</sub> | _(no comments)_ |
| `A6-032` | 1+ | Merge `scripts/kyverno-policies.sh` and `scripts/kyverno-violations.sh` into one `scripts/kyverno.sh` subcommand script.<br><br><sub>_consolidate-script_ · Both are thin kubectl wrappers; one entry point is easier to discover._</sub> | _(no comments)_ |
| `A6-033` | 1+ | Reduce `scripts/diag-component.sh` to only the `platform-secret` (custom-shape) handler.<br><br><sub>_refactor_ · The argocd / crossplane / eso / ingress-nginx / kyverno branches are uniform `kubectl get pods + describe + events` snippets the agent can run directly._</sub> | • **A1** (A1→A6-009 · _replace-not-remove_ · phase 1+): Extend A6-033 (reduce diag-component.sh to platform-secret only) with a generic "describe-everything-with-this-label" replacement that subsumes the deleted branches in 10 lines. — _Generic version stays small; coverage stays high._<br>• **A3** (A3→A6-009 · _regression-extension_ · phase 1+): Pair A6-033 (reduce diag-component.sh) with retention of a script-mode test fixture for `platform-secret` since it's the surviving custom path — easy to break in the slimming. — _Refactor-to-smaller often loses the one special case._ |
| `A6-034` | 1+ | Delete `tests/unit/test_diag_component.sh`.<br><br><sub>_removal_ · 15-assertion lint defending a script whose generic branches are about to be inlined._</sub> | _(no comments)_ |
| `A6-035` | 2+ | Drop the `--platform-secrets` mode from `scripts/argocd-apps.sh` and let the agent run the underlying `kubectl get platformsecret -A` directly.<br><br><sub>_simplify_ · The mode is just three pre-baked kubectl calls; not worth a script-mode flag in a sandbox with direct kubectl._</sub> | _(no comments)_ |
| `A6-036` | 0+ | Remove the embedded `MAX_LINES = 100` truncation logic from `post-comment.py` before deletion lands.<br><br><sub>_simplify_ · Truncation existed because GH comment length matters; in-PR-body summary written by the agent has no such limit._</sub> | _(no comments)_ |
| `A6-037` | 2+ | Remove the "dispatch a workflow to read the cluster" instruction from `ai/handoff.md`'s "Critical behavioral rules" and replace with "run kubectl directly".<br><br><sub>_refactor_ · Handoff still references the workflow-dispatch debug loop pattern that's now slower than the direct path._</sub> | • **A5** (A5→A6-007 · _refactor_ · phase 2+): A6-037 (handoff rule update) should also reference A5-013 (kick `argocd app sync --prune` immediately) as the replacement pattern. — _Replaces dispatch idiom with sandbox idiom._ |
| `A6-038` | 1+ | Delete the `Crossplane v2.0.1 composition-reconciler bug` workaround sentinels in `terraform_data.crossplane_aws_provider.triggers_replace` (PR #68's `"provisioner-command-v2"` marker) once the 2.2 upgrade lands.<br><br><sub>_removal_ · The string-sentinel hash trick only existed to force a replace when the manifest body wasn't part of the hash input; v2.2 + the `sha256(manifest)` pattern subsumes it._</sub> | • **A1** (A1→A6-010 · _safety-net-extension_ · phase 1+): Pair A6-038/039 (remove Crossplane v2.0 workarounds) with adding a CloudWatch alarm on `crossplane_controller_reconcile_errors_total > 0` so any regression after the cleanup is immediately visible. — _Defensive removal needs replacement alarms._<br>• **A2** (A2→A6-007 · _preserve-via-test_ · phase 1+): After A6-038/A6-039 removal (Crossplane v2.0.1 workarounds), add an A2 chaos test specifically targeting the failure modes those workarounds defended against to confirm v2.2 truly handles them. — _Workaround removal needs a regression net._<br>• **A3** (A3→A6-007 · _regression-extension_ · phase 1+): Before A6-038 (remove `provisioner-command-v2` sentinel), add a regression test that PR #68's manifest-hash pattern produces a non-empty `triggers_replace` for the affected resource. — _Don't remove the workaround until the replacement is proven equivalent._<br>• **A5** (A5→A6-013 · _parallel-apply_ · phase 1+): A6-038/A6-039 (Crossplane workaround removals) unlock A5-014 (parallel provider install + IRSA pre-create) by removing the manual deployment-delete hack from the chain. — _Workaround removal restores DAG parallelism._ |
| `A6-039` | 1+ | Remove the `kubectl delete deploy -l pkg.crossplane.io/provider=provider-family-aws` hack from `terraform/management/helm.tf` after 2.2 upgrade.<br><br><sub>_removal_ · Workaround for v2.0 not rolling Deployment on DeploymentRuntimeConfig change — 2.2's unified reconciler is expected to handle this natively._</sub> | • **A2** (A2→A6-007 · _preserve-via-test_ · phase 1+): After A6-038/A6-039 removal (Crossplane v2.0.1 workarounds), add an A2 chaos test specifically targeting the failure modes those workarounds defended against to confirm v2.2 truly handles them. — _Workaround removal needs a regression net._<br>• **A3** (A3→A6-008 · _regression-extension_ · phase 1+): A6-039 (remove the `kubectl delete deploy` hack post-v2.2) deserves an explicit chainsaw scenario that mutates DeploymentRuntimeConfig and asserts Crossplane rolls the Deployment natively — proves the removal is safe. — _Locks the v2.2 promise as a verified property, not a hope._<br>• **A5** (A5→A6-013 · _parallel-apply_ · phase 1+): A6-038/A6-039 (Crossplane workaround removals) unlock A5-014 (parallel provider install + IRSA pre-create) by removing the manual deployment-delete hack from the chain. — _Workaround removal restores DAG parallelism._ |
| `A6-040` | 0+ | Drop the `if jentic bridge` / `if direct egress` branching from `AGENTS.md` (§6.x throughput mode etc.).<br><br><sub>_simplify_ · Multi-substrate language was needed when sessions varied; now the assumption is "you have direct everything."_</sub> | _(no comments)_ |
| `A6-041` | 2+ | Delete the `Look for a green Chainsaw run on this SHA` curl+python machinery in `chainsaw-verify.yml`.<br><br><sub>_removal_ · Subset of A6-008 — the SHA-matching workaround is the only thing the verifier does._</sub> | _(no comments)_ |
| `A6-042` | 0+ | Remove the `permissions: { issues: write, pull-requests: write }` and PR-comment-fallback branches from `terraform-test.yml`.<br><br><sub>_simplify_ · They exist only to support the auto-comment that's being removed._</sub> | _(no comments)_ |
| `A6-043` | 2+ | Delete `phase-2-diagnose.yml`'s `Bug 3` / `Bug 4` named sections and consider committing the equivalent kubectl queries as a one-page `docs/phase-2-debug-cheatsheet.md` instead.<br><br><sub>_refactor_ · The diagnose dump is a frozen-in-time pre-debug-loop snapshot for two specific bugs; cheatsheet form is reusable._</sub> | • **A1** (A1→A6-012 · _dedupe-extension_ · phase 2+): Pair A6-043 (Bug 3/4 sections → cheatsheet) with an auto-test that every "bug class" in `ai/TESTING-PLAN.md` has a matching cheatsheet entry — keeps docs honest. — _Cheatsheets rot without enforcement._<br>• **A2** (A2→A6-009 · _preserve-via-test_ · phase 2+): Pair A6-043 (cheatsheet replacing diagnose dump) with an A2 e2e test that exercises every cheatsheet snippet and asserts they still produce useful output. — _Cheatsheets rot; tests don't._<br>• **A3** (A3→A6-012 · _regression-extension_ · phase 2+): A6-043 (convert diagnose dump to cheatsheet) — keep one synthetic golden-output sample of each diagnose section in `tests/fixtures/` so a future agent can recognize the pattern in the wild. — _Cheatsheets without examples lose discoverability._<br>• **A5** (A5→A6-008 · _refactor_ · phase 2+): A6-043 cheatsheet (replacing phase-2-diagnose sections) should embed the A5-027 event-bus subscriptions so devs can stream evidence not query for it. — _Cheatsheet becomes live, not static._ |
| `A6-044` | 2+ | Inline `tests/chainsaw/run.sh` invocation into a developer-facing `make chainsaw` target.<br><br><sub>_consolidate-script_ · The run.sh wrapper exists because the chainsaw workflow needed a single entry point; locally a Make target is friendlier._</sub> | • **A5** (A5→A6-010 · _dedupe_ · phase 2+): A6-044 (`make chainsaw`) is the same Makefile as A5→A6-009 and A5-003 — one Makefile, three callers. — _Single Makefile is the orchestration plane._ |
| `A6-045` | 0+ | Remove the per-PR-required `head_sha` gating language in AGENTS.md §6.7 ("manual-verify-then-PR pattern").<br><br><sub>_refactor_ · The whole pattern was a way to keep CI green by-construction because dispatched runs were the only way to test; once tests run in-sandbox, normal push-CI is fine._</sub> | • **A5** (A5→A6-011 · _refactor_ · phase 0+): A6-045 (drop `head_sha` gating) enables A5-007 watcher-driven hand-off, since the agent no longer needs to manually pin SHAs. — _Removes manual gate enabling event-driven pipeline._ |
| `A6-046` | 2+ | Delete `tests/unit/test_chainsaw_kind_config.sh` if the kind config gets exercised by every local chainsaw run anyway.<br><br><sub>_dedupe_ · Static lint of a config that is now executed live every push._</sub> | • **A2** (A2→A6-011 · _preserve-via-test_ · phase 2+): Before A6-046 (delete chainsaw kind-config lint), add an A2 smoke test that a broken kind-config produces a clear chainsaw error within 30s of `make chainsaw`. — _Replace static lint with live early-fail._<br>• **A3** (A3→A6-010 · _regression-extension_ · phase 2+): A6-046 (delete chainsaw kind config lint) should be replaced with a runtime assertion in `tests/chainsaw/run.sh` that the config file parses + has the expected node count. — _Replace static lint with runtime invariant rather than dropping entirely._<br>• **A5** (A5→A6-005 · _speculative-test_ · phase 2+): A6-046 (test_chainsaw_kind_config removal) composes with A5-047 — kind chainsaw runs at session start as a background subagent. — _Live exercise replaces static lint._ |
| `A6-047` | 0+ | Collapse the duplicated "discover Route53 zone" logic (in `terraform-test.yml`, `scripts/route53-records.sh`, `scripts/aws-creds-check.sh`, `ai/handoff.md`) into one helper.<br><br><sub>_dedupe_ · Same `aws route53 list-hosted-zones &#124; jq` snippet repeated in four places — was acceptable when each lived in its own execution substrate._</sub> | • **A1** (A1→A6-014 · _dedupe-extension_ · phase 0+): Pair A6-047 (collapse duplicate Route53 zone discovery) with a `terraform_remote_state` data source that exports it once, plus a one-line `scripts/env.sh` sourcing — single source, two consumers. — _Eliminates the four copies cleanly._<br>• **A5** (A5→A6-012 · _dedupe_ · phase 0+): A6-047 (dedupe Route53 zone discovery) writes the answer once into `/tmp/session.env` per A5→A1-001 — same cache, two callers. — _Reinforces the session-env pattern._ |
| `A6-048` | 0+ | Remove the `bug-class registry`-style additions to `ai/TESTING-PLAN.md` that are entirely about CI-substrate bug classes (e.g. "AWS account rotated; no Route53 zone" preflight row).<br><br><sub>_refactor_ · These rows guard against dispatch-time surprises that are now caught by the agent during interactive use._</sub> | • **A3** (A3→A6-011 · _regression-extension_ · phase 0+): When A6-048 prunes CI-substrate bug-class registry rows, move them to a `historical/` section instead of deleting — future agents searching for the bug string should still find context. — _Bug 3 recurred because nobody searched prior occurrences; preserve searchability._ |
| `A6-049` | 2+ | Drop the `gh workflow run chainsaw.yml --ref ... -f commit_sha=...` instructions from `chainsaw-verify.yml`'s error message.<br><br><sub>_removal_ · The whole error path is dead with A6-008/A6-009._</sub> | _(no comments)_ |
| `A6-050` | 0+ | Remove `mcp__github__*` entries from skill `allowed-tools:` frontmatter where the corresponding skill is being removed (ext-github, terraform-ci-watch capability branches).<br><br><sub>_refactor_ · Tool-allow lists pin obsolete bridges into place even if the skill body no longer uses them._</sub> | • **A1** (A1→A6-015 · _refactor-extension_ · phase 0+): Extend A6-050 (remove mcp__github__* allowed-tools from frontmatter) with a lint asserting every skill's `allowed-tools:` is referenced at least once in its body — defends against future tool-list rot. — _Same class of bug, broader defense._<br>• **A3** (A3→A6-015 · _regression-extension_ · phase 0+): A6-050 (remove obsolete mcp__github__* from allowed-tools) — add a `tests/unit/test_skill_tool_allowlist.sh` that any future skill removal also prunes its tool entries, preventing dangling allow-lists. — _Allow-list drift is silent and hard to spot in review._ |

### A6 — General comments (no source-ID anchor)

Cross-review rows targeted at this agent but without an explicit `A{N}-NNN` reference in the comment text; preserved verbatim.

| Comment ID | From | Phase | Comment | Category & justification |
|---|---|---|---|---|
| `A1→A6-011` | A1 | 0+ | Add a "removal-quality" lint that fails if a workflow/script is deleted without either (a) replacement script committed, or (b) ADR in `ai/decisions/` justifying loss-of-capability. | _meta-test_ — Prevents capability regression as a side effect of cleanup sprints. |
| `A4→A6-001` | A4 | 0+ | Before deleting `ext-github`, capture the "one useful curl recipe" each skill contains into a `curl-recipes.md` so institutional memory survives. | _documentation_ — Removing skills loses some useful patterns silently. |
| `A4→A6-007` | A4 | 2+ | Replace `phase-2-diagnose.yml` with `scripts/phase-2-diagnose.sh` that emits the SAME section headings — preserves grep-compatibility of old PR comments. | _refactor_ — Deleting outright breaks PR-search continuity. |
| `A4→A6-008` | A4 | 2+ | Before deleting `chainsaw-verify.yml`, extract its SHA-pinning concept into `runbook-verify-then-pr.md` since the pattern is still occasionally useful. | _documentation_ — Pattern outlives the workflow that implemented it. |
| `A4→A6-013` | A4 | 0+ | Replace `post-comment.py` with a `gh pr comment --body-file` one-liner pattern documented in AGENTS.md — same outcome, fewer LoC. | _refactor_ — Inline use-site documentation prevents future re-invention. |
| `A4→A6-018` | A4 | 0+ | When collapsing `terraform-test.yml`, emit the same step-name structure in the new script's output so log-grep patterns survive. | _refactor_ — Operators have muscle memory on the step names. |
| `A4→A6-029` | A4 | 0+ | Keep `aws-creds-check.sh` as a single-line wrapper that ALSO checks region+account against sandbox-allowlist before delegating to `aws sts` — guardrail value beats LoC value. | _refactor_ — Removing the guardrail risks a wrong-account apply. |
| `A4→A6-030` | A4 | 1+ | Before deleting `k8s-logs.sh`, ensure its `--previous` flag handling is moved into the diag.sh subcommand so previous-container logs remain easy. | _refactor_ — The flag is the one non-trivial bit; don't lose it. |
| `A4→A6-033` | A4 | 1+ | When trimming `diag-component.sh`, keep one generic `component=any` branch that prints the standard kubectl triad — preserves the muscle memory entry point. | _refactor_ — Removing the generic branch forces every operator to relearn. |
| `A4→A6-037` | A4 | 2+ | Replace the "dispatch a workflow to read the cluster" rule with a positive-form `prefer-direct-kubectl.md` skill — codify the new pattern, don't just delete the old. | _documentation_ — A removed rule leaves a gap; a replacement rule fills it. |
| `A4→A6-038` | A4 | 1+ | When removing the v2.0.1 sentinel, add a `migration-checklist-crossplane-2.2.md` that lists every sentinel-removal and the verification probe per. | _runbook_ — Workaround removals are a class of silent regressions. |
| `A4→A6-040` | A4 | 0+ | When dropping multi-substrate language from AGENTS.md, add a one-line "if you find yourself wanting a bridge, see runbook-direct-access.md" pointer. | _refactor_ — Future agents may resurrect bridges absent guidance. |
| `A4→A6-043` | A4 | 2+ | When converting diagnose dump to cheatsheet, also add `make diagnose-bug-N` targets per known bug class so the cheatsheet is also executable. | _consolidate-script_ — Cheatsheet that runs is better than cheatsheet that's read. |
| `A4→A6-045` | A4 | 0+ | When dropping head_sha gating, add a `runbook-when-to-pin-sha.md` covering the two cases where it's still right (rollback, hotfix). | _refactor_ — Removing the pattern entirely loses a real-but-rare use case. |
| `A4→A6-047` | A4 | 0+ | When collapsing the Route53 zone discovery duplication, emit a single-source-of-truth value into `/tmp/sandbox-env.json` for all consumers. | _dedupe_ — One file beats one function — file survives shell restarts. |
| `A4→A6-050` | A4 | 0+ | Before removing `mcp__github__*` from allowed-tools lists, audit for any skill that quietly relies on a github MCP tool for read-only inspection — keep those. | _refactor_ — Allow-list removal must be paired with use-site audit, else breakage. |
| `P→A6-001` | PRIMARY | 0+ | Mark every removal candidate with a "blast radius" tag (workflow / skill / script / doc) and require the replacement be merged first, then the removal — never the reverse. | _refactor_ — Avoids the "we deleted the workaround before the replacement was proven" failure mode. |
| `P→A6-002` | PRIMARY | 0+ | Keep an `ARCHITECTURE-CHANGES.md` ledger of every workaround-removed with a one-line "was for X, replaced by Y, removed in PR #Z"; durable institutional memory. | _dedupe_ — Future sessions don't reintroduce the workaround thinking it's new. |
| `P→A6-003` | PRIMARY | 2+ | The `phase-2-diagnose.yml` workflow can stay but be repointed: instead of running diagnose logic inline, it just invokes `scripts/diagnose/snapshot.sh` (which agents can also run locally) — single source of truth. | _consolidate-script_ — Cuts duplication; keeps the dispatch entry-point for users who don't have kubectl handy. |
| `P→A6-004` | PRIMARY | 0+ | The `terraform-ci-watch` skill's capability-profile branching (`gh` / GitHub MCP / ext-github) collapses to one path now; trim the skill to that path only and keep a `legacy/` note for archaeology. | _simplify_ — Removes the largest source of conditional complexity in the skill ecosystem. |
| `P→A6-005` | PRIMARY | 0+ | Replace bespoke `aws-creds-check.sh` / `k8s-logs.sh` / `route53-records.sh` thin wrappers with one-line snippets in `docs/runbook.md` — the wrappers added a layer without value. | _removal_ — Less to maintain, more discoverable in the runbook. |

