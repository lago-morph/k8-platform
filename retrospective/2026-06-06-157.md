# Retrospective — auto-009 long run: CI-red clear, phase 3-6 stack landing, provider-SA bootstrap fix

- **UTC date**: 2026-06-06 (verified via `date -u +%Y-%m-%d`)
- **Last PR**: #157 (highest PR number covered by this retro)
- **Branch at write time**: chore/auto-009-retro-reformat
- **Sibling artifacts**: [./2026-06-06-157/](./2026-06-06-157/)
- **Scope**: full auto-009 run (account 211125540973, us-east-1)

## Commit hashes by PR

### PR #153 — CI render fix + crossplane/yq version pins + ext-github workflow-write endpoint (merged 2026-06-06, `c3a6cb3`)

- `2fa4218` fix(ci): pin crossplane render orchestrator image to v2.3.0 (unblock unit-tests)
- `736052b` chore(tooling): pin yq version in versions.env + add sandbox-setup.sh (CI/sandbox parity)
- `803ef5f` ci(unit-tests): source yq + crossplane CLI pins from versions.env (OI-2026-06-06-1)
- `f8d6be4` skill(ext-github): add Contents-PUT endpoint for workflow-file writes

### PR #154 — record phase-4 Alloy + phase-5 DB decisions (merged 2026-06-06, `c438954`)

- `79ecd22` docs(decisions): record phase-4 Alloy (Option A: hub-addons AppProject) + phase-5 DB (general XDatabase XRD, RDS-backed)

### PR #144 — auto-007 phase 3-6 trunk / scope envelope (merged 2026-06-06, `ae807b3`)

- `b1247e8` docs(auto-007): scope envelope for phases 3-6 long run
- `f21b646` docs(auto-008): spoke GitOps delivery brief (R1 + R2 revision)
- `6f1e6d0` docs(auto-008): finalize spoke GitOps decision (R2 second wave)
- `947b7ae` docs(auto-007): run summary, open-issues blockers, CI-sync runbook
- `fafd339` Retrospective 2026-06-05-148: 1 skill, 5 agents-file rules, 1 ADR draft

### PR #145 — phase-3 spoke GitOps foundation (merged 2026-06-06, `a8616a0`)

- `b71bc54` feat(phase3-spoke): GitOps foundation for the platform spoke
- `eba9605` docs(auto-009): live phase-3 completion runbook
- `f882c41` docs(handoff): auto-007 live state — phases 0-1 verified, phase-3 pending

### PR #146 — phase-6 workload1 cluster scaffolding (merged 2026-06-06, `22f9974`)

- `21f4278` feat(phase6-workload1): GitOps scaffolding for the first workload cluster

### PR #147 — phase-4 observability stack scaffolding (merged 2026-06-06, `53493ad`)

- `5edcf08` feat(phase4-observability): GitOps scaffolding for the central obs stack

### PR #148 — phase-5 Keycloak auth scaffolding (merged 2026-06-06, `2dff004`)

- `7febe91` feat(phase5-keycloak): GitOps scaffolding for Keycloak auth

### PR #156 — provider-SA bootstrap fix (merged 2026-06-06, `11b91bb`)

- `1e4bc94` fix(spec-s9): deterministic composition render-diff + bootstrap goldens
- `008cd60` docs(auto-009): diagnose mgmt provider bootstrap failure (run 27054926075)
- `a0bf9ea` fix(mgmt): collapse duplicate family-aws Provider + order children + bootstrap diagnostics (OI-2026-06-06-2)

### PR #155 — auto-009 scope envelope + stack-landing plan (merged 2026-06-06, `bcd4008`)

- `ea1172d` auto-009 scope envelope: to phase 6 clean (live rebuild + land stack + 4/5/6)
- `42e1dc8` auto-009: stack-landing plan (no chainsaw needed; #145 helm.tf conflict to resolve)

### PR #157 — auto-009 run summary + retrospective + handoff banner (merged 2026-06-06, `fd7d142`)

- `f8f89a5` auto-009 run summary: stack landed (#144-148) + provider-SA fix in live-validation
- `c531bb3` auto-009 summary: validation-1 confirmed duplicate-Provider root cause; fix refined (orphan cleanup), destroy ask withdrawn; validation-2 running
- `6739a8f` auto-009 closeout: #156 live-green + merged (phase-1 reproducibly green); retrospective (L1-L7)
- `f335d57` docs(handoff): auto-009 banner — stack landed + provider-bootstrap deadlock fixed; next session 0-3 rebuild then 4/5/6

## Part 1 — what happened

### Phase 1 — clear the CI-red blocker and build a workflow-write path

`main` was CI-red on every push: `crossplane render` defaulted its orchestrator image to the floating `:stable` tag, which had drifted to v1.20.9 and started rejecting `internal` with `unexpected argument`. The fix pinned the orchestrator to `CROSSPLANE_CHART_VERSION` (v2.3.0) and sourced the crossplane/yq pins from `versions.env` for CI/sandbox parity (#153, `OI-2026-06-06-1` closed).

The unplanned-but-decisive sub-thread: editing `.github/workflows/*` from the web sandbox was blocked. Both the git-push OAuth token and the GitHub MCP write tools lack the `workflow` scope (anthropics/claude-code #61189), and this was confirmed to be a **regression** — this repo's own 2026-05-28 web session had pushed `unit-tests.yml` over plain git. Rather than treat it as an immutable wall, the run wired a durable path: the jentic PAT was granted Contents+Workflows write and exposed through the `ext-github` skill as a Contents-PUT endpoint (`create_or_update_file_contents`, `op_12ee1daaad73b14b`, SKILL.md §8), live-fired on `unit-tests.yml`. Lesson L1.

### Phase 2 — land the auto-007 phase 3-6 stack on main

The feared part of this phase was an expensive, multi-cycle chainsaw-gated merge train across five stale PRs. One read-only investigation subagent dissolved that fear: it established the stack touched no `crossplane/**` or `tests/chainsaw/**` paths (so no chainsaw run was needed) and that `chainsaw-verify` was non-required anyway (#144 had merged with it red). Lesson L7. The train then collapsed into five fast conflict-resolve-and-merge steps (#144→#145→#146→#147→#148, helm.tf / run.sh / platform-spoke.yaml unioned), each CI-green. The first stack-resolution subagent ran on the shared clone and collided with the lead's git state; every later resolution subagent used `isolation: worktree` and stayed clean. Lesson L2. Phase-4 Alloy (hub-addons AppProject) and phase-5 DB (general `XDatabase` XRD, RDS-backed) decisions were recorded in #154.

### Phase 3 — diagnose → review → fix → validate the provider-SA bootstrap deadlock

The live rebuild took phase-0 base green (run 27054644974, confirming the fresh GH-Actions secrets), then phase-1 management failed on a crossplane provider-bootstrap deadlock that had also blocked auto-005 and auto-007 (`OI-2026-06-06-2`). The diagnosis went through three real adversarial reviewers (L6): reviewer #1 caught that the all-six-providers-RUNTIME=False evidence was consistent with both the duplicate-Provider hypothesis *and* a package-manager-wide stall; reviewer #2 confirmed the rename was safe for the DRC→SA→IRSA chain (DRC binds by `runtimeConfigRef`, not Provider name) and flagged the chainsaw harness name-alignment; reviewer #3 added the `depends_on` race fix.

The fix carried a broadened failure-path diagnostic dump. Validation-1 (run 27055996205) failed — but the dump captured the crossplane Lock condition `cannot build DAG: node ...provider-family-aws already exists`, which **confirmed** the duplicate-Provider root cause on the very run that tested the fix (L4), answering reviewer #1's objection with evidence rather than a re-run. The failure was only because the wedged cluster's orphaned old `provider-family-aws` lingered (`kubectl apply` does not prune), so the fix was refined with an idempotent orphan-cleanup. Validation-2 (run 27056287208) passed green: EKS ACTIVE, 3 nodes Ready, all providers Healthy, the pinned `serviceaccount/upbound-provider-family-aws` present, the one-Provider assertion satisfied, ArgoCD UI HTTP 200. #156 merged (`11b91bb`); the recurring deadlock is resolved (L5, ADR-worthy). The no-destroy envelope held — the fix self-heals a wedged cluster in place rather than requiring a `terraform destroy`. Two timer-driven loops (stack train + live track) ran in parallel cleanly; `send_later` was unavailable, so single-notification `Bash run_in_background` sleep-timers backstopped the no-webhook `workflow_dispatch` runs.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | ~13 (1 stack-landing investigation, ~4 stack conflict-resolutions, 1 provider-SA diagnosis, 3 adversarial reviewers, 1 provider-SA implementation, plus diagnostic/poll helpers) |
| PRs opened / merged | 10 / 10 (#144, #145, #146, #147, #148, #153, #154, #155, #156, #157) |
| Real-world bugs discovered + fixed | 3 (CI render `:stable` drift; `workflow`-scope write block; duplicate family-Provider DAG deadlock) |
| Tests added (before / after) | n/a — scaffolding + bootstrap fix; no chainsaw runs this train |
| Spec amendments | 1 (spec-s9 deterministic composition render-diff + bootstrap goldens) |
| Scenarios driven / skipped | live phase-0 + phase-1 driven green / phase-2 chainsaw + phase-3 platform-cluster sync skipped (budget) |
| Files touched at major refactors | crossplane mgmt bootstrap (family Provider naming + depends_on + orphan-cleanup + assertion) |

## Part 2 — skills summary

No skill candidates. The run's durable lessons are environment/transport quirks (L1, L2, L3) and workflow disciplines (L4, L7) best captured as agents-file rules, plus one architectural decision (L5) captured as an ADR. The reusable orchestration patterns the run exercised — workflow-file writes via the jentic bridge, worktree-isolated git subagents, read-only investigation dispatch, adversarial review rounds — are already covered by existing skills (`ext-github`, `subagent-prompting`, `parallel-subagent-fanout`, `autonomous-run`). None of the session's material demonstrates a new, self-contained, multi-step skill that those do not already cover, so per the "no speculative skill candidates" bar this retro proposes none.

## Part 3 — agents-file suggestions

Proposed additions to the project's agents file (`AGENTS.md`). Each rule has its own per-rule file in the sibling directory at `./2026-06-06-157/AGENTS-MD-<hash>-<kebab-name>.md` — the per-rule file contains exactly two sections: `# agent instruction` (the verbatim rule text the CI/CD assembler concatenates into `AGENTS.md`) and `# justification` (the persuasion text the assembler strips out).

This Part 3 mirrors that content inline so the retrospective reviewer has every proposed rule + its justification in one place at review-decision time.

### Suggestion 1: Write `.github/workflows/*` from a web sandbox via the ext-github Contents-PUT endpoint — `AGENTS-MD-1cb9e6ae7e`

- **Per-rule file**: [./2026-06-06-157/AGENTS-MD-1cb9e6ae7e-write-github-workflows-via-ext-github-contents-put.md](./2026-06-06-157/AGENTS-MD-1cb9e6ae7e-write-github-workflows-via-ext-github-contents-put.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Write `.github/workflows/*` from a web sandbox via the ext-github Contents-PUT endpoint.** To create or edit any file under `.github/workflows/` from a Claude-Code-on-the-web sandbox, do NOT use `git push` or the GitHub MCP write tools — both lack the `workflow` OAuth scope and will 403/reject. Use the `ext-github` skill's Contents-PUT endpoint (`create_or_update_file_contents`): base64-encode the whole file, pass the current blob `sha` on an update, target the feature branch, then run `git fetch && git reset --hard origin/<branch>` to reconcile the local tree.
>
> *Grounded in: auto-009 — git push and GitHub MCP both 403 on the missing `workflow` scope; the jentic PAT (Contents+Workflows write) wrote `unit-tests.yml` instead.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The web sandbox's git-push OAuth token and the attached GitHub MCP write tools both silently lack the `workflow` scope (anthropics/claude-code #61189), so every attempt to push a `.github/workflows/*` change fails with a 403 — and this is a confirmed regression, not an immutable platform limit (this repo's own 2026-05-28 web session pushed `unit-tests.yml` over plain git). Without a written rule, the next agent rediscovers the 403 from scratch, burns a debugging cycle proving it isn't a credential typo, and may wrongly conclude workflow edits are simply impossible from the web. The durable workaround already exists and is wired in: a jentic PAT granted Contents+Workflows write, exposed through the `ext-github` skill as the Contents-PUT endpoint `op_12ee1daaad73b14b`. The marginal cost of the rule is one endpoint call plus a `git fetch && git reset --hard` to reconcile — seconds — versus a multi-minute dead-end every time an agent needs to touch CI from the web.

### Suggestion 2: Dispatch git-operating subagents with `isolation: worktree` — `AGENTS-MD-b9a2e4f87f`

- **Per-rule file**: [./2026-06-06-157/AGENTS-MD-b9a2e4f87f-git-subagents-use-worktree-isolation.md](./2026-06-06-157/AGENTS-MD-b9a2e4f87f-git-subagents-use-worktree-isolation.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Dispatch git-operating subagents with `isolation: worktree`.** When dispatching a subagent that performs any git operation (checkout, merge, commit, push, rebase), pass `isolation: worktree`. A non-isolated subagent shares the lead agent's working tree; if it `git checkout`s a different branch mid-run, the lead's stop-hook will flag the subagent's in-flight merge as "uncommitted changes" and concurrent git state collides.
>
> *Grounded in: auto-009 — the first stack-resolution subagent ran on the shared clone and collided; every later resolution subagent used worktree isolation and stayed clean.*

**Justification** (verbatim from the per-rule file's `# justification` section):

In auto-009 the first stack-resolution subagent ran on the shared clone and `git checkout`ed a different branch mid-run. The lead agent's stop-hook then flagged the resulting working-tree state as "uncommitted changes" — when it was actually the subagent's in-flight merge — producing a false alarm that had to be untangled by hand before work could continue. Every subsequent resolution subagent was dispatched with `isolation: worktree` and the collision never recurred. The failure mode is structural: any two agents sharing one working tree will fight over `HEAD`, the index, and dirty files, and the symptoms (phantom uncommitted changes, branch-switch surprises) are confusing precisely because they look like the lead agent's own mistake. The marginal cost of the rule is a single field on the dispatch call; the cost of omitting it is a corrupted-looking working tree mid-merge and the time to diagnose that the "dirty" state belongs to a different agent entirely.

### Suggestion 3: Use `actions_get` (single run), not `actions_list`, for run status — `AGENTS-MD-922039b3e7`

- **Per-rule file**: [./2026-06-06-157/AGENTS-MD-922039b3e7-actions-get-not-actions-list-for-run-status.md](./2026-06-06-157/AGENTS-MD-922039b3e7-actions-get-not-actions-list-for-run-status.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Use `actions_get` (single run), not `actions_list`, for CI run status.** To check the status or conclusion of a specific workflow run, call `mcp__github__actions_get` with method `get_workflow_run` — it returns one run compactly. Do NOT call `mcp__github__actions_list` for status: it returns 380KB+ payloads that overflow the tool-result cap and must be sliced from a saved file. Reserve `actions_list` for when the list itself is genuinely needed, and then parse it from a persisted file.
>
> *Grounded in: auto-009 — `actions_list` overflowed the tool-result cap on every call; `actions_get` returned the run status compactly.*

**Justification** (verbatim from the per-rule file's `# justification` section):

During auto-009's live-rebuild track the agent repeatedly needed one thing: the status/conclusion of a single in-flight workflow run. Reaching for `mcp__github__actions_list` returned 380KB+ payloads every time — large enough to overflow the tool-result cap, forcing the result to be persisted to a file and sliced back out before the one status field could be read. `mcp__github__actions_get` (`get_workflow_run`) returns exactly that one run compactly, so the right tool is a direct, cheap call. The asymmetry is stark: the wrong tool costs a cap overflow plus a save-and-slice round-trip on a poll loop that may run dozens of times across a live track, while the right tool costs a single small response. The rule is a one-line tool-selection reminder; without it an agent naturally lists-then-filters and pays the overflow tax on every poll.

### Suggestion 4: Bake the confirming diagnostics into a fix whose acceptance is a live run — `AGENTS-MD-0275bbd372`

- **Per-rule file**: [./2026-06-06-157/AGENTS-MD-0275bbd372-bake-confirming-diagnostics-into-live-validated-fix.md](./2026-06-06-157/AGENTS-MD-0275bbd372-bake-confirming-diagnostics-into-live-validated-fix.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Bake the confirming diagnostics into a fix whose acceptance is a live run.** When a fix can only be validated by observing a live run (e.g. a cluster apply, a CI workflow), build the diagnostics that would *confirm the root cause* into the fix itself, on the failure path. That way a single live run both tests the fix and gathers the evidence that distinguishes the real cause from competing hypotheses — instead of leaving the cause a hypothesis that needs a second run to prove.
>
> *Grounded in: auto-009 — a broadened failure-path dump baked into the provider-SA fix captured the `cannot build DAG ... already exists` Lock condition, confirming the duplicate-Provider root cause on the very run that tested the fix.*

**Justification** (verbatim from the per-rule file's `# justification` section):

Live-run validation is expensive — a cluster apply or a CI cycle can take many minutes and can only be triggered serially. In auto-009 the provider-SA fix carried a broadened failure-path dump (`describe providerrevision`, `get lock -o yaml`, crossplane controller logs). Validation-1 failed, but the dump captured the crossplane Lock condition `cannot build DAG: node ...provider-family-aws already exists` — which *confirmed* the duplicate-Provider root cause and directly answered adversarial reviewer #1's objection ("is this really the duplicate, or a package-manager-wide stall?"). Had the diagnostics not been baked in, that failed run would have produced no evidence, the root cause would have stayed a labelled hypothesis, and a whole additional live cycle would have been needed just to instrument-and-rerun. The marginal cost is a few extra describe/log commands on a code path that only executes when the fix fails anyway; the payoff is that every live run does double duty as both test and proof.

### Suggestion 5: De-risk a feared merge-train with a read-only investigation subagent first — `AGENTS-MD-c3cc38b530`

- **Per-rule file**: [./2026-06-06-157/AGENTS-MD-c3cc38b530-read-only-investigation-subagent-to-derisk-merge-train.md](./2026-06-06-157/AGENTS-MD-c3cc38b530-read-only-investigation-subagent-to-derisk-merge-train.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **De-risk a feared merge-train with a read-only investigation subagent first.** Before committing to a multi-cycle, CI-gated merge train, dispatch one read-only investigation subagent to establish which gates actually apply: which PRs touch the paths the expensive gate watches, and whether that gate is required. A train that *looks* like it needs N expensive CI cycles often collapses into a few fast conflict-resolve-and-merge steps once you know the gate does not apply.
>
> *Grounded in: auto-009 — one read-only subagent established the auto-007 stack touched no chainsaw paths and `chainsaw-verify` was non-required, turning a feared chainsaw-gated train into five fast merges.*

**Justification** (verbatim from the per-rule file's `# justification` section):

The instinct when facing a stack of stale PRs is to assume the worst about CI: that each merge will trigger the heaviest gate and the train will take many slow cycles. In auto-009 a single read-only investigation subagent dissolved that fear cheaply — it established that the auto-007 phase 3-6 stack touched no `crossplane/**` or `tests/chainsaw/**` paths (so no chainsaw run was needed) and that `chainsaw-verify` was non-required anyway (PR #144 had merged with it red). The feared multi-cycle chainsaw-gated train became five fast conflict-resolve-and-merge steps. The subagent is read-only and runs in parallel, so it costs almost nothing against the lead's budget; the alternative is either serially waiting on expensive gates that may not even apply, or merging blind and discovering the gate matters after the fact. One cheap reconnaissance dispatch reshapes the entire plan from "slow and gated" to "fast and serial".

## Part 4 — proposed ADRs

Architectural decisions made in this session that the user may want to record as ADRs. Each candidate has a **full draft** in the sibling directory, carrying a frozen `ADR-<hash>` ID. The user can adopt any draft by invoking the `adr` skill, which moves it to `docs/adr/NNNN-kebab-title.md` while preserving the ID.

| ID | Title | Draft |
|----|-------|-------|
| ADR-6be12097dc | Crossplane v2 explicit family Provider must carry the dependency-derived name | [./2026-06-06-157/ADR-6be12097dc-crossplane-single-named-family-provider.md](./2026-06-06-157/ADR-6be12097dc-crossplane-single-named-family-provider.md) |

One-line rationale per draft appears in each draft's `## Context` opening sentence; no need to duplicate it here.
