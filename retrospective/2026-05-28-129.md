# Retrospective — Audit-wiring follow-ups + iterative bug-find through a 6-PR stack

- **UTC date**: 2026-05-28 (verified via `date -u +%Y-%m-%d` and `python3 -c "import datetime; print(datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%d'))"` — both returned `2026-05-28`)
- **Last PR**: #129 (highest PR number covered by this retro)
- **Branch at write time**: `chore/retrospective-2026-05-28-129`
- **Sibling artifacts**: [./2026-05-28-129/](./2026-05-28-129/)
- **Scope**: full session (from `c89685c` — the predecessor merge point at session start)

## Commit hashes by PR

### PR #124 — `chore/audit-wiring-fixes-2026-05-01` (merged 2026-05-28)

- `536f3a7` fix(tests/unit): use label-based selectors in test_helm_render argocd block
- `06ccb60` ci(unit-tests): add run.sh catch-all step to gate every test in run.sh
- `8134690` fix(scripts): make shellcheck-clean so run.sh catch-all passes CI

### PR #125 — `chore/audit-wiring-fixes-2026-05-02` (merged 2026-05-28)

- `3d90f57` fix(integration): replace em-dash in XR description with ASCII hyphen
- `839d3e3` feat(pre-chainsaw-audit): add Check G — kubectl -n binding vs literal-namespace apply
- `b31cc87` feat(hook): hard-wire pre-chainsaw-audit before agent-initiated chainsaw dispatches

### PR #126 — `chore/audit-wiring-fixes-2026-05-04` (merged 2026-05-28)

- `01d1858` chore: delete stale handoff-shaped root files

### PR #127 — `chore/audit-wiring-fixes-2026-05-03` (merged 2026-05-28)

- `3d6e675` docs(AGENTS): backup direct-query at ETA+50% when chainsaw webhook silent
- `2e835ee` docs(AGENTS §6.10): correct mechanism — sandbox suspension, not webhook reliability

### PR #128 — `chore/audit-wiring-fixes-2026-05-05` (merged 2026-05-28)

- `392989f` docs(AGENTS): evidence-vs-hypothesis discipline + open-issues register
- `f0c27ff` fix(chainsaw/catch-block): enumerate XRs with -A so default-namespace XRs are diagnosed
- `ff206ce` fix(pre-chainsaw-audit): hook narrows to dispatch UUID; Check D slices before catch
- `8950d1a` docs(open-issues): split OI-2026-05-28-1 into Issue A + Issue B with diagnosis
- `5c4dc40` docs(open-issues): update §6.14 → §6.18 reference after rebase renumber

### PR #129 — `chore/audit-wiring-fixes-2026-05-06` (merged 2026-05-28)

- `0e31154` fix(chainsaw/composition-drift): restore Composition from /tmp snapshot, not cwd-relative on-disk path
- `e834a8a` fix(chainsaw/composition-drift): strip read-only fields from pristine snapshot

## Part 1 — what happened

### Phase 1 — handoff-task plan (Task 1 + Task 2)

Goal: work through Section 1 of `handoff-followups-2026-05-28.md`. Tasks 1 (close the unit-test CI wiring gap) and 2 (hard-wire `pre-chainsaw-audit.sh` via a PreToolUse hook) were prescriptive; Tasks 3 (probe missing chainsaw completion webhook) and 4 (delete stale handoff docs) were smaller cleanup. Branch convention per user: `chore/audit-wiring-fixes-2026-05-NN`, stacked PRs.

Planned approach: linear, Task 1 → 2 → 3 → 4. What actually happened: Task 1 surfaced a misattribution in the existing `test_helm_render.sh` "known-broken" comment ("4 argocd Ingress assertions" was off by one and the cause wasn't yq-selectors-drifted-from-chart-shape, it was wrong-name selectors against a chart that prefixes with release-name). The user paused me to investigate and explain why; the fix turned out to be label-based selectors plus a Mike Farah yq null-coalesce fix for the `command + args` concatenation. Task 1 also surfaced two real shellcheck violations in production scripts (`scripts/wait-for-claim.sh`, `scripts/crossplane-trace.sh`, `scripts/_lib/k8s-helpers.sh`) that had been silently red because the per-step `unit-tests.yml` list never invoked them — exactly the wiring gap Task 1 was about. Fixed in the same PR.

Task 2 introduced a PreToolUse hook in `.claude/settings.json` plus `scripts/pre-chainsaw-audit-hook.sh` wrapper, added Check G to the audit script, and fixed an em-dash in `tests/integration/11_platform_secret_e2e.sh` that would otherwise have made the audit go red on every dispatch. The hook's filter was `tool_input contains "chainsaw.yml"` — too broad, as we'd discover in Phase 4.

### Phase 2 — Task 3 probe (live chainsaw dispatch)

Goal: dispatch chainsaw against PR #125's HEAD and observe whether the `completed` webhook delivers (the predecessor session's report was that it didn't, for the green run of auto-003 PR #111 ef410ac).

What actually happened: the dispatch went through (audit GREEN locally). The chainsaw run failed at `composition-drift`'s "wait for XR Ready" (245s timeout, `asm-secret` MR Unready). I framed this as "AWS-provider cold-start, not a regression my work introduced" in the status reply. The user pushed back hard: "What is your evidence... That sounds like a hypothesis, not a conclusion founded on evidence." I had no positive evidence for the cold-start mechanism — only the fits-the-pattern observation that later scenarios on the same run succeeded. The user authorized fixing the test_helm_render selectors during Task 1 (option a) and dispatching chainsaw with a fresh subscription for Task 3.

The user then asked for a re-dispatch (option a) AND a catch-block diagnostic improvement (option b). I did the re-dispatch first; the result surprised me — the re-run produced a DIFFERENT failure pattern: composition-drift's XR became Ready in time, but the next three scenarios cascade-failed at ~250s each with `recoveryWindowInDays: 7` (expected 0). Reading the catch block's stderr (which was diagnostically empty in run 1 because XRs lived in `default`, not in `($namespace)`), I noticed the actual root cause: `error: the path "crossplane/compositions/platform-secret.yaml" does not exist` — the composition-drift cleanup step's relative path didn't resolve from chainsaw's CWD, and the `|| true` had been swallowing the error.

### Phase 3 — discipline rules + Issue B fix (PRs #128 / #129)

User's response to the diagnostic walkthrough was sharp: "Never try to ignore errors that are not diagnosed. This is horrible engineering discipline. At the very least add to a durable register of issues that must be followed up." And "You should never dress hypotheses as conclusions." Both became §6.17 and §6.18 in AGENTS.md, plus `docs/open-issues.md` as the canonical register format. OI-2026-05-28-1 was opened with Issue A (intermittent XR-Ready timeout, still hypothesis-level) and Issue B (deterministic cleanup-path bug, root-caused with verbatim log).

PR #128 packaged: the §6.17/§6.18 discipline rules, the register file, the catch-block fix (`-A` across namespaces), and three bug fixes in the work I'd just authored (hook filter over-broad, audit Check D's 60% heuristic, register reference numbers). PR #129 fixed Issue B itself with two iterations — the first iteration's pristine snapshot still failed on `resourceVersion` optimistic-locking; the second stripped the server-managed metadata fields.

Re-dispatch of chainsaw against PR #129 HEAD: GREEN. Run `26555037975` ran every scenario clean. Issue B confirmed fixed; Issue A didn't surface this run (1 data point of "didn't fail", insufficient to close).

### Phase 4 — merge cascade + sandbox-suspension correction

Goal: merge all 6 PRs bottom-up. The user gave explicit authorization to merge after the §6.10 correction landed.

What actually happened: the §6.10 amendment in PR #127 framed the missing-webhook problem as "webhook delivery is not 100% reliable". The user corrected: "This happens because the sandbox gets suspended, not necessarily because the webhooks are unreliable." I'd misattributed the mechanism — a sharper §6.17 violation than the cold-start one because it was about a claim I made *while authoring the §6.17 rule*. Fixed in PR #127 with `docs(AGENTS §6.10): correct mechanism — sandbox suspension, not webhook reliability`.

Merge cascade: #124 → main (clean). #125 → main (clean, GitHub auto-rebased after #124). #126 → main. #127 → main. #128 → conflict on AGENTS.md (PR #123 had landed §6.14-§6.16 mid-session, so my §6.14/§6.15 needed renumbering to §6.17/§6.18); resolved by taking HEAD on conflicts and renumbering, force-pushed. #129 → conflict on AGENTS.md and `docs/open-issues.md` carrying #128's old commits' deltas; resolved by taking HEAD, force-pushed, merged. All 6 PRs landed.

The user's "Welp" message during the merge dance was sandbox-restart-triggered (sandbox suspension exhausted my live working-tree state); the chainsaw success webhook for run `26555037975` did not surface as a `<github-webhook-activity>` envelope on resume, requiring one direct-API query when the user said "Check on run".

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 0 |
| PRs opened / merged | 6 / 6 |
| Real-world bugs discovered + fixed | 8 (helm_render selectors, shellcheck SC1091/SC2148, audit Check D heuristic, hook over-broad filter, em-dash in integration test, composition-drift cleanup path, composition-drift rv-conflict, `|| true` mask) |
| Tests added (before / after) | catch-all step gates +17 tests; no new test files |
| Spec amendments | 5 (AGENTS.md §6.10 amendment, §6.17, §6.18; PR #123 also landed §6.14-§6.16 mid-session) |
| Scenarios driven / skipped | 6 chainsaw scenarios (all 6 dispatched 3× across runs 26552671925 / 26553581065 / 26555037975) |
| Files touched at major refactors | 7 chainsaw scenarios re-synced from canonical catch block |

## Part 2 — skills summary

No skill candidates from this session. The lessons learned are agents-file rules (six proposed below) and architectural decisions (three ADR drafts in Part 4), not workflow skills. Most of what could have been a skill in this session is already covered by an existing one — `update-config` covers the PreToolUse hook authoring, `pre-dispatch-static-audit` covers running the audit, `ext-github` covers the dispatch + log fetch, `stacked-pr-on-feature-branch` covers the stacking.

## Part 3 — agents-file suggestions

Proposed additions to `AGENTS.md`. Each rule has its own per-rule file in the sibling directory at `./2026-05-28-129/AGENTS-MD-<hash>-<kebab-name>.md` carrying the `# agent instruction` body (concatenated into `AGENTS.md` by the CI/CD assembler) and the `# justification` (stripped by the assembler). This Part 3 mirrors both inline for retro-review.

### Suggestion 1: Pipe-test PreToolUse hook filters with dispatch and read shapes — `AGENTS-MD-78b8a7517e`

- **Per-rule file**: [./2026-05-28-129/AGENTS-MD-78b8a7517e-pipe-test-pretooluse-hook-filters.md](./2026-05-28-129/AGENTS-MD-78b8a7517e-pipe-test-pretooluse-hook-filters.md)

**Proposed agent instruction:**

> **Pipe-test PreToolUse hook filters with dispatch and read shapes.** When wiring a PreToolUse hook that gates an `mcp__*__execute` matcher, pipe-test the wrapper command against at least three synthetic JSON inputs before pushing: (a) a non-MCP tool call, (b) an `mcp__*__execute` *read* operation that mentions the gated resource (e.g. `list_workflow_runs` targeting `chainsaw.yml`), and (c) the actual *dispatch* operation. Confirm the wrapper only fires on (c). The filter must distinguish the dispatch UUID from any read query that happens to name the same workflow.
>
> *Grounded in: 2026-05-28 PR #125 audit hook over-filtered on `chainsaw.yml` substring and blocked `list_workflow_runs` on PR #128 review; PR #128 fix narrowed to `op_2acb005c9f3704ad`.*

**Justification:** The PR #125 hook's filter was `tool_input contains "chainsaw.yml"`. That matched the actual workflow_dispatch (correct) AND every subsequent `list_workflow_runs`, `list_jobs_for_workflow_run`, and `download_job_logs` call that named the workflow (incorrect — these are reads that don't provision anything). The first time I needed to inspect a chainsaw run's status, the hook blocked the read query and the audit ran against an unrelated PR's working tree. I spent 5 minutes diagnosing the false-positive before realizing the filter was over-broad. The marginal cost of pipe-testing three synthetic inputs is ~30 seconds; the cost of NOT pipe-testing is the same 5-15 minutes I spent unwinding the hook's misfire, multiplied by the number of agents who'd hit it in the field.

### Suggestion 2: Use label-based selectors in helm-render assertions — `AGENTS-MD-ad1be8f782`

- **Per-rule file**: [./2026-05-28-129/AGENTS-MD-ad1be8f782-helm-render-label-based-selectors.md](./2026-05-28-129/AGENTS-MD-ad1be8f782-helm-render-label-based-selectors.md)

**Proposed agent instruction:**

> **Use label-based selectors in helm-render assertions.** When asserting on rendered helm-chart resources, select by `metadata.labels["app.kubernetes.io/name"]` (or the chart's equivalent stable label), not by `metadata.name`. Chart release-name prefixing varies across charts and across resource kinds within a chart, and a name-based selector that works for `ServiceAccount` can silently return empty for `Deployment` in the same chart.
>
> *Grounded in: 2026-05-28 PR #124 fix to `test_helm_render` argocd block — 4 assertions on `metadata.name=="argocd-server"` for `Deployment`/`Ingress` returned empty because the chart prefixes those names with `argo-cd-` (release name); `ServiceAccount` happened to NOT be prefixed, so the SA assertion passed.*

**Justification:** `test_helm_render.sh` had been marked `continue-on-error: true` in CI for weeks with the comment "4 argocd Ingress assertions fail because the yq selectors don't match the rendered chart shape." The note was correct in count but wrong in attribution — the assertions hadn't drifted; they had never matched. The `argo-cd` Helm chart inconsistently prefixes resource names with the release name: ServiceAccounts skip the prefix (so the SA-IRSA assertion passed), Deployments and Ingresses keep it (so the Deployment/Ingress assertions silently returned empty against `metadata.name=="argocd-server"`). The fix was switching to `metadata.labels["app.kubernetes.io/name"]=="argocd-server"`, a label every standard Kubernetes Helm chart carries identically across all resource kinds. The marginal cost is zero — the yq expression is the same length.

### Suggestion 3: Strip server-managed metadata when snapshotting cluster state for cleanup restore — `AGENTS-MD-84bf99a020`

- **Per-rule file**: [./2026-05-28-129/AGENTS-MD-84bf99a020-strip-server-metadata-on-snapshot.md](./2026-05-28-129/AGENTS-MD-84bf99a020-strip-server-metadata-on-snapshot.md)

**Proposed agent instruction:**

> **Strip server-managed metadata when snapshotting cluster state for cleanup restore.** When a test or script needs to mutate a Kubernetes resource and restore the original via `kubectl apply`, fetch with `kubectl get -o yaml` and strip these read-only fields before saving: `metadata.resourceVersion`, `metadata.uid`, `metadata.creationTimestamp`, `metadata.generation`, `metadata.managedFields`, `metadata.annotations["kubectl.kubernetes.io/last-applied-configuration"]`, and `status`. Without the strip, the cleanup apply hits optimistic-locking ("the object has been modified; please apply your changes to the latest version and try again") and fails.
>
> *Grounded in: 2026-05-28 PR #129 composition-drift restore — first iteration failed on stale `resourceVersion` in the pristine snapshot saved by `kubectl get -o yaml`.*

**Justification:** PR #129's first attempt at fixing the composition-drift cleanup did the obvious thing: save the live Composition to `/tmp/composition-pristine.yaml` before mutating, apply the mutation, then re-apply the pristine snapshot at cleanup. The chainsaw dispatch came back red with `Error from server (Conflict): the object has been modified; please apply your changes to the latest version and try again`. The pristine snapshot included `metadata.resourceVersion: 1271`, but the mutation step's apply had bumped the cluster's rv to 1272, so the restore's apply hit Kubernetes' standard optimistic-concurrency check. The marginal cost of the strip is a single `yq 'del(.metadata.resourceVersion, ...)'` pipe inserted before the save. The cost of NOT stripping is the 8-minute chainsaw round-trip to discover the conflict.

### Suggestion 4: Never silence cleanup failures with `|| true` — `AGENTS-MD-6e0243fa6a`

- **Per-rule file**: [./2026-05-28-129/AGENTS-MD-6e0243fa6a-no-or-true-cleanup-mask.md](./2026-05-28-129/AGENTS-MD-6e0243fa6a-no-or-true-cleanup-mask.md)

**Proposed agent instruction:**

> **Never silence cleanup failures with `|| true`.** Test and CI cleanup steps that depend on succeeding (re-applying state, deleting resources, restoring config) MUST fail loudly when they fail. The `|| true` shell idiom masks the underlying error and lets contamination propagate to subsequent scenarios. If a cleanup is genuinely best-effort, guard with `[ -f X ] && cmd` or an explicit `if ! cmd; then echo WARN; fi` so the warning surfaces.
>
> *Grounded in: 2026-05-28 PR #129 composition-drift — the original cleanup ran `kubectl apply -f crossplane/compositions/platform-secret.yaml || true` with a path that didn't resolve from chainsaw's CWD; the `|| true` masked the error and cascaded into three downstream scenario failures.*

**Justification:** The composition-drift scenario's "restore the Composition (cleanup)" step had been live for weeks. The kubectl call emitted `error: the path "crossplane/compositions/platform-secret.yaml" does not exist` to stderr; the `|| true` swallowed the exit code. Every subsequent scenario in the same chainsaw run reconciled against the mutated composition and timed out their golden asserts at ~250 seconds each — 750+ seconds of CI wall-clock burned to surface a bug that would have failed the cleanup step in 2 seconds with `set -eu` and no `|| true`.

### Suggestion 5: Prefer structural slicing over percentage heuristics in static audits — `AGENTS-MD-c8a51cef82`

- **Per-rule file**: [./2026-05-28-129/AGENTS-MD-c8a51cef82-structural-slicing-not-percentage.md](./2026-05-28-129/AGENTS-MD-c8a51cef82-structural-slicing-not-percentage.md)

**Proposed agent instruction:**

> **Prefer structural slicing over percentage heuristics in static audits.** When a static-audit check scopes its grep to a portion of a file (e.g., "everything before the catch block", "the apply section"), slice by a keyword anchor (`awk '/^[[:space:]]*catch:[[:space:]]*$/ { exit } { print }'`) rather than by file-size percentage. Percentage heuristics break when comments or new content push a legitimately-allowed line past the threshold, producing false positives that are silent until they fire.
>
> *Grounded in: 2026-05-28 PR #128 `pre-chainsaw-audit.sh` Check D — "first 60% of file" heuristic broke when the catch-block change added comments to `meta-catch-fires/chainsaw-test.yaml`, pushing the legitimate `describe.namespace: ($namespace)` line just inside the threshold and triggering a false-positive that blocked subsequent dispatches.*

**Justification:** Check D was written with the heuristic "apply steps are typically before catch, so flag any `namespace: ($namespace)` in the first 60% of the file". That worked on every existing scenario at authoring time. When PR #128's catch-block change added five comment lines to `meta-catch-fires/chainsaw-test.yaml` (growing it from 65 to 73 lines), the file's `describe.namespace: ($namespace)` at line 43 — legitimate, inside the catch block — moved from 66% to 59% of the file and went RED. Check G in the same script already used slice-by-keyword and never had this problem; Check D was the outlier and it bit me.

### Suggestion 6: After session resume from suspension, verify status of in-flight dispatches before continuing — `AGENTS-MD-6163a071e9`

- **Per-rule file**: [./2026-05-28-129/AGENTS-MD-6163a071e9-verify-inflight-after-resume.md](./2026-05-28-129/AGENTS-MD-6163a071e9-verify-inflight-after-resume.md)

**Proposed agent instruction:**

> **After session resume from suspension, verify status of in-flight dispatches before continuing.** When the sandbox suspends mid-wait (typically while waiting for a long CI run), webhooks that arrive during suspension don't deliver as `<github-webhook-activity>` envelopes on resume. The first action after resume — before authoring new work, before answering a user question that depends on the run's outcome — is one direct-API query against any in-flight dispatch's run id. This complements §6.10 and §6.15: those rules cover the active-wait case; this one covers the resumed-after-suspension case.
>
> *Grounded in: 2026-05-28 PR #129 chainsaw run 26555037975 (success conclusion) — completion arrived during sandbox idle, no webhook envelope on resume; user `Check on run` was what prompted the direct query that surfaced the green status.*

**Justification:** Claude Code-on-the-web sandboxes suspend after inactivity (the user-visible "welp" / sandbox restart pattern). Webhooks queued for a suspended sandbox do not appear as `<github-webhook-activity>` envelopes when the sandbox resumes. In the PR #129 session, I dispatched chainsaw at 04:31Z, the sandbox went idle, and the run completed at 04:43Z. No envelope arrived on resume. The marginal cost of one direct-API query at session resume is one tool call; the cost of skipping it is indefinite stranding plus the agent inventing reasons why the run "must still be in progress" (an §6.17 violation if dressed as conclusion).

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-3920c7d919 | Hard-wire pre-chainsaw-audit via PreToolUse hook, not soft-wire via skill description | [./2026-05-28-129/ADR-3920c7d919-hard-wire-pre-chainsaw-audit-via-pretooluse-hook.md](./2026-05-28-129/ADR-3920c7d919-hard-wire-pre-chainsaw-audit-via-pretooluse-hook.md) |
| ADR-a52e64a8c7 | docs/open-issues.md is the durable register of undiagnosed problems | [./2026-05-28-129/ADR-a52e64a8c7-open-issues-register.md](./2026-05-28-129/ADR-a52e64a8c7-open-issues-register.md) |
| ADR-0d9a0eeb80 | Chainsaw catch block enumerates XRs across all namespaces, not the per-test scratch namespace | [./2026-05-28-129/ADR-0d9a0eeb80-catch-block-all-namespaces.md](./2026-05-28-129/ADR-0d9a0eeb80-catch-block-all-namespaces.md) |
