# Retrospective — Phase 0 spec authoring fanout, pipelined implementation plan, and gantt-fix

- **UTC date**: 2026-05-25 (verified via `date -u +%Y-%m-%d` cross-checked against `python3 datetime.now(UTC)`)
- **Last PR**: #81 (highest PR number covered by this retro)
- **Branch at write time**: claude/kind-feynman-S6hmw-fix-gantt
- **Sibling artifacts**: [./2026-05-25-81/](./2026-05-25-81/)
- **Scope**: full session (from branch divergence at 87e2e19, no prior retro in this session)

## Commit hashes by PR

### PR #80 — claude/kind-feynman-S6hmw (merged 2026-05-25)

- `8680add` Add SPEC-TEMPLATE, IMPLEMENTATION-PLAN, and 49 spec authoring outputs (in progress)
- `8ba9bd0` Phase 0 fanout: more spec authoring outputs (LA4 + LC2/LC6 final content)

### PR #81 — claude/kind-feynman-S6hmw-fix-gantt (merged 2026-05-25)

- `780bdac` Clarify the pipelined-rollout gantt diagram
- `5a3f232` Add reusable prompt for the Phase 1 + Phase 2 implementation session

## Part 1 — what happened

### Phase 1 — Clustering analysis and first-pass planning

Goal: help the user decide how to cluster the 15 existing specs (already covered by `CLUSTERING-REVIEW.md`) together with the items in `larger-list-preferences.md` (Tiers S/D/A/B/C, ordered S → D → B1+B2+B6 → A → rest of B → C). The user explicitly noted that the first phase would be writing specs for items lacking them.

The agent read the three input documents, inventoried which items had specs (15) vs which needed authoring (34 new), and produced a first-pass plan as a chat response. The user then expanded the requirements: implement a lot of these by phase, with each phase implemented in parallel and pipelined against soak of the prior phase; mermaid diagrams per phase; every spec must gain a "Testing suggestions (unit/integration/e2e)" section, and the 15 existing specs need the same augmentation; produce a template for the spec-authoring subagents because the existing specs are closer to implementation plans than to specs.

### Phase 2 — Template authoring + 49-subagent Phase 0 fanout

Goal: dispatch all 49 spec-authoring subagents (34 new + 15 augmentations) in maximum parallelism. The agent read three exemplar SPEC-*.md files, distilled the canonical 13-section template into `SPEC-TEMPLATE.md`, and dispatched all 49 subagents in a single mega-message with `run_in_background: true`, each on sonnet. Briefs followed strict discipline: absolute output paths, prohibition on committing or editing other files, and a tight word cap on final reports. Subagent completion notifications streamed in over ~10 minutes via the harness's webhook system; the agent committed in two batches as files landed.

A notable failure mode appeared mid-fanout: the stop-hook fired because 28 of the 49 specs were uncommitted when the agent first yielded. Resolved by adopting a batch-commit cadence — one commit at ~25 completions, a second when late-arriving content landed.

### Phase 3 — Rethinking the implementation plan during fanout

Goal (the user's directive while subagents were running in background): produce a phase-based implementation plan with per-phase mermaid diagrams that explicitly accounts for Phase N+1 implementation happening while Phase N is being debugged in the live architecture. The agent wrote `IMPLEMENTATION-PLAN.md` with an 8-phase pipelined model. The phase boundaries were drawn primarily by **file-locality** (not ROI tier) so that the cross-phase conflict zones — `.claude/skills/*/SKILL.md`, `tests/unit/run.sh`, `tests/chainsaw/**`, `terraform/**/providers.tf`, and ~10 other hot files — would be touched by as few phases as possible. The plan ships with a top-level Gantt, a 14-row hot-files table, a per-phase mermaid, a pipelined-timing sequence diagram, and a summary table.

### Phase 4 — Phase 0 PR (#80) lifecycle

Goal: review the 49 subagent outputs, refresh the PR description from "subagents still running" to "fanout complete", spot-check spec quality. Spot-checked 5 specs (SPEC-S2, SPEC-LB6, SPEC-LC4, SPEC-A1, SPEC-C2) via a single dispatched subagent that reported pass/fail per criterion. Result: 4 PASS, 1 minor length overrun on SPEC-C2 (475 lines vs 400 target, accepted as non-blocking). PR description was rewritten; PR merged.

### Phase 5 — Gantt-fix and Phase 1+2 prompt PR (#81)

Goal: the user shared a screenshot of the original Gantt diagram and said "I don't understand what's going on." The agent diagnosed three concrete issues: (a) implement bars were 2 cycles, soak bars only 1, so soak chunks looked like text labels rather than visible bars; (b) `after`-chain syntax obscured when phases actually overlap; (c) `axisFormat %s` collapsed adjacent integer labels into "0 0 0 1 1 1" repetition. Rewrote the Gantt with explicit start times (each cycle = 1 unit, implement and soak both 1 cycle wide), added a "how to read" preface and a worked example. Validated via the `validate_and_render_mermaid_diagram` tool, extracted the rendered PNG, sent it to the user.

The user then asked for a copy-pastable prompt to use in the next session to implement Phase 1 + Phase 2 together with maximum parallelism, with per-task model selection (sonnet vs opus). The agent wrote `PHASE-1-2-AGENT-PROMPT.md` — a self-contained brief with a two-wave dispatch design (7 parallel subagents in Wave 1, 3 in Wave 2 each consuming a Wave 1 output), per-task model assignments, hot-files coordination notes, and a merge-order table.

The agent committed the prompt file but failed to open the PR — reported "branch is pushed" and ended the turn. The user came back with "Or, uh, you didn't create a PR. Do that." PR #81 was opened, CI was pending, no review comments arrived, the user merged it.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 49 (34 new specs + 15 augmentations) + 1 spot-check subagent |
| PRs opened / merged | 2 / 2 (#80, #81) |
| Real-world bugs discovered + fixed | 2 (broken Gantt diagram, missing PR creation) |
| Tests added (before / after) | 0 / 0 (planning + spec session, no code) |
| Spec amendments | 49 (all 49 specs authored or augmented) |
| Files touched at major refactors | ~52 (49 SPECs + SPEC-TEMPLATE + IMPLEMENTATION-PLAN + PHASE-1-2-AGENT-PROMPT) |
| Documents committed | 52 |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `pipelined-multiphase-rollout-planner` | SKILL-SPEC-61948bb3d1 | high | multi-phase planning with file-locality boundaries | [./2026-05-25-81/SKILL-SPEC-61948bb3d1-pipelined-multiphase-rollout-planner.md](./2026-05-25-81/SKILL-SPEC-61948bb3d1-pipelined-multiphase-rollout-planner.md) |

Only one skill candidate this session. The 49-subagent fanout pattern is already covered by the user's existing `parallel-subagent-fanout` skill; this session exercised that skill rather than producing a new one. The reusable contribution of this session is the planning shape: how to draw phase boundaries that support pipelined execution.

## Part 3 — agents-file suggestions

Proposed additions to `AGENTS.md`. Each rule has its own per-rule file in the sibling directory carrying `# agent instruction` (concatenated into `AGENTS.md` by the CI/CD assembler) and `# justification` (stripped by the assembler). Mirrored inline here for review.

### Suggestion 1: Validate Mermaid syntax before committing — `AGENTS-MD-b5748bd3fa`

- **Per-rule file**: [./2026-05-25-81/AGENTS-MD-b5748bd3fa-validate-mermaid-before-committing.md](./2026-05-25-81/AGENTS-MD-b5748bd3fa-validate-mermaid-before-committing.md)

**Proposed agent instruction:**

> **Validate Mermaid syntax before committing.** Before committing a Mermaid diagram to the repository, validate it via the `mcp__*__validate_and_render_mermaid_diagram` tool (or the equivalent local CLI). A diagram that fails to render is a documentation bug the reader will never see it work. Save the rendered PNG if the diagram is non-trivial; attach it to the PR for review-time clarity.
>
> *Grounded in: 2026-05-25 session where a broken Gantt mermaid in IMPLEMENTATION-PLAN.md shipped via PR #80 and was only caught when the user shared a screenshot.*

**Justification:**

PR #80 shipped a Gantt diagram in `IMPLEMENTATION-PLAN.md` that rendered as a confusing mess: implement bars were 2 cycles, soak bars only 1, the `after-p1` chain syntax obscured the actual overlap, and `axisFormat %s` produced "0 0 0 1 1 1" axis labels. The agent never validated the diagram before committing — and the bug wasn't caught until the user screenshotted the broken render and asked "can you fix the mermaid diagram? I don't understand what's going on." A second PR (#81) was needed to fix it.

The cost of validating: one tool call (~3 seconds) plus an optional PNG attached to the PR. The cost of NOT validating: a full PR round-trip to fix a documentation bug, plus the cognitive overhead of the user trying to parse a broken diagram. The validator tool exists, is cheap, and returns a usable PNG you can hand to the user as proof the diagram reads correctly. Validate every mermaid before commit.

### Suggestion 2: Push-with-merge-intent must end in an open PR — `AGENTS-MD-bd538e1bbb`

- **Per-rule file**: [./2026-05-25-81/AGENTS-MD-bd538e1bbb-push-with-merge-intent-must-end-in-pr.md](./2026-05-25-81/AGENTS-MD-bd538e1bbb-push-with-merge-intent-must-end-in-pr.md)

**Proposed agent instruction:**

> **Push-with-merge-intent must end in an open PR.** If the goal of pushing a branch is to merge it (i.e., this is not a draft / WIP push), the push step is followed in the same turn by `create_pull_request` (or `gh pr create`). Do not report "branch is pushed" as the end state unless "pushed without PR" was the explicit intent. The harness's git-status check covers uncommitted-tree drift but does not cover unopened PRs.
>
> *Grounded in: 2026-05-25 session where the gantt-fix branch was pushed but no PR was opened; the user had to prompt "Or, uh, you didn't create a PR. Do that."*

**Justification:**

The 2026-05-25 session pushed the `claude/kind-feynman-S6hmw-fix-gantt` branch with two commits (gantt fix + Phase 1+2 prompt), reported "branch is pushed" to the user, and ended the turn. No PR was opened. The user had to come back with "Or, uh, you didn't create a PR. Do that."

This is a quiet but reliable failure mode: the agent thinks "the work is on the remote, my job is done" because the local git operations succeeded. But on a fork-and-PR workflow, the remote branch alone is not a deliverable — the user can't review or merge it without a PR. The agent's session-ending status was indistinguishable from "everything succeeded" when in fact a required step was missing.

Cost of the rule: one extra tool call (`create_pull_request`) per push-with-merge-intent — usually under 5 seconds. Cost of not having the rule: a missed PR is missed wall-clock; the user notices when they next check GitHub, then has to interrupt their flow to prompt the agent to finish. The harness's stop-hook git-status check catches uncommitted files but does NOT detect "pushed branch without PR" — this is a known gap.

### Suggestion 3: Subagent briefs use absolute paths and prohibit cross-task edits — `AGENTS-MD-6065d877c7`

- **Per-rule file**: [./2026-05-25-81/AGENTS-MD-6065d877c7-subagent-briefs-absolute-paths-and-no-cross-edits.md](./2026-05-25-81/AGENTS-MD-6065d877c7-subagent-briefs-absolute-paths-and-no-cross-edits.md)

**Proposed agent instruction:**

> **Subagent briefs use absolute paths and prohibit cross-task edits.** Every subagent brief that creates or edits files MUST: (a) name the output file as an absolute path starting with `/home/user/`; (b) explicitly state "Do NOT commit. Do NOT modify any other file"; (c) include a "Report under N words" upper bound on the subagent's final message. Briefs lacking (a) cause directory drift when the subagent's working directory differs from the parent's; briefs lacking (b) cause cross-spec edits when subagents share a worktree; briefs lacking (c) inflate the parent context with verbose intermediate reports.
>
> *Grounded in: 2026-05-25 session 49-subagent Phase 0 fanout where this discipline kept all 49 outputs in clean, non-overlapping files.*

**Justification:**

The 2026-05-25 session dispatched 49 background subagents in parallel — 34 authoring new specs and 15 augmenting existing specs. Every brief followed the discipline: an absolute output path (`/home/user/k8-platform/ai/brainstorming/specs/SPEC-<id>-<slug>.md`), a hard prohibition on committing or modifying any other file, and a tight word cap on the final report. Result: all 49 subagents wrote exactly the files they were assigned to; no spurious commits; no cross-file conflicts; the parent context was protected from 49 long-form summaries.

The cost of the discipline is one extra paragraph in each brief — under a minute of brief-authoring time. The cost of skipping it: the agent has seen prior sessions where a subagent without (a) wrote to a relative path that resolved against a different CWD; where a subagent without (b) "helpfully" modified an adjacent file and clobbered another subagent's work; where a subagent without (c) returned a 2000-word essay that flooded the parent's context. The asymmetry is decisive: one paragraph upfront vs hours of cleanup downstream.

This rule pairs with the "Batch-commit during high-fanout subagent waves" rule (`AGENTS-MD-97813da854`): the no-commit prohibition is what makes batch-commit work — only the parent commits, in batches, with coherent commit messages.

### Suggestion 4: Batch-commit during high-fanout subagent waves — `AGENTS-MD-97813da854`

- **Per-rule file**: [./2026-05-25-81/AGENTS-MD-97813da854-batch-commit-during-fanout.md](./2026-05-25-81/AGENTS-MD-97813da854-batch-commit-during-fanout.md)

**Proposed agent instruction:**

> **Batch-commit during high-fanout subagent waves.** When ten or more background subagents are concurrently writing to disk, commit their outputs in batches rather than per-completion. Sensible cadences: every ~10 completions, every stop-hook trigger, every user prompt that yields the turn. Per-completion commits create a noisy commit stream that obscures the per-batch narrative; not committing at all causes the harness stop hook to block when the parent agent tries to yield. Use a commit message like "Phase N fanout: batch K outputs" so the commit log groups by batch, not by individual file.
>
> *Grounded in: 2026-05-25 session where the stop hook blocked mid-fanout because 28 of 49 spec files were uncommitted; resolved by committing in two batches.*

**Justification:**

The 2026-05-25 session dispatched 49 background subagents in parallel. Notifications streamed in over ~10 minutes as each subagent completed and wrote its spec file. Two failure modes are present:

1. **Per-completion commit:** 49 commits, each one file, each with a one-line "SPEC-Sx authored" message. The commit log becomes useless — readers have to fold the 49 commits back into one logical batch to understand what happened.
2. **No commits until all 49 done:** the harness's `~/.claude/stop-hook-git-check.sh` blocks when the parent tries to yield the turn back to the user (because there are uncommitted changes). The user gets an awkward "stop hook feedback" message instead of a clean turn boundary.

The session hit failure mode 2: 28 of the 49 spec files were uncommitted when the parent agent first yielded; the stop hook blocked. Resolved by adopting batch-commit cadence — one commit at ~25 completions, a second when the remaining late-arrivers landed.

Cost of the rule: a few extra commit messages, one per batch — under a minute total. Cost of not having it: stop-hook noise and an unreadable per-file commit log. This rule pairs with "Subagent briefs use absolute paths and prohibit cross-task edits" (`AGENTS-MD-6065d877c7`): the per-task no-commit rule is what makes the parent's batch-commit coherent.

### Suggestion 5: Persist user-facing planning artifacts as files — `AGENTS-MD-e8173acf64`

- **Per-rule file**: [./2026-05-25-81/AGENTS-MD-e8173acf64-persist-planning-artifacts-as-files.md](./2026-05-25-81/AGENTS-MD-e8173acf64-persist-planning-artifacts-as-files.md)

**Proposed agent instruction:**

> **Persist user-facing planning artifacts as files.** When the user requests a plan, design document, prompt template, or other reviewable artifact, write it to a file in the repo BEFORE summarizing in chat. Chat is ephemeral; the file is the durable artifact that survives context truncation and re-grounds the next session. This applies even when the user has not explicitly said "write a file". Exception: trivial responses (one paragraph, no structure) stay in chat.
>
> *Grounded in: 2026-05-25 session where IMPLEMENTATION-PLAN.md and PHASE-1-2-AGENT-PROMPT.md were created as files; the prompt file is now what the user will copy-paste into the next session.*

**Justification:**

The 2026-05-25 session produced two major planning artifacts: `IMPLEMENTATION-PLAN.md` (8-phase pipelined rollout for 49 specs) and `PHASE-1-2-AGENT-PROMPT.md` (copy-pastable kickoff for the next session). Both were committed to the repo as durable files. The chat-summary versions were short pointers to those files, not duplicates.

This pattern is what makes the next session work. When the user starts the Phase 1+2 implementation session, they will copy-paste the inner block of `PHASE-1-2-AGENT-PROMPT.md` — a file that survived this session's context window because it lives in git. If the prompt had been only in chat, the user would have had to dig through this session's transcript to find it, or worse, ask the next agent to re-derive it.

Cost of the rule: file write + commit instead of chat output — under 30 seconds. Cost of not having it: context-truncated sessions cannot recover the artifact; the user has to re-explain or the agent has to re-derive. The asymmetry is brutal: the agent's chat output costs are charged once per session and burn context for everyone else in the session; a committed file is read on demand and free thereafter.

The rule is broad: it applies to plans, prompts, runbooks, design notes, ADR drafts — anything the user might want to reference in a future session. The narrow exception (trivial responses stay in chat) is to avoid bloating the repo with one-paragraph utterances that genuinely belong in conversation.

## Part 4 — proposed ADRs

Two architectural decisions made in this session. Both have full drafts in the sibling directory; rationale is in each draft's `## Context` opening.

| ID | Title | Draft |
|----|-------|-------|
| ADR-7c127807eb | Pipelined multi-phase rollout model with file-locality phase boundaries | [./2026-05-25-81/ADR-7c127807eb-pipelined-multi-phase-rollout-model.md](./2026-05-25-81/ADR-7c127807eb-pipelined-multi-phase-rollout-model.md) |
| ADR-7c98f2751b | Mandatory Testing-suggestions section in SPEC template | [./2026-05-25-81/ADR-7c98f2751b-mandatory-testing-suggestions-section.md](./2026-05-25-81/ADR-7c98f2751b-mandatory-testing-suggestions-section.md) |
