# Retrospective — auto-016: fresh-account bring-up, EKS SLR regression fix, RDS/EC2 IAM narrowing drafts

- **UTC date**: 2026-06-09 (verified via system-reminder `currentDate` context, consistent with existing session context)
- **Last PR**: #214 (highest PR number covered by this retro)
- **Branch at write time**: claude/auto-016-run-summary
- **Sibling artifacts**: [./2026-06-09-214/](./2026-06-09-214/)
- **Scope**: full session (from branch divergence — unattended run auto-016)

## Commit hashes by PR

### PR #209 — claude/k8s-platform-auto-016-35v9sl (open) — trunk + scope envelope

- *(scope envelope commit — trunk PR for the run)*

### PR #210 — claude/auto-016-oi2-hello-e2e (open) — hub→spoke hello e2e check

- *(hard bounded-poll hub→spoke hello e2e check; no self-gating skip stub)*

### PR #211 — claude/auto-016-rds-iam-narrowing (open, draft) — RDS IAM narrowing

- *(narrow database/RDS permissions; sentinel-gated draft; lint 18/0)*

### PR #212 — claude/auto-016-ec2-iam-narrowing (open, draft) — EC2 IAM narrowing

- *(narrow networking/EC2 permissions; stacked on #211; sentinel-gated draft; lint 23/0)*

### PR #213 — claude/auto-016-eks-slr-getrole-fix (open) — EKS SLR GetRole regression fix

- *(fix zero-nodes regression: add EKSServiceLinkedRoles Sid; proven live; lint 13/0)*

### PR #214 — claude/auto-016-run-summary (open) — run summary + handoff + open-issues audit

- *(run-summary-auto-016.md; docs/open-issues.md auto-016 update; ai/handoff.md update)*

## Part 1 — what happened

### Phase 1 — orientation and scope envelope

Read the handoff and open-issues register; confirmed a fresh empty account
(`471112679140`, us-east-1). Posted the scope envelope (PR #209). The run goal
was: re-validate the auto-015 IAM narrowing on a fresh-account CREATE path, drive
the hub→spoke hello e2e, ship RDS and EC2 permission narrowings, and flip the
remaining skipped checks.

### Phase 2 — substrate bring-up and the headline regression

Dispatched `terraform-test.yml apply-and-verify` for base then management; both
green (CI runs `27174339355`, `27174531578`). On the management apply the auto-015
narrowed Crossplane IRSA policy applied from the start. The spoke `XPlatformCluster`
XR was synced via GitOps. Spoke cluster came up ACTIVE — but **with zero nodes**.
Diagnosis: `iam:GetRole` was scoped to `role/k8-platform-*`, but EKS
`CreateNodegroup` validates the AWS service-linked role
`AWSServiceRoleForAmazonEKSNodegroup` via `iam:GetRole` on
`role/aws-service-role/eks-nodegroup.amazonaws.com/*` — a path the narrowing
excluded. The fix (PR #213) adds an `EKSServiceLinkedRoles` Sid scoped to
`arn:aws:iam::*:role/aws-service-role/eks-nodegroup.amazonaws.com/*` and a
matching `iam:CreateServiceLinkedRole`. Applied live; nodegroup went ACTIVE
immediately. This was the run's most important finding: a fail-closed regression
in auto-015's narrowing that was invisible to auto-015 because its nodegroup
pre-existed the narrowing.

### Phase 3 — re-validation of the identity narrowing on the CREATE path

With the SLR fix applied, the spoke ACCESS create path was re-driven: OIDC
provider, 3 Roles, RolePolicy, 2 AccessEntries, and 2 AccessPolicyAssociations
all reconciled `Ready=True` under the narrowed policy on a fresh account. This is
the primary success criterion for the run — OI-2026-06-08-1's identity narrowing
re-validated on the CREATE path.

### Phase 4 — three recurring bring-up gaps, each hand-fixed again

Three open issues that auto-012 documented, decided, and deferred without durable
code fixes each bit again in identical form:

- **OI-2026-06-07-4** (hub→spoke SG rule): ArgoCD could not reach the spoke API
  on 443; the spoke cluster SG had no inbound rule from the hub nodes. Live-fixed
  (authorized hub-SG + VPC-CIDR → spoke cluster SG on 443). Durable Composition
  fix still owed.
- **OI-2026-06-07-3** (ELB subnet tags): the NLB couldn't place; 6 shared subnets
  not tagged `kubernetes.io/cluster/k8-platform-services=shared`. Live-fixed.
  Durable base-terraform fix still owed.
- **OI-2026-06-07-2** (placeholder overlays vs. bootstrap selfHeal): hand-overlaid
  domain + cert did NOT stick — bootstrap reverts them — so the NLB never gets a
  valid cert. This escalated from "workaround available" to **now BLOCKING** the
  hello e2e: `hello.platform.<domain>` unreachable. Needs ADR-0005 cluster-facts
  ConfigMap, not another hand-overlay.

### Phase 5 — RDS/EC2 narrowing decision brief and sentinel-gated drafts

Authored the RDS and EC2 IAM narrowing decision brief
(`planning/test-overhaul/decisions/auto-016-001-rds-ec2-iam-narrowing.md`), ran
two adversarial rounds with 6 real reviewers, and shipped both as sentinel-gated
draft PRs (#211, #212). Applied the RDS narrowing live against the management CI
run (run `27176069142`, green clean plan diff, ongoing-reconcile green). EC2
narrowing is a draft; one reviewer recommended deferral.

### Phase 6 — wrong-cluster kubectl mishap

During spoke namespace setup the agent issued `kubectl create namespace` through
the SSM relay with `-c k8-platform-mgmt` (the hub) while intending to operate on
the spoke. The command succeeded silently; the namespace landed on the hub. The
discrepancy surfaced only on a subsequent spoke-side operation that didn't find
the expected namespace.

### Phase 7 — run summary and open-issues audit

Authored the run summary (PR #214), updated `ai/handoff.md`, and performed a
full status audit of `docs/open-issues.md` — resolving two entries
(OI-2026-06-04-6-1 Issue A, OI-2026-06-06-4) and recording the new SLR
regression and the three recurrences.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | ~8 (6 adversarial reviewers × 2 rounds, status/summary subagents) |
| PRs opened / merged | 6 / 0 |
| Real-world bugs discovered + fixed | 1 (EKS SLR GetRole regression, PR #213, proven live) |
| Tests added (before / after) | lint gate additions in #211, #212, #213 (13/18/23 checks) |
| Decision brief rounds × reviewers | 1 brief × 2 rounds × 3 reviewers = 6 |
| Open issues recurred | 3 (OI-2026-06-07-2, -3, -4) |
| Fresh-account create paths validated | 1 (identity narrowing: OIDC+Roles+RolePolicy+AccessEntries+AccessPolicyAssociations) |

## Part 2 — skills summary

No new skill specs proposed for this run. The run's findings feed directly into
AGENTS.md rules (Part 3) and an ADR draft (Part 4). The `live-policy-create-path-validation`
skill (SKILL-SPEC-c171288fe9) from retro 2026-06-08-207 remains the closest
related skill — this run adds the "SLR paths must be covered" dimension to its
checklist but does not warrant a new spec.

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| *(none new)* | — | — | — | — |

## Part 3 — agents-file suggestions

Proposed additions to the project's agents file (`AGENTS.md`). Each rule has its
own per-rule file in the sibling directory. This Part 3 mirrors both the
`# agent instruction` and `# justification` sections inline.

### Suggestion 1: Enumerate and exercise all create paths for resource-scoped IAM narrowings — `AGENTS-MD-7286e40df4`

- **Per-rule file**: [./2026-06-09-214/AGENTS-MD-7286e40df4-enumerate-all-create-paths-iam-narrowing.md](./2026-06-09-214/AGENTS-MD-7286e40df4-enumerate-all-create-paths-iam-narrowing.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Enumerate and exercise all create paths for resource-scoped IAM narrowings.** When narrowing a resource-scoped IAM action (such as `iam:GetRole`) for a Crossplane provider role, enumerate every create path that the target service invokes and exercise each one on a fresh-account bring-up before declaring the narrowing validated. Explicitly allow AWS service-linked-role paths (`role/aws-service-role/*`) for any service (EKS, RDS, etc.) that validates service-linked roles at create time. A narrowing validated on one create path (e.g. spoke-access entry creation) is NOT validated for other paths (e.g. nodegroup creation) — each path must be independently proven. If any create path cannot be exercised in the current session, document it in `docs/open-issues.md` and do not mark the narrowing complete.
>
> *Grounded in: auto-016 — auto-015 narrowed `iam:GetRole` to `role/k8-platform-*` and validated only the spoke-ACCESS path; the narrowing silently broke EKS `CreateNodegroup` (which validates `AWSServiceRoleForAmazonEKSNodegroup` via `iam:GetRole` on `role/aws-service-role/eks-nodegroup*`), causing the spoke to come up with 0 nodes on a fresh-account bring-up.*

**Justification** (verbatim from the per-rule file's `# justification` section):

auto-015 narrowed `iam:GetRole` to `Resource: arn:aws:iam::*:role/k8-platform-*` and validated it by proving the spoke-ACCESS create path (OIDC provider, Roles, RolePolicy, AccessEntries, AccessPolicyAssociations all `Ready=True`). That was one create path out of at least two: the other is EKS nodegroup creation, where AWS internally calls `iam:GetRole` on the AWS-owned service-linked role `AWSServiceRoleForAmazonEKSNodegroup` (path `role/aws-service-role/eks-nodegroup.amazonaws.com/*`). The narrowing excluded that path. On a fresh-account bring-up in auto-016, where the narrowed policy applied from the start, nodegroup creation failed closed — the spoke came up with **zero nodes**, and the entire app stack stalled. auto-015 never caught this because its nodegroup was created *before* the narrowing was applied.

The cost of not having this rule: a regression that is invisible on the session that ships the narrowing (because that session's nodegroup already exists), invisible in CI static lints (they check actions, not runtime service-linked-role paths), and only surfaces on the *next* fresh-account bring-up — potentially a full unattended run later. In auto-016, diagnosing and fixing it burned roughly an hour of unattended run time and a live patch. The fix (PR #213) was a one-line Sid addition to `irsa.tf`; the detection cost was a zero-node spoke and a full diagnostic sequence.

The marginal cost of adopting this rule: before declaring any resource-scoped IAM narrowing complete, spend 5–10 minutes listing the AWS API calls the controlled service makes at create time (EKS nodegroup creation → validates SLR; RDS instance creation → validates SLR; ACM cert provisioning → different path). One extra `aws iam simulate-principal-policy` per path (noting §6.39's limitation — use as a pre-flight only, not a substitute for a live create-path proof). The asymmetry is severe: the rule adds minutes of due diligence; the absence of it adds hours of fresh-account debugging.

---

### Suggestion 2: Ship the durable fix for a recurring bring-up gap in the same run — `AGENTS-MD-35f13e26b1`

- **Per-rule file**: [./2026-06-09-214/AGENTS-MD-35f13e26b1-ship-durable-fix-recurring-gap-same-run.md](./2026-06-09-214/AGENTS-MD-35f13e26b1-ship-durable-fix-recurring-gap-same-run.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Ship the durable fix for a recurring bring-up gap in the same run.** When a fresh-account bring-up requires a manual unblock for a problem that is already documented in `docs/open-issues.md` as a known gap with a decided fix, ship the durable code fix as a PR in the SAME run. Do not re-apply the live workaround and move on. If the durable fix is genuinely too large for the current run, explicitly re-escalate the open issue as blocking (updating its status and rationale in `docs/open-issues.md`) rather than silently re-paying the cost. A gap that has been live-fixed and deferred without a code PR will recur on every subsequent fresh account.
>
> *Grounded in: auto-016 — the hub→spoke SG rule (OI-2026-06-07-4), ELB subnet tags (OI-2026-06-07-3), and placeholder-overlay-vs-bootstrap (OI-2026-06-07-2) were each live-fixed in auto-012 but not given durable code fixes; all three recurred in auto-016 and required another round of manual unblocking.*

**Justification** (verbatim from the per-rule file's `# justification` section):

auto-012 found and live-fixed three bring-up gaps: the hub→spoke SG rule (ArgoCD couldn't reach the spoke API on 443), the ELB subnet tags (spoke NLB couldn't be placed), and the placeholder overlay vs. bootstrap selfHeal conflict (spoke apps would revert to placeholders). All three were documented in `docs/open-issues.md` with decisions and next steps. None received a durable code fix. In auto-016, a fresh-account bring-up hit all three again in exactly the same form, requiring the same live interventions — adding a SG rule, tagging 6 subnets, attempting hand-overlays — plus the additional diagnosis time to re-confirm that these were the same known gaps.

The pattern is: "decide + document → live fix → move on → next account pays the cost again." After two accounts pay the same cost, the pattern is established. The rule breaks the cycle by requiring the code fix to land in the same run that applies the live fix, or by explicitly re-escalating the issue as blocking (which triggers a different response than "open issue, deferred").

The marginal cost of adopting this rule: at unblock time, spend the 30–90 minutes to author and open the code PR (the Composition edit for the SG rule, the Terraform variable for the subnet tags, the cluster-facts ConfigMap for the overlay gap). This is more work than a live fix alone. The cost of not adopting it: every subsequent fresh-account run re-pays the full investigation + live-fix cost, and the gap grows in severity over time (in auto-016, OI-2026-06-07-2 escalated from "workaround available" to "now blocking the hello e2e"). Asymmetry: one extra PR per gap vs. unbounded recurrence cost per fresh account.

---

### Suggestion 3: Confirm the relay target cluster before any mutating kubectl command — `AGENTS-MD-3f064ff017`

- **Per-rule file**: [./2026-06-09-214/AGENTS-MD-3f064ff017-confirm-relay-target-cluster-before-mutating.md](./2026-06-09-214/AGENTS-MD-3f064ff017-confirm-relay-target-cluster-before-mutating.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Confirm the relay target cluster before any mutating kubectl command.** Before issuing a mutating `kubectl` command through the SSM relay (`kubectl create`, `apply`, `delete`, `patch`, `label`, `annotate`), confirm that the `-c <cluster>` flag matches the intended cluster by running a read-only probe first — for example, `kubectl get nodes -c <cluster>` or echoing the active context. The hub (`k8-platform-mgmt`) and the spoke (`k8-platform-services`) are both reachable through the same relay helper, and the `-c` flag is the only differentiator. A mutating command issued with the wrong `-c` flag silently operates on the wrong cluster; there is no rejection or warning.
>
> *Grounded in: auto-016 — the agent ran `kubectl create namespace` through the relay with `-c k8-platform-mgmt` (hub) while intending to operate on the spoke, creating namespaces on the wrong cluster.*

**Justification** (verbatim from the per-rule file's `# justification` section):

In auto-016 the agent issued a `kubectl create namespace` command through the SSM relay intending to act on the spoke (`k8-platform-services`), but passed `-c k8-platform-mgmt` (the hub). The command succeeded silently — the relay accepted it, kubectl reported the namespace created, and there was no error. The namespace was created on the hub, not the spoke. The agent did not notice immediately; the discrepancy only surfaced when a subsequent spoke-side operation did not find the namespace it expected.

The failure mode is particularly dangerous because (a) `kubectl create` on the wrong cluster is not reversible without a subsequent `kubectl delete` on the correct cluster, (b) the relay gives no indication which cluster was actually targeted beyond echoing the `-c` value back, and (c) when debugging a multi-cluster operation, an unexpected namespace on the hub is easy to overlook.

The marginal cost of adopting this rule: one `kubectl get nodes -c <cluster>` call before any mutating command — 2–5 seconds, one line of output that confirms the node names and cluster identity. The cost of not adopting it: namespace/resource contamination on the wrong cluster, which must be diagnosed and cleaned up, and which can cause false-green observations (the resource exists, just on the wrong cluster).

---

### Suggestion 4: Prefer targeted API queries over bulk list responses for large GitHub Actions state — `AGENTS-MD-f7e4e30a46`

- **Per-rule file**: [./2026-06-09-214/AGENTS-MD-f7e4e30a46-prefer-targeted-query-over-bulk-actions-list.md](./2026-06-09-214/AGENTS-MD-f7e4e30a46-prefer-targeted-query-over-bulk-actions-list.md)

**Proposed agent instruction** (verbatim from the per-rule file's `# agent instruction` section):

> **Prefer targeted API queries over bulk list responses for large GitHub Actions state.** Do not call `mcp__github__actions_list` or equivalent bulk-list endpoints to find the status or run-id of a specific workflow run — these responses can exceed 390 KB and will overflow the context window. Instead, use a targeted query (e.g. `mcp__github__actions_get` with a known run-id, a filtered `gh run list --workflow` call, or parsing a previously-saved file for the few fields needed).
>
> *Grounded in: auto-016 — `mcp__github__actions_list` returned ~390 KB JSON that overflowed the context; the agent recovered by parsing a saved file.*

**Justification** (verbatim from the per-rule file's `# justification` section):

In auto-016 a call to `mcp__github__actions_list` returned approximately 390 KB of JSON — the full run history for all workflows in the repository. This overflowed the available context window and forced the agent to recover by locating a previously-saved file and parsing the few fields needed (run-id, status, conclusion) from that file instead. The detour cost several tool call round-trips and risked losing in-progress state.

The list endpoint aggregates every run across every workflow; it is almost never the right tool for "what is the status of run X" or "what is the run-id for the most recent dispatch of workflow Y." The targeted alternatives are cheaper by two orders of magnitude: `mcp__github__actions_get` with a known run-id returns a single object, and `gh run list --workflow <file> --limit 5` returns a handful of rows. Either fits in a few hundred bytes.

The marginal cost of adopting this rule: replace one `actions_list` call with a run-id lookup or a scoped `gh run list`. The cost of not adopting it: a 390 KB response that consumes the majority of the available context window and forces a recovery path, with a real risk of truncating the conversation history mid-run in an unattended session.

---

## Part 4 — proposed ADRs

Architectural decisions made in this session that the user may want to record as
ADRs. Each candidate has a full draft in the sibling directory.

| ID | Title | Draft |
|----|-------|-------|
| ADR-c7e9f8c3bc | Complete CREATE-path coverage for resource-scoped IAM narrowings | [./2026-06-09-214/ADR-c7e9f8c3bc-complete-create-path-coverage-iam-narrowings.md](./2026-06-09-214/ADR-c7e9f8c3bc-complete-create-path-coverage-iam-narrowings.md) |

One-line rationale: the auto-016 zero-node regression proved that a narrowing
validated on one create path is NOT validated on all of them; the ADR records the
required checklist (enumerate all create paths including SLR validation paths,
exercise each on a fresh account) as a durable architectural constraint.
