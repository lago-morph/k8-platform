# Retrospective — 2026-06-07 (auto-013, PRs #170–#177)

**Mode:** FORWARD (contemporaneous) · **Run:** auto-013 (overnight, autonomous) ·
**Scope:** the test-overhaul P1 static scaffold + fresh-account substrate bring-up
+ the read-only P0-spike. Authored before context truncation.

## Commit hashes by PR

| PR | Branch | One-line |
|----|--------|----------|
| #170 | `claude/blissful-faraday-RsPY2` | scope envelope |
| #171 | `claude/test-overhaul-p1-compute-gates` | `mgmt_live_verify` derived gate |
| #172 | `claude/test-overhaul-p1-coverage-deriver` | Pipeline-mode coverage deriver |
| #173 | `claude/test-overhaul-p1-live-orchestrator` | inverted-skip live orchestrator |
| #174 | `claude/test-overhaul-p1-live-evidence-gate` | FAIL-closed live-evidence gate |
| #175 | `claude/test-overhaul-p1-verifier-iam` | scoped verifier/reaper IAM role |
| #176 | `claude/test-overhaul-p1-skip-register-lint` | SKIP_REGISTER lint |
| #177 | `claude/test-overhaul-run-summary` | summary + retro + handoff |

---

## Part 1 — What happened

Executed FINAL-PLAN §16's front of the rollout. Brought the substrate up live on
the fresh account (phase-0 base + phase-1 management apply-and-verify, both green),
then ran a read-only P0-spike via the `kube-diagnose` workflow that **confirmed the
identity spine on the real cluster**: `source: IRSA` exactly, no static-cred AWS
ProviderConfig, the provider pod under SA `upbound-provider-family-aws` with
web-identity env and no `AWS_ACCESS_KEY_ID`, all providers Healthy, all four XRDs
v2-Namespaced and Established. Shipped the P1 static scaffold as 6 stacked,
hermetically-tested implementation PRs covering the load-bearing mechanisms
(coverage deriver §4.5, inverted-skip orchestrator §4.4, live-evidence gate §4.3,
scoped verifier identity §3.4, SKIP_REGISTER lint §4.6, `mgmt_live_verify` §4.1).

## Part 2 — What went well

- **Interleaving the long-pole bring-up with durable authoring.** Dispatched
  phase-0→1 via jentic and authored cluster-independent P1 code while EKS came up,
  per AGENTS §6.10 (no foreground polling). The management cluster finished green
  exactly as the scaffold was completing, so the read-only spike landed real
  evidence in the same run.
- **Hermetic test seams.** Every load-bearing mechanism (orchestrator tabulation,
  evidence-gate logic, register lint) was made unit-testable with injected fixtures
  (`LIVE_CHECKS_ROOT`, `LIVE_EVIDENCE_FIXTURE`, `LIVE_SKIP_REGISTER`), so ~67
  assertions run with no cluster — the scaffold is provable on push.
- **The plan's "verify what you extract" discipline.** The coverage deriver's
  extractor was proven byte-identical to the committed oracle across all four real
  Compositions before relying on it — the round-2 draft's silent-drift bug cannot
  recur.

## Part 3 — Proposed durable additions (see sibling files)

Each is authored as its own sibling file; the agent-instruction + justification are
duplicated inline here for review at decision time.

### AGENTS-MD-7f79d7ff24 — Never `git checkout <ref> -- .` during stacked work

**# agent instruction**
Never run `git checkout <ref> -- .` or `git restore --source=<ref> .` to "reset"
or "clean up" the working tree while doing stacked-branch work. It silently
overwrites tracked files with another ref's content, spuriously reverting your
own stack's committed changes in the working tree (the commits survive, but the
tree diverges from HEAD and a careless `git add -A` would commit the reverts). To
discard uncommitted changes, use `git checkout HEAD -- <path>` or `git restore
<path>`; to move between branches, use `git switch`/`git checkout <branch>`.

**# justification**
In auto-013 a `git checkout main -- .` issued to "tidy up" between stacked
branches overwrote `compute-gates.sh` and `test_compute_gates.sh` with main's
(pre-PR-171) versions, deleting the `mgmt_live_verify` work from the working tree
while HEAD still carried it. Caught by a system file-change reminder and recovered
with `git checkout HEAD -- .`, but a less-attentive `git add -A && commit` would
have pushed a silent revert into the stack. The `-- .` / `--source` forms target
an arbitrary ref, not HEAD — the footgun is that they look like a harmless reset.

### AGENTS-MD-4896fc948d — Sentinel-pad empty fields when reading yq `@tsv` in bash

**# agent instruction**
When piping `yq -r '... | @tsv'` (or any tab-separated output) into a bash
`while IFS=$'\t' read -r ...` loop, emit a non-empty sentinel for every
optional field (`(.x // "∅")`) and normalize it back to empty in bash. Tab is an
IFS-whitespace character, so `read` collapses runs of tabs and drops empty middle
fields — shifting every later field one position left and silently corrupting the
parse.

**# justification**
In auto-013's SKIP_REGISTER lint, an entry with an empty `oi` field but
`security: true` produced `…\t\ttrue`; `read` collapsed the empty `oi` and read
`security` into `oi`, so a disabled security check with no OI cross-link passed
the lint. The unit test caught it; the fix was sentinel-padding every field.
This is a generic bash/yq trap that will recur wherever TSV records have optional
middle fields.

## Part 4 — Carried forward / open

- P0-spike: `spec.crossplane.resourceRefs` on a live XR (needs a provisioned XR);
  drive-a-claim ⇒ AccessDenied half.
- The jentic workflow-integration capstone (deferred — must be dispatch-validated).
- P2–P6.
- The 4 §14 owner-decisions' carried sub-tasks (spoke CIDR/AccessEntry; tighten
  `Resource:"*"`; ArgoCD `role_policy_arns`).

## Metrics

| Metric | Value |
|---|---|
| PRs opened | 8 (#170–#177) |
| Implementation PRs | 6 (#171–#176) |
| New unit test files | 5 |
| New hermetic assertions | ~67 |
| Live runs (all green) | 3 (base, management, kube-diagnose spike) |
| Subagents dispatched | 0 |
| AGENTS-MD rules proposed | 2 |
| ADR drafts | 1 |
| SKILL-SPEC drafts | 0 (no genuinely new reusable skill demonstrated) |
| Destructive-command near-miss | 1 (recovered) |
