# Retrospective — post-build-#3 session tail: owner Q&A, the phase-5 scope gap, and cross-session convergence on the Task-3 design

- **UTC date**: 2026-06-12 (verified via `date -u +%Y-%m-%d`)
- **Last PR**: #237 (highest PR authored in the covered scope)
- **Branch at write time**: retro/2026-06-12-237
- **Scope**: since `6b7545c` (the merge of this session's previous retrospective, 2026-06-12-236) — per the session-scope check. Covers PR #237 itself, the owner Q&A that followed (status report, phases-0–5 assessment, Keycloak design explainer), and the review of the concurrently-merged #238/#239 + retro 2026-06-12-228. Owner-directed despite the thin commit count ("pull main … then retrospect").
- **Sibling artifacts**: none — no skill or ADR candidates; remedies classified per `ai/LESSONS.md` §3 in Part 3.

## Commit hashes by PR

### PR #237 — retro/2026-06-12-236 (merged 2026-06-12)

- `cd996c8` Retrospective 2026-06-12-236: 0 skills, 4 classified remedy proposals, 0 ADR drafts

(The concurrently-merged #238/#239 and retro PR #229/2026-06-12-228 were authored by a parallel session and are reviewed, not covered, here.)

## Part 1 — what happened

### Phase 1 — the lost status prompt

After the session's hand-off summary, the owner asked "I thought I asked for status. My prompt got lost?" — a status request sent between the 20:40 shutdown notice and the 21:06 message never reached this session. The harness delivers no negative acknowledgement, so neither side could see the drop. Cost: one confused round-trip. The mitigation already practiced (milestone summaries pushed proactively at each state change) is the only available hedge; see Part 3.

### Phase 2 — the phases-0–5 honest assessment exposed a scope-representation gap

Producing the owner's requested "how much is left for phases 0–5 verified working" required grepping `ai/REQUIREMENTS.md` REQ-AUTH and cross-checking `crossplane/` by hand, because **no gate artifact represents phase 5's federation half**. `SUBSTRATE-READINESS.md`'s definition of done is the substrate chain (hello-200); its rows now carry keycloak-boot evidence — but nothing in any checklist names the absent pieces: the cognito broker block (deliberately removed in #233), the un-composed EKS `IdentityProviderConfig` (zero hits in `crossplane/`), the realm's account-ephemeral-values mechanism, and a REQ-AUTH-10 federation oracle. A reader of SUBSTRATE alone would over-estimate phase-5 completeness; the truthful answer lived only in this session's derivation. Part 3 proposes fixing the artifact, not relying on the derivation.

### Phase 3 — reviewing the parallel session's merges (#238/#239, retro 2026-06-12-228)

Owner direction: pull main and read before retrospecting. Findings:

- **No conflicts, active convergence.** The parallel retro's R1 (catch-block namespacing) was already closed by its own status audit as "implemented as a superset" by this session's `b072caa`. Adopted L32 (environment bisect before content iteration) is exactly the move this session's green-on-fresh-account chainsaw run completed from the other arm — the run that split environment from content for the 23:58→04:5x red window. L33 (batch commits during SHA-gated dispatches) names the mechanism behind this session's wrong-SHA near-miss neighborhood; the two sessions' dispatch-hygiene findings are complementary (L33 covers pushes superseding runs; retro-236 Proposal 1 covers the SHA input itself).
- **ADR-12992f055b** (SecretVersion writes MR-owned ASM values) is the Task-3 design, fully drafted with the live-proven rejection trail. Its stated open dependency — "unvalidated by the gate through no fault of its own; the catch-block fix is the prerequisite for the decisive run" — **is now satisfied by this session**: the catch-block fix is merged and chainsaw is proven green on the current account (run 27429434084). The re-landing session inherits a decisive gate.

### Metrics

| Metric | Value |
|--------|-------|
| PRs opened / merged in scope | 1 / 1 (#237) |
| Owner questions answered | 3 (status/remediation, phases-0–5 assessment, Keycloak design) |
| Cross-session artifacts reviewed | 4 (#238, #239, retro-228 + its ADR draft) |
| Contradictions found between the two sessions' records | 0 |

## Part 2 — skills summary

None proposed (nothing recurred; the Q&A used existing repo artifacts).

## Part 3 — remedy proposals (classified per `ai/LESSONS.md` §3)

### Proposal 1 — represent the phase-5 federation gap in SUBSTRATE-READINESS — class (a), checklist-as-gate amendment

**Gap**: "phase 5 verified working" has no artifact representation; the federation half is absent from every checklist, so completeness claims silently shrink to "keycloak boots" (Phase 2 above). **Action**: add explicit pending rows to `SUBSTRATE-READINESS.md` — (9) cognito broker live-wired into the realm from terraform outputs, (10) EKS `IdentityProviderConfig` composed into platform-cluster + `kc:*` access proven, (11) REQ-AUTH-10 federation oracle (headless OIDC flow using the CI-generated Cognito test credentials) — each starting at `pending clean-build verification`. When the federation kinds are composed, `tests/coverage/expected-coverage.txt` picks them up mechanically via the existing deriver. Cheap (one docs PR now), and it makes the next "how much is left" answer readable from the gate file instead of re-derived.

### Proposal 2 — update ADR-12992f055b's dependency note when re-landing — class (d) observation / next-session pointer

The draft's "Open dependency" paragraph is satisfied (catch-block fix merged; gate green on this account, run 27429434084). The Task-3 session should re-land the chain per the ADR, flip its Status on adoption, and swap the #231 self-flagging quarantine test in the same PR — the test was built to force exactly that coupling.

### Observations (class d, no action)

- **Message loss is real and invisible**: a user prompt can drop with no NACK. The practiced mitigation — proactive milestone summaries so the owner is never more than one state-change behind — is the only available hedge; no mechanical check can see a message that never arrived.
- **Two concurrent sessions produced zero record conflicts** because both wrote through the same artifacts (OI register, LESSONS, SUBSTRATE, retros) and one ran a status audit against the other's merged work before adopting remedies. The pattern held without coordination machinery; worth naming, not yet worth a rule.

## Part 4 — proposed ADRs

None. The session tail made no architectural decisions; the relevant draft (ADR-12992f055b) belongs to the parallel session and needs only the status update noted in Proposal 2.
