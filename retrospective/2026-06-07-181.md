# Retrospective — human-readable summaries, sandbox-kubectl feasibility, model-aware dispatch

- **UTC date**: 2026-06-07 (verified via `date -u` and `python3 datetime.UTC`, agree)
- **Last PR**: #181 (highest PR covered)
- **Branch at write time**: claude/retro-2026-06-07-181
- **Scope**: since the 2026-06-07-177 retrospective (the tail of this session, after the auto-013 run)
- **Sibling artifacts**: [./2026-06-07-181/](./2026-06-07-181/)

## Commit hashes by PR

### PR #178 — claude/human-readable-summaries (merged 2026-06-07)
- `a7ca8f8` docs(human-readable): adopt human-scoped-deliverables for run summaries

### PR #179 — claude/handoff-sandbox-kubectl-feasibility (merged 2026-06-07)
- `83fc658` docs(handoff): next phase — sandbox kubectl access feasibility

### PR #180 — claude/model-aware-dispatch-skill (merged 2026-06-07)
- `84bafef` feat(skill): model-aware-dispatch — right model per subagent for speed

### PR #181 — claude/sandbox-kubectl-task-def (merged 2026-06-07)
- `c3114de` docs(task): standalone sandbox-kubectl build spec + handoff sequencing

All four merged to `main` bottom-up alongside the auto-013 P1 stack (#170–#176) in one tower.

## Part 1 — what happened

This was the post-overnight tail of the session, driven turn-by-turn by the owner
rather than autonomously.

### Phase 1 — make output human-readable

The owner stopped an in-progress edit to point out the real problem: the run
summary read like a database dump. We vendored the owner's `human-scoped-deliverables`
skill from another repo (cloned to `/tmp` after `WebFetch`/API listing 403'd on the
private API path), then rewrote the auto-013 run summary in that style (lead with the
story, tables, small diagrams, exact references moved to an audit-trail footer),
taught the `autonomous-run` skill to (a) post a plain-language objective + happy-path
to the user *before* the scope envelope and (b) write the morning summary in the
human-scoped style with a four-part opening, and added a top-of-file AGENTS.md
principle requiring human-readable output for human-facing artifacts.

### Phase 2 — assess sandbox kubectl access

Pure feasibility, no implementation. Confirmed the blocker is TLS (the egress gateway
trusts only public roots; EKS serves a private-CA API cert), ruled out "give the
cluster a public API cert" (managed EKS owns that cert), and confirmed a public-cert
front-end is feasible — comparing Lambda / NLB-passthrough / reverse-proxy / SSM-tunnel
by kubectl coverage and footprint. Written into the handoff as the next phase.

### Phase 3 — capture the model-mix pattern and launch the next session

Authored the `model-aware-dispatch` skill (opus orchestrator; haiku/sonnet/opus
subagents by task difficulty; optimize for speed, not tokens; not fast mode), added a
one-line AGENTS.md pointer, wrote a standalone task spec for the sandbox-kubectl
focused session that references the skill, added a handoff sequencing note, then
merged the full 12-PR tower (#170–#181) bottom-up to `main` and produced the kickoff
prompt.

### Metrics

| Metric | Value |
|--------|-------|
| PRs opened / merged (this scope) | 4 / 4 (#178–#181) |
| PRs merged in the closing tower (total) | 12 (#170–#181) |
| Skills vendored / authored | 1 vendored (`human-scoped-deliverables`) / 1 authored (`model-aware-dispatch`) |
| AGENTS.md principles added | 2 (human-readable output; subagent model-choice) |
| Subagents dispatched | 0 |
| Operational mishaps recovered | 1 (stacked-PR base selection — see Part 3) |

## Part 2 — skills summary

No new skill *candidates* to extract: the one reusable skill this scope demonstrated
(`model-aware-dispatch`) was authored as a deliverable during the session, and
`human-scoped-deliverables` was vendored, not discovered. Nothing else rose to the bar.

## Part 3 — agents-file suggestions

### Suggestion 1: Base a stacked PR on the branch carrying the files it edits — `AGENTS-MD-3b7577c63e`

- **Per-rule file**: [./2026-06-07-181/AGENTS-MD-3b7577c63e-stacked-pr-base-selection.md](./2026-06-07-181/AGENTS-MD-3b7577c63e-stacked-pr-base-selection.md)

**Proposed agent instruction:**

> **Base a stacked PR on the branch carrying the files it edits.** "When a stacked PR
> edits a file, set its base to the branch that already holds the most recent version
> of that file — not `main` and not an older sibling — or switching to the new branch
> restores the older file versions and risks committing a silent regression."
>
> *Grounded in: 2026-06-07 — a feasibility branch was first based on the run-summary
> branch instead of the human-readable branch, so checking it out showed the
> human-readable edits as reverted.*

**Justification:**

In this session the sandbox-kubectl feasibility branch was first created off the
run-summary branch (#177), but the human-readable edits to `AGENTS.md` and the
autonomous-run skill lived on the *next* branch up (#178). Checking out the new branch
restored the older versions of those files, and the harness flagged them as
"reverted" — a confusing near-miss that a careless `git add -A && commit` would have
turned into a real regression pushed onto the stack. The fix (re-base onto #178) cost
two git commands; the marginal cost of the rule is a single moment's thought about
"which branch has the newest copy of what I'm about to edit." The asymmetry — seconds
of forethought versus a silently-reverted file shipped in a PR — is steep.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-891fcd621c | Human-facing output must be human-readable, calibrated by the human-scoped-deliverables skill | [./2026-06-07-181/ADR-891fcd621c-human-readable-output.md](./2026-06-07-181/ADR-891fcd621c-human-readable-output.md) |
