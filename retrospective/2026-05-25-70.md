# Retrospective — late-2026-05-24 IRSA-root-cause cascade + sandbox capability upgrade

- **UTC date**: 2026-05-25 (verified via `date -u +%Y-%m-%d` and `python3 -c "import datetime; print(datetime.datetime.now(datetime.UTC).strftime('%Y-%m-%d'))"` — both reported `2026-05-25`)
- **Last PR**: #70 (highest PR number covered by this retro)
- **Branch at write time**: `retro/2026-05-25-70`
- **Sibling artifacts**: [./2026-05-25-70/](./2026-05-25-70/)
- **Scope**: full session (from branch divergence; no prior retro committed in this session)

## Commit hashes by PR

### PR #64 — `claude/great-maxwell-m2zb7` (merged 2026-05-24)

- `704523d` fix(crossplane): set spec.admission + autogen annotation on ClusterPolicy 09 (Bug 3)
- `60aa386` chore(diag): print full XR yaml + walk every resourceRef (composite-not-Ready)

### PR #65 — `claude/great-maxwell-m2zb7` (merged 2026-05-24)

- `d525b2f` fix(diag): repair YAML literal block broken by inline Python heredoc

### PR #66 — `claude/great-maxwell-m2zb7` (merged 2026-05-24)

- `799dd31` fix(crossplane): pin provider-family-aws SA name to match IRSA trust subject

### PR #67 — `claude/great-maxwell-m2zb7` (merged 2026-05-24)

- `375663c` fix(crossplane): hash inline manifest into triggers_replace so SA-name pin actually applies

### PR #68 — `claude/great-maxwell-m2zb7` (merged 2026-05-24)

- `af1bfe8` fix(crossplane): force provider Deployment rebuild after DeploymentRuntimeConfig change

### PR #69 — `chore/handoff-…-late-2026-05-24` (merged 2026-05-24)

- `89b6170` docs(handoff): rewrite NEW SESSION QUICKSTART for late-2026-05-24 capability upgrade
- `baa213c` docs(agents,handoff): codify AWS account ephemerality + scrub hardcoded ID

### PR #70 — `chore/phase-2-lifecycle-as-runbook` (merged 2026-05-25)

- `7bb707c` chore(phase-2): inline lifecycle steps as runbook, drop obsolete workflow modes

## Part 1 — what happened

### Phase 1 — Bug 3 fix + diagnose enhancement (PRs #64, #65)

Entry state: prior diagnose run [26351930369](https://github.com/lago-morph/k8-platform/actions/runs/26351930369) had shown `crossplane-resources` Application stuck OutOfSync (Bug 3) and PlatformSecret claims sitting Waiting (Bug 4 / 5). Root-cause for Bug 3: Kyverno was defaulting `spec.background`, `spec.admission`, and the `pod-policies.kyverno.io/autogen-controllers` annotation onto every ClusterPolicy when those fields were absent; ArgoCD therefore perceived eternal drift on `crossplane/policies/09-platform-secret-namespace-allowed.yaml`.

The Bug 3 fix landed in PR #64: declared the three defaulted fields explicitly on the source manifest and extended `tests/unit/test_kyverno_policy_lint.sh` with a fourth section asserting that every ClusterPolicy under `crossplane/policies/` sets all three explicitly.

The same PR enhanced `phase-2-diagnose.yml` to dump the probe XR's full yaml (previously truncated by `head -120`, hiding `.status.conditions`) and to walk every resourceRef on the XR. The enhancement was authored on the branch, then dispatched against the branch — and failed with `"Workflow does not have 'workflow_dispatch' trigger"`. PR #65 was the repair: the diag enhancement had embedded a Python heredoc inside the `run: |` block, but the Python body lines started at column 1, terminating the YAML literal block early. GitHub Actions silently refused to register the malformed workflow on the branch; the dispatch error message was misleading. Rewrote the loop in pure shell using `go-template` + tab-separated `read`. Validated with `python -c "yaml.safe_load(open(...))"` before push.

### Phase 2 — IRSA SA-name root cause (PRs #66, #67, #68)

With the enhanced diag on main, dispatched it again ([run 26353150253](https://github.com/lago-morph/k8-platform/actions/runs/26353150253)) and delegated the 166 KB log to a subagent with a 6-target extraction brief. Verdict came back in under 500 words: the IRSA trust policy in `terraform/management/irsa.tf:98` was bound to ServiceAccount `crossplane-system:upbound-provider-family-aws`, but Crossplane was generating a hash-suffixed SA name (`provider-family-aws-24aaab54a3a0`) because `DeploymentRuntimeConfig.spec.serviceAccountTemplate.metadata.name` wasn't pinned. AssumeRoleWithWebIdentity therefore failed for OIDC subject mismatch; the ASM Secret MR never reconciled; the XR stayed Ready=False; the claim sat in Waiting.

PR #66 pinned the SA name in the DeploymentRuntimeConfig manifest. The first apply on main ([run 26354235231](https://github.com/lago-morph/k8-platform/actions/runs/26354235231)) reported `Apply complete! Resources: 0 added, 0 changed, 0 destroyed.` Initially read this as success — a 7-second apply on a management cluster should have set off alarms, and the log's `No changes. Your infrastructure matches the configuration.` was the smoking gun. The actual cause: `terraform_data.crossplane_aws_provider.triggers_replace` only watched the IRSA arn and the chart version, not the manifest body. Manifest-only edits were invisible.

PR #67 extracted the manifest into `local.crossplane_aws_provider_manifest` and added `sha256(local....)` to `triggers_replace` — mirroring the existing convention from `terraform_data.argocd_bootstrap` (helm.tf:360–363). The next apply ([run 26354934978](https://github.com/lago-morph/k8-platform/actions/runs/26354934978)) correctly replaced the resource: `Plan: 1 to add, 0 to change, 1 to destroy`, `deploymentruntimeconfig configured`, `Apply complete! Resources: 1 added, 0 changed, 1 destroyed`.

Verification diag ([run 26355033199](https://github.com/lago-morph/k8-platform/actions/runs/26355033199)) → second subagent extraction → **partial success**. The SA `upbound-provider-family-aws` now existed on the cluster with the correct IRSA annotation. But the running provider pod was still mounted on the OLD hash-suffixed SA. Root cause: Crossplane's Provider controller only re-renders its Deployment when the Provider object itself changes — a DeploymentRuntimeConfig edit alone creates the new SA but doesn't flip the Deployment's `serviceAccountName`. PR #68 added a `kubectl delete deploy -l pkg.crossplane.io/provider=provider-family-aws --wait=false` after the kubectl apply, so Crossplane recreates the Deployment from the now-current config. PR #68 also added a `"provisioner-command-v2"` sentinel string to `triggers_replace` — the command-body change lived *outside* the hashed manifest local, so without the sentinel the apply would have no-op'd again (the same bug class as #66 → #67, one layer up).

### Phase 3 — handoff for sandbox capability change (PR #69)

User notified that the next session would have direct `aws` CLI admin + `kubectl` + unlimited network. This obsoletes the jentic-bridge workflow for cluster diagnostics: the prior 3-minute dispatch + 170 KB log + subagent extraction loop becomes a single `kubectl get`. Authored a NEW SESSION QUICKSTART rewrite in `ai/handoff.md` capturing PRs #64–#68, the new sandbox capabilities, and the immediate next steps for a fresh agent.

First draft hardcoded the late-2026-05-24 account ID (`413117505476`) in ~12 places: Environment State table, Phase states, Live AWS resources block, ArgoCD URL, S3 backend bucket, ACM SAN. User flagged the anti-pattern: *"The account is ephemeral, and is deleted in full usually between sessions … treating it as such will just confuse the next agent."* Scrubbed every hardcoded ID, replaced with `<account-id>` placeholders or "derive from `aws sts get-caller-identity`". Added a new `AGENTS.md` §8.1 codifying the rule. User also flagged that the next session must build up from phase 0 again (the cluster from this session is on the now-rotated account); updated phase-states accordingly with "code-only — needs apply" instead of "applied". Added a "for the next /self-retrospective" marker in the handoff at user request — surfacing the ephemerality lesson and the "applied successfully ≠ change reached cluster" finding for the user-triggered retro.

### Phase 4 — surfaced orphan branch, trimmed obsolete workflow modes (PR #70)

User noticed an unexpected branch on the remote: `revert-58-feat/phase-2-lifecycle-tooling`. Investigation: PR #58 (phase-2 lifecycle tooling — `mode=teardown-phase-2 / verify-absent / rebuild` on `integration-tests.yml`, plus `ai/PHASE-2-LIFECYCLE-PLAN.md`) was actually merged on 2026-05-24 03:55 UTC; the handoff incorrectly said it was still draft. Someone (the user, GitHub UI Revert button) had created a revert branch but never opened the revert PR — orphan.

User confirmed the workflow's reason-to-exist (dispatching teardown from a sandbox without cluster access) was now obsolete given the capability upgrade. Asked for a partial: drop the obsolete workflow modes, keep the runbook content. PR #70 inlined the verbatim bash from each removed workflow mode into `ai/PHASE-2-LIFECYCLE-PLAN.md` as copy-pasteable runbook blocks, then trimmed `integration-tests.yml` back to just the `test` mode + `test_filter` input.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 3 (log analysis — composite-not-Ready, post-IRSA verification, terraform-apply audit) |
| PRs opened / merged | 7 / 7 (#64, #65, #66, #67, #68, #69, #70) |
| Real-world bugs discovered + fixed | 6 (Kyverno drift; YAML heredoc; IRSA SA-name pin; triggers_replace miss; command-body sentinel miss; Provider Deployment not rolled) |
| Tests added (before / after) | the existing `tests/unit/test_kyverno_policy_lint.sh` got a fourth section asserting Kyverno defaulting fields; no other unit tests touched |
| Spec amendments | 1 — `AGENTS.md` §8.1 added (account ephemerality) |
| Scenarios driven / skipped | terraform-test `apply` ×2; phase-2-diagnose ×2; integration-tests not driven (account rotation between sessions made it moot) |
| Files touched at major refactors | `terraform/management/helm.tf` (4 PRs), `.github/workflows/phase-2-diagnose.yml` (3 PRs), `ai/handoff.md` (2 PRs), `AGENTS.md` (1 PR) |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `terraform-data-hash-all-deps` | SKILL-SPEC-9914e10966 | high | extract + hash | [./2026-05-25-70/SKILL-SPEC-9914e10966-terraform-data-hash-all-deps.md](./2026-05-25-70/SKILL-SPEC-9914e10966-terraform-data-hash-all-deps.md) |
| `workflow-yaml-precheck` | SKILL-SPEC-67e03061b1 | medium | one-liner check | [./2026-05-25-70/SKILL-SPEC-67e03061b1-workflow-yaml-precheck.md](./2026-05-25-70/SKILL-SPEC-67e03061b1-workflow-yaml-precheck.md) |
| `subagent-log-extraction` | SKILL-SPEC-8afdbefab5 | medium | brief composer | [./2026-05-25-70/SKILL-SPEC-8afdbefab5-subagent-log-extraction.md](./2026-05-25-70/SKILL-SPEC-8afdbefab5-subagent-log-extraction.md) |

## Part 3 — agents-file suggestions

Proposed additions to `AGENTS.md`. Each rule has its own per-rule file in the sibling directory; the CI/CD assembler concatenates the `# agent instruction` body into `AGENTS.md` and discards the `# justification`. The inline copy below mirrors both for retro-review convenience.

### Suggestion 1: Apply success is necessary, not sufficient — `AGENTS-MD-6a87fcea71`

- **Per-rule file**: [./2026-05-25-70/AGENTS-MD-6a87fcea71-apply-success-is-necessary-not-sufficient.md](./2026-05-25-70/AGENTS-MD-6a87fcea71-apply-success-is-necessary-not-sufficient.md)

**Proposed agent instruction**:

> **Apply success is necessary, not sufficient.** When a `terraform apply` or `kubectl apply` returns a green exit, that confirms only the tool's own state operation completed — NOT that the intended cluster state has changed. Before reporting an apply as successful, verify with an evidence query: `kubectl get` the affected object, check its `.spec` / `.status` matches the intent, and watch downstream controllers reconcile. Treat `Apply complete! Resources: 0 added, 0 changed, 0 destroyed` after a manifest edit as a *failure signal*, not success — it usually means `terraform_data.triggers_replace` is missing a dependency.
>
> *Grounded in: terraform-test run 26354235231 — `Apply complete! 0 added` after PR #66's manifest edit, silently no-op'd by a `triggers_replace` miss; only caught by reading the plan/apply log lines.*

**Justification**:

This was the dominant failure mode of the late-2026-05-24 IRSA-fix cascade: three independent times (PR #66, PR #67's first apply, PR #68's first apply) a green tool exit was misread as "the change reached the cluster". Each misread cost an extra dispatch cycle (~3 minutes apply + ~3 minutes diagnose + log analysis) and forced another PR. The marginal cost of the rule is one `kubectl get` per apply (≤2 seconds). The asymmetric payoff is high: catching one missed apply pays for the next 100 verification steps. The same anti-pattern applies to `kubectl apply` (success means the API server accepted the manifest, not that the controller reconciled it) and to ArgoCD sync operations (Synced/Healthy lags the actual workload state by the controller's reconcile interval).

### Suggestion 2: Validate workflow YAML when editing heredocs — `AGENTS-MD-0964799bc7`

- **Per-rule file**: [./2026-05-25-70/AGENTS-MD-0964799bc7-validate-workflow-yaml-when-editing-heredocs.md](./2026-05-25-70/AGENTS-MD-0964799bc7-validate-workflow-yaml-when-editing-heredocs.md)

**Proposed agent instruction**:

> **Validate workflow YAML after any heredoc edit.** When editing a `.github/workflows/*.yml` file in a way that touches a `run: |` block — embedded scripts, inline Python, jq filters, `<<-EOT` style heredocs — run `python -c "import yaml; yaml.safe_load(open('<path>'))"` before pushing. GitHub Actions silently refuses to register a workflow that doesn't parse, and the dispatch API returns the misleading error `"Workflow does not have 'workflow_dispatch' trigger"` regardless of the actual cause. Consequence: any non-default-branch dispatch of the edited workflow will fail with a confusing error until the YAML is fixed.
>
> *Grounded in: PR #65 — an unindented Python heredoc inside a `run: |` block terminated the YAML literal early; dispatch of the enhanced phase-2-diagnose against the branch failed for ~10 minutes with the misleading trigger error.*

**Justification**:

This rule has the highest signal-to-cost ratio of any in the session. Marginal cost: one `python -c yaml.safe_load` invocation per workflow-file edit (≤200 ms). Cost of skipping: ~10 minutes of "why is dispatch returning a trigger error when the trigger is literally on line 18" confusion, plus an extra PR to repair the parse error before any further verification is possible. The misleading dispatch error message is the trap — without YAML-validating locally, the symptom does not point at the cause. The rule also catches the related class of bugs in `run: |` blocks (unindented body, missing block scalar marker, tab-vs-space mixing) that GitHub Actions handles in the same silent-rejection way.

### Suggestion 3: Hash every dependency into `terraform_data.triggers_replace` — `AGENTS-MD-15a88ad1de`

- **Per-rule file**: [./2026-05-25-70/AGENTS-MD-15a88ad1de-hash-every-dependency-into-terraform-data-triggers-replace.md](./2026-05-25-70/AGENTS-MD-15a88ad1de-hash-every-dependency-into-terraform-data-triggers-replace.md)

**Proposed agent instruction**:

> **`terraform_data` resources with `local-exec` must hash every dependency.** When a `terraform_data` resource carries a `local-exec` provisioner that templates an inline YAML or command body, `triggers_replace` MUST cover every distinct body the command depends on — manifest content via `sha256(local.<body>)`, file-backed bodies via `filesha1("<path>")`, command-body changes via an explicit version sentinel. Listing only the templated input values (an IAM role ARN, a chart version) is NOT enough: a body-only edit silently no-ops at apply time, reporting `Apply complete! Resources: 0 added, 0 changed, 0 destroyed`. The pattern to copy in this repo is `terraform_data.argocd_bootstrap`'s `filesha1(...)` in helm.tf.
>
> *Grounded in: PR #66's manifest pin was a no-op until PR #67 added `sha256(local.crossplane_aws_provider_manifest)`; PR #68 then needed a `"provisioner-command-v2"` sentinel for a command-body-only change.*

**Justification**:

The IRSA-fix chain consumed five PRs (#66, #67, #68 — plus #69, #70 on the related infrastructure) over an extended debug loop. Two of those PRs (#67, #68) existed *purely* to undo the consequence of missing dependency hashes in `triggers_replace`. The marginal cost of the rule is two lines (`sha256(local.x)` plus the `locals { x = <<-EOT … EOT }` extraction) per `terraform_data` resource — once per resource, forever. The cost of not having the rule is one silent no-op apply per edit, which costs a full diagnose-and-rediscover cycle (~10 minutes on a small cluster, much more on a real EKS apply). The pattern is mirrorable from `terraform_data.argocd_bootstrap` (helm.tf:360–363), so adoption is mechanical: every existing `terraform_data` resource in `terraform/management/helm.tf` should be audited against this rule.

### Suggestion 4: Delegate large CI logs to a subagent with extraction targets — `AGENTS-MD-7f4656c13a`

- **Per-rule file**: [./2026-05-25-70/AGENTS-MD-7f4656c13a-delegate-large-ci-logs-to-a-subagent-with-extraction-targets.md](./2026-05-25-70/AGENTS-MD-7f4656c13a-delegate-large-ci-logs-to-a-subagent-with-extraction-targets.md)

**Proposed agent instruction**:

> **Delegate any CI log over 50 KB to a subagent with explicit extraction targets — never with "summarize this".** When a workflow log exceeds ~50 KB, do not read it inline; structural noise (timestamps, setup steps, prefixed lines) will eat the context budget. Save the log to `/tmp/<name>.log`, then dispatch a general-purpose subagent with a brief that names: (a) the exact `jq` query to extract the body from a `{result:{output:...}}` envelope, (b) the verbatim section headers to find, (c) the verbatim error / status strings to quote, (d) a word cap (≤600 words), and (e) what verdict the subagent must return. The subagent returns quoted evidence; the main agent reasons over it. A vague "summarize this log" produces a paraphrase that may lose exact strings the next fix depends on.
>
> *Grounded in: three subagent dispatches against ~75–170 KB diagnose logs this session (composite-not-Ready root-cause, post-IRSA-fix verification, terraform-apply audit) — each returned quoted evidence in <500 words and drove the next concrete fix.*

**Justification**:

The MCP tool result limit (~165 KB) is below the typical phase-2-diagnose log size (~170 KB). Even when the log fits, inlining it consumes context the main agent needs to reason about the *next fix*, not the *current evidence*. Three times this session, a carefully briefed subagent extracted the exact log lines that proved (or disproved) a hypothesis: "did the SA name get pinned?", "did the Deployment roll?", "did the claim go Ready?". Each subagent returned 300–500 words of quoted evidence and a one-sentence verdict. The cost of delegation is one Agent tool call (~30s); the cost of skipping is either a tool-result-size error (forcing re-dispatch) or context exhaustion that derails the working hypothesis. The rule pairs with the existing `subagent-prompting` reference card — this one specializes it for the CI-log case.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-f3feb391df | Hash inline manifest bodies into `terraform_data.triggers_replace` | [./2026-05-25-70/ADR-f3feb391df-hash-inline-manifest-bodies-into-terraform-data-triggers-replace.md](./2026-05-25-70/ADR-f3feb391df-hash-inline-manifest-bodies-into-terraform-data-triggers-replace.md) |
| ADR-bc50754311 | AWS test account identifiers are ephemeral and must never be hardcoded in repo artifacts | [./2026-05-25-70/ADR-bc50754311-aws-test-account-identifiers-are-ephemeral-and-must-never-be-hardcoded-in-repo-artifacts.md](./2026-05-25-70/ADR-bc50754311-aws-test-account-identifiers-are-ephemeral-and-must-never-be-hardcoded-in-repo-artifacts.md) |
| ADR-667388a141 | Direct-access sandbox supersedes the jentic bridge for cluster operations | [./2026-05-25-70/ADR-667388a141-direct-access-sandbox-supersedes-the-jentic-bridge-for-cluster-operations.md](./2026-05-25-70/ADR-667388a141-direct-access-sandbox-supersedes-the-jentic-bridge-for-cluster-operations.md) |
