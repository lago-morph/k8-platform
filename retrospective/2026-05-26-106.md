# Retrospective — Crossplane v1→v2 migration EXECUTION (auto-002)

- **UTC date**: 2026-05-26 (verified via `date -u +%Y-%m-%d` and Python `datetime.UTC`, both agreed)
- **Last PR**: #106 (highest PR number covered)
- **Branch at write time**: `claude/v2-exec-run-summary`
- **Sibling artifacts**: [./2026-05-26-106/](./2026-05-26-106/)
- **Scope**: full session (from branch divergence; no prior retro committed in this session)

## Commit hashes by PR

### PR #101 — `claude/crossplane-v1-v2-migration-exec-88kDe` (merged 2026-05-26)

- `d4c92bb` chore(decisions): scope envelope for v2 migration execution run

### PR #102 — `claude/v2-exec-seg2-terraform` (merged 2026-05-26)

- `a157f58` feat(seg-2): terraform/helm.tf v2 migration assertions + IRSA gates

### PR #103 — `claude/v2-exec-seg4-pr-t1` (merged 2026-05-26)

- `48a390a` feat(seg-4): regen kubeconform schema store against v2.5.0 .m.upbound.io CRDs
- `3e46ac9` fix(scripts): crossplane-trace + diag-component v2 .m.upbound.io updates
- `7fc36b3` test(fixtures): bump trace + kubeconform fixtures to v2 .m.upbound.io shape

### PR #104 — `claude/v2-exec-wave2-cutover` (merged 2026-05-26)

- `6e67128` feat(seg-1): migrate XRDs to apiextensions/v2 namespaced
- `acebf59` feat(seg-1): migrate Compositions to v2 .m.upbound.io + managementPolicies
- `fa6a90e` chore(seg-1): ArgoCD glob+ns updates, example XRs, Kyverno 09 delete, live XR + namespace, render-fixture inputs
- `c052a9b` feat(seg-3): chainsaw run.sh ClusterProviderConfig heredoc
- `acef59b` feat(seg-3): platform-secret scenarios drop spec.resourceRef.name walk
- `680adcd` feat(seg-3): integration tests 05/06/11 v2 .m groups + scope:Namespaced
- `1db591d` feat(seg-3): unit composition + XRD tests assert v2 shapes
- `5044815` phase-2-A4: chainsaw catch hook + meta-test + enforcer (cherry-picked from #91)
- `6f938d3` A4: fix meta-test marker check to use chainsaw structural frames
- `c0162f8` A4: use permissive regex for meta-test CATCH markers (ANSI codes)
- `907b8aa` fix(seg-3+91): v2-ify catch-block.yaml + 6 scenario pastes
- `dfc8289` merge: SEG-1 manifests into Wave 2 cutover
- `19129bc` merge: SEG-3 tests + #91 rebase + v1 residue patches into Wave 2 cutover
- `b3e35b0` fix(seg-3): drop last 2 v1 claim refs in tests/chainsaw/run.sh
- `84d64ba` fix(seg-3): mikefarah yq v4 syntax for providerConfigRef.kind assertion

### PR #105 — `claude/v2-exec-hotfix-xrd-connsec` (OPEN as of run end)

- `6596027` fix(seg-1): remove XR-level connection secrets — v2 rejects them
- `6a47acf` fix(seg-3): chainsaw scenarios use v2 XR kinds + Established-only

### PR #106 — `claude/v2-exec-run-summary` (OPEN as of run end, this retrospective will be added)

- `891d008` docs(run): summary of 2026-05-26 autonomous v1→v2 migration execution

## Part 1 — what happened

### Phase 1 — orientation (06:14 – 06:18 UTC)

Read the planning documents end-to-end (`40-final-plan.md` plus 5 SEG plans + situation doc + AGENTS.md + testing-guidelines.md), then verified pre-flight state: PRs #97/#98/#99/#100 already merged from prior session; PR #91 still open with v1-flavored catch hook; PR #94 closed-not-merged per plan disposition. Scope envelope authored to `decisions/auto-002-scope-envelope.md` and committed as PR #101.

### Phase 2 — parallel non-blocking PRs (06:18 – 06:31 UTC)

Two parallel subagents in worktree-isolation:
- **SEG-4 PR-T1** (tooling regen): bumped `fetch-crds-for-kubeconform.sh` to v2.5.0; pre-flight `curl -I` confirmed all 9 `*.aws.m.upbound.io_*.yaml` URLs return 200; legacy `*.aws.upbound.io_` URLs dropped; regenerated 51 schemas (was 53); fixed `crossplane-trace.sh` L215 case branch; updated `diag-component.sh`; edited 6 trace JSON + 5 kubeconform + 1 composition-render fixture.
- **SEG-2** (Terraform/IRSA): `helm.tf` Function v1beta1→v1 with unconditional pre-delete; rollout-status + SA post-check; `terraform.tfvars.example` bump; ArgoCD bootstrap gate documented (Option A).

Both PRs (#102, #103) opened ready-for-review, all CI green, merged after lead-agent verification. Total wall-clock for parallel phase: ~13 min.

### Phase 3 — terraform-test IRSA probe (06:35 – 06:36 UTC), 1-minute hard fail

Dispatched `terraform-test phase=management apply-and-verify`. **Failed in 68 seconds.** Per testing-guidelines §10, fetched log first: `Error: Unable to find remote state`. The phase-1 plan tried to read phase-0 base state via `terraform_remote_state` — but the state file didn't exist. **The AWS account had been rotated since the previous session per AGENTS.md §8.1**; the `ai/handoff.md` "phase 0 applied" line was stale. Re-strategized: open Wave 2 first, then handle the rotated-account terraform applies after Wave 2 lands.

### Phase 4 — Wave 2 cutover authoring (06:36 – 07:10 UTC)

Two more parallel worktree subagents:
- **SEG-1** (manifests, 11+ files): XRDs to `apiextensions.crossplane.io/v2` + scope:Namespaced + no claimNames; Compositions with `.m.upbound.io` groups, managementPolicies, ClusterProviderConfig; example claims rewritten as v2 XRs; Kyverno policy 09 deleted; ArgoCD glob tighten; live XR + namespace. **Two schema-driven deviations**: `connectionSecretKeys` kept (kubeconform schema said it was valid — this turned out to be wrong, see Phase 6), and `vpcConfig[0]`/`scalingConfig[0]` array→single-object form (v2 .m.upbound.io CRDs changed this — kubeconform caught it locally).
- **SEG-3** (tests + #91 rebase + 6-file residue): chainsaw `run.sh` ClusterProviderConfig heredoc; 3 platform-secret scenarios drop v1 walk; integration tests 05/06/11; 4 unit tests v2 assertions; cherry-pick #91; catch-block.yaml v2-ified; 5 scenario re-pastes.

Lead agent combined both branches into `claude/v2-exec-wave2-cutover`, no conflicts. Pushed @ `19129bc`.

### Phase 5 — chainsaw _smoke iteration (07:10 – 07:13 UTC)

Dispatched chainsaw with `scenario_filter=_smoke`. **Strike 1 (run 26437348706)**: failed after ~3 min on `crd/platformsecrets.platform.k8-platform.io` NotFound wait. Per §10, fetched log: SEG-3 plan §2.3 had only patched the ProviderConfig heredoc but missed L283 + L323-324 v1 claim CRD references. Fixed in `b3e35b0`. **Strike 2 (run 26437569007)**: GREEN.

Wave 2 PR (#104) opened. Push triggered unit-tests + chainsaw-verify. Unit-tests FAILED on `composition_providerConfigRef_kinds_present` — yq syntax `// empty` not supported by mikefarah yq v4 (CI's binary). Fixed in `84d64ba`. Re-dispatched chainsaw _smoke; GREEN. PR #104 merged.

### Phase 6 — IRSA probe gate, full chainsaw, hotfix iteration (07:19 – 07:50 UTC)

Dispatched `terraform-test phase=base apply-and-verify` on post-Wave-2 main → GREEN (~3 min). Then `phase=management apply-and-verify` → GREEN (~15 min). **IRSA probe gate satisfied.**

Dispatched chainsaw FULL scenario set. **Strike 1 (run 26439096757, 5min)**: `apply the PlatformCluster XRD` failed with `XR connection secrets aren't supported in apiextensions.crossplane.io/v2`. The kubeconform schema accepted `connectionSecretKeys`; the live admission webhook rejected it. SEG-1 plan §3 Q-3 had anticipated exactly this — but the subagent skipped the verification step because kubeconform was green. Hotfix branch `claude/v2-exec-hotfix-xrd-connsec`: removed `connectionSecretKeys` from XRD + 3 `writeConnectionSecretToRef` sites. `chainsaw _smoke` on hotfix already green; dispatched full set on hotfix.

**Strike 2 (run 26439680403, ~10min)**: `xrd-establishes` failed on `--for=condition=Offered` wait (v2 has no claim CRD, so Offered never appears) + assertion on v1 claim CRD `platformclusters.platform.k8-platform.io` (never created in v2) + 3 dry-run blocks using `kind: PlatformCluster` (v1 claim) instead of `kind: XPlatformCluster`. Plus 3 platform-secret scenarios used `kind: PlatformSecret` (v1) instead of `kind: XPlatformSecret`. SEG-3 plan §2.3 said scenario 00 had "No edit needed" — that was wrong. Fixed in `6a47acf`.

**Strike 3 (run 26440276628, ~21min)**: `xrd-establishes` PASS (9s); `smoke-harness-works` PASS (5s). The 3 platform-secret real-AWS scenarios timed out at exactly 245s with `Ready=False, message: "Unready resources: asm-secret"`. **Same shape as the original `00-situation.md` §1 symptom**, but now on v2.5.0 — meaning the asm-secret MR can't authenticate to AWS. Per testing-guidelines §10.1, environmental: the GitHub Actions secrets still point at the OLD AWS account. **STOPPED** debugging code per §10.1 ("If the failure cause is environmental, document it in the PR body and stop debugging code").

### Phase 7 — end-of-run protocol (08:25 – 08:40 UTC)

Opened PR #105 (hotfix) ready-for-review, documenting the AWS-creds rotation as the blocker. Wrote `run-summary-2026-05-26.md` as PR #106. Invoking self-retrospective (this artifact). Auto-subscribed to PRs #105 and #106.

### Metrics

| Metric | Value |
|---|---|
| Subagents dispatched | 4 (all worktree-isolated) |
| PRs opened / merged | 6 / 4 (#101, #102, #103, #104 merged; #105 + this retro's #106 open) |
| Real-world bugs discovered + fixed | 6 — (1) `run.sh` v1 claim CRD waits; (2) yq `// empty` syntax; (3) `connectionSecretKeys` admission rejection; (4) `Offered` condition + v1 claim CRD assertions in `xrd-establishes`; (5) `kind: PlatformSecret`/`PlatformCluster` in scenario apply blocks; (6) `vpcConfig[0]`/`scalingConfig[0]` array→single-object form (v2 .m schema) |
| Tests added / modified | 4 unit tests v2-ified + 1 enforcer cherry-picked from #91 + 6 v1 fixtures regenerated |
| Spec amendments | 0 (plan docs are frozen per scope envelope) |
| Scenarios passing (post-hotfix, sandbox) | 2/5 chainsaw (xrd-establishes + smoke); 3/5 blocked on rotated AWS creds (environmental) |
| Files touched at major refactors | ~75 across all 6 PRs (heavy on schema regen + fixtures) |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|---|---|---|---|---|
| `kubeconform-vs-admission-check` | SKILL-SPEC-ac65496714 | high | dispatch helper | [./2026-05-26-106/SKILL-SPEC-ac65496714-kubeconform-vs-admission-check.md](./2026-05-26-106/SKILL-SPEC-ac65496714-kubeconform-vs-admission-check.md) |
| `environmental-preconditions-first` | SKILL-SPEC-e13d9440d2 | high | pre-debug check | [./2026-05-26-106/SKILL-SPEC-e13d9440d2-environmental-preconditions-first.md](./2026-05-26-106/SKILL-SPEC-e13d9440d2-environmental-preconditions-first.md) |

## Part 3 — agents-file suggestions

### Suggestion 1: Dispatch live chainsaw before relying on kubeconform alone — `AGENTS-MD-3451636206`

- **Per-rule file**: [./2026-05-26-106/AGENTS-MD-3451636206-dispatch-live-before-merge.md](./2026-05-26-106/AGENTS-MD-3451636206-dispatch-live-before-merge.md)

**Proposed agent instruction**:

> **Dispatch live chainsaw before relying on kubeconform alone for v2 Crossplane manifest changes.** "For any PR that migrates Crossplane manifests across a major API-version boundary (e.g. v1 → v2 group rename, XRD apiextensions/v1 → v2), dispatch `chainsaw.yml` against the branch SHA and confirm at least `xrd-establishes` passes BEFORE merging. The static kubeconform schema can accept fields the live admission webhook rejects (observed for `connectionSecretKeys` on v2 XRDs). Schema-pass is necessary but not sufficient."
>
> *Grounded in: 2026-05-26 v1→v2 migration, chainsaw runs 26439096757 and 26440276628.*

**Justification**: The 2026-05-26 v1→v2 migration cost a hotfix PR (#105) plus two extra chainsaw iterations because the SEG-1 subagent trusted kubeconform alone to validate the v2 XRD shape. Kubeconform's `compositeresourcedefinition_v2.json` schema accepts `connectionSecretKeys` (the field exists in the CRD for back-compat); the live v2 admission webhook explicitly rejects it with `"XR connection secrets aren't supported in apiextensions.crossplane.io/v2"`. The plan flagged this exact possibility in SEG-1 §3 Open Q-3 ("verify after XRD apply"); the subagent skipped that step because kubeconform was green. Cost of NOT adopting the rule: 2 chainsaw re-iterations × ~10 min each + a hotfix PR after Wave 2 merged. Cost of adopting: one extra `chainsaw.yml` dispatch + ~5 min wait, BEFORE merge instead of after.

### Suggestion 2: Re-check environmental preconditions on each rotated account — `AGENTS-MD-2b4a4b7b2c`

- **Per-rule file**: [./2026-05-26-106/AGENTS-MD-2b4a4b7b2c-recheck-env-before-code.md](./2026-05-26-106/AGENTS-MD-2b4a4b7b2c-recheck-env-before-code.md)

**Proposed agent instruction**:

> **Re-check environmental preconditions on each rotated account before diagnosing code failures.** "At session start AND when any CI failure shows infrastructure-level errors (`Unable to find remote state`, `AccessDenied`, 245s reconcile timeouts on real-AWS scenarios), verify: (1) `aws sts get-caller-identity` succeeds with the expected account; (2) the state bucket for phase 0 exists for the current account; (3) the GitHub Actions repo secrets match the current account. If any precondition fails, the in-repo handoff doc is stale per AGENTS.md §8.1 — DO NOT continue with code-hypothesis debugging."
>
> *Grounded in: 2026-05-26 v1→v2 migration, terraform-test run 26436447517 wasted iteration on stale handoff before recognizing account rotation.*

**Justification**: testing-guidelines §10.1 already says this; this rule strengthens it with a session-start gate. The 2026-05-26 session dispatched `terraform-test phase=management apply-and-verify` based on the handoff doc's "phase 1 applied" line — and burned a fast-fail run (68s) before realizing the AWS account was rotated. By session-end, the same rotated-account state was suspected (correctly) of blocking the 3 platform-secret chainsaw scenarios. A 60-second session-start check (`aws sts get-caller-identity` + listing the state bucket) would have surfaced the rotation before any code dispatch.

### Suggestion 3: Treat plan Open Questions as adversarial-execution checkpoints — `AGENTS-MD-2eb541b6f4`

- **Per-rule file**: [./2026-05-26-106/AGENTS-MD-2eb541b6f4-plan-open-questions-are-real.md](./2026-05-26-106/AGENTS-MD-2eb541b6f4-plan-open-questions-are-real.md)

**Proposed agent instruction**:

> **Treat plan Open Questions as adversarial-execution checkpoints, not curiosities.** "When executing a multi-segment migration plan, every `§3 Open Question` left unresolved by the plan must be VERIFIED at execution time, not skipped. The plan-authoring session marked them open because the answer wasn't known then; resolving them is part of the execution scope. Default behavior must be: explicitly run the verification command the plan suggests, before any code change that depends on the answer."
>
> *Grounded in: 2026-05-26 v1→v2 migration — SEG-1 plan §3 Open Q-3 anticipated v2 might reject `connectionSecretKeys`; the SEG-1 subagent kept the field based on kubeconform schema-pass alone, requiring a hotfix PR after Wave 2 merged.*

**Justification**: A plan's §3 Open Questions encode known unknowns. Skipping them is a common-mode failure across multi-segment plans. The 2026-05-26 migration had 5 plan-flagged open questions; 4 were resolved by execution-time observation, but Q-3 (XRD `connectionSecretKeys` survival on v2) was dismissed because kubeconform validated cleanly. The plan's exact wording was "Verify after XRD apply"; the subagent treated kubeconform as the apply, which is wrong. Cost of adopting: each open question becomes a verification step in the segment's execution; ~5-30 min per question. Cost of NOT adopting: the question's failure mode lands as a post-merge hotfix.

### Suggestion 4: Use mikefarah yq v4 syntax in repo tests — `AGENTS-MD-64a049fc32`

- **Per-rule file**: [./2026-05-26-106/AGENTS-MD-64a049fc32-yq-v4-mikefarah-syntax.md](./2026-05-26-106/AGENTS-MD-64a049fc32-yq-v4-mikefarah-syntax.md)

**Proposed agent instruction**:

> **Use mikefarah yq v4 syntax (not jq syntax) in tests under `tests/unit/`.** "CI installs mikefarah yq v4 (currently v4.44.3 per the `unit-tests.yml` step). Do NOT use jq-style fallbacks like `// empty`; use `select(.field != null)` filters or empty-string fallbacks `// \"\"`. Locally a different yq variant may forgive the syntax; CI will not. Verify any new yq assertion locally with the same binary CI installs."
>
> *Grounded in: 2026-05-26 v1→v2 migration, PR #104 unit-tests failure on `composition_providerConfigRef_kinds_present`.*

**Justification**: PR #104 went red on a yq syntax error (`Error: 1:73: invalid input text "empty"`) that the SEG-3 subagent's local test pass didn't catch. The sandbox-installed yq apparently tolerates `// empty`; mikefarah v4 in CI does not. Cost of adopting: one-line awareness check in any PR adding yq queries. Cost of NOT adopting: a CI red-then-fix cycle per offending query.

### Suggestion 5: Worktree-isolated subagents cannot dispatch sub-subagents — `AGENTS-MD-b41f073395`

- **Per-rule file**: [./2026-05-26-106/AGENTS-MD-b41f073395-no-sub-subagent-dispatch.md](./2026-05-26-106/AGENTS-MD-b41f073395-no-sub-subagent-dispatch.md)

**Proposed agent instruction**:

> **Worktree-isolated subagents cannot dispatch sub-subagents.** "When the lead agent dispatches a subagent in worktree-isolation mode, the subagent does NOT have access to the Agent/Task tool. AGENTS.md §6.4 adversarial test-plan review must therefore be performed INLINE by the worktree subagent (which is acceptable per §6.4's allowance for `general-purpose`) OR by the lead agent before the worktree dispatch. Do NOT brief a worktree subagent with `dispatch a reviewer subagent`; it will fail silently and proceed without review."
>
> *Grounded in: 2026-05-26 v1→v2 migration, all 4 worktree subagents (PR-A/B/Wave2-C/D) reported `Agent tool not available; inline review only`.*

**Justification**: AGENTS.md §6.4 requires adversarial test-plan review by subagent. All 4 worktree subagents in the 2026-05-26 run honestly documented "Agent tool not available; inline review performed instead" — proving the contract was unworkable as written for nested dispatch. The inline reviews still surfaced findings (e.g., SEG-3 caught the `spec.scope: Namespaced` defaulting trap), but the lead-agent brief should not REQUIRE a tool the subagent doesn't have. Cost of adopting: lead-agent briefs must specify "perform inline review" for worktree subagents. Cost of NOT adopting: subagents silently degrade to no review, or waste context attempting an impossible dispatch.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|---|---|---|
| ADR-80191c7707 | XR-level connection secrets removed from v2 | [./2026-05-26-106/ADR-80191c7707-xr-conn-secrets-removed.md](./2026-05-26-106/ADR-80191c7707-xr-conn-secrets-removed.md) |
| ADR-c7f74e2fb6 | Kubeconform is not a sufficient gate for v2 CRD changes | [./2026-05-26-106/ADR-c7f74e2fb6-kubeconform-not-sole-gate.md](./2026-05-26-106/ADR-c7f74e2fb6-kubeconform-not-sole-gate.md) |
