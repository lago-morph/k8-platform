# Retrospective — auto-005 long-run: build phases 0-2 live on a fresh account; CDN/provider/flake fixes

- **UTC date**: 2026-06-05 (verified via `date -u` and `python3 datetime.UTC`, agreed)
- **Last PR**: #142 (merged 2026-06-05)
- **Scope**: full session (from branch divergence; no prior retro committed this session)
- **Branch at write time**: claude/long-run-BYIB6
- **Sibling artifacts**: [./2026-06-05-142/](./2026-06-05-142/)

## Commit hashes by PR

### PR #142 — claude/long-run-BYIB6 (merged 2026-06-05, merge `534a0ce`)

- `63b49e6` docs(auto-005): scope envelope for long-run-BYIB6
- `2e47817` docs(handoff): mark next-run task A (ArgoCD-creds output) DONE
- `93499e3` docs(handoff): refresh stale Pending follow-ups list
- `ad58a6b` fix(chainsaw): sweep ASM secrets by MR enumeration, not dead prefix filter
- `45672c6` fix(tests): kill pipefail+grep-q SIGPIPE flake in unit suite
- `c4588c5` docs(auto-006): Round-1 decision brief — ASM external-name fix
- `ea87866` AGENTS §8.5/§8.6: check creds via Actions; build-everything-tested-first
- `7c99c04` docs(auto-005): correct mandate to build phases 0-3 + live; session plan
- `cf45162` docs(auto-005): add phase-3 spoke execution plan
- `99cc974` fix(management): vendor crossplane chart — runner gets 403 from CDN
- `e635a1e` fix(management): wait for provider package install before SA check
- `2fe3f14` fix(management): drop by-label provider delete/rollout (label doesn't match)
- `bc95384` test(chainsaw): install crossplane from vendored chart (CDN 403s runner)
- `59cd0c6` docs(handoff): phase 0+1 VERIFIED live this session; phase 2 in flight
- `1ee7716` docs(handoff): rewrite for auto-005 wrap-up; prune resolved/historical content
- `788a316` docs(handoff): fix phase-3 framing (drop jentic/workflow-write tangent)
- `bf3b50c` docs(handoff): phase-3 sync — agent drives ArgoCD directly from the sandbox

## Part 1 — what happened

### Phase 0 — autonomous-run setup, then a wrong-scoped envelope

The session opened under the `autonomous-run` skill. I surveyed the repo, found
the handoff's "next-run task A" (ArgoCD creds output) already done (PR #141),
and — wrongly — concluded that the live build needed an unverifiable AWS account
and scoped it OUT, drafting a code-only-followups envelope. I shipped a few real
code-only fixes (ASM cleanup-trap, a unit-suite SIGPIPE flake) before hitting
the phase-3 chainsaw-verify gate and asking the user how to handle it.

### Phase 1 — the course correction

The user answered "Stop" — not to end the run, but because I was on a tangent.
The real mandate: **build phases 0-3 and work live; check creds instead of
assuming them stale; build everything already tested; start the long cluster
build first**. I recorded these as AGENTS §8.5/§8.6, corrected the scope, and
dispatched `phase=base apply-and-verify` immediately.

### Phase 2 — the live build, one bug per CI cycle

Phase 0 went green in ~3 min (and confirmed the creds were current — by Actions,
not assumption). Phase 1 (management) then surfaced **three real bugs on the
fresh account**, each found by reading the failing job log first (§6.9) and
fixed in one cycle:

1. `helm_release.crossplane` 403 on `charts.crossplane.io` from the runner (200
   from the sandbox) → vendored the digest-verified chart.
2. `crossplane_aws_provider` provisioner checked the provider SA before the
   package manager created it (fresh-install race) → added a `Healthy` wait.
3. …then the Provider was Healthy but the v2.5.0 family-provider Deployment
   carries no `pkg.crossplane.io/provider` label, so the by-label delete/rollout
   matched nothing → a **diagnostics dump** revealed the real shape; dropped the
   by-label dance for a label-agnostic SA check.

Phase 1 went green (run 27024349261): EKS cluster ACTIVE, ArgoCD UI HTTPS 200.
Phase 2 chainsaw was 4/6 — the 2 fails were the known OI-2026-05-28-1
ResourceExistsException flake (diagnosed via a subagent on the full log).

### Phase 3 — blocked, then the real misconceptions surfaced

Phase 3 needs ArgoCD to sync `platform-cluster-claim`. I framed this as "blocked
— can't create an argocd-sync workflow" and tried to author one via the GitHub
MCP (404) and then via jentic's contents API (`Resource not accessible by
personal access token`). The user corrected two misconceptions: (a) `ext-github`
/ jentic is for **driving** GitHub Actions, not authoring workflow files; and
(b) **the permissive sandbox can call provisioned public APIs (ArgoCD) directly**
using install-time credentials (the Terraform-output admin cred) — manual-sync
stays. "You and other sessions keep getting hung up on this."

### Phase 4 — wrap-up

Account expired. I got all work into PR #142, rewrote the handoff (pruning
resolved/historical content and fixing the phase-3 framing twice until it
reflected the sandbox-direct-API mechanism), and merged #142 to main on the
user's instruction.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 2 (1 adversarial test-plan review; 1 chainsaw-log diagnosis) |
| PRs opened / merged | 1 / 1 (#142) |
| Real-world bugs discovered + fixed | 5 (CDN-403, provider race, provider label, ASM cleanup-trap, SIGPIPE flake) + 1 known flake recurrence |
| CI workflows dispatched | base ×1, management ×4, chainsaw ×2 |
| Phases brought up live | 0, 1 (verified); 2 (4/6) |
| AGENTS rules added in-session | 2 (§8.5, §8.6) |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `vendor-blocked-helm-chart` | SKILL-SPEC-797eab9bd5 | med | CI / supply-chain | [./2026-06-05-142/SKILL-SPEC-797eab9bd5-vendor-blocked-helm-chart.md](./2026-06-05-142/SKILL-SPEC-797eab9bd5-vendor-blocked-helm-chart.md) |

## Part 3 — agents-file suggestions

Seven proposed `AGENTS.md` rules. Each has a per-rule file in the sibling
directory (carrying `# agent instruction` + `# justification`); both are
mirrored inline here for review.

### Suggestion 1: Drive provisioned public APIs directly from the permissive sandbox — `AGENTS-MD-1ce5216eb0`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-1ce5216eb0-drive-provisioned-public-apis-from-sandbox.md](./2026-06-05-142/AGENTS-MD-1ce5216eb0-drive-provisioned-public-apis-from-sandbox.md)

**Proposed agent instruction:**
> **Drive provisioned public APIs directly from the permissive sandbox.** "When the agent has provisioned a service that exposes a public, internet-facing endpoint with a publicly-trusted TLS cert (e.g. ArgoCD behind an internet-facing NLB at `argocd.management.<domain>`), call that service's API DIRECTLY from the sandbox — the sandbox has permissive network egress. Obtain the credential created at install time (e.g. the `argocd_admin_password` / `argocd_server_url` Terraform outputs, §10.1) and run `argocd login` then `argocd app sync` from the sandbox. Do NOT assume the service is CI-only or unreachable and do NOT build a CI workflow to proxy it. Only the EKS kube-API (private CA) and reading Terraform state / AWS APIs without creds genuinely require CI."
>
> *Grounded in: 2026-06-05 auto-005 — sessions stalled phase-3 by treating ArgoCD as unreachable and hunting for a sync workflow.*

**Justification:** Multiple sessions (this one included) stalled phase-3 provisioning by concluding "I can't reach the cluster / I need a CI sync workflow", then hunting for or trying to author a workflow they had no scope to create. The reality is cheap: ArgoCD is exposed on a public NLB with an ACM cert, the admin credential is already a Terraform output, and the sandbox can `argocd login` + `argocd app sync` directly. Marginal cost: one mental check. Cost of missing it: a whole tangent — the user named the recurrence: "You and other sessions keep getting hung up on this."

### Suggestion 2: ext-github / jentic drives GitHub Actions, not file writes — `AGENTS-MD-b14680f60d`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-b14680f60d-ext-github-drives-actions-not-file-writes.md](./2026-06-05-142/AGENTS-MD-b14680f60d-ext-github-drives-actions-not-file-writes.md)

**Proposed agent instruction:**
> **ext-github / jentic drives GitHub Actions; it does not author workflow files.** "Use the `ext-github` skill (jentic broker) to DISPATCH workflows and READ runs/jobs/logs — that is its purpose and PAT scope (`k8-platform-actions-only`: Actions read+write, Contents read). Do NOT use it to create or edit `.github/workflows/` files or any repo file; its token has no write scope (a contents PUT returns `Resource not accessible by personal access token`). Neither the git-push OAuth app nor the GitHub MCP can write workflow files either. If a task seems to need a NEW workflow, first ask whether an existing workflow or a non-workflow mechanism achieves it."
>
> *Grounded in: 2026-06-05 auto-005 — agent tried to create an argocd-sync workflow via jentic's contents API; wrong tool, and it failed.*

**Justification:** The agent spent a tangent trying to PUT a new `.github/workflows/argocd-sync.yml` through jentic's contents API, conflating ext-github's Actions:write (dispatch) with the GitHub PAT `workflow`/`repo` write scope. Two live probes returned `Resource not accessible by personal access token`. Documenting the boundary saves the next session from the same dead end and from over-claiming "I can create the workflow".

### Suggestion 3: Never `producer | grep -q` under `set -o pipefail` — `AGENTS-MD-3bb4730159`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-3bb4730159-never-producer-pipe-grep-q-under-pipefail.md](./2026-06-05-142/AGENTS-MD-3bb4730159-never-producer-pipe-grep-q-under-pipefail.md)

**Proposed agent instruction:**
> **Never `producer | grep -q` under `set -o pipefail` when the producer emits more than one line.** "`grep -q` exits on first match and closes the pipe; a still-writing upstream (`yq`, `awk`, `kubectl`) takes SIGPIPE (141), and `pipefail` propagates that 141 as the pipeline status — an intermittent false failure. Capture the producer output to a variable and `grep -q … <<<"$var"` (a here-string has no upstream process to SIGPIPE), or use a `case`/parameter-expansion match."
>
> *Grounded in: 2026-06-05 auto-005 — a ~10% flake in `test_platform_cluster_composition.sh` traced to `yq … | grep -qF` under pipefail.*

**Justification:** A real ~10% CI flake (`composition_policy_AmazonEKSWorkerNodePolicy`) red-failed builds intermittently with no code defect (2-3/20-30 runs). The capture+here-string fix removes the whole bug class and was verified 0/30. Marginal cost trivial; cost of the antipattern is non-deterministic CI that erodes trust in every red check.

### Suggestion 4: Vendor a Helm chart when the CDN 403s the runner — `AGENTS-MD-a820699f52`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-a820699f52-vendor-helm-chart-when-cdn-403s-runner.md](./2026-06-05-142/AGENTS-MD-a820699f52-vendor-helm-chart-when-cdn-403s-runner.md)

**Proposed agent instruction:**
> **When a Helm chart repo returns 403/blocks the CI runner, vendor the digest-verified chart instead of fetching at apply time.** "If `helm`/Terraform fails to fetch a chart index from a public repo on the GitHub runner (e.g. `charts.crossplane.io/stable/index.yaml` → 403) while the same URL serves 200 elsewhere, the CDN is restricting the runner egress — do NOT just retry. Download the chart `.tgz`, verify its `sha256` against the repo `index.yaml` `digest`, commit it under `vendor/`, and install from the local path (drop `repository`/`version`). Keep the version variable driving the filename so vendored copies stay in lockstep. Document the revert path."
>
> *Grounded in: 2026-06-05 auto-005 — `charts.crossplane.io` 403d the runner across two management applies; vendoring unblocked it.*

**Justification:** Two deterministic 403s 4 min apart, with 200 from the sandbox, ruled out a transient; retrying burns ~16-min cycles. Vendoring the digest-verified chart made the apply hermetic and arguably more secure. Marginal cost: one ~15 KB tarball in git; cost of not recognizing the pattern: a session blocked on an external CDN.

### Suggestion 5: Verify a Crossplane v2 provider's IRSA SA by pod, not label — `AGENTS-MD-f60b16dcd9`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-f60b16dcd9-verify-crossplane-v2-provider-sa-by-pod.md](./2026-06-05-142/AGENTS-MD-f60b16dcd9-verify-crossplane-v2-provider-sa-by-pod.md)

**Proposed agent instruction:**
> **Verify a Crossplane v2 provider IRSA ServiceAccount by the pod `serviceAccountName`, not by the `pkg.crossplane.io/provider` label.** "The Upbound v2.5.0 family-provider Deployment (`provider-family-aws-<hash>`) carries NO `pkg.crossplane.io/provider` label, so `delete`/`rollout status -l pkg.crossplane.io/provider=provider-family-aws` match nothing even when the Provider is `Healthy`. Wait for `provider.pkg.crossplane.io/<name> --for=condition=Healthy`, then check `kubectl -n crossplane-system get pods -o jsonpath` of `spec.serviceAccountName` includes the expected SA (and the SA object exists). Do not gate on the provider label selector."
>
> *Grounded in: 2026-06-05 auto-005 — a by-label delete/rollout matched nothing on v2.5.0 and failed the management apply.*

**Justification:** A heavily-commented provisioner gated on a label that matches NOTHING on the v2.5.0 family provider even when Healthy; that cost two failed applies before a diagnostics dump revealed the truth. Checking the pod's `serviceAccountName` is label-agnostic and verifies the actual IRSA contract.

### Suggestion 6: Inject a diagnostics dump to debug an opaque CI provisioner — `AGENTS-MD-9039112788`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-9039112788-inject-diagnostics-to-debug-opaque-ci.md](./2026-06-05-142/AGENTS-MD-9039112788-inject-diagnostics-to-debug-opaque-ci.md)

**Proposed agent instruction:**
> **When a CI provisioner/step fails opaquely and you have no live cluster/API access, inject a best-effort diagnostics dump and re-run before hypothesizing.** "Rather than guess at remote state from the sandbox, add a non-fatal dump of the actual state to the failing step (e.g. `kubectl get deploy,sa --show-labels`, pod `serviceAccountName`s, `get providers,providerrevisions` — each `|| true`), push, re-dispatch, and read the truth from the next run log. Evidence beats hypotheses (§6.17); one extra CI cycle that prints reality is cheaper than several spent speculating."
>
> *Grounded in: 2026-06-05 auto-005 — a diagnostics dump revealed the v2.5.0 provider Deployment label-less shape that fixed the apply.*

**Justification:** Without cluster access, the agent burned a CI cycle on a wrong hypothesis (a package-manager race) before a one-time diagnostics dump showed the real cause (a non-matching label selector). Marginal cost: a few `|| true` lines that stay useful; cost of guessing: wasted cycles and hypothesis-as-conclusion errors.

### Suggestion 7: Use the capability you have before asking, in a build session — `AGENTS-MD-38ee9c5ae4`

- **Per-rule file**: [./2026-06-05-142/AGENTS-MD-38ee9c5ae4-use-capability-before-asking-in-build-mode.md](./2026-06-05-142/AGENTS-MD-38ee9c5ae4-use-capability-before-asking-in-build-mode.md)

**Proposed agent instruction:**
> **In a delegated/long build session, exhaust available tools and make a defensible call before asking the user.** "When the user has delegated execution (build everything tested, use the skill, go), do not stop on an `AskUserQuestion` for something a tool or a defensible default can resolve — check the capability first (dispatch the probe, try the install, read the output) per §6.12/§8.5. Reserve user questions for genuine forks with cost/irreversibility and no defensible default. Asking when the path was available reads as not-listening and wastes a round-trip."
>
> *Grounded in: 2026-06-05 auto-005 — the agent asked about creds/mechanism the user expected it to resolve itself (check yourself, use it).*

**Justification:** The user pushed back repeatedly ("check yourself", "use it", an explicit "Stop") whenever the agent asked or assumed instead of using a capability it had. An unnecessary question is a round-trip cost and a trust cost; the rule's marginal cost is seconds.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-ff523c98c8 | Vendor the Crossplane Helm chart instead of fetching from charts.crossplane.io at apply time | [./2026-06-05-142/ADR-ff523c98c8-vendor-crossplane-helm-chart.md](./2026-06-05-142/ADR-ff523c98c8-vendor-crossplane-helm-chart.md) |
