# Retrospective — PR #111 chainsaw fix iterations, merge train, and helper-wiring audit

- **UTC date**: 2026-05-28 (verified via `date -u +%Y-%m-%d` and `python3 datetime.UTC` — both agree)
- **Last PR**: #121 (highest PR number covered by this retro)
- **Branch at write time**: `chore/handoff-followups-2026-05-28`
- **Sibling artifacts**: [./2026-05-28-121/](./2026-05-28-121/)
- **Scope**: full session (from branch divergence) — no retrospective was committed earlier in this session

## Commit hashes by PR

### PR #111 — `claude/seg-4-c4-reauthor` (merged 2026-05-28)

This session authored the final four commits in the PR's chainsaw-fix iteration:

- `9addd6d` fix(seg-4 PR-T3): assert all 3 v2 XR conditions in composition-drift
- `71308af` fix(seg-4 PR-T3): POSIX sh in composition-drift script blocks
- `2f476c0` fix(seg-4 PR-T3): kubectl namespace must be 'default', not $namespace
- `ef410ac` fix(seg-4 PR-T3): retarget composition-drift mutation to unpatched field

Merge: `36f0303` (PR #111 → main).

### PR #110, #112, #113, #114, #115 (merged 2026-05-28)

Merged in suggested order with no new commits authored by this session — see Phase 2 below for the merge sequence and merge SHAs.

### PR #121 — `chore/handoff-followups-2026-05-28` (merged 2026-05-28)

- `f959400` chore(handoff): follow-ups + helper-wiring audit for 2026-05-28 session

Merge SHA in main was not captured before this retro began.

## Part 1 — what happened

### Phase 1 — PR #111 chainsaw four-round fix loop

The session opened with a focused task: apply the PR #111 fix per `handoff-recovery.md` (one specific change: widen the chainsaw `wait for XR Ready` assert from `[Ready]` to all three v2 conditions, plus widen the conditions enforcer's `find` scope to include `_meta/`). The handoff implied the fix was one-shot.

It wasn't. The conditions fix itself worked — chainsaw round 1 (`26549617475`) cleared the conditions assert in 4.6s (down from a 247-second timeout). But the same chainsaw round surfaced a second bug: `set -o pipefail` rejected by `/bin/sh` in the next step's `script:` block. That bug was a known class — commit `9103d9a` had fixed it for the three `platform-secret/` scenarios — but the `_meta/composition-drift` scenario, salvaged separately from closed PR #94, had been missed. The existing enforcer (`test_chainsaw_script_shell_portable.sh`) caught it locally but was not wired into `unit-tests.yml`.

Round 2 (`26549768952`, on `71308af`) fixed the bash-isms and surfaced bug 3: `kubectl annotate xplatformsecret drift-probe -n "$NAMESPACE"` failing with `NotFound` because the XR was applied at `namespace: default` (literal, per the earlier fix `8526f46`) but the imperative kubectl call still used the chainsaw scratch namespace. Same class as `8526f46`; that earlier fix only touched the declarative `apply:` step.

Round 3 (`26549900324`, on `2f476c0`) fixed the namespace and surfaced bug 4: the meta-test's `yq` mutation of `forProvider.region` was a no-op because the composition's `FromCompositeFieldPath` patch overrides region from `spec.region` on every reconcile. This was a test-design flaw inherited from the PR #94 salvage — the scenario had never worked against this composition shape. Pausing here, the agent surfaced three options to the user via `AskUserQuestion`; the user chose option A (retarget the mutation to `recoveryWindowInDays`, an unpatched base field that the golden asserts).

Round 4 (`26550478501`, on `ef410ac`) finally landed green. Chainsaw completed at 02:17:38Z — but the completion webhook did not arrive into the session. The agent stayed silent on the no-poll discipline (AGENTS.md §6.10) until the user prompted: "An action completed 8 minutes ago, was that what you were waiting for?" The agent then queried directly via `ext-github`, confirmed green, and re-dispatched `chainsaw-verify.yml` to flip its `push`-triggered failure to a fresh green.

### Phase 2 — merge train

With PR #111 green, the user issued one explicit instruction: "Merge all PRs in your suggested order." Six PRs merged sequentially with no conflicts:

| Order | PR | Merge SHA |
|---|---|---|
| 1 | #110 scope envelope auto-003 | `a3179e1` |
| 2 | #112 post-rotation handoff | `b720a10` |
| 3 | #115 AGENTS.md §6.10 | `735e17a` |
| 4 | #111 SEG-4 PR-T3 chainsaw goldens | `36f0303` |
| 5 | #113 run summary 2026-05-28 | `3f399ce` |
| 6 | #114 retrospective 2026-05-28-113 | `2131567` |

### Phase 3 — handoff + helper-wiring audit (PR #121)

The user asked for a handoff file covering the session's suggested follow-ups, plus a full audit of helpers authored "in the last few days" to confirm each is either auto-used or sufficiently advertised. The agent computed two diffs: (a) tests in `tests/unit/run.sh` not in `.github/workflows/unit-tests.yml` — 17 of 39; (b) helpers / skills / AGENTS rules added since 2026-05-22 and their wiring state. The audit's headline finding was that `scripts/pre-chainsaw-audit.sh` (a static-audit helper for chainsaw, mandated by AGENTS.md §6.13) exists and would have caught two of this session's four chainsaw rounds in seconds — but the `pre-dispatch-static-audit` skill's trigger phrases are user-facing only, and no `PreToolUse` hook enforces the rule for agent-initiated dispatches.

The handoff (`handoff-followups-2026-05-28.md`) was committed on a fresh `chore/handoff-followups-2026-05-28` branch, pushed, opened as PR #121, and merged by the user a few minutes later.

### Metrics

| Metric | Value |
|--------|-------|
| Subagents dispatched | 0 |
| PRs opened / merged | 1 / 7 (PR #121 opened + 6 prior PRs merged + #121 merged) |
| Real-world bugs discovered + fixed | 4 (chainsaw rounds 1-4 on PR #111) |
| Tests added (before / after) | 0 / 0 (existing enforcers exercised; no new tests authored) |
| Spec amendments | 0 (Section 2 audit identifies pending amendments, doesn't apply them) |
| Scenarios driven / skipped | 1 / 0 (PR #111 `_meta/composition-drift` driven to green) |
| Files touched at major refactors | 2 (`chainsaw-test.yaml` + `test_chainsaw_xr_conditions_complete.sh`) |

## Part 2 — skills summary

| Skill | ID | Priority | Approx scope | Spec |
|-------|----|----------|--------------|------|
| `helper-wiring-audit` | SKILL-SPEC-4b58a33e43 | medium | Audit + recommend wiring fixes | [./2026-05-28-121/SKILL-SPEC-4b58a33e43-helper-wiring-audit.md](./2026-05-28-121/SKILL-SPEC-4b58a33e43-helper-wiring-audit.md) |

Only one new skill candidate. The PR #111 iterative fix loop is well-covered by existing skills (`pre-dispatch-static-audit`, `terraform-ci-watch`, `crossplane-claim-verify`); the gap is that `pre-dispatch-static-audit`'s triggers don't fire for agent-initiated dispatches — addressed via the AGENTS.md rule in Part 3, not a new skill.

## Part 3 — agents-file suggestions

Proposed additions to the project's agents file (`AGENTS.md`). Each rule has its own per-rule file in the sibling directory at `./2026-05-28-121/AGENTS-MD-<hash>-<kebab-name>.md` — the per-rule file contains exactly two sections: `# agent instruction` (the verbatim rule text the CI/CD assembler concatenates into `AGENTS.md`) and `# justification` (the persuasion text the assembler strips out). This Part 3 mirrors that content inline so the retrospective reviewer has every proposed rule + its justification in one place at review-decision time.

### Suggestion 1: Pre-dispatch static audit before agent-initiated chainsaw dispatches — `AGENTS-MD-e4a1ff8632`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-e4a1ff8632-pre-dispatch-static-audit-agent-initiated.md](./2026-05-28-121/AGENTS-MD-e4a1ff8632-pre-dispatch-static-audit-agent-initiated.md)

**Proposed agent instruction**:

> **Pre-dispatch static audit is mandatory before every agent-initiated chainsaw.yml dispatch.** Before any `mcp__*__execute` call (or `gh workflow run`) that targets `chainsaw.yml`, run `scripts/pre-chainsaw-audit.sh` from the repo root, read the output, and fix every FAIL before dispatching. AGENTS.md §6.13 sets this rule for user-initiated dispatches; it applies equally to agent-initiated dispatches even when the agent's working text doesn't match the `pre-dispatch-static-audit` skill's trigger phrases.
>
> *Grounded in: PR #111 chainsaw rounds 1-4 (2026-05-28), each surfacing a different bug class the audit script encodes.*

**Justification**:

PR #111's chainsaw fix took four cloud-CI iterations (~25 minutes total) to land green. Rounds 1 and 2 (missing 3-condition assert; `set -o pipefail` under `/bin/sh`) are exactly two of the six checks that `scripts/pre-chainsaw-audit.sh` already encodes — running the audit once before the first dispatch would have flagged both in seconds and collapsed two rounds into zero. The script exists. AGENTS.md §6.13 mandates it for user-initiated chainsaw dispatches. The `pre-dispatch-static-audit` skill catalogs it. None of those fired because the agent's working text didn't match the skill's user-facing trigger phrases, and the agent didn't re-read §6.13 at dispatch time. The marginal cost of adopting the rule is one bash invocation per chainsaw dispatch — ~3 seconds. The cost of not adopting it, paid this session, was ~15 minutes of unnecessary iteration plus the cognitive load of peeling off bugs one at a time. The asymmetry is overwhelming.

### Suggestion 2: Enforcer scope must cover every directory of the bug class — `AGENTS-MD-a44b351b9c`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-a44b351b9c-enforcer-scope-every-directory.md](./2026-05-28-121/AGENTS-MD-a44b351b9c-enforcer-scope-every-directory.md)

**Proposed agent instruction**:

> **Enforcers must scan every directory the bug class can appear in, not just the directory of the first instance.** When you author OR extend an enforcer (a `tests/unit/test_*.sh`, a kubeconform exclude list, a kyverno audit policy, a static-audit grep), the `find` / `grep` / glob scope MUST include every directory of the same artifact type — even directories that don't yet contain the bug class. New scenarios land in those directories and silently regress otherwise.
>
> *Grounded in: `test_chainsaw_xr_conditions_complete.sh` scanned `platform-secret/` + `platform-cluster/` but not `_meta/`; PR #111's first chainsaw round (2026-05-28) re-discovered the conditions-array bug in `_meta/composition-drift`.*

**Justification**:

The original conditions enforcer was authored against the bug class surfaced by chainsaw run `26544796570`. Its `find` scope listed exactly the two directories where the bug had already appeared. When SEG-4 PR-T3 added `_meta/composition-drift/chainsaw-test.yaml` carrying the same v1-shape `[type: Ready]` assert (salvaged from PR #94), the enforcer silently skipped it. Chainsaw round 1 of PR #111 re-discovered the bug at runtime. The enforcer fix was a one-line addition of `tests/chainsaw/_meta` to the `find` scope — a fraction of the cost of the 247-second cloud-CI failure that caught it. The pattern recurs: kubeconform exclude lists scoped to known-bad files, kyverno audit policies scoped to known-affected namespaces, grep patterns anchored to known-bad subtrees. Widening scope at author time is zero marginal cost; narrow scope is a regression that lands in CI rather than at commit.

### Suggestion 3: Use Bash run_in_background not Monitor for single-notification waits — `AGENTS-MD-5bdc52acff`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-5bdc52acff-bash-bg-not-monitor-single-notification.md](./2026-05-28-121/AGENTS-MD-5bdc52acff-bash-bg-not-monitor-single-notification.md)

**Proposed agent instruction**:

> **Use `Bash` with `run_in_background: true` for single-notification waits; reserve `Monitor` for streams of multiple events.** A Monitor that just sleeps and ticks is an anti-pattern — every tick is a chat-visible notification, the monitor doesn't end on the event you care about, and there is no surfaced way to stop it early. For "tell me when X completes", use `Bash` with `run_in_background: true` and an `until` loop that exits when the condition is met.
>
> *Grounded in: 2026-05-28 chainsaw-verify wait — armed a Monitor as a generic ticker, received 29 useless tick notifications before timeout.*

**Justification**:

After re-dispatching `chainsaw-verify.yml` for SHA `ef410ac`, the agent needed exactly one notification: "the verifier finished". It armed `Monitor` with a `while true; sleep 6; echo tick; done` body — Monitor emits each stdout line as a notification, so every tick polluted the chat. The agent had no `TaskStop` tool surfaced; the monitor ran out its 180-second timeout, producing 29 useless tick events. The correct tool for "one notification when condition is met" is `Bash` with `run_in_background: true` and an `until <check>; do sleep 2; done` body — it exits on success and emits exactly one completion notification. The Monitor tool description explicitly calls this out, but the discipline failed in practice. Codifying the rule gives every future session a check at tool-choice time, before the noise materializes.

### Suggestion 4: Webhook backup poll at 1.5x expected ETA — `AGENTS-MD-ae0308b2ac`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-ae0308b2ac-webhook-backup-poll.md](./2026-05-28-121/AGENTS-MD-ae0308b2ac-webhook-backup-poll.md)

**Proposed agent instruction**:

> **When a webhook subscription is the agreed completion channel and 1.5x the expected ETA has elapsed with no event, do a single direct-API status query as a backup.** PR-activity subscriptions occasionally drop `workflow_run completed` events without dropping the surrounding `check_suite` failure events; silence on the channel does not mean the work is still running. Confirm with one direct-API call before assuming.
>
> *Grounded in: 2026-05-28 PR #111 chainsaw run 26550478501 — completion event for the green run never delivered via `mcp__github__subscribe_pr_activity`; user noticed before agent did.*

**Justification**:

PR #111 was subscribed via `mcp__github__subscribe_pr_activity` from the session's start. Verifier-workflow failure events delivered reliably (four). The chainsaw run for the final SHA `ef410ac` completed `success` at 02:17:38Z — and no webhook arrived. The agent's discipline was "wait for the completion webhook before any further status query" (AGENTS.md §6.10). It waited eight minutes; the user prompted before the agent acted. The risk of strict no-poll discipline is silent webhook loss — the agent stays blocked indefinitely. A single ETA-gated direct query costs one tool call and resolves the ambiguity without violating §6.10's spirit. Asymmetry: one tool call versus an indefinite stall and social cost.

### Suggestion 5: Re-verify salvaged tests against current artifact shape — `AGENTS-MD-ef4d793b31`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-ef4d793b31-re-verify-salvaged-tests.md](./2026-05-28-121/AGENTS-MD-ef4d793b31-re-verify-salvaged-tests.md)

**Proposed agent instruction**:

> **Tests salvaged from a closed or abandoned PR MUST be re-verified against the current shape of every artifact they assert about, before their pass/fail signal is trusted.** Compositions, CRDs, XRDs, and schemas evolve; a test that was correct against the salvage-source's artifacts may be silently wrong (no-op assertion, misdirected mutation, dead binding) against current `main`. Re-verification is at minimum: (a) does each `kubectl get` / `kubectl apply` target a current resource? (b) does each mutation propagate to a field the assertion actually reads? (c) does each assertion match the current rendered output, not the salvage-era one?
>
> *Grounded in: PR #111's salvaged `_meta/composition-drift` scenario from closed PR #94 — its yq mutation targeted `forProvider.region`, which the current composition's `FromCompositeFieldPath` patch overrides, so the mutation never propagated; failed silently as "meta-test broken" on chainsaw round 4 (2026-05-28).*

**Justification**:

The `_meta/composition-drift` chainsaw scenario was lifted from closed PR #94 as part of SEG-4 PR-T3. It carried four latent bugs against the current composition shape, peeled off across four chainsaw rounds; the last (mutation target overridden by a patch) was a salvage-era design that no enforcer can catch statically. All four were silently inherited because nobody re-walked the scenario against current `main` artifacts at salvage time. The re-verification checklist is ~5 minutes of reading; the cost paid this session was ~25 minutes of cloud-CI iteration plus the user's attention. As long as salvaging closed PRs remains a pattern, the rule earns its place.

### Suggestion 6: tests-unit-run-sh and unit-tests-yml must stay in sync — `AGENTS-MD-1390a5c6a8`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-1390a5c6a8-run-sh-and-unit-tests-yml-in-sync.md](./2026-05-28-121/AGENTS-MD-1390a5c6a8-run-sh-and-unit-tests-yml-in-sync.md)

**Proposed agent instruction**:

> **Every test in `tests/unit/run.sh` MUST also be enumerated in `.github/workflows/unit-tests.yml`'s per-step list, OR the workflow must end with a `run.sh` catch-all step that invokes it.** Per-step CI is preferred for separate-failure diagnosability but silently drifts when tests are added to `run.sh` without a paired workflow edit. The catch-all alternative trades UI clarity for guaranteed coverage. Either pattern is acceptable; the gap between them is not.
>
> *Grounded in: 2026-05-28 audit found 17 of 39 tests in `tests/unit/run.sh` missing from `unit-tests.yml`'s per-step list, including the two enforcers whose absence caused PR #111's chainsaw rounds 1-2 (POSIX-sh + 3-conditions).*

**Justification**:

`test_chainsaw_script_shell_portable.sh` and `test_chainsaw_xr_conditions_complete.sh` were both added to `run.sh` as TDD regression gates for the exact bug classes PR #111 hit. Both were correctly scoped, both pass locally — and neither was in the CI workflow's per-step list. PR #111's HEAD `9addd6d` had a green `unit-tests` check despite the local enforcers being red, because CI didn't run them. Result: two chainsaw rounds (~10 minutes of cloud-CI time + agent context) re-discovering bugs the enforcers were authored specifically to catch. Fix options are documented in `handoff-followups-2026-05-28.md` Task 1; the contract is single-source-of-truth.

### Suggestion 7: chainsaw-verify manual re-dispatch when chainsaw completes after push — `AGENTS-MD-424b3f676e`

- **Per-rule file**: [./2026-05-28-121/AGENTS-MD-424b3f676e-chainsaw-verify-manual-redispatch.md](./2026-05-28-121/AGENTS-MD-424b3f676e-chainsaw-verify-manual-redispatch.md)

**Proposed agent instruction**:

> **After dispatching `chainsaw.yml` and confirming it completed green, if the corresponding `chainsaw-verify` workflow already ran on the same SHA (via push trigger) and went red, manually re-dispatch `chainsaw-verify` via `workflow_dispatch`.** The verifier does not have a `workflow_run` listener — it does not auto-re-fire when chainsaw completes. Without the manual re-dispatch the PR check stays red even though chainsaw is genuinely green.
>
> *Grounded in: PR #111 (2026-05-28) — chainsaw-verify ran on push of `ef410ac` while chainsaw was still in flight; required a manual `workflow_dispatch` of `chainsaw-verify.yml` to flip the PR check from red to green.*

**Justification**:

`chainsaw-verify.yml` triggers on `push` (path-filtered) and `workflow_dispatch`. There is no `workflow_run` listener to re-fire when `chainsaw.yml` completes. The intended workflow assumes the agent dispatches chainsaw, waits for green, then pushes; the actual flow this session inverted that order. The fix is one `workflow_dispatch` call, three lines of `ext-github` invocation. Without the rule, the next agent will repeat the diagnostic loop ("chainsaw is green; why is the PR red?") and the user will see a red PR check on otherwise-green work.

## Part 4 — proposed ADRs

| ID | Title | Draft |
|----|-------|-------|
| ADR-d6f12b0820 | Prefer runtime meta-test sanity checks over pre-commit static parsing for bash-script bug classes | [./2026-05-28-121/ADR-d6f12b0820-runtime-meta-test-vs-static-parsing.md](./2026-05-28-121/ADR-d6f12b0820-runtime-meta-test-vs-static-parsing.md) |

One-line rationale per draft appears in each draft's `## Context` opening sentence.
